---
id: w9/m165/t007
title: Test coverage for the shipped behavior
worker: worker9
status: done
estimate: 45m
depends_on: [w9/m165/t005]
---

## Objective

Pin the command mapping with tests that would have failed on 2026-09-21.

## Context

The matrix that matters is (service type) x (runtime) x (which field the client sent). The bug lived in two cells that nobody had a case for: docker+cron+startCommand on create, and docker+cron on read-back.

| type        | runtime | client sent     | expected                 |
| ----------- | ------- | --------------- | ------------------------ |
| cron_job    | docker  | `startCommand`  | stored + readable        |
| cron_job    | docker  | `dockerCommand` | stored + readable        |
| cron_job    | go      | `startCommand`  | stored + readable (green today) |
| web_service | docker  | `dockerCommand` | stored + readable (green today) |
| web_service | docker  | `startCommand`  | per t003's rule          |

## Steps

1. Table-test `specFromCreate` over that matrix.
2. Table-test the read projection for the same cells, asserting read-back equals what was stored.
3. Add a round-trip test (create → read → clone-shaped read) so the `--from` clone path is covered.
4. Mutation-spot-check the new assertions.

## Files

- `lego/backend/internal/apps/*_test.go`

## Acceptance criteria

- [ ] Every matrix cell asserted; the two broken cells fail pre-fix.
- [ ] A round-trip test covers create → read → clone.
- [ ] Mutation spot-check recorded.

## Out of scope

- The live shell verifier (t004).

## Outcome (2026-09-21)

`internal/apps/cron_docker_command_test.go` — create from the native spelling,
docker-spelling precedence, the docker web-service control, the stored-command
read projection, the PATCH round trip through the response the CLI prints, the
native-cron control, cross-surface agreement, both-spellings emission, and the
docker-web projection guard. Mutation-checked: reverting the create fold fails
`TestRESTCreateKeepsDockerCronCommandFromNativeSpelling`; reverting the read fold
fails three others. Full backend suite and `make lint-backend` (0 issues) green.
