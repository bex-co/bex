---
id: w9/m165/t008
title: Closeout
worker: worker9
status: done
estimate: 30m
depends_on: [w9/m165/t007]
---

## Objective

Close the milestone once a Docker cron demonstrably runs the command it was given.

## Context

- The DoD is observable end to end and should be re-proven on a fresh disposable fixture, not inferred from tests.
- Note the migration question t002 raised: crons created before this fix have an empty stored command. Decide and record whether any live cron needs its command re-set.

## Steps

1. Re-verify every DoD bullet live on a fresh free docker cron; include one scheduled run whose log proves the configured command ran rather than the image entrypoint.
2. Delete the fixture and confirm absence.
3. Set remaining tasks `status: done`, move each `tNNN.md` to `done/`, mark rows `— **DONE**`, set `**Status:** done`.
4. Move `.pm/w9/m165/` to `.pm/w9/done/m165/`, check `- [x]` in `.pm/w9/README.md`.
5. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w9/m165/`, `.pm/w9/README.md`

## Acceptance criteria

- [ ] Every DoD bullet re-verified live, commands and outputs recorded.
- [ ] One scheduled run proves the configured command executed.
- [ ] Pre-existing-cron migration question answered on the record.
- [ ] Board status consistent in all three places; fixtures deleted.

## Out of scope

- Work this milestone deferred to a note.

## Outcome (2026-09-21)

DoD verified live on dev-9 with the pinned CLI (`services create/get/update/
create --from`), not only in tests — the clone leg is what proved the third
half. All four DoD journeys pass; the native cron control stays green.
