---
id: w9/m166/t005
title: Test coverage and live CLI regression
worker: worker9
status: todo
estimate: 40m
depends_on: [w9/m166/t003, w9/m166/t004]
---

## Objective

Demonstrate that the original customer commands now reject without applying anything, while valid updates still run successfully.

## Context

An assertion on HTTP status alone passes on the buggy implementation. State, revision, deployment count, and successful-change audit events must be checked before and after the same failing request.

## Steps

1. Add behavioral tests for both observed field pairs and the representative late failures from t002. Confirm that they fail against the pre-fix execution path; avoid tests that merely assert table order.
2. Run the affected backend patch/policy/rollout suites and required backend checks. Include a positive multi-field rollout and single-field/empty-patch controls.
3. On an authorized environment running the fix, create a fresh free image fixture with the pinned CLI and replay t001's commands. Record exact exits, error body, unchanged state/no new deployment on refusal, and real HTTP success after a valid update.
4. Delete only proven-owned fixtures, then verify detail/list absence and former runtime URL teardown. Record the tested revision; do not mark a code-only result as live acceptance.

## Files

- Apps patch, policy, audit, and configuration-rollout tests under `lego/backend/internal/apps/`
- This milestone's README acceptance evidence

## Acceptance criteria

- [ ] Tests fail on the original bug and pass on the fix, asserting side effects as well as errors.
- [ ] Required checks pass and the live CLI acceptance/cleanup results identify the tested deployment.

## Out of scope

- Snapshot-only or mock-echo tests; production paid resources.
