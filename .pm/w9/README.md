# w9 — project workstream (worker9)

**Worker:** worker9. This is a general-purpose bex workstream. It may take work anywhere in the project; the milestones below are scheduled work and historical records, not a permanent purpose, specialty, or ownership boundary.

## Local dev environment

Develop against `.pm/w9/dev-9/`, this worker's own isolated stack on the shared local kind/CAPD cluster — never the shared cluster's default `auth`/`bex-system` namespaces or standard ports (5173/4433/4445/8090/8091/5432), which any other worker's session may also be using. `dev-9` gets its own Kratos + Hydra + Mailpit (namespace `dev-9-auth`) and app namespace (`dev-9`), reusing the shared cluster's CNPG operator and bex operator, plus a locally-built `bex-api` on dedicated ports derived from N=9 (`dev-9/ports.env`) so it never collides with any other workstream's `dev-N`.

- `bash scripts/dev-env.sh 9 up` — bring it up (idempotent — safe to re-run)
- `bash scripts/dev-env.sh 9 status` — health check (processes, pods, HTTP) + a pass/fail verification inventory
- `bash scripts/dev-env.sh 9 down` — tear it down (leaves the shared cluster and every other workstream's `dev-N` untouched)
- `bash scripts/dev-env.sh 9 clean` — reclaim `logs/` and `bin/` (refuses while the environment is up)

`up` prints the dashboard command to point at it once bex-api is running. One shared
implementation serves every workstream since `w1/m72`; `.pm/w9/dev-9/` keeps only
`ports.env` (a generated record of the derivation), this README, and `.gitignore`.

## Milestones

- [x] **m1** — Deploy detail page: Manual Deploy jumps to a per-deploy page with its logs (9 tasks) ← from user request 2026-07-14
- [x] **m2** — Render CLI compatibility: run the official CLI against bex-api → `docs/cli-compatibility-checklist.md` (7 tasks) ← user decision 2026-07-14 (`/pm-brainstorm` round 8): never build a CLI from scratch (new `.pm/DO_NOT_DO.md` entry); verify `render-oss/cli` as the fifth surface instead
- [x] **m3** — Managed Postgres rename: stable `dpg-…` identity + mutable name, rolled through prod and every `dev-*` environment (12 tasks) ← user request 2026-07-14 + the Postgres half of `.pm/w1/done/021.md` / `docs/cli-compatibility-checklist.md` — done 2026-07-15, moved to `done/m3/`
- [x] **m10** — Env vars: `generateValue` + cursor pagination (8 tasks) ← from `/pm-brainstorm more milestones for each worker` 2026-07-14 (the ADR006/ADR018 env-vars row's two documented omissions; `w1/m35`'s generateValue prerequisite). Reassigned from w8 to w9 for capacity before closeout
- [x] **m37** — Maintenance mode: user-toggled interstitial + custom page (10 tasks) ← from `/pm-brainstorm more milestones for each worker` round 6, 2026-07-14; filed under worker1's ownership (`Worker: worker1` in its own README) but physically queued in w9's directory for capacity. `App.spec.maintenanceMode {enabled, uri}` — REST/GraphQL/MCP/dashboard, operator reuses the w1/m4 activator as the responder. DoD verified live on the mock cluster 2026-07-15.
- [x] **m4** — cli-compat verify completeness: guard every fixed RC (6 tasks) ← from `/pm-brainstorm` round 9, 2026-07-15 (the m2 checklist's own "not yet covered by this script" residual: postgres lifecycle, services CRUD, deploys, logs, environments, logout legs — so RC2/RC7–RC12 can't silently regress) — done 2026-07-15: `scripts/cli-compat.sh verify` now covers every bex-api-calling family (52 assertions, one green run) + `scripts/cli-compat.sh mutation-check` proves each leg non-vacuous; moved to `done/m4/`
- [x] **m5** — Secret-file list pagination (7 tasks) ← promotes `005` 2026-07-15 (Render gives the route the env-vars `cursor`/`limit` contract; bex accepts and ignores the params, GraphQL/MCP/dashboard unpaged — the w9/m10 stable-name cursor pattern on its sibling route)
- [x] **m6** — Key Value stable `red-` id + rename (10 tasks) ← from `/pm-brainstorm` round 12, 2026-07-15 (closeout-residual sweep: `w9/done/m3` left KeyValue explicitly out of scope; checklist RC5 calls it "the one documented name-as-id datastore deviation"); mirrors m3's Postgres `dpg-` migration while the pattern is warm — done 2026-07-15, live-proven in dev-9 with official CLI v2.21.0, moved to `done/m6/`
- [x] **m38** — One error dialect: every error path speaks Render's shape (6 tasks) ← from `/pm-brainstorm` round 13, 2026-07-15 (mechanical-consistency mining: the auth gate answers every unauthenticated request via `http.Error` — text/plain, no `message` — plus the GraphQL decode 400, four metrics 400s, and the deploy-hook 405; `core.WriteErr` everywhere, regression-tested)
- [x] **m41** — Datastore Render-metadata consistency: audit + hygiene (6 tasks) ← from `/pm-brainstorm` round 19, 2026-07-15 (shipped-diff mine: `w6/016` verified resolved via `resourcemeta` adoption; one stale-comment escapee + a field-by-field cross-sibling re-verify of `owner`/`region`/`dashboardUrl`/`updatedAt` on every read path) — done 2026-07-15, audit clean (no drift found), cross-sibling parity tests added, moved to `done/m41/`
- [x] **m39** — CLI-surface evidence & dialect chores: 429 escapee + revoke dialect + checklist evidence (9 tasks) ← from `/pm-brainstorm` round 16, 2026-07-15; absorbs notes `006`–`009` (the w8/m16 pattern) + the round-16 shipped-diff mine's one genuine m38 escapee (`logs/rest.go:134` — the SSE-limit 429 still emits bare `{"error"}`) — done 2026-07-15, live-proven in dev-9 with official CLI v2.21.0, moved to `done/m39/`
- [x] **m40** — Key Value `spec.name` backfill rollout: dev fleet + production (5 tasks) ← from `/pm-brainstorm` round 19, 2026-07-15 (closeout-residual sweep's one unowned finding: `w9/done/m6/README.md:36` deferred running `keyvalue-name-migrate.sh` against dev-1…dev-10 + production as "operational rollout" and filed it nowhere; m39's live probes just proved prod/CLI access workable)
- [x] **m44** — Sample lifecycle verification on prod: deploy → up → suspend → down → resume → up → delete → gone for every `examples/` sample via the official Render CLI (9 tasks) ← user request 2026-07-17 right after m43 shipped — done 2026-07-17: **7/7 samples GREEN on prod** (static-site last, after Argo re-enable rolled the operator onto the publish-Job fix), pipeline green, Argo automation restored; every sweep follow-up (`010` no-Dockerfile static publish, `011` crash-loop failureReason surfacing, `012` publish-fix verify + pre-deploy sibling, `013` digest-pinned tenant images) **implemented** the same day, notes in `done/`; the one residual — codifying `012`'s manual prod fixes into infra/gitops — filed as `014` (promoted to `w1/m57` 2026-07-30, note in `done/`); milestone (originally misfiled at `.pm/w7/m44/`) moved to `done/m44/`
- [x] **m43** — Zot per-App credential activation: first deploy of a new App must pull without a manual zot restart (7 tasks) ← from live prod failure 2026-07-17 (Render-CLI deploy of `examples/hello-go`: build pushed, tenant pod `ImagePullBackOff` on zot 401 until `rollout restart statefulset/zot`; `registry/creds.go:31` hot-reload assumption false) — done same day: verify-then-bounce shipped (`registry.EnsureActive` + reconcile gate), prod-verified hands-free (`hello-go-m43b` → `live` in ~90s, operator bounced zot itself); moved to `done/m43/`. ⚠ rolled out via manual image import; Argo automated sync suspended on `bex-operator` + `bex-platform-prod` until `/ship` lands (re-enable commands in `done/m43/done/t004.md`)
- [x] **m55** — Dead-id detail URLs redirect home: `/databases/dadsfasd` & co. land on `/` with a toast, never an inline dead-end (8 tasks) ← user request 2026-07-17 (`/pm for w9`); route survey found all 8 id-bearing detail-route families settle to bespoke inline "Not found" blocks via the shared `loadRouteResource` seam — centralized into one redirect-on-not-found hook (errors still stay put inline) — done 2026-07-17, live-verified via `yarn dev:local` + Playwright on all 8 families + the nested deploy id + the `/d/` alias shim; moved to `done/m55/`
- [x] **m42** — Datastore metadata parity on GraphQL/MCP: region · dashboardUrl · updatedAt (8 tasks) ← from `/pm-brainstorm` round 21, 2026-07-15 (m41's own closeout residual: the audit recorded the GraphQL/MCP omission in ADR018 as a follow-up and built the `resourcemeta` seam + parity-test harness this milestone finishes the job with; dashboard-gap mine confirmed the GraphQL-only dashboard structurally cannot show datastore region today)
- [x] **m56** — Official CLI service create/update parity (10 tasks) ← user request 2026-07-18 (`$pm fix services create and services update parity in docs/cli-compatibility-checklist.md for w9`); closes the flag-level gaps exposed after completed m2/m4 command-family coverage
- [x] **m57** — Service internal addresses: slug Service + fromService fix (ADR041 D1–D3) (9 tasks) ← from [docs/ADR041-service-addresses.md](../../docs/ADR041-service-addresses.md), user request 2026-07-25 ("achieve address parity with render.com") — done 2026-07-26: the bare-name gap repro'd live on a fresh CAPD mock cluster (`wget: bad address 'api'`) then fixed the same day — bidirectional slug-named ClusterIP Service (one `applyClusterIPService` helper, pre-dispatch convergence for every type, squat guard), `fromService` host/hostport now inject the sibling's slug via typed apply-time refs (forward refs + idempotent re-apply tested), private-service `status.URL` = `http://<slug>:<port>` on the same predicate; Render evidence captured docs-fallback (pserv has **no** REST url field; `<hostname>-discovery` finding recorded); all suites + lint green; moved to `done/m57/`
- [x] **m58** — Connect-style internal-address surface: REST/GraphQL/MCP + dashboard (ADR041 D4) (7 tasks) ← from [docs/ADR041-service-addresses.md](../../docs/ADR041-service-addresses.md), same request; gated on m57 — done 2026-07-26: `serviceDetails.internalAddress` bex-extension on REST/MCP + GraphQL `internalAddress` (Render's REST has no such field — capture-driven; pserv's `url` now correctly **omitted**, the one structural REST fix), dashboard Connect menu Internal section + pserv **Service Address** header (dead cluster-internal link replaced), deploy-detail inherits via the shared chrome; simplify hoisted the address contract to `types/v1alpha1` (`EffectivePort`/`InternallyAddressable`/`InternalAddress` — operator + backend now derive one address, five 3000-default copies collapsed); ADR041 → Accepted, ADR018 Internal-address row added; all three suites + lint green; moved to `done/m58/`
- [x] **m59** — Native `bex` CLI launcher: upstream Render CLI import with isolated Bex config (7 tasks) — **DONE 2026-08-16**, moved to `done/m59/` ← user handoff 2026-08-02; extends m2's unmodified-client compatibility evidence into a distributable Bex command without forking upstream. Implementation + live device-auth/config-isolation evidence landed earlier; closed on confirmation that `bex-cli/v0.1.0` was released (2026-08-15, GitHub release "bex CLI 0.1.0": 4 platform binaries + checksums + sigstore signature), meeting the "released binary" DoD.
- [x] **m60** — Entry-chunk diet: strip the ~500 KB of dead weight route-splitting can't touch (9 tasks) — **DONE 2026-08-16**, moved to `done/m60/` ← from the 2026-08-16 perf sweep (follow-on to `w5/m67`, handed to w9 by user direction); **absorbs `w5/043`**. Cut initial JS **1,148 → 475 kB gzip (−58.6%)**: replaced the lucide-react namespace barrel with a static 5-icon map (dominant win), a build-only Vite plugin strips ~4,900 locale `description` strings, the non-default locale (zh) is lazy-loaded via `ensureLanguage`/`resources-zh.ts`, `OryToaster`/react-intl is `React.lazy`-mounted, and guardrails landed (`ANALYZE=1` visualizer, React/Radix/Apollo `manualChunks`, `sourcemap: false`). t009 attribution confirmed every allowlisted route page already splits and no heavy lib is entry-pinned. 2,121 dashboard tests green (i18n tests preload zh in setup; lazy-toaster test waits for mount). Render parity **omitted** (pure bundling perf, no surface change).
- [x] **m61** — bex-api response compression: gzip the :8090 read paths (5 tasks) — **DONE 2026-08-16**, moved to `done/m61/` ← from the same sweep (API leg). `withGzip` (`internal/api/gzip.go`) wraps `Handler()` outermost, negotiates `Accept-Encoding`, and content-type-sniffs so `text/event-stream` responses pass through uncompressed with per-event flush (route-allowlist-free); pooled writers, drops stale `Content-Length`, sets `Vary`, preserves `Flusher`/`Hijacker`/`Unwrap`. Measured **−93.8%** (metrics) / **−95.4%** (logs) on representative payloads; 5 unit tests (incl. SSE-untouched) + `make lint-backend` 0 issues green. Live prod-curl + cli-compat leg was `044` — **verified on prod 2026-08-17** (edge + pod-direct gzip, authenticated `/v1/services` decode, `healthz` identity), note in `done/044.md`; the live check **caught a real m61 gap** — `withCORS`'s `Set("Vary","Origin")` clobbered `withGzip`'s `Vary: Accept-Encoding` (only Cloudflare masked it at the edge), fixed by making CORS `Add` the header + 2 composed-chain regression tests (`gzip_test.go`). Render parity **omitted** (transparent transfer-encoding, no API-shape change).
- [x] **m62** — Polling hygiene + hydration fetch-policy: stop paying for tabs nobody is looking at (7 tasks) — **DONE 2026-08-16**, moved to `done/m62/` ← from the same sweep (data-fetching leg). Visibility-gated the metrics/usage/deploy pollers with `skipPollWhenHidden` (+ the `database-insights` `parameterOverrides` leg); the payment-required dialog stops its 2s poll once a method binds (re-arms on reopen, via adjust-state-during-render); unwound the deploy-detail waterfall so header/timeline/log mount in parallel with per-region skeletons (child queries `skip` until the deploy window arrives, ranged log queries immediately); and added `PRIMED_FETCH_POLICY` (cache-first) to the overview list hooks so an SSR/prefetch-primed navigation renders warm without a duplicate mount fetch — the seam m68's loaders land on. 2,136 tests + ESLint + tsc green. Render parity **omitted** (changes when/whether requests fire, not results).
- [x] **m63** — Log-list virtualization + honest loading states on the detail tabs (7 tasks) — **DONE 2026-08-16**, moved to `done/m63/` ← from the same sweep (rendering leg). Shipped: **ANSI parsed once at ingest** (`makeLogLine` → `line.spans`), killing the per-SSE-frame whole-buffer re-parse (the dominant live-tail cost); **metrics loading shimmer** (`MetricSection` shows a chart-height skeleton while a first fetch is in flight instead of the child chart's "No data in range" flash; event-timeline skeleton rows); **Logs/Scaling skeletons** (shared `LogPanelSkeleton` replaces the viewer spinner + fills `NonStaticRoute`'s new `loadingFallback`; Scaling reserves the manual-card slot). **Row virtualization was attempted and reverted** (`@tanstack/react-virtual` wouldn't render under jsdom — unverifiable, and too risky on the most-watched screen without a test harness) → deferred to `045`. 2,140 tests + ESLint + tsc green. Render parity: UI loading states only.
- [x] **m68** — Intent-preload data prefetch: kill the navigation fetch waterfall (7 tasks) — **DONE 2026-08-16**, moved to `done/m68/` ← from `/pm-brainstorm` 2026-08-16 (proposal 2), relocated from `w5/m68`. Added `prefetchInParallel` (`common/lib/prefetch.ts`) + route loaders that warm the Apollo cache on hover-intent: the overview primes Services/Projects/Databases/KeyValues (keyed on the persisted workspace), blueprints got a loader + `PRIMED_FETCH_POLICY`. Service/static **detail was already prefetched** (title loader primes `ServerDocument`, `useServer` cache-first). Pairs with m62's cache-first mounts so covered pages render warm with no duplicate mount fetch. 2,165 tests + ESLint + tsc green; SSR dehydration untouched. Live latency spike + hover-prefetch devtools/Playwright walk deferred → `046` (app not raisable amid the deploy storm; mechanism = the pattern detail routes already ship). Render parity **omitted** (changes *when* data loads, not the result).
- [x] **m69** — Perceived-latency polish: content skeletons + retire redundant pending config (7 tasks) — **DONE 2026-08-16**, moved to `done/m69/` ← from `/pm-brainstorm` 2026-08-16 (proposal 3), relocated from `w5/m69`.
- [x] **m83** — Log-list virtualization: only-visible rows on the live tail (6 tasks) — **DONE 2026-08-16**, moved to `done/m83/` ← from `045` (deferred from `w9/done/m63` t001 — the row-virtualization half reverted as unverifiable under jsdom). Virtualized `log-line-list.tsx` with `@tanstack/react-virtual` (spacer-flow window preserving wrap/nowrap + in-window selection); scoped jsdom geometry harness (`src/test/virtual-geometry.ts`) proves a **constant ~34 DOM rows for 1,000 and 5,000-line buffers** (96.6%/99.3% fewer nodes); `DEFAULT_MAX_LINES` raised 1,000→5,000; 2,180 tests + lint + tsc + build green. Live flame-graph confirmation deferred → `047`. Render parity **omitted** (pure DOM-rendering optimization, no surface change). Added `ListPageSkeleton`/`FormPageSkeleton` as the `pendingComponent` for the 8 rendering list/create routes (agents/blueprints/env-groups/webhooks/notifications/usage + blueprints.new/webhooks_.new) so a slow nav shows the page's content shape, not the bare `RoutePending` spinner (`static.index`/`static.new` are redirect-only → correctly none). Audited the 9 `pendingMs:0` + 2 `pendingComponent:RouteComponent` (all detail routes) → **retained** (detail frame skeleton at 0ms is still right), documented in `AGENTS.md`. `list-route-skeletons.test.ts` guards it; 2,167 tests + ESLint + tsc green. UI-only; Render parity: list-loading UX.
- [x] **m84** — Role-aware dashboard controls: disable-with-reason instead of always-fail (8 tasks) ← promotes `w1/047` via `/pm-brainstorm` "what to do for w9" 2026-08-17; `w1/m68`'s `can_operate`→`can_create` verb move left the dashboard rendering controls that 403 on save for contributors — adds a viewer-capabilities projection + disable-with-reason plumbing across the m68 four controls and the `can_view_sensitive`/`can_manage_billing`/`can_manage` boundaries. Render parity included.
- [x] **m85** — Dashboard typecheck covers test files: stop silent fixture rot (5 tasks) ← promotes `w5/041` via the same brainstorm; `dashboard/tsconfig.app.json` excludes `**/*.test.tsx` from `tsc -b` so fixtures drift silently — add a test-inclusive typecheck pass + CI wiring + a guard tripwire, fixing all surfaced drift. Render parity omitted (build-config/type repair, no surface change).
- [x] **m86** — Honest error states for datastore metric charts (6 tasks) ← promotes `w5/045` via the same brainstorm; `DatastoreMetricsPanel` swallows every error into the empty "No data in range" state (what hid the `w5/m71` wrong-identifier bug for months) — surface `result.error` as a distinct error card across the metrics panels. Render parity included (light/UI).
- [x] **m87** — Finish the role-aware disable-with-reason sweep (7 tasks) ← promotes `w9/048` via `/pm-brainstorm for w9` 2026-08-18; `w9/m84` gated four `can_create` controls with disable-with-reason but left env-var writes, the Build Filters editor, and the datastore lifecycle danger zone "editable → 403 on save" for contributors — thread `canCreate` through the same `useCapabilities` pattern so no contributor-facing `can_create` write control lies about what it can do. Render parity included (light/UI).
- [x] **m88** — Authenticated `/new/workspace` Render-shape + catalog prices (9 tasks) ← user request 2026-08-19 after Playwright comparison of `dashboard.render.com/new/workspace` vs `dashboard.bex.co/new/workspace`; uses ADR030 workspace fees (Hobby $0 / Pro $17.50 / Scale $349.30). Render parity included.
- [x] **m89** — Public auth-surface bugfixes from live dashboard hunt (8 tasks) ← Playwright hunt of `dashboard.bex.co` 2026-08-19 (logout no-session failure, raw `/auth/device` 400, verification copy mismatch, login empty-submit / duplicate `id=password`); polish residuals in `051`
- [x] **m90** — Security-audit run-1 remediation (9 tasks) ← from security-audit run-1 (`~/security-audit-skill/bex9/run-1`), user handoff 2026-08-20: one confirmed MEDIUM (Blueprint apply bypasses the dunning/billing-enforcement gate) + six accepted hardening notes — **DONE 2026-08-20**: billing gate closed + regression-proven; PromQL/LogQL `%q` escaping; unclassified-error redaction unified across REST/GraphQL/MCP via `core.IsPublicError`; single-use nonce on the `:8091` mint hop; registrycreds workspace-scoping + nonce rollback-on-error + proxy-CIDR caveat docs; full backend suite + `make lint-backend` green
- [x] **m91** — Cron job schedule validation + Render form consistency (8 tasks) ← QA pass of cron jobs on `dashboard.bex.co` + comparison vs `dashboard.render.com/cron/new`, 2026-08-20: confirmed bug — the create form validates the crontab but the **Settings edit form** accepts `99 99 * * *`, saves it, and flips the App `Running → Failed`; plus Render-parity polish (validate on edit, "New Cron Job" chrome, live schedule preview + pre-fill + UTC note). Sibling notes `052`–`054`
- [x] **m92** — Agent-sessions dashboard QA-hunt fixes (12 tasks) ← two independent live-Playwright QA hunts of `dashboard.bex.co/agents` 2026-08-21 (assistant + a relayed parallel-session hunt), cross-verified against source: fake `#0` PR badge (`prNumber` zero-vs-null), sidebar search practically non-functional against long task titles (subsequence fuzzy-match over-matches prose), stale "Recent" tab heading + malformed `archived=%22true%22` query param, unbounded raw task text flooding the DOM/accessible name, Failed sessions with a persisted transcript showing "conversation stream unavailable", "N turns" pluralization + a broken idle-banner sentence, sidebar list status disagreeing with the same session's detail page, and a redundant unbounded 5s poll duplicating the SSE stream. Sibling note `058` (content-moderation flag, not code work)
- [x] **m93** — Read/write round-trip parity: prove every service shape a client can create, it can also update (7 tasks) ← follows the shipped **w4/052** fix (`cca2f571d`, `internal/apps.effectiveRuntime`); post-fix root-cause with user: not a typing/OpenAPI-shape defect but a read/write round-trip gap (two internal encodings of one fact — `spec.builder` vs `spec.runtime` — plus a monoculture parity-verifier fixture that only ever set `--runtime` explicitly). Turns the one-field fix into a class guard: a create-matrix readback invariant + `effectiveRuntime` totality guard (backend) and build-strategy-diverse fixtures in the live services parity verifier
- [ ] **m94** — **BLOCKED (needs this fix deployed to `api.bex.co` before the live `bex ea sandboxes` probes in [blocked/m94/t004.md](blocked/m94/t004.md) can run; production still serves the pre-fix image)** — [Sandbox `sbx-` typed IDs](blocked/m94/README.md) (8 tasks; t001–t003 + t005–t007 **DONE** 2026-09-20 — bex mints the tenant-facing `sbx-<xid>` itself, every read path reports it, every write path dual-accepts pre-m94 substrate ids, REST/GraphQL/MCP agree, backend + `lego/cli` contract tests mutation-checked; only t004 live-verify and t008 closeout remain) ← `/qa-find-bugs-cli` live hunt 2026-09-20: production mints substrate UUIDs, which the pinned `ea sandboxes copy` parser rejects (`^(sbx-…):`), contradicting the `sbx-<xid>` contract in `models.go:64`; blocks w7/m150's CLI round trip. Sibling notes `062` (e2e browser helper vs production device flow), `063` (checklist psql verify-full re-grade)
- [ ] **m164** — **BLOCKED (one gate now, and it is yours: the decision to stop publishing AAAA for the datastore wildcards — parity with Render, but it removes IPv6 reachability for datastore endpoints. The live-session gate was cleared by the 2026-09-21 sweep-4 run, which completed the device ceremony and finished t001; t003/t005 still want a live session but are downstream of the decision anyway)** — [Dual-stack IP allow-list trap: a datastore denies the client's IPv6 with an opaque TLS EOF](blocked/m164/README.md) (9 tasks; **t001 DONE** — Key Value **proven affected**, identically: with an IPv4-only allow list a `redis-cli --tls` to the v6 literal dies `SSL_connect failed: unexpected eof while reading` while the v4 literal answers `PONG`, and adding the `/128` makes v6 answer too. Worse than the Postgres half in one way — `connection-info`'s `cliCommand` and `externalConnectionString` both target the hostname, which resolves AAAA-first, so the trap is reachable by copy-pasting a string the product itself emitted, with no CLI involved. Note b′ has no RESP analogue; **t004 DONE** — `w4/m116/t004` corrected and closed as not-reproduced, its "Ruled out: allow-list intact" line having checked an IPv4 `/32` against an IPv6 failure; **t002 research DONE** — Render publishes **no** AAAA on any datastore host (6 checked), bex's AAAA comes from `scripts/datastore-dns-cloudflare.sh` reconciling the LB's exact A/AAAA set, and candidate (b) is infeasible at the proxy because the deny decision lands after `'S'` is answered and the proxy never terminates TLS — a narrow feasible (b′) at the SSLRequest preamble is written up in the milestone) ← live `/qa-find-bugs-cli` sweep 2026-09-21. Sibling notes `065` (incomplete branding-residual inventory), `066` (two shipped CLI fixes still unreleased)

- [x] **m165** — [A Docker cron job's command is dropped on create and invisible on read](done/m165/README.md) (8 tasks) — **DONE 2026-09-21** by `/loopx w9`. Three halves, the third found only by exercising the fix live: create no longer discards the cron's `startCommand` on the docker branch; the read projection folds a docker cron's `dockerCommand` from `Spec.Command` (the fold the native branch and blueprint generation already did); and — with both shipped and every bex-side read looking correct — `create --from` *still* cloned an empty command, because the pinned client resolves a cron's command only through `AsNativeEnvironmentDetails().StartCommand`, so a docker cron now emits the command in **both** spellings. Verified live on dev-9 with the pinned CLI across create → read → update → clone; mutation-checked unit tests plus three new verifier legs (`docker-cron-create`/`-update`/`-clone`) close the docker-shaped hole in `w9/done/m56`'s "never a silent no-op" claim. Decision recorded in ADR006 + the checklist's `--cron-command` row.

## Approved queue (priority order, 2026-09-08)

- [x] **[059 — Reject blank project names across API surfaces](done/059.md)** — fixed with w4/058 on 2026-09-08; shared service validation and REST/GraphQL/MCP regression coverage.
- [x] **[060 — Repair name-conflict E2E Hydra compatibility](done/060.md)** (transferred from w5/052) — **DONE 2026-09-20** by `/loopx w9`: all five legs pass with dated output and confirmed fixture cleanup. The cluster was never broken — only its *published* kubeconfig was (rewritten to an in-network address), so republishing it with `mock-cluster.sh`'s own atomic procedure cleared the 2026-09-16 blocker without a reprovision. The run then surfaced four real drifts the v26.2.0 pin bump alone could never have covered (bex-api's fail-open refusal without OpenFGA, `image.ownerId` now required by the validated schema, ADR043 per-tenant namespaces instead of `default`, and `create_web_service`'s new required MCP arguments) plus a missing Traefik `Middleware` CRD, which every hosted App now needs to reconcile at all. The harness installs those CRDs itself (chart 41.0.0, CRDs only) and now tails the bex-api/operator logs on a timeout instead of dying silently.

`m93` closed 2026-09-08: live `image-healthcheck` + `web-builder-roundtrip` on dev-9 with CLI pin `fe8a6188119e` / API HEAD `9024a9672`; moved to `done/m93/`. The two inbox notes preserve their original evidence and replace their source queue entries. No new milestone or duplicated closing tasks are needed.

## CLI-hunt follow-up (`/qa-find-bugs-cli` 2026-09-20, siblings of m94)

- [ ] **062** — **BLOCKED (needs only the production run now: a disposable human identity's `CLI_USER_EMAIL`/`CLI_USER_PASSWORD`, which this session cannot read from `.env`)** — **local acceptance leg GREEN 2026-09-21** (`✓ bex browser login, Bex config isolation, refresh rotation, and logout revocation` on dev-9), and the local flow does pass through `/auth/device`, so the new authorize leg is exercised rather than skipped. Getting there fixed two non-helper blockers: `064` below, and an unverified-address test identity that Kratos refused with `session_verified_address_required` (rendered by the UI as "invalid credentials"). — [`render-cli-auth-browser.cjs` cannot complete the production device flow](blocked/062.md). **The fix is done and shipped:** the helper now drives whichever auth page is on screen (login → authorize device → reauth → consent → success) instead of a fixed sequence, handling `/auth/device` and the collapsed-password reauth conditionally so the local path is unchanged. **The acceptance run is not** — it requires the live device flow. Dispositioned 2026-09-20 by `/loopx w9`.
- [x] **[063 — Re-grade the CLI checklist `psql` row for the verify-full era](done/063.md)** — **DONE 2026-09-20** by `/loopx w9`. The row claimed `sslmode=require` evidence that predates w4/m95's deliberate `verify-full` switch; it now records both CA-less failure modes, the supported `PGSSLROOTCERT` + connection-info-CA path that returned the probe row on 2026-09-20, and that this is intended behavior rather than a bex gap. `docs/render-artifacts/psql-cli.md` gained a dated superseded-mode banner with its historical rows left intact; `pgcli` explicitly not re-probed; prettier clean.

- [x] **[064 — dev-N Hydra sent `bex login` to the PRODUCTION dashboard](done/064.md)** — **DONE 2026-09-21** by `/loopx w9` (found while clearing `062`'s local half). The `dev-N` Hydra overlay overrode `urls.login`/`consent` but not `urls.device.*`, so base's `https://dashboard.bex.co/auth/device` won and every workstream's local device login authenticated against **production** — failing as "invalid credentials" with no sign it had left the machine. The template now pins both device URLs to `__DASHBOARD_PORT__`, mirroring the `overlays/local` values. Affects every `dev-N`, not just dev-9.

## CLI-hunt follow-up (`/qa-find-bugs-cli` 2026-09-21, siblings of m164)

- [ ] **[071 — Log levels are detected in JSON but not logfmt, so `--level` is dead for Go `slog`'s default output](071.md)** (sweep 5, 2026-09-22) — a fixture logging all three shapes got `level: error` for `{"level":"error",…}`, `unknown` for `level=error msg=…` (logfmt), and `unknown` for plain `ERROR …`. The third is correct and deliberate (`log-shipper.yaml:208-213` rejects substring guessing, rightly) — but logfmt is structured, not a guess, and it is what `slog.NewTextHandler` and logrus's default `TextFormatter` emit, so those services cannot use `--level` at all. Fix is one `stage.logfmt` beside the existing `stage.json` (`:218`), before the normalization template at `:229`; the five-bucket cardinality budget is unchanged. ~30-45m, needs a gitops sync.

- [x] **[065 — The CLI branding residual inventory in `docs/bex-cli.md` is incomplete](done/065.md)** — **DONE 2026-09-21** by `/loopx w9`. The list showed eight residuals and none of the three resource-not-found messages — the most common error a user hits. It is now **reproducible rather than hand-enumerated**: the section leads with the grep recipe (re-run at step 4 of every pin-bump re-diff) and groups the current v2.27.0 output by what a user meets — resource-not-found per type, then auth/workspace state (`ErrLogin`, `ErrNoWorkspace`, `ErrTokenExpired`, …), then `RunE`-printed usage hints, then the identity/copy items. Nothing was rewritten: the standing leave-the-`RunE`-class-alone decision stands and is restated; it now just has the real scope in front of it.
- [ ] **067** — **BLOCKED (the scope decision is yours: should bex support sandbox env vars at all? That is a milestone, not a bug fix)** — [`bex ea sandboxes create --env-var` is refused by naming an internal JSON field](blocked/067.md). **Item 1 shipped 2026-09-21:** `--env-var`/`--env-file` and `--snapshot-id` now refuse **by name** (`SANDBOX_ENV_UNSUPPORTED` / `SANDBOX_SNAPSHOTS_UNSUPPORTED`, naming the flags) in the shape `SANDBOX_NETWORK_POLICY_UNSUPPORTED` already set, instead of strict decoding's `unknown field "env"`; a create without them is untouched. Not live-verified — dev-9 runs without OpenSandbox (routes unmounted) and the production token is expired, the same gate as m94.
- [ ] **066** — **BLOCKED (your go-ahead to cut the release — publishing a tag and binaries is outward-facing and hard to reverse, so this drain does not take it on its own authority)** — [Cut a bex CLI release: two shipped fixes are on main but unreleased](blocked/066.md). Premise re-verified 2026-09-21: newest tag is still `bex-cli/v0.2.1` (`423855d55`) and neither `34484344f` (cobra usage errors printing upstream `render …` on all four canaries) nor `6208c7500` (psql TLS trust) is an ancestor of it. **To clear:** run `/release cli`.
- [x] **[068 — A free web service cannot be cloned: bex refuses the `maintenanceMode` it just emitted](done/068.md)** — **DONE 2026-09-21** by `/loopx w9`. `validateMaintenanceEligibility` gated on the field being *present* rather than *enabled*, so the inert `{enabled:false, uri:""}` every read emits (required on `webServiceDetails` per `w4/125`) was refused on a free plan and `create --from` was impossible. Now gated on enabling; enabling on free still fails with the same named error. Verified live on dev-9 (a free repo-backed web service clones); the new guard echoes a free service's **whole** `serviceDetails` read-back into a create, so the next plan-gated field cannot reintroduce the class.
- [x] **[069 — A prebuilt-image service cannot be cloned: bex refuses the `branch` it just emitted](done/069.md)** — **DONE 2026-09-21** by `/loopx w9`, found while live-verifying `068`. With that blocker cleared, an image-backed clone hit the same class one field over: the read published the CRD's default `branch: "main"` on a service with no repo, and bex's own create refuses a branch on a prebuilt image (the deliberate `prebuiltImageSourceFields` policy). The read was wrong — `AppView.Branch` already documents itself as "empty for an image-backed App" — and now reports none. Verified live; mutation-checked guard. The `068` guard did not catch it because `branch` is top-level, not inside `serviceDetails` — extending it to the whole read object is the named next step.
- [x] **[070 — One guard for "a service's read must be re-sendable as a create"](done/070.md)** — **DONE 2026-09-21** by `/loopx w9`, filed as `069`'s named next step and closed in the same run. The invariant behind `w4/052`, `068` and `069` now has a single guard: nine service shapes GET themselves and echo **every create-settable top-level key** plus `serviceDetails` back as a `dryRun` create. The echoable set is reflected off `createServiceRequest`, so a field added tomorrow is covered without extending a list; the only translated spelling is the nested `image` object the pinned client itself maps from top-level `imagePath`. Mutation-checked: reverting `068` and `069` fails it on three shapes.

## Cron preview follow-up

- [x] **[061 — Truthful cron schedule previews](done/061.md)** (transferred from w4/099) — **DONE 2026-09-16** by `/loopx w9`. A new `stepShape` helper in `cron.ts` bounds both step branches against the existing `FIELDS` specs: divisor steps keep their "Every N minutes/hours" preview, a step wider than its field collapses to what actually runs (`*/70` → "Every hour", `0 */30` → "Every day at 00:00"), and an in-range non-divisor (`*/40` minutes, `0 */5` hours) falls through to the generic help text rather than naming an interval cron never runs. Red-tested (3 new cases fail pre-fix); both consumers share the helper, so Create and Settings agree; `yarn typecheck`/`lint`/`test` (3481 tests) green; backend vectors and REST/GraphQL/MCP acceptance untouched.
