---
id: w9/m164/t007
title: Simplify the code this milestone changed
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m164/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, altitude — behavior-preserving only.

## Context

- Likely surface: `lego/operator/internal/sniproxy/`, the two proxy `main.go` route-resolution paths, and any backend/dashboard code candidate (c) added.
- The shared-helper seam is the point: if t003 introduced a second place that decides allow/deny, that is exactly what this task should collapse.

## Steps

1. Run `/simplify` over this milestone's diff.
2. Apply the findings that are behavior-preserving; defer anything that changes semantics to its own note.

## Files

- Whatever t003/t005 touched.

## Acceptance criteria

- [ ] `/simplify` findings applied or explicitly deferred with a reason.
- [ ] No behavior change: the t003 and t005 acceptance checks still pass.

## Out of scope

- Bug hunting (that is `/code-review`).
