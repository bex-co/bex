---
id: w9/m164/t003
title: Implement the decision across Postgres and Key Value
worker: worker9
status: todo
estimate: 90m
depends_on: [w9/m164/t002]
---

## Objective

Ship t002's decision on both datastore front doors at once, so the two cannot drift.

## Context

- Both proxies share the allow-list primitive: `lego/operator/internal/sniproxy/allowlist.go:34-65`, called from `pg-sni-proxy/main.go:248` and `kv-sni-proxy/main.go:145`. A fix at the shared helper covers both; a fix at one call site does not.
- The denial today is `return dbRoute{}, false` (`pg-sni-proxy/main.go:253`) — the connection is closed with nothing written, which is what reaches psql as `SSL error: unexpected eof while reading`.
- If the decision includes candidate (c), the caller-address surfacing must land on REST, GraphQL and MCP together (see `docs/ADR006-bex-api.md`) plus the dashboard Access-control panel — that is what t006 checks.

## Steps

1. Implement the chosen candidate(s). Keep the shared-helper seam shared.
2. If a DNS record changes, land it through the same mechanism that owns it (do not hand-edit a provider console without recording it in `infra/`).
3. Verify live on a disposable free fixture of each type, with the failing journey from the milestone DoD.

## Files

- `lego/operator/internal/sniproxy/allowlist.go`
- `lego/operator/cmd/pg-sni-proxy/main.go`, `lego/operator/cmd/kv-sni-proxy/main.go`
- `infra/` and/or `lego/operator/config/manager/manager.yaml` if the AAAA record is in scope
- `lego/backend/internal/` + `dashboard/` if candidate (c) is in scope

## Acceptance criteria

- [ ] The milestone DoD's failing journey passes live on a free Postgres fixture.
- [ ] The same journey passes on a free Key Value fixture, or t001 proved Key Value is unaffected and that is cited here.
- [ ] The both-families control still returns the probe row (no regression).
- [ ] The anti-enumeration property is preserved: an unknown hostname and a denied source are still indistinguishable to a source that has no route.

## Out of scope

- The verifier legs (t005) and the parity sweep (t006).
