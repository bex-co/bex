---
id: w9/m164/t009
title: Closeout
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m164/t008]
---

## Objective

Close the milestone once its definition of done actually holds live — not merely once the code is written.

## Context

- The DoD is observable end-to-end: a dual-stack client with an IPv4-only allow list must never get a bare `SSL error: unexpected eof while reading` from a bex datastore endpoint.
- Re-run the milestone's failing journey and its both-families control on fresh disposable fixtures before closing.

## Steps

1. Re-verify every DoD bullet live on fresh free fixtures; delete them.
2. Set every remaining task `status: done`, move each `tNNN.md` to `done/`, mark each row `— **DONE**` and set `**Status:** done` in the milestone README.
3. Move `.pm/w9/m164/` to `.pm/w9/done/m164/` and check `- [x]` in `.pm/w9/README.md`.
4. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w9/m164/`, `.pm/w9/README.md`

## Acceptance criteria

- [ ] Every DoD bullet re-verified live, with the commands and outputs recorded.
- [ ] Board status consistent in all three places.
- [ ] Fixtures deleted and their absence confirmed.

## Out of scope

- Any work the milestone deferred to a note.
