---
id: w9/m164/t004
title: Re-point `w4/m116/t004` at this cause and correct its "ruled out" line
worker: worker9
status: done
estimate: 30m
depends_on: [w9/m164/t001]
---

## Objective

`w4/m116/t004` is blocked on a cause it recorded as UNVERIFIED, and the one thing it explicitly ruled out is the actual cause. Correct the record so nobody spends the milestone's budget on the wrong subsystem.

## Context

- `w4/m116/t004` ("Resume must restore the Postgres data path, not just the status") records: after suspend → resume, status reads `available` while the external path returns `SSL error: unexpected eof while reading` from server `2a01:4f8:c01e:3d1f::1:5432` across 4 probes over ~6 min.
- Its "Ruled out" line reads: `allow-list intact (162.224.81.143/32 still present)`. That is an **IPv4** `/32`, and the failing server address is the **IPv6** literal — the check confirmed the wrong family. This is exactly the trap this milestone documents.
- Its listed candidate causes (proxy route not re-registered on wake, backend still starting, stale `ExternalHost`/certificate) are all unsupported by that evidence.
- Counter-evidence gathered 2026-09-21 on a fresh free fixture with **both** families allow-listed: suspend → resume went `creating → available`, and `bex psql -c 'SELECT 1'` returned the probe row on the **first** `available` reading and on 5 consecutive probes over ~2.5 min. t004's premise did not reproduce.
- The same misreading is in `w9/done/063.md` ("One transient `SSL error: unexpected eof` (IPv6) self-recovered on retry — single occurrence, not filed") and in `w4/m116/t004` line 22 ("two isolated first-connection EOFs … then self-healed"). Happy-Eyeballs flapping between an allowed v4 and a denied v6 explains both without any self-healing.

## Steps

1. Update `w4/m116/t004`'s Context: strike the incorrect "Ruled out" line, cite this milestone as the proven cause, and record the 2026-09-21 non-reproduction of its premise.
2. Re-scope or close t004 on that basis — if resume never actually lied about `available`, say so rather than leaving a blocked task implying an operator wake bug.
3. Reconcile `w4/m116`'s README status line and task table with whatever t004 becomes.
4. Add a one-line correction to `w9/done/063.md` so its "transient, not filed" note does not mislead the next reader.

## Files

- `.pm/w4/blocked/m116/t004.md`, `.pm/w4/blocked/m116/README.md`
- `.pm/w9/done/063.md`

## Acceptance criteria

- [ ] `w4/m116/t004` no longer claims the allow list was ruled out, and names this milestone as the cause.
- [ ] t004 is either closed as not-reproduced or re-scoped to something the evidence actually supports; `w4/m116`'s README agrees with it.
- [ ] `w9/done/063.md`'s transient-EOF line is annotated.

## Out of scope

- Unblocking the rest of `w4/m116` (t003, t005 and its closeout have their own gates).
