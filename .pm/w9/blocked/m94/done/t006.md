---
id: w9/m94/t006
title: Simplify
worker: worker9
status: done
estimate: 25m
depends_on: [w9/m94/t005]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Context

- Standing closing task.

## Steps

1. Run `/simplify` scoped to the m94 diff (resolver reuse, no per-route ID ad-hoc parsing).

## Files

- (whatever m94 touched)

## Acceptance criteria

- [ ] `/simplify` findings applied or explicitly declined with reasons.

## Out of scope

- Behavior changes.

## Outcome (2026-09-20)

Reviewed the m94 diff for reuse/simplification: id resolution lives in exactly
one helper pair (`canonicalID` / `resolveSandbox`) with no per-route parsing,
and an earlier attempt to carry a second `Sandbox.SubstrateID` field was
withdrawn — it forced every producer (including test fakes) to set two ids and
bought nothing, since an unstamped agent-session sandbox's canonical id already
IS its substrate id. Known cost, accepted and documented: resolving a public id
costs one tenant-scoped list, bounded by the workspace's live sandbox capacity.
