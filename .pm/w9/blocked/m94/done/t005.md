---
id: w9/m94/t005
title: Render parity
worker: worker9
status: done
estimate: 30m
depends_on: [w9/m94/t004]
---

## Objective

Confirm the `sbx-` ID reads identically on REST, GraphQL, and MCP, and matches Render's sandbox ID shape.

## Context

- Standing closing task (render parity included: tenant-facing ID change on three surfaces).

## Steps

1. Compare `id` on `GET /v1/sandboxes`, the sandbox GraphQL type, and the MCP sandbox tool for one fixture.
2. Compare against Render's documented `sbx-` shape; flag drift as follow-up, never silently diverge.

## Files

- `lego/backend/internal/sandbox/rest.go`
- `lego/backend/internal/sandbox/graphql.go`
- `lego/backend/internal/sandbox/mcp.go`

## Acceptance criteria

- [ ] Same canonical ID on all three surfaces, evidenced.
- [ ] Any drift filed as follow-up work, not silently left.

## Out of scope

- Dashboard sandbox UI (none exists for this surface).

## Outcome (2026-09-20)

`TestCanonicalIDAgreesAcrossRESTGraphQLMCP` reads one stamped sandbox through
REST get, the GraphQL `sandboxes` query, and the MCP `list_sandboxes` tool and
asserts a single identical `sbx-` id — the three surfaces all serialize the same
`Sandbox` value, so there is one canonical id by construction. Shape matches
Render's documented `sbx-` ids (and the pinned client's own parser). No drift
found, so nothing filed as follow-up.
