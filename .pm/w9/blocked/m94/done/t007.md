---
id: w9/m94/t007
title: Test coverage
worker: worker9
status: done
estimate: 45m
depends_on: [w9/m94/t005]
---

## Objective

Cover the ID mapping's failure modes with behavioral tests: unmapped IDs, cross-workspace IDs, token bound to a stale form.

## Context

- Standing closing task. t003 pins the contract; this task covers the failure neighbors.

## Steps

1. Unmapped/foreign ID → non-enumerating 404 on get/exec-mint/stop (no existence leak between forms).
2. Token minted against one form refused when redeemed for the other (where applicable).
3. Race-free mapping under concurrent create/resolve (if a mapping table was added).

## Files

- `lego/backend/internal/sandbox/*_test.go`

## Acceptance criteria

- [ ] Failure-mode tests fail pre-fix logic and pass post-fix; no tautological asserts.

## Out of scope

- Load/stress testing.

## Outcome (2026-09-20)

Failure neighbors covered in `publicid_test.go`: an id that was never minted, a
stamped id belonging to another workspace, and a vanished legacy UUID all return
the same named 404 on get and stop (no existence leak between forms, never a
500); the exec path is proven to bind the substrate id; and the agent-session
carve-out is pinned by a test that fails if a public id is ever stamped on that
path. No concurrency test was needed — no mapping table was added.
