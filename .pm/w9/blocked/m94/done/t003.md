---
id: w9/m94/t003
title: Contract + regression tests for the ID shape
worker: worker9
status: done
estimate: 40m
depends_on: [w9/m94/t002]
---

## Objective

Pin the `sbx-` contract so no future substrate or mapping change can silently return UUIDs again.

## Context

- Precedent: `lego/cli/sandbox_contract_test.go` drives the pinned `pkg/sandbox.Repo` against `lego/cli/testdata/sandbox-exec-contract.json`; extend the pattern to IDs.
- Pinned parser under test: `cmd/sandboxcopy.go` `parseSandboxCopyArgs`.

## Steps

1. Add a backend test asserting create/list/get return `^sbx-` IDs and resolve legacy UUIDs (table: create, get, list, exec-mint, stop).
2. Add a CLI-side test feeding a bex-minted `sbx-` ID through the pinned `parseSandboxCopyArgs` (upload + download arg orderings) — it must parse, not error.
3. Add a negative: a bare UUID still fails the parser (documents why the server must never emit one).

## Files

- `lego/backend/internal/sandbox/service_test.go`
- `lego/cli/sandbox_contract_test.go` (or a focused new test file)

## Acceptance criteria

- [ ] New failing-before/passing-after tests in both modules.
- [ ] `cd lego/cli && go test ./...` and backend `go test ./internal/sandbox/` green.

## Out of scope

- w7/m150's transfer-behavior tests (that milestone owns them).

## Outcome (2026-09-20)

`lego/backend/internal/sandbox/publicid_test.go` (create mints + stamps; list /
get / stop report the canonical id and dual-accept both forms; unknown, foreign,
and legacy ids all 404 non-enumerating; the exec ticket binds the substrate id
and its pod name). `lego/cli/sandbox_id_contract_test.go` drives the REAL bex
binary through the pinned `ea sandboxes copy`: a bex-shaped `sbx-` id parses and
reaches `POST /v1/sandboxes/<id>/files/upload/token`, while the exact UUID
production used to mint is refused client-side with `must be a sandbox path` and
sends nothing. Mutation-checked: reverting `canonicalID` to `raw.ID` fails the
two backend contract tests.
