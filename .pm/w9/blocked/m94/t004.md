---
id: w9/m94/t004
title: Live-verify with the distributed CLI, update evidence
worker: worker9
status: todo
estimate: 40m
depends_on: [w9/m94/t003]
---

## Objective

Prove the shipped behavior end to end with the real `bex` binary and record dated evidence in the compatibility ledger.

## Context

- QA baseline 2026-09-20 (pre-fix): UUID `c372c97e-…` minted; copy rejected client-side; exec exit-7/stop/list verified working on UUID IDs.

## Steps

1. Against a deployed API with the fix: `bex ea sandboxes create --plan=starter --timeout=600 -o json` → record the `sbx-` ID.
2. `bex ea sandboxes copy ./probe <sbx-id>:/tmp/probe` → must pass arg parsing (server 404 is acceptable until w7/m150; client-side rejection is not).
3. `exec -- true` (exit 0), `list` (shows the `sbx-` ID), `stop --confirm`, `list` (absent).
4. Update `docs/cli-compatibility-checklist.md` `ea sandboxes` rows with the dated result; delete the fixture.

## Files

- `docs/cli-compatibility-checklist.md`

## Acceptance criteria

- [ ] All four CLI journeys evidenced with dates, IDs redacted to shape (`sbx-…`).
- [ ] Fixture deleted; no `qa-` residue in the target workspace.
- [ ] Checklist diff cites this milestone.

## Out of scope

- The m150 byte round trip (owned by w7/m150/t005).

## Status 2026-09-20 — BLOCKED (gate)

The server-side change is implemented, tested, and shipped, but this task needs
the fix RUNNING on a deployed API: the live probes address production
(`https://api.bex.co/v1/`) with the distributed `bex` binary, and production
serves the pre-fix image until this commit deploys. Nothing here can be
evidenced before that.

Run after the deploy lands (workspace `bex-canary`, as the source hunt used):

1. `bex ea sandboxes create --plan=starter --timeout=600 -o json` → expect `"id": "sbx-…"`.
2. `bex ea sandboxes copy ./probe <sbx-id>:/tmp/probe` → must pass arg parsing (a server 404 is the expected outcome until w7/m150 ships; a client-side `must be a sandbox path` is a failure).
3. `bex ea sandboxes exec <sbx-id> -- true` → exit 0; `list` shows the `sbx-` id; `stop --confirm`; `list` no longer shows it.
4. Replace the "live re-verification outstanding" sentence in the `ea sandboxes create` row of `docs/cli-compatibility-checklist.md` with the dated result.
