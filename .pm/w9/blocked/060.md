# name-conflict-e2e.sh pins Hydra v2.2.0, which cannot pass auth-bootstrap-client.sh's lifespan assert

Why: the bootstrap script asserts `device_authorization_grant_access_token_lifespan` round-trips on the Render CLI client, and empirical probing (w5/m86 t007, 2026-09-07) shows neither Hydra v2.2.0 nor v2.3.0 stores that client field (only `refresh_token_grant_access_token_lifespan`); v26.2.0 round-trips both — so `scripts/name-conflict-e2e.sh`'s bootstrap step (Hydra pin at its line ~112) fails today and needs the pin bumped to v26.x, then a full run of that script to confirm nothing else in it depends on v2.x behavior. Surfaced while building `scripts/auth-obs-e2e.sh`, which pins v26.2.0 for this reason; `auth-oauth21-e2e.sh` (v2.2.0) is unaffected because it never runs the bootstrap script's lifespan assert.

## Scheduling

Transferred from `w5/052` to `w9/060` by user approval on 2026-09-07. Original evidence above is preserved; this replaces the original queue entry, not a completed implementation.

- **Worker:** worker9. **Estimate:** 40–60m. **Depends on:** Docker available; priority 3 after w9/059 (scheduling only).
- **Goal:** reliable acceptance coverage for the hosting resource-name conflict contract.
- **Files:** `scripts/name-conflict-e2e.sh`; reference `scripts/auth-obs-e2e.sh` and `scripts/auth-bootstrap-client.sh`.
- **Acceptance:** use the Hydra version proven by the sibling auth-observability harness, run the complete name-conflict scenario, record dated results, and confirm fixture cleanup. Startup alone is insufficient.
- **Render parity:** omitted; test harness repair changes no tenant-facing contract.

## 2026-09-16 — pin bumped; BLOCKED on the verification run

**The code half is done.** `scripts/name-conflict-e2e.sh:112` now pins `oryd/hydra:v26.2.0` (was `v2.2.0`), with a comment recording why, mirroring `scripts/auth-obs-e2e.sh:150` — the sibling harness that already proved this version.

Premise re-verified before changing anything:

- The lifespan assert at `scripts/auth-bootstrap-client.sh:229-235` is **unconditional** — it loops both `device_authorization_grant_access_token_lifespan` and `refresh_token_grant_access_token_lifespan` and fails with "hydra too old for per-client lifespans?".
- `name-conflict-e2e.sh:126` runs that bootstrap script, so its own `v2.2.0` pin could never pass.
- The note's claim that the sibling `v2.2.0` pins are unaffected **holds**: `auth-oauth21-e2e.sh` and `webhooks-verify.sh` both pin `v2.2.0` but neither calls `auth-bootstrap-client.sh` (verified by grep across `scripts/*.sh`). They were correctly left alone.

**What is NOT done — this is the whole remaining gate.** The note's acceptance is explicit that "startup alone is insufficient": the complete name-conflict scenario must run, with dated results and confirmed fixture cleanup. **That run has not happened.** The pin bump is therefore unverified beyond static reasoning.

**Blocker: the local CAPD app cluster is not usable.** The script requires `KUBECONFIG=infra/local/bex.kubeconfig` with the App CRD installed (`name-conflict-e2e.sh:28-29,101-102`); it creates real `App` CRs. Sequence observed:

1. `kubectl` default context was `hetzner-prod` — **not** used; running this script against production would create fixtures there.
2. The stored kubeconfig pointed at `127.0.0.1:55003`, refused — root cause was the CAPD load balancer `bex-lb` having **exited 2 days ago**. Restarting it brought the API up on `:55000`, but the CAPI-issued client cert no longer verified against it, and CAPI reported the cluster `AVAILABLE False / CP AVAILABLE 0`.
3. `bash scripts/mock-cluster.sh` was run (CLAUDE.md pre-approves it, including reprovision when diagnostics point there). It reinstalled the management cluster and CAPI, then **timed out waiting for the app cluster to provision**, leaving the previous `bex` app cluster deleted.
4. A cluster rebuild was subsequently observed **in flight from another actor** (a freshly recreated `bex-mgmt`, namespaces 16s old, while this session's process had already exited) — so it was left alone rather than raced with a second `mock-cluster.sh`.
5. Current state: `bex` and `bex-mgmt` both exist, `bex-lb` is back on `:55003`, but the cluster still reports `CP AVAILABLE 0 / AVAILABLE False`, and `infra/local/bex.kubeconfig` has been rewritten to the in-network address `172.22.0.3:6443`, which is unreachable from the host.

**To clear this:** a healthy local mock cluster with the App CRD installed and a host-reachable `infra/local/bex.kubeconfig`, with no other session mid-rebuild. Then run `KUBECONFIG=infra/local/bex.kubeconfig bash scripts/name-conflict-e2e.sh` to completion, record the dated result here, and confirm fixture cleanup (the script's trap removes the `App` CRs and the throwaway Hydra/Postgres containers). Ports it needs free: 24491/24492 (Hydra), 25491 (Postgres), 18291/18293 (api/cp).
