---
id: w9/m165/t006
title: Simplify the code this milestone changed
worker: worker9
status: todo
estimate: 30m
depends_on: [w9/m165/t005]
---

## Objective

Run `/simplify` over this milestone's diff — reuse, simplification, efficiency, altitude — behavior-preserving only.

## Context

- The likely target is the create-side command resolution in `rest.go:455-505`, which currently interleaves runtime branching, top-level/nested precedence, and a type-specific cron bridge in one block. If t001 added a fourth condition to it, that block is the thing to lift into a named helper.
- The read projection (`render.go:489-505`) and the blueprint generator hold near-duplicate knowledge of the same mapping; if t002 left two copies, collapse them.

## Steps

1. Run `/simplify` over the diff.
2. Apply behavior-preserving findings; defer anything semantic to its own note.

## Files

- Whatever t001/t002/t004 touched.

## Acceptance criteria

- [ ] Findings applied or explicitly deferred with a reason.
- [ ] t001/t002/t004 acceptance checks still pass.

## Out of scope

- Bug hunting (that is `/code-review`).
