---
id: w1/m147/t007
title: Test coverage
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m147/t005]
---

## Objective

Meaningful tests for what m147 shipped, with failure modes first.

## Steps

1. **Backend:**
   - over-quota refusals on every path, for env and files, bytes and counts, with no store write and no App patch;
   - an in-quota control save rolls out;
   - staged `save_only` mode is bounded too;
   - shrinking an already-over-quota map follows the t004 rule;
   - the env-group bulk patch is still refused (control).
2. **Guard:** the t002 test is proven red with the check removed.
3. **Dashboard:**
   - aggregate draft validation at the boundary (512 KiB and 512 KiB + 1; 500 and 501 files);
   - the upload path uses the aggregate check;
   - a server refusal shows the server's sentence, and a transport failure shows the generic copy.

## Acceptance criteria

- [x] Each new test is shown failing on the pre-fix code.
- [x] Backend `go test ./internal/secrets/... ./internal/envgroups/...`, the dashboard services suite, and lint are green.

## Result (2026-09-15)

1. **Backend** (`secrets/batch_quota_test.go`):

   - over-quota refusals for file bytes (the 614,400-byte upload), the 501st file, env bytes and the 501st key, each in both `deploy` and `save_only` modes, with no store write and no App patch;
   - a mixed patch restores the env map when its files half is refused;
   - the revision-aware path refuses an oversized value with no CAS write;
   - the in-quota control rolls out;
   - the t004 rule on an already-over-quota map: a delete is allowed, a same-size edit is allowed, and an add is refused with the store unchanged;
   - the refusal is identical across REST, GraphQL and MCP.

   The env-group control's three surfaces refuse identically too (`envgroups/quota_guard_test.go`).

2. **Guards.** `secrets/quota_guard_test.go` and `envgroups/quota_guard_test.go` each run every exported writer plus a reflection sweep. The secrets guard fails on `PatchEnvironment` with `batch.go` reverted to `HEAD`.
3. **Dashboard** (`environment-draft.test.ts`):

   - exactly 512 KiB is valid, one byte over is flagged;
   - the 614,400-byte upload is flagged;
   - size is measured in UTF-8 bytes, not characters;
   - the 500th file is valid, the 501st is flagged;
   - the 501st env var is flagged, as is an oversized value;
   - saved opaque rows are never flagged, so shrinking stays possible;
   - a more specific error wins over the limit.

   The upload path reads files up to the 1 MiB read guard into the draft, so the aggregate validation flags them inline. Server refusal and transport copy for this editor's save were covered by `w1/m145` (`mutationErrorMessage` at `service-environment-editor.tsx`'s save) and verified live there (m145 § Live re-probe 6).

4. **Red proofs.**
   - Backend, with `batch.go` at `HEAD`: every new batch-quota test fails, and so does the secrets guard's `PatchEnvironment` case.
   - Dashboard, with `environment-draft.ts` at `HEAD` (only the two new constants kept so the test file compiles): the 5 limit-flagging tests fail. The 6 that still pass assert valid drafts or pre-existing behavior.
   - Both files were restored byte-for-byte.
5. **Green.**
   - `go test ./...` across the backend;
   - `golangci-lint ./internal/secrets/... ./internal/envgroups/...` reports 0 issues;
   - dashboard `services` and `env-groups` pass (86 files / 639 tests);
   - `yarn typecheck` and `yarn lint` pass.
