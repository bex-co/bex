---
id: w1/m162/t004
title: "Check in `examples/shared-package-monorepo` as the build fixture"
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m162/t002]
---

## Objective

Turn the untracked scaffold into a tracked example with a README and a `render.yaml` declaring a Node web service at `rootDir: apps/api` and a worker at `rootDir: apps/worker-api`, both importing `packages/shared`. It is the fixture t006 deploys and the regression case for t009.

## Context

- Untracked today: `examples/shared-package-monorepo/{package.json,apps/api/package.json,apps/worker-api/package.json,packages/shared/{package.json,index.mjs}}`; `package.json` references `scripts/rehearse-changes.mjs` and `test/*.test.mjs`, which do not exist yet.
- Sibling examples: `examples/whoami-app.yaml`, `examples/hello-go/`.
- `scripts/app-apply.sh <render.yaml>` turns a `render.yaml` into App CRs.

## Steps

1. Add `apps/api/server.mjs` (HTTP on `PORT`, answers `sharedGreeting("api")`) and a minimal `apps/worker-api` entry; drop or implement the dangling `rehearse`/`test` scripts.
2. Add `render.yaml` with `rootDir` per service and a build filter including `packages/shared/**`.
3. Add a README: what the example proves and how to deploy it with `scripts/app-apply.sh`.

## Files

- `examples/shared-package-monorepo/**`

## Acceptance criteria

- [ ] `npm install && npm run start:api` works locally from the repo root.
- [ ] `DRY_RUN=1 scripts/app-apply.sh examples/shared-package-monorepo/render.yaml` prints two Apps with the expected `rootDir`s.

## Out of scope

- Any bex code change.
