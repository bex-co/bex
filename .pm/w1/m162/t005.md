---
id: w1/m162/t005
title: "Correct ADR004 and ADR018 row 69"
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m162/t002, w1/m162/t003]
---

## Objective

Rewrite the rootDir sentences in ADR004 (:186, :284) to the new contract — rootDir is the working directory, the whole repo is the context/clone — and correct ADR018 row 69, which today claims ✅ on scoping alone. Cite t001's artifact. State the trigger-scoping rule (a shared package outside rootDir needs a build filter) as parity, not divergence.

## Context

- `docs/ADR004-app-deployment.md:186,284`
- `docs/ADR018-render-parity.md:69`
- `docs/render-artifacts/root-directory-monorepo.md` (t001)

## Steps

1. Edit the three locations; keep the row ✅ only if t002+t003 make it true, otherwise mark ◐ with the exact gap.
2. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR004-app-deployment.md`
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] ADR004 no longer describes rootDir as scoping the build context.
- [ ] ADR018 row 69 cites the artifact and reflects the shipped behavior.

## Out of scope

- Other ledger rows.
