---
id: w1/m162/t006
title: "Live: the example monorepo deploys with `rootDir: apps/api` and serves the shared greeting"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m162/t004, w1/m162/t005]
---

## Objective

Prove the definition of done on a real cluster. Deploy the example on `dev-1` (or production with explicit approval), first on the pre-fix operator to capture the failing build (`npm install` cannot resolve `@shared-package-monorepo/shared`), then on the fixed operator, and record both in the milestone README.

## Context

- `bash scripts/dev-env.sh 1 up` — this workstream's stack; `scripts/app-apply.sh examples/shared-package-monorepo/render.yaml`.
- The failing symptom must be captured before the fix rolls, per the m156–m158 pattern.

## Steps

1. Capture the pre-fix build failure (log excerpt, timestamp).
2. Roll the fixed operator; redeploy; curl the service URL.
3. Record both observations in the milestone README under `## Live`.

## Files

- `.pm/w1/m162/README.md`

## Acceptance criteria

- [ ] The pre-fix build log shows the unresolved workspace package.
- [ ] Post-fix, the service answers `hello-from-shared:api:1.0.0`.

## Out of scope

- Production rollout (that is `deploy.yml`'s job once shipped).
