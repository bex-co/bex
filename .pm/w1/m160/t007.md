---
id: w1/m160/t007
title: Test coverage
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m160/t005]
---

## Objective

Add tests that fail on each defect: the never-served phase decision (failed build and cancel) and the worker transition that never ends.

## Files

- `lego/operator/internal/controller/*_test.go`

## Acceptance criteria

- [x] A test fails when `fail` keys on `status.image`.
- [x] A test fails when the canceled-release path keys on `status.image`.
- [x] A test fails when a worker's transition never reaches Ended.
- [x] The served-predecessor control is pinned.

## Out of scope

- Coverage of untouched controller areas.
