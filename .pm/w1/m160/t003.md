---
id: w1/m160/t003
title: 'Blast radius: every "a prior release exists" decision, and every path that starts a transition'
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m160/t001, w1/m160/t002]
---

## Objective

Enumerate every decision keyed on `status.image` versus `status.activeRevision`, and every path that starts an autoscaling transition without a matching completion, so neither defect survives somewhere else.

## Steps

1. Grep the controller for `Status.Image` and classify each use: identity of a served release, or a plain image reference.
2. Do the same for transition starts, listing which path completes each.
3. Record the table in the m160 README, with anything left unfixed filed as an inbox note.

## Files

- `lego/operator/internal/controller/`
- `.pm/w1/m160/README.md`

## Acceptance criteria

- [ ] The README carries both tables, each row either covered by a test or explicitly out of scope.

## Out of scope

- Fixing unrelated defects the sweep turns up; file them.
