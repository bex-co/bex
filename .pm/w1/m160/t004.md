---
id: w1/m160/t004
title: "Live: a never-served first release, then a failed build"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m160/t003]
---

## Objective

Run the m160 definition of done on production after the fix deploys, recording the pre-fix result first.

## Context

- Fixture: a throwaway free web service `qa-<yyyymmdd>-…` from `bex-co/bex` `examples/hello-go`, made to crash-loop on its first release (for example an entrypoint that exits non-zero), so `status.image` is set and `status.activeRevision` is not.
- Then a second release whose build fails (a missing `dockerfilePath`), and a third that is canceled mid-build.
- Autoscaling is paid-only: record the worker bullet as test-only unless a paid worker is approved.

## Steps

1. Capture the pre-fix phase for both the failed-build and the canceled cases.
2. After the fix deploys, repeat and record the transcripts in the m160 README.
3. Run the served-predecessor control.
4. Delete every fixture.

## Files

- `.pm/w1/m160/README.md`

## Acceptance criteria

- [ ] Each DoD bullet has a live transcript or a recorded reason it is test-only.
- [ ] All fixtures are deleted.

## Out of scope

- Buying a plan.
