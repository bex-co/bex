---
id: w1/m148/t003
title: Restart copy and confirmation tell the truth, and the stale `BuildCommit` comment is corrected
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m148/t001]
---

## Objective

After t001, the restart confirmation describes exactly what restart does, and both dashboard restart entry points ask the same question.

## Context

- Row confirmation copy: `dashboard/src/features/services/locales/en.ts:311-313` ("The service's pods roll with no downtime…", plus zh).
- Header **Restart service** has no confirmation (`manual-deploy-button.tsx:110-115`).
- Stale comment: `lego/backend/internal/deploys/service.go:401-403`.

## Steps

1. Rewrite the confirmation to match t001's mechanism. For example: "Restarts the running release (commit `abc1234`) with no downtime. It does not deploy newer commits." For a rebuild mechanism, say that it rebuilds the same commit.
2. Give the header Restart the same confirmation as the row.
3. Correct the `Trigger` doc comment to describe the real pinning behavior.

## Acceptance criteria

- [ ] Both restart entry points show the same, accurate confirmation (component tests plus one live check).
- [ ] The comment matches `deploys/service.go:552-561`.
