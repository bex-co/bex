---
id: w1/m139/t009
title: Test coverage
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m139/t007]
---

## Objective

Every in-task fix from t002–t006 carries a regression test that fails without the fix. Where a live check passed but the code path had no test pinning the observed behavior (e.g. per-type scaling policy, `sync: false` preservation on apply), add one so the next "reasoned from code shape" claim has a test to point at instead.

## Context

- Backend table tests under the touched `internal/*` packages; operator envtest for reconcile-side observations; `scripts/dev-env.test.sh` for any harness fix.

## Steps

1. List each fix and each newly observed behavior; add or extend one test per item.
2. Mutation spot-check: revert one fix locally and confirm its test fails.

## Files

- Test files beside the touched code.

## Acceptance criteria

- [ ] Each fix has a failing-without-fix test.
- [ ] Per-type scaling policy and `sync: false` round-trip are pinned by tests.

## Out of scope

- Tests for behaviors this sweep did not touch.
