---
id: w1/m139/t005
title: "Blueprint round-trip: export for disk/cron/static/health-check kinds + sync:false on apply (m114); MCP + render.yaml create legs and the first-deploy Canceled probe (m130 / m46 residue)"
worker: worker1
status: todo
estimate: 60m
depends_on: [w1/m139/t001]
---

## Objective

m114 fixed the exporter emitting the tenant-prefixed CR name and verified only a `web` service; whether the generated `render.yaml` is valid for other kinds and whether `sync: false` round-trips on apply were left open. m130 left `w6/m46`'s DoD residue standing: the MCP and `render.yaml` create surfaces and the first-deploy `Canceled` probe were never exercised after the fix. Exercise all of it on dev-1.

## Context

- `.pm/w6/done/m114/README.md:120-` — four carried items: other kinds (disk, cron schedule, static publish path, health-check path); whether REST/GraphQL/MCP expose the generate verb; `sync: false` round-trip; any other internal identifier in the output.
- `.pm/w6/done/m130/README.md:118-` and `.pm/w6/done/m46/README.md:86` — MCP + Blueprint create legs and the `Build queued → Deploy canceled` first-deploy symptom.
- Exporter/generate verb: `lego/backend/internal/blueprints/`; validator: `render blueprints validate` via `lego/cli` (`scripts/cli-compat.sh` shows the token dance) or the REST validate endpoint.
- ADR049 is the `render.yaml` contract.

## Steps

1. On dev-1 create: a `web_service` with `spec.disk` and a health-check path, a `cron_job` with a schedule, a `static_site` with a publish path, and an env var with `sync: false`. Export the Blueprint via REST, GraphQL, and MCP (record which surfaces expose the verb).
2. Validate the export (CLI `blueprints validate` and the REST validator); grep it for any `tea-`/CR-internal identifier.
3. Apply the export back via the Blueprint create/sync leg over REST and via MCP; confirm every resource plans `update` (not `create`) and the `sync: false` env var is left untouched.
4. Create a fresh `background_worker` via MCP and another via `render.yaml`; watch the first deploy from `Build queued` — it must reach `Build started` and a terminal state other than an unexplained `Canceled`.
5. Evidence lines; annotate m114 / m130 / m46 bullets; fix ≤30m defects with a test, file the rest.

## Files

- `.pm/w1/m139/README.md`, the three source READMEs
- `lego/backend/internal/blueprints/` only on a confirmed defect

## Acceptance criteria

- [ ] Exports for all four kinds validate; no internal identifier appears; verb availability per surface recorded.
- [ ] Re-apply plans `update` for every resource and preserves the `sync: false` value.
- [ ] Both first deploys reach a real terminal state with a recorded cause; no unexplained `Canceled`.

## Out of scope

- The Blueprint unsupported-field subset (per-resource `region`, `checksPass`, …) — documented structural non-goals; and live auto-sync, owned by `w8/m38`.
