---
id: w1/m139/t006
title: "Small live checks: cron 99 99 * * * → 400 with the App still healthy (w9/m91); a pre-fix private_service loses its Ingress on reconcile (m46)"
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m139/t001]
---

## Objective

Two short live checks their milestones called "a formality" and "worth doing once deployed", neither run.

## Context

- `.pm/w9/done/m91/README.md:36-37` — save `99 99 * * *` on a live cron job → 400 on every surface, and the App stays healthy (no reconcile loop). Parser: `lego/backend/internal/apps/service.go:1145,3693` (`cron.ParseStandard`).
- `.pm/w6/done/m46/README.md:54` — a `private_service` that had an Ingress before the fix keeps it until the operator reconciles it; confirm reconcile removes it and that the orphaned TLS Secret is inert. Simulate "pre-fix" by hand-creating an Ingress with the operator's labels for a fresh private service, then trigger a reconcile.

## Steps

1. On dev-1: create a `cron_job`; PATCH its schedule to `99 99 * * *` via REST, GraphQL, MCP, and the dashboard form — each must refuse with 400 / validation error and the same message; `kubectl get apps.app.bex.co` shows the App unchanged and `Ready`; operator logs show no error loop.
2. Create a `private_service`; confirm no Ingress. Hand-apply an Ingress carrying the operator's owned-artifact labels for it; annotate the App to force a reconcile; confirm the Ingress is deleted and the service is unreachable on the platform subdomain. Record what happens to any TLS Secret.
3. Evidence lines; annotate the two source bullets; fix/file.

## Files

- `.pm/w1/m139/README.md`, `.pm/w9/done/m91/README.md`, `.pm/w6/done/m46/README.md`

## Acceptance criteria

- [ ] Four-surface 400 recorded with identical error shape; App healthy afterwards.
- [ ] Ingress removal on reconcile observed; subdomain returns the platform 404, not the private service.

## Out of scope

- Changing the schedule grammar; Render's own form behavior (documented in ADR038).
