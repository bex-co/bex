---
id: w1/m139/t003
title: "Free-tier sleep and wake: create → idle past the window → hibernate → wake on request (m116); note how wake-up requests are labelled in Loki"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m139/t001]
---

## Objective

m116 fixed `idleTTLSeconds: 0` disabling auto-sleep and closed on envtests that force the idle state; the real create → idle → hibernate → wake sequence was never run. Run it once, end to end, on dev-1.

## Context

- `.pm/w6/done/m116/README.md:36,105` — the unrun DoD sequence and four "unverified this run" items (skip (1) `eden-dash-v3` — production; do (2) an untiered bare-CR App, (3) the `zh` locale string, (4) the activator env question).
- Activator wake path: `lego/operator/cmd/activator/main.go:136-182` (`wakeApp` on inbound request advances `app.bex.co/last-active`); hibernate transition written by `app_controller.go` ~`:1814`.
- Idle window is a spec/tier value — set the shortest value the CRD's CEL rules allow on the test App so the idle observation fits the task.
- Events: `service_hibernated` / `service_woken` appear in the Events feed (deliberately not push-notified, w6/m47).
- While the App is asleep, hit its public host and look at the request's Loki stream labels — `w6/m131:38` reports wake requests carry `app=bex-activator-tea-<xid>-<app>` rather than the App's own name. Record what dev-1 shows; that is the input for the proposed w1 inbox note on activator log attribution.

## Steps

1. Create a free `web_service` with the minimum idle window, plus one bare-CR App with `Tier == ""` via `kubectl apply` in `dev-1`.
2. Wait past the window; confirm `status.phase: Hibernated`, the Deployment scaled to zero, and the `service_hibernated` event on REST + dashboard Events feed.
3. `curl` the public host: interstitial (w1/m40) then the App; confirm `service_woken` and `status.phase: Running`.
4. Query dev-1's Loki for the wake request; record the `app` label value. Check the `zh` copy for the idle-timeout setting renders.
5. Evidence lines; annotate m116's bullets; fix/file.

## Files

- `.pm/w1/m139/README.md`, `.pm/w6/done/m116/README.md`

## Acceptance criteria

- [ ] Hibernate and wake both observed with phase, events, and pod count recorded, for the tiered and the bare-CR App.
- [ ] The Loki `app` label of a wake request is recorded verbatim.
- [ ] Any FAIL fixed with a test or filed with `file:line`.

## Out of scope

- Fixing activator log attribution here (separate inbox note, proposal 5 of the 2026-09-09 brainstorm).
- Anything on production `eden-dash-v3`.
