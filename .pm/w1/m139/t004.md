---
id: w1/m139/t004
title: "Datastore plan parity: GraphQL createKeyValue free-plan forcing (m127), GraphQL/MCP Blueprint plan exposure + live plan update (m125), Key Value parameter capture (m133)"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m139/t001]
---

## Objective

Three datastore-side claims closed as "read from code, not exercised": that GraphQL `createKeyValue` forces the same free-plan persistence as REST, that GraphQL and MCP expose the same Blueprint plan as REST and that databases/key-values plan `update` (not `create`) for existing resources, and that Key Value does not capture operator-owned parameters the way Postgres did before m133.

## Context

- `.pm/w6/done/m127/README.md:112` — GraphQL `createKeyValue` free-plan forcing unverified; REST path is the reference.
- `.pm/w6/done/m125/README.md:116` — GraphQL/MCP plan exposure; databases/key-values `update` planning "read from `:97`/`:111`, not measured".
- `.pm/w6/done/m133/README.md:112-113,150` — REST/MCP parameter **write** paths unexercised; Key Value conflation unchecked. Key Value insights/parameters surface in `lego/backend/internal/keyvalues/`.
- Blueprint plan verbs: `lego/backend/internal/blueprints/` (REST `plan`, GraphQL, MCP tool).

## Steps

1. Create a free Key Value via REST and via GraphQL `createKeyValue`; compare `persistenceMode` and plan on both results and on the KeyValue CR. Then create one through the dashboard form and compare.
2. Create a Postgres + Key Value + web service on dev-1; fetch the Blueprint plan for a `render.yaml` naming all three via REST, GraphQL, and MCP; all three should say `update` for each and agree field-for-field.
3. Postgres: attempt an operator-owned parameter write via REST and via MCP — expect the m133 guard's refusal on both. Key Value: read its parameters surface and check whether any operator-owned key is echoed back into a writable field; try the equivalent write.
4. Evidence lines; annotate the three source bullets; fix ≤30m mismatches with a test, file the rest.

## Files

- `.pm/w1/m139/README.md`, the three source READMEs
- `lego/backend/internal/{keyvalues,blueprints,databases}/` only on a confirmed mismatch

## Acceptance criteria

- [ ] REST vs GraphQL vs dashboard `createKeyValue` outcomes recorded and identical (or FAIL filed).
- [ ] REST/GraphQL/MCP Blueprint plans for the same stack recorded and identical, each saying `update` for existing resources.
- [ ] Postgres parameter guard observed on REST and MCP; Key Value capture question answered with evidence.

## Out of scope

- Changing plan or persistence policy.
