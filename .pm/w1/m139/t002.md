---
id: w1/m139/t002
title: "Scaling matrix: background_worker, private_service, static_site, cron_job follow the same replica policy the web_service m118 scaled live"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m139/t001]
---

## Objective

m118 settled the free/paid instance-count policy and scaled only a `web_service` live; the other four service types were declared identical "by inspection" of shared `Scale` / `effectiveReplicas` code. Observe them.

## Context

- `.pm/w6/done/m118/README.md:61` (3) — the unverified sibling claim; `:20` — the decided policy the writes must match on all four surfaces.
- `POST /v1/services/{id}/scale` — `lego/backend/internal/apps/rest.go:697-736`; GraphQL `scaleService`; MCP scale tool; dashboard Scaling card.
- Operator enforcement of the cap regardless of CR contents (m118 t-tasks) — `kubectl get apps.app.bex.co -n dev-1 -o yaml` shows `spec`/`status` replicas.

## Steps

1. On dev-1 create one free and one paid instance of each type (`background_worker`, `private_service`, `static_site`, `cron_job`) via REST (`scripts/app-apply.sh` or curl against `ports.env`'s API port).
2. For each: scale via REST to N above the free cap, then via GraphQL, then MCP; record the response, the CR's `spec.replicas`/`status`, and the pod count. Expected: identical policy outcome to the m118 web_service capture; `static_site` and `cron_job` should refuse or ignore per their type semantics — record what actually happens.
3. Check the dashboard Scaling card copy for each type matches the observed behavior.
4. Evidence lines into the README; annotate m118's bullet. Fix ≤30m mismatches in-task with a table-test case; file the rest.

## Files

- `.pm/w1/m139/README.md`, `.pm/w6/done/m118/README.md`
- `lego/backend/internal/apps/{service,rest,graphql,mcp}.go` only on a confirmed mismatch

## Acceptance criteria

- [ ] Eight PASS/FAIL lines (4 types × free/paid) with surface, request, response, pod count.
- [ ] Every FAIL fixed with a test or filed with `file:line`.

## Out of scope

- Reopening the m118 policy decision itself.
