---
id: w1/m139/t001
title: "Bring up dev-1 beside the live dev-3 stack; record the two-workstream isolation walk w1/m72/t007 waived"
worker: worker1
status: todo
estimate: 30m
depends_on: []
---

## Objective

Stand up w1's isolated stack while another workstream's stack is genuinely running, and record the isolation walk `w1/m72` closed without — it is the precondition for every other task here and the one piece of m72's DoD that was waived rather than observed.

## Context

- `bash scripts/dev-env.sh 1 up` / `status` / `down`; ports derived from N=1 in `.pm/w1/dev-1/ports.env`; Kratos + Hydra + Mailpit in `dev-1-auth`, apps in `dev-1`.
- `w3/m79` is in progress on `dev-3` (agent-session stack) — do **not** touch `dev-3`, `auth`, `bex-system`, or the standard ports (5173/4433/4445/8090/8091/5432).
- `.pm/w1/done/m72/README.md:3,17` — the waived walk: two workers' stacks up at once, both `status` green, each dashboard's Kratos flows return to its own origin.
- `w1/080` added `VITE_SSR_API_URL` guidance and the SSR-port preflight warning; confirm it fires correctly or stays silent as appropriate.

## Steps

1. Confirm `dev-3` is up (`bash scripts/dev-env.sh 3 status`, read-only). Then `bash scripts/dev-env.sh 1 up` and `1 status` — all checks pass.
2. Record: both `status` outputs green at the same time; `kubectl get ns` shows `dev-1`, `dev-1-auth`, `dev-3*` side by side; no port in `dev-1/ports.env` appears in `dev-3/ports.env`; a Kratos error flow started from the dev-1 dashboard origin returns to the dev-1 origin.
3. Write the evidence lines into `.pm/w1/m139/README.md` § Evidence; annotate `.pm/w1/done/m72/README.md` t007 row with `→ walked live in w1/m139/t001 (date)`.

## Files

- `.pm/w1/m139/README.md`, `.pm/w1/done/m72/README.md`
- `scripts/dev-env.sh` only if a real defect blocks bring-up (fix + `scripts/dev-env.test.sh` case)

## Acceptance criteria

- [ ] `dev-1` and `dev-3` are simultaneously green with disjoint ports and namespaces; evidence recorded.
- [ ] Dashboard against dev-1 logs in and lands on its own origin after an auth error flow.
- [ ] Any bring-up defect found is fixed with a test or filed with `file:line`.

## Out of scope

- Anything on `dev-3` beyond read-only `status`.
