---
id: w1/m152/t006
title: Simplify
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m152/t005]
---

## Objective

Run `/simplify` over the code m152 changed (release config snapshots, the canceled settle, the truth invariant and the pending indicator). Keep it behavior-preserving; for example, one snapshot helper shared by service Secrets and group Secrets.

## Acceptance criteria

- [ ] `/simplify` findings are applied or recorded as declined, with the reason.
- [ ] Operator `make test`, backend `go test ./...`, dashboard `yarn test` and lint are green.
