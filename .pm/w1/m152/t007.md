---
id: w1/m152/t007
title: Test coverage
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m152/t005]
---

## Objective

Meaningful tests for what m152 shipped.

## Steps

1. **Operator envtest:**
   - canceled `config_change` over a prior release leaves the Deployment template (and its pods) unchanged;
   - an old-release pod restarting after a save reads the release's snapshot values;
   - m52 (no prior release) and m104 (image restore) controls.
2. **Backend:** cancel leaves the saved env value in place, and the next deploy ships it.
3. **Dashboard:** the pending-change indicator after a canceled config deploy clears after the next successful deploy.

## Acceptance criteria

- [ ] Each new test is shown failing on the pre-fix code.
- [ ] Operator `make test`, backend `go test ./...`, dashboard `yarn test` and lint are green.
