---
id: w1/m158/t001
title: "Worker replica convergence under both holds: a scale-only patch of the prior Deployment plus the worker's parked or running status"
worker: worker1
status: todo
estimate: 1h
depends_on: []
---

## Objective

While a worker's newer release waits on its pre-deploy step or its image, keep its replicas following suspend, resume, manual scale and autoscale on the prior release's Deployment, and report the parked or running status. There is no Service or Ingress to converge.

## Context

- **The pre-deploy hold.** `holdUnpassedRelease` returns `held=false` for a worker (`app_controller.go:4588`).
- **The artifact hold.** It comes from `w1/m157`, scoped to web and private services.
- **Worker status.** `reconcileWorkerStatus` (`:2590-2627`) writes the worker's parked and running status.

## Steps

1. Give both holds a worker branch: a scale-only patch of the existing Deployment, with the template kept on the prior release.
2. Report Hibernated when suspended, otherwise Running from the scale this pass wrote, keeping the failed verdict's Ready message where one stands.
3. Keep a first release, and a worker with no Deployment, on today's path.

## Files

- `lego/operator/internal/controller/app_controller.go`

## Acceptance criteria

- [ ] Suspend, resume and manual scale over a failed pre-deploy verdict or a failed build move a worker's replicas on the prior template, with no Job created.
- [ ] A worker's manual scale while its pre-deploy step runs takes effect, and the release rolls once the step passes.
- [ ] `make test` passes.

## Out of scope

- Web and private services (`w1/m156`, `w1/m157`).
