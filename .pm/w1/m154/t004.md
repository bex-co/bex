---
id: w1/m154/t004
title: Simplify
worker: worker1
status: todo
estimate: 15m
depends_on: [w1/m154/t003]
---

## Objective

Run `/simplify` over the code m154 changed: the lifecycle projection, the grace-period helper and any per-path handling. Keep it behavior-preserving. For example, compute the drain and grace period in one helper rather than at each call site.

## Acceptance criteria

- [ ] `/simplify` findings are applied, or recorded as declined with the reason.
- [ ] Operator `make test` and `make lint` are green.
