---
id: w1/m154/t005
title: Test coverage
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m154/t003]
---

## Objective

Meaningful tests for what m154 shipped.

## Steps

1. **Unit** (projection):
   - a web or private App gets `preStop.sleep` with the chosen seconds;
   - its grace period is `drain + maxShutdownDelaySeconds`, or `drain + 30` when unset;
   - a disk-attached App keeps `Recreate` with no drain change, and the worker behavior matches t002's decision;
   - a second projection over the stored object is a no-op (no perpetual roll).
2. **Envtest:** reconciling an existing App that lacks the drain rolls it exactly once. `maxShutdownDelaySeconds` round-trips unchanged through the CR.
3. **Controls:** the existing probe-split tests (`w7/m80`), the disk `Recreate` tests and the rollout-headroom guards in `scripts/gitops-validate.sh` pass unchanged.

## Acceptance criteria

- [ ] Each new test is shown failing on the pre-fix code.
- [ ] Operator `make test` and `make lint` are green.
