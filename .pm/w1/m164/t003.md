---
id: w1/m164/t003
title: "Reconciler test for both shapes plus an envtest for crash-loop-then-cancel"
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m164/t002]
---

## Objective

Pin the decision: a store-reconciler test that closes the deploy row as `canceled` with no successful generation for the never-served shape and identically to today for the legacy shape, and an operator envtest for a first release that never becomes Ready followed by a cancel.

## Context

- `lego/backend/internal/store/reconciler_test.go` (or the file beside `reconciler.go`); the PG-gated suite needs `BEX_TEST_DB_URI`.
- `lego/operator/internal/controller/` envtests for release identity (`release_identity_test.go`).

## Steps

1. Add the two reconciler cases.
2. Add the envtest; assert `status.releaseGeneration` and the deploy row status.
3. Run both suites (backend against a real Postgres).

## Files

- `lego/backend/internal/store/reconciler_test.go`
- `lego/operator/internal/controller/release_identity_test.go`

## Acceptance criteria

- [ ] Both tests fail on the pre-t002 code and pass after.

## Out of scope

- Broader deploy-row lifecycle tests.
