---
id: w1/m155/t007
title: Render parity
worker: worker1
status: todo
estimate: 20m
depends_on: [w1/m155/t006]
---

## Objective

REST, GraphQL, MCP and the dashboard agree on the metrics contract m155 changed, and every remaining difference from Render is written down.

## Steps

1. **Same answer on every surface.** For one fixture and window, REST `aggregateBy=statusCode`, GraphQL `aggregateBy:["STATUS_CODE"]` and MCP `aggregateHttpRequestCountsBy:"statusCode"` return the same per-code counts. Label names may differ only where each surface mirrors its own Render counterpart, and the record says so.
2. **Limit metrics.** REST `cpu-limit`, GraphQL `CPU_LIMIT` and MCP `cpu_limit` / `cpu_usage` agree.
3. **Ledger and ADR.** Update `docs/ADR018-render-parity.md` rows 197–198, and ADR010's REST parameter list.

## Acceptance criteria

- [ ] The cross-surface comparison transcript is in the m155 README.
- [ ] ADR018 and ADR010 match production.
