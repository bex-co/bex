---
id: w1/m155/t008
title: Simplify
worker: worker1
status: todo
estimate: 15m
depends_on: [w1/m155/t007]
---

## Objective

Run `/simplify` over the code m155 changed: the REST metrics routes and parameter parsing, the MCP name mapping, and the filter adapters. Keep it behavior-preserving. For example, use one `aggregateBy` mapping shared by REST, MCP and GraphQL, and one Render-to-bex metric name table.

## Acceptance criteria

- [ ] `/simplify` findings are applied, or recorded as declined with the reason.
- [ ] `cd lego/backend && go test ./...` and `make lint` are green.
