---
id: w1/m155/t009
title: Test coverage
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m155/t007]
---

## Objective

Meaningful tests for what m155 shipped, beyond t006's matrix.

## Steps

1. **REST.**
   - `aggregateBy=statusCode` over a fake source with 200, 404 and 501 returns three labelled series.
   - `host` returns a coded `400`.
   - With no parameter, the single series is unchanged.
2. **REST routes.**
   - `cpu-limit` / `memory-limit` values equal the GraphQL values for the same fake source.
   - `filters/*` return Render's shapes, a `503` when the store is unwired, and refuse a cross-workspace resource.
3. **MCP.** Each Render metric name maps to the same series as its bex id, and an unknown name still errors.
4. **Controls.** Existing GraphQL group-by tests, and REST `cpu` / `instance-count` tests, pass unchanged.

## Acceptance criteria

- [ ] Each new test is shown failing on the pre-fix code.
- [ ] `cd lego/backend && go test ./...` and `make lint` are green.
