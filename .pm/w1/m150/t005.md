---
id: w1/m150/t005
title: Simplify
worker: worker1
status: todo
estimate: 15m
depends_on: [w1/m150/t004]
---

## Objective

Run `/simplify` over the code and config m150 changed (Traefik values, Terraform listeners, `scripts/gitops-validate.sh`, and any t003 changes). Keep it behavior-preserving; for example, fold the listener expectations into one table in the gate instead of a second special case.

## Acceptance criteria

- [ ] `/simplify` findings are applied or recorded as declined, with the reason.
- [ ] `bash scripts/gitops-validate.sh` is green.
