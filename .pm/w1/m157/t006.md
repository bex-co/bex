---
id: w1/m157/t006
title: Test coverage
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m157/t004]
---

## Objective

Pin the behavior this milestone shipped with tests that fail on the pre-fix code.

## Context

- `lego/operator/internal/controller/wake_over_failed_release_test.go` (the `w1/m156` fixtures)
- `failed_build_prior_release_phase_test.go`

## Steps

1. Resume over a recorded build failure: replicas return, the route follows once ready, the template and Build condition stay, and no Job is created.
2. Idle sleep, then wake, over a recorded build failure.
3. A parked service with a build in flight: a positive requeue no later than the build's poll, the Building phase kept, and a wake that scales up.
4. A disk restore in progress is not scaled up.
5. A legacy Ready-only marker is not held and not erased.
6. Show each test failing with the hold not called, then passing.

## Files

- `lego/operator/internal/controller/wake_over_failed_build_test.go`

## Acceptance criteria

- [ ] Every case above is covered, and its failure message on the pre-fix code is recorded in the m157 README.
- [ ] `make test` passes.

## Out of scope

- Workers (`w1/m158`).
