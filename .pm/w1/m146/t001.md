---
id: w1/m146/t001
title: "`FollowLogs` keeps the stream open: heartbeat, and attach to each new app pod as it appears (the `followBuildLogs` watch shape)"
worker: worker1
status: done
estimate: 75m
depends_on: []
---

## Objective

A live subscription to an App ends only on client disconnect, authorization loss, App deletion, or a real error. Never merely because every pod present at subscribe time has exited.

## Context

- `lego/backend/internal/logs/service.go:846-884`: a one-shot pod list, a fan-out of `streamPodLogs`, a `close(ch)` after `wg.Wait()`, then `return nil` on the closed channel.
- `lego/backend/internal/core/base.go:1644-1652`: `AppPodsIn` has no phase filter.
- Precedent: `followBuildLogs` (`service.go:917-`), which keeps watching and attaches to build phases as they start (w6/m123).
- `lego/backend/internal/logs/rest.go:353-417` (`subscribeStream`) and `:323-351` (`subscribeWebSocket`) are the two transports.
- Live evidence: m146 README § Evidence.

## Steps

1. Replace the one-shot fan-out with a loop. Keep a set of followed pod names, re-list `AppPodsIn` on a short interval, and start `streamPodLogs` for unseen pods, respecting `q.keepPod` and the instance filter (`translateInstanceFilter` must be re-applied to newly seen pods).
2. Stop treating "all current follows returned" as end of stream. The loop exits on `ctx.Done()` or an emit error only.
3. Add a heartbeat callback to the emit contract (or a separate ticker in each transport): an SSE comment line, a WebSocket ping, and a documented NDJSON keepalive that Render CLI tolerates (verify against the pinned `render-oss/cli` decoder before choosing a format).
4. Make sure a Succeeded pod is replayed once per subscription (as today) and not re-followed on every re-list.

## Files

- `lego/backend/internal/logs/service.go`, `rest.go`, `podlogs.go` (if the follow signature changes)

## Acceptance criteria

- [x] An envtest/fake-client test: an App with one Succeeded pod, subscribed. The stream is still open after the replay, and a second pod created later has its lines emitted on the same stream (`TestAppTailOutlivesAFinishedRunAndStreamsTheNextOne`).
- [x] An App with zero pods, subscribed: still open, heartbeats arrive, and a pod created later streams (`TestAppTailWithNoPodsStaysOpenHeartbeatsAndStreamsTheFirstPod`).
- [x] A Running pod's tail behaves exactly as before. The existing subscribe, resume and revalidation tests are green; the recorder-based ones now disconnect as the client via `serveSubscribe`, because the stream no longer ends on its own.

## Result (2026-09-15)

`FollowLogs` → `followLogs(ctx, q, emit, heartbeat)` → `followAppPods` (`lego/backend/internal/logs/service.go`). The loop:

- re-lists `AppPodsIn` every `TailRelistInterval` (default 2 s);
- attaches only once the app container has started (`appContainerStarted`), so a Pending cron run is picked up when it runs, not dropped;
- follows each container life once, re-attaching when `RestartCount` rises. Before this change, a crash-looping pod's new life reached the client only because the whole stream ended and the browser reconnected. Keeping the stream open would have silently lost that path;
- re-applies the caller's instance selectors to pods that appear later;
- exits only on ctx or on an emit/heartbeat error.

Heartbeats run every `TailHeartbeatInterval` (default 25 s) from the emit goroutine:

- SSE writes `: keepalive`;
- NDJSON writes a blank line;
- WebSocket sends a ping (`rest.go`). The pinned `render-oss/cli` tails over gorilla WebSocket, whose ping handler answers without surfacing a frame.

`streamPodLogs` was removed as dead code. Tests are in `tail_idle_test.go`; all 8 fail against `HEAD`'s one-shot follow, verified with a shim that only adds the new fields.

## Out of scope

- Server-side resume beyond the existing `Last-Event-ID` handling.
