---
id: w1/m146/t003
title: "Aliases and sibling states: the WebSocket transport, NDJSON/Render CLI tail, and zero-pod states (suspended, hibernated, pre-first-pod)"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m146/t001]
---

## Objective

Every entrypoint to `FollowLogs` and every "no running instance" state gets the fixed behavior, verified live rather than inferred.

## Steps

1. **WebSocket** (`rest.go:195` → `subscribeWebSocket`): on an idle cron job, the socket stays open and receives pings, and a new run's line arrives.
2. **Render CLI:** `render logs --resources <cron srv-id> --tail` through the `bex` launcher (`lego/cli`). It keeps running across an idle period and prints the next run's line. Record the exact command and output.
3. **Zero-pod states, each on a `qa-` fixture:** a suspended service, a hibernated free `web_service` (idle past the 15 min window), and a just-created service before its first pod. On each, the Logs page makes 1 subscribe over 60 s and shows no banner. For the hibernated one, a wake request's app log line appears in the open tail.
4. Record before/after for each in the m146 README Evidence section.

## Acceptance criteria

- [ ] All five entrypoints/states have a dated PASS line with the command or click and the observation.
- [ ] Any state that still ends the stream is fixed here (≤30m) or filed as `w1/NNN` with `file:line`.
