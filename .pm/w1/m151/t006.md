---
id: w1/m151/t006
title: Test coverage
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m151/t004]
---

## Objective

Meaningful tests for what m151 shipped.

## Steps

1. **Unit** (`idle_test.go`, table-driven with a fake `ActivityReader`):
   - stamp elapsed plus activity since the stamp → no hibernate, and `last-active` advances;
   - stamp elapsed with no activity → hibernate;
   - reader error or nil reader → no hibernate;
   - a stale series or counter reset after a real idle period is not activity;
   - 5xx requests count as activity.
2. **Envtest:** a free web App past its window with an activity reader reporting requests stays at 1 replica with its Ingress on its own Service, then hibernates once the reader goes quiet for the window.
3. **Controls:** the existing `TestShouldAutoHibernate`, maintenance and wake tests pass unchanged.

## Acceptance criteria

- [ ] Each new test is shown failing on the pre-fix code.
- [ ] Operator `make test` and lint are green.
