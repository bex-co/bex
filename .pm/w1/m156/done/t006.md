---
id: w1/m156/t006
title: Test coverage
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m156/t004]
---

## Objective

Pin the behavior this milestone ships with tests that assert real outcomes and failure modes.

## Context

- t001's envtest (wake over a failed pre-deploy and over a failed build) and t002's table cells.

## Steps

1. **Show each test fails pre-fix.** Revert the fix locally and confirm it fails, then restore it.
2. **Cover the negatives.**
   - A first release that fails its pre-deploy still reads Failed and has nothing to wake.
   - A failed verdict never re-creates the Job or rolls the template.
3. **Run the suites.** `make test` and `make lint` from `lego/operator/`.

## Files

- `lego/operator/internal/controller/*_test.go`

## Acceptance criteria

- [ ] Each new test has a recorded pre-fix failure, and the suites are green.

## Out of scope

- End-to-end kind tests.
