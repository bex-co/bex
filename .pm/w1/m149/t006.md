---
id: w1/m149/t006
title: Test coverage
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m149/t004]
---

## Objective

Meaningful tests for what m149 shipped.

## Steps

1. **Operator envtest:**
   - pre-deploy failure over a live release → phase `Running`;
   - first-release pre-deploy failure → `Failed`;
   - no `Failed` while the pre-deploy is running;
   - the exit code is captured from the terminated container;
   - a real timeout is distinct.
2. **Store:**
   - the ordering race that produced the timeout text is pinned;
   - `PreDeployFailed` rows never take the timeout fallback;
   - build and rollout timeout wording unchanged (control).
3. **Logs:** predeploy records are labelled `predeploy`.

## Acceptance criteria

- [ ] Each new test is shown failing on the pre-fix code.
- [ ] Operator `make test`, backend `go test ./internal/store/... ./internal/logs/...`, and lint are green.
