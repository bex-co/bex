---
id: w1/m149/t004
title: Render parity
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m149/t001, w1/m149/t002, w1/m149/t003]
---

## Objective

Service phase, deploy status and reason for a pre-deploy failure are identical on REST, GraphQL, MCP and the dashboard, and match Render, or diverge on the record.

## Steps

1. Reproduce a pre-deploy failure over a live release. Record the service phase, deploy status and `failureReason` from REST `GET /v1/services/{id}` plus `…/deploys`, GraphQL, MCP, and the dashboard header and deploy page.
2. Decide the free-plan question and record it. Render: "The pre-deploy command is available for paid web services, private services, and background workers." bex allows it on free web services (`lego/backend/internal/apps/service.go:3413-3414`). Either gate it or record the divergence with its reason.
3. Update `docs/ADR018-render-parity.md` row 71 with the phase invariant, the reason wording, and the plan rule.

## Acceptance criteria

- [ ] The surface table is recorded in the m149 README.
- [ ] ADR018 row 71 is updated. Any surface disagreement is filed as `w1/NNN`.
