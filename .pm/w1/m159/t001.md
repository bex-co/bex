---
id: w1/m159/t001
title: The Key Value and Postgres Details cards translate status and plan instead of printing wire values
worker: worker1
status: todo
estimate: 40m
depends_on: []
---

## Objective

A datastore's Details card must report the same state as the header badge beside it, in every locale, and must never print a wire value or a plan id.

## Context

- `keyvalue.$keyValueId.tsx:293-294` and `databases.$databaseId.tsx:363-364` render `status` and the plan id raw.
- The header badge on the same page already maps those values — reuse its mapping rather than adding a second one.
- `w1/done/085.md` has the live transcript: header **Suspended** over a Status row reading **available**.

## Steps

1. Find the header badge's status mapping and reuse it for the Details row, covering every status the API can return.
2. Map the plan id to its display name through the same catalogue the plan picker uses.
3. Check both cards in `en` and `zh`, suspended and available.

## Files

- `dashboard/src/routes/**/keyvalue.$keyValueId.tsx`
- `dashboard/src/routes/**/databases.$databaseId.tsx`
- the shared status/plan mapping they reuse

## Acceptance criteria

- [ ] A suspended store reads Suspended in the header and in the Details Status row, in `en` and `zh`.
- [ ] Neither card renders a raw status string or a plan id.
- [ ] `yarn test` and `yarn lint` pass.

## Out of scope

- The datastore status pipeline itself; only its presentation.
