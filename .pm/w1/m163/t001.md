---
id: w1/m163/t001
title: "Render evidence: suspend on a cron job or static site acts immediately regardless of the latest build"
worker: worker1
status: todo
estimate: 20m
depends_on: []
---

## Objective

Capture, with dated quotes, that on Render suspending a cron job stops scheduled runs at once and suspending a static site stops serving it, independent of whether the most recent build failed or is still running — and that resume and schedule edits likewise take effect without a new successful build.

## Context

- Render docs: cron jobs (suspend/resume, schedule edits), static sites (suspend), and the deploy docs' 'previous deploy stays live after a failed deploy'.
- bex: `w1/done/m157` and `w1/done/m158` READMEs record the same contract for web, private and worker services.

## Steps

1. Read the docs; write `docs/render-artifacts/cron-static-suspend-over-failed-build.md` with quotes and date.
2. State the contract t002/t003 must meet in one paragraph.

## Files

- `docs/render-artifacts/cron-static-suspend-over-failed-build.md` (new)

## Acceptance criteria

- [ ] The artifact states the immediate-suspend contract for both types with quotes and a date.

## Out of scope

- bex changes.
