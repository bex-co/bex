---
id: w1/m145/t004
title: Render parity
worker: worker1
status: todo
estimate: 30m
depends_on: [w1/m145/t001, w1/m145/t002, w1/m145/t003]
---

## Objective

Confirm the schedule contract and refusal shape are consistent across REST, GraphQL, MCP, Blueprint and the dashboard, and compare with render.com.

## Steps

1. Send `schedule: "0 0 * * 7"` and `"0 0 ? * *"` through REST `POST /v1/services` + `PATCH /v1/services/{id}`, GraphQL `createService`/`updateCronJob`, MCP `create_cron_job`/`update_service`, and a `render.yaml` Blueprint validate. Record each response. All must agree with t001's vector table.
2. Render: check the pinned Render OpenAPI schema (`schedule` field description/pattern) and, if a Render account is available, create a cron job with `0 0 * * 7`. `render.com/docs/cronjobs` (2026-09-14) does not document either case. If Render accepts `7`, file the normalize-on-write question as a follow-up; do not change the contract here.
3. Update the `docs/ADR018-render-parity.md` cron row with the settled acceptance set and the evidence.

## Acceptance criteria

- [ ] A per-surface response table for both schedules is recorded in the m145 README.
- [ ] Render's behavior is recorded, or explicitly noted as unobtainable, with the reason.
- [ ] Any drift is filed as a `w1/NNN` note carrying `file:line`.
