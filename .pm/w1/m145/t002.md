---
id: w1/m145/t002
title: "`useCreateService` and `useCronJob` show the server's refusal (w6/037's contract) instead of their generic toast"
worker: worker1
status: todo
estimate: 30m
depends_on: []
---

## Objective

Apply `serverRefusalReason(err) || t(generic)` to the two hooks this hunt drove live. After t001, the cron schedule no longer reaches the server invalid, but every other refusal on these two paths still does: an invalid name the client check misses, a `maxShutdownDelaySeconds` out of range, a static-site-only field on a web service, and the command-permission refusals.

## Context

- `dashboard/src/features/services/hooks/use-create-service.ts:135-142`: after the cap-limit and `isNameConflictError` branches, it falls back to `toast.error(t("services.createError"))`. The `currentWorkspaceId == null` branch at `:101-103` is not a server refusal and stays generic.
- `dashboard/src/features/services/hooks/use-cron-job.ts:40-41`: bare `catch`.
- Contract and precedent: `dashboard/src/common/lib/graphql-error.ts:31-42` (`refusalReason` / `serverRefusalReason`); `dashboard/src/features/services/hooks/use-field-mutation.ts` (w6/037, `d247d210`).
- Live evidence: m145 README § Evidence 1 and 3 (both responses carry `bad request: schedule must be…`; both toasts were generic).

## Steps

1. `use-create-service.ts`: in the final `else`, `toast.error(serverRefusalReason(err) || t("services.createError", { name }))`. Keep the payment-cancel, cap-limit and conflict branches ahead of it.
2. `use-cron-job.ts`: `catch (err) { toast.error(serverRefusalReason(err) || t("services.deployError")) }`.
3. Throttled errors: if `isThrottledError(err)`, keep whatever throttling copy the app uses elsewhere rather than raw text. Check how `useFieldMutation` handles it and match it.
4. Update `__tests__/use-create-service.test.ts:145,164,206`. Assert the server sentence for a GraphQL refusal and the generic copy for a network error. Add the same pair to `__tests__/use-cron-job.test.ts`.

## Files

- `dashboard/src/features/services/hooks/use-create-service.ts`, `use-cron-job.ts` and their `__tests__`

## Acceptance criteria

- [ ] The m145 DoD's two `page.route` probes (rewrite → server sentence; abort → generic) pass for `CreateService` and `UpdateCronJob`.
- [ ] Unit tests cover both arms for both hooks, and are shown red on the pre-fix code.

## Out of scope

- The other 77 sites (t003).
