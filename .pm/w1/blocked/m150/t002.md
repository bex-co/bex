---
id: w1/m150/t002
title: Enable PROXY protocol on the Terraform `http`/`https` listeners and prove the allowlist sees the client
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m150/t001]
---

## Objective

The Hetzner load balancer sends the real client address to Traefik on :80 and :443. A service allowlist then admits exactly the listed public CIDRs, which is the m150 Definition of done, live.

## Context

- `infra/terraform/main.tf:201-235`: `hcloud_load_balancer_service.http` / `.https` set `proxyprotocol = false`. `postgres` and `valkey` (`:237-271`) already set `true`.
- `scripts/gitops-validate.sh:168-173` expects `proxyprotocol=true` only for `postgres | valkey`.
- `w2/done/m57/done/t010.md` records the permanent CI handoff order: header-capable consumers first, listeners second. Reuse it.
- m150 README § Evidence 3 is the probe transcript to re-run.

## Steps

1. **Flip the listeners.** Set `proxyprotocol = true` on `http` and `https` only; `ssh` stays `false`. Update the gate's expected set, and any comment or doc that states "SSH/HTTP/HTTPS remain unchanged" (grep `docs/` and `scripts/`).
2. **Apply in order.** Apply only after t001's Traefik rollout is live, using the same CI ordering `w2/m57` t010 uses. Watch the load balancer's health checks on `31218` and `31976`.
3. **Re-run the probes live** on a throwaway `qa-` web service:
   - every m150 DoD bullet;
   - entries `[203.0.113.0/24]` → `403`;
   - a throwaway `qa-` static site with an allowlist containing `<me>/32` → `200`, and another CIDR only → `403`;
   - an environment-layer rule with the same two cases.

   Delete the fixtures.

## Files

- `infra/terraform/main.tf`
- `scripts/gitops-validate.sh`
- any ordering/handoff workflow the Terraform apply runs through

## Acceptance criteria

- [ ] Every m150 DoD bullet passes live, with the transcript pasted into the m150 README.
- [ ] Static site and environment-layer rules behave the same (transcript).
- [ ] The `ssh` listener is still `proxyprotocol = false`, and health checks stay green through the change.

## Out of scope

- Consumers of the new client address beyond the allowlist (t003).
