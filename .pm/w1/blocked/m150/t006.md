---
id: w1/m150/t006
title: Test coverage
worker: worker1
status: todo
estimate: 40m
depends_on: [w1/m150/t004]
---

## Objective

Meaningful tests for what m150 shipped.

## Steps

1. **Gate:**
   - the `http` and `https` listeners must be `proxyprotocol = true`, and `ssh` `false`;
   - both Traefik entrypoints trust exactly `10.10.0.7/32`.

   Show each check failing on a mutated fixture (the existing `scripts/*.test.sh` pattern).
2. **Operator:** the emitted allowlist middleware has no `ipStrategy`, so a forwarded header can never decide a match. Cover both the service and environment layers, on the web and static-site paths.
3. **Consumers:** regression tests t003 identified for callers that behave correctly today (e.g. `clientip.go` keys for trusted and untrusted peers).

## Acceptance criteria

- [ ] Each new test is shown failing on the pre-fix code or config.
- [ ] `bash scripts/gitops-validate.sh`, operator `make test`, backend `go test ./internal/core/...`, and lint are green.
