---
id: w1/m159/t003
title: Seven count strings get native plurals, and the locale test fails when a count message has none
worker: worker1
status: done
estimate: 40m
depends_on: []
---

## Objective

Restore the `_one`/`_other` rule `w6/done/062` set, and make the locale tests enforce it so the next `{count}` string cannot regress.

## Context

- `w1/done/098.md` lists the seven keys, including the Blueprint review's "1 resources to sync" and the surviving `(s)` in `envGroups.serviceCount` and `services.scaleSuccess`.
- `locale-parity.test.ts` compares locales key by key but never checks plural forms.

## Steps

1. Convert the seven keys to `_one`/`_other` in `en` and `zh`, updating their call sites to pass `count`.
2. Extend the locale test: an `en` message containing `{count}` must have plural forms, and its locale twins must match.
3. Run the full dashboard suite.

## Files

- `dashboard/src/features/*/locales/{en,zh}.ts`
- the call sites named in `w1/done/098.md`
- `dashboard/src/**/locale-parity.test.ts`

## Acceptance criteria

- [x] The Blueprint review reads "1 resource to sync" and "3 resources to sync".
- [x] No user-visible string carries `(s)`.
- [x] The new locale check fails on a deliberately un-pluralized `{count}` message.

## Out of scope

- Translating strings beyond the seven and their twins.
