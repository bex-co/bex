---
id: w1/m163/t005
title: "Render parity"
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m163/t004]
---

## Objective

Check the change is consistent across every surface it exposes — REST, GraphQL and MCP in `lego/backend/` and the `dashboard/` UI — and against the equivalent render.com behavior. Flag drift as follow-up work rather than silently diverging.

## Context

- `docs/ADR006-bex-api.md` (Render-compatible API contract), `docs/ADR018-render-parity.md` (ledger row to update)

## Steps

1. List every surface the milestone touched.
2. Compare field names, semantics and error shapes across REST/GraphQL/MCP/UI.
3. Compare against Render's documented behavior; record any accepted divergence in the ledger.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Every touched surface agrees; drift is either fixed or filed as follow-up with a ledger note.

## Out of scope

- New features Render has that this milestone did not touch.

## Outcome — 2026-09-21

REST/GraphQL/MCP/dashboard delegate to shared lifecycle and cron intent writers. Existing focused backend parity/suspend/resume tests passed. ADR018 cron row links the dated artifact and explicitly limits unverified Render timing claims.
