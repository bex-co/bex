---
id: w1/m161/t003
title: "Live: a client-only WebSocket holds a free service past its idle window"
worker: worker1
status: todo
estimate: 45m
depends_on: [w1/m161/t002]
---

## Objective

Prove the DoD on production with a free service and a client that only sends.

## Context

- `w1/m151` verified the server→client direction live; reuse its fixture shape and the scratchpad WebSocket client/server.
- Scanner traffic on `*.onbex.co` resets idle clocks — watch the request log and pick a window with none, as `w1/m151` and `w1/m157` had to.

## Steps

1. Record the pre-fix result: a client-only WebSocket, then hibernation on schedule.
2. After the plugin and operator roll, repeat and record the transcript.
3. Run both controls (server→client keeps it awake; a genuinely idle service still sleeps).
4. Delete the fixture.

## Files

- `.pm/w1/m161/README.md`

## Acceptance criteria

- [ ] Each DoD bullet has a live transcript, with any scanner interference called out.
- [ ] All fixtures are deleted.

## Out of scope

- Load testing the plugin.
