---
id: w1/m161/t002
title: The operator's activity read sums both directions
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m161/t001]
---

## Objective

`w1/m151`'s activity signal must treat traffic in either direction as activity.

## Context

- `lego/operator/internal/controller/activity.go`: `activityQueries` reads `bex_websocket_egress_bytes_total`.
- The query must tolerate the ingress counter being absent, so an operator ahead of the plugin roll keeps working.

## Steps

1. Read both counters and treat any increase as activity.
2. Keep behavior identical when the new counter does not exist yet.

## Files

- `lego/operator/internal/controller/activity.go`

## Acceptance criteria

- [x] An increase in either counter defers hibernation.
- [x] With only the old counter present, behavior is exactly `w1/m151`'s.
- [x] `make test` passes.

## Out of scope

- Changing the idle window or the activity cadence.
