---
id: w1/m149/t005
title: Simplify
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m149/t004]
---

## Objective

Run `/simplify` over the code m149 changed (reuse / simplification / efficiency; behavior-preserving). A likely candidate is one "settle a failure over a prior release" helper shared by the build, rollout and pre-deploy failure paths, instead of three variants.

## Acceptance criteria

- [ ] Findings are applied or declined with a reason; operator `make test`, backend tests and lint are green.
