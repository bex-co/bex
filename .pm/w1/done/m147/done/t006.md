---
id: w1/m147/t006
title: Simplify
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m147/t005]
---

## Objective

Run `/simplify` over the code m147 changed (reuse / simplification / efficiency; behavior-preserving). A likely candidate is one shared "apply patch then check quota" mutate helper used by both the service and env-group bulk patches, so the two cannot drift apart again.

## Acceptance criteria

- [x] Findings are applied or declined with a reason; backend and dashboard tests and lint are green.

## Result (2026-09-15)

This was a single-pass review of the m147 diff, not the three-agent `/simplify` run. The change covers `secrets/batch.go` (one helper and three call sites), `environment-draft.ts` (one validator), two row branches and two copy keys.

- **Applied: one helper for the three batch call sites.** `patchWithinQuota(before, after, within)` serves the sparse env mutate, the sparse files mutate and the revision-aware path, so the "shrink allowed, grow refused" rule lives in one place instead of three copies.
- **Applied: one validator for both lenses.** `flagOverLimit` in the dashboard works over the existing `RowLens` for env vars and files alike. The row components type their `error` prop as `DraftValidation["env" | "files"][string]`, so a new code can't be added to the validator without the renderer's type noticing.
- **Declined: one mutate helper shared with the env-group batch patch.** The env-group patch validates cloned maps before a revision claim (`envgroups/patch.go:179-189`). The service patch checks inside `updateMapCAS`'s retry loop. The two call shapes differ, so a shared "apply then check" helper would be longer than the two call sites. The real risk of drift is the **rule**: the env-group patch still refuses a shrink of an already-over-quota map. That is a behavior change for env groups and outside m147's scope, filed as `.pm/w1/100.md`.
- **Green.** `go test ./...` (backend) and `golangci-lint ./internal/secrets/... ./internal/envgroups/...` pass with 0 issues. The dashboard `services` and `env-groups` suites (639 tests), `yarn typecheck` and `yarn lint` pass.
