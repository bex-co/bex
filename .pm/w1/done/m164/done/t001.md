---
id: w1/m164/t001
title: "Backend evidence: what the deploy reconciler does with `releaseGeneration` 0 and with legacy Apps lacking `activeRevision`"
worker: worker1
status: done
estimate: 45m
depends_on: []
---

## Objective

Before changing what the operator reports, establish from `lego/backend/internal/store/reconciler.go` how an open deploy row is closed when `app.Status.ReleaseGeneration` is 0, and how Apps that predate `status.activeRevision` (the legacy fallback's real customers) currently get their rows closed. Record the findings in the milestone README so t002's choice is evidence-based.

## Context

- `lego/backend/internal/store/reconciler.go` compares `app.Status.ReleaseGeneration` against the open deploy's generation.
- `lego/operator/internal/controller/release_identity.go` `successfulReleaseGeneration`: after the `rev-` parse, `if app.Status.Image != "" && app.Status.ObservedGeneration > 0 {{ return app.Status.ObservedGeneration }}`.
- Caller: `prepareAppReleaseDecision`'s canceled branch.
- `w1/blocked/106.md` (now `done/106.md`) and `w1/blocked/m160` t003's blast-radius notes.

## Steps

1. Read the reconciler's closing rules for open rows; note every branch keyed on `ReleaseGeneration`.
2. Query production/dev for Apps with empty `status.activeRevision` to size the legacy population (read-only).
3. Write the findings and the recommended value for a never-served release into the milestone README.

## Files

- `.pm/w1/m164/README.md`

## Acceptance criteria

- [x] The README states what happens today for `ReleaseGeneration == 0` and for legacy Apps, with file:line citations and the legacy count.

## Out of scope

- Code changes.
