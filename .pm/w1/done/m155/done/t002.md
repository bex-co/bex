---
id: w1/m155/t002
title: "REST serves /v1/metrics/cpu-limit and /v1/metrics/memory-limit"
worker: worker1
status: done
estimate: 15m
depends_on: []
---

## Objective

Render's documented `GET /metrics/cpu-limit` and `GET /metrics/memory-limit` answer with the same series GraphQL `CPU_LIMIT`/`MEMORY_LIMIT` and MCP `cpu_limit`/`memory_limit` already return, instead of a bare `404`.

## Context

- `metricPaths` (`lego/backend/internal/metrics/rest.go:35-47`) lacks both segments.
- `MetricCPULimit` and `MetricMemoryLimit` exist (`metrics/service.go:46`, `:48`, served at `:438`, `:877`, `:1030`) and GraphQL maps them (`graphql.go:40`, `:42`).
- Live at filing: REST `404 "404 page not found"` for both. GraphQL `CPU_LIMIT` returned `0.1` cpu, and MCP `cpu_limit` returned a series.

## Steps

1. Add `"cpu-limit": MetricCPULimit` and `"memory-limit": MetricMemoryLimit` to `metricPaths`.
2. Confirm Render's documented parameters (`resource`, `service`, `instance`, `resolutionSeconds`) pass the validator and reach the handler.
3. Probe live (m155 DoD bullet 3).

## Files

- `lego/backend/internal/metrics/rest.go`

## Acceptance criteria

- [ ] Both paths return `200` with series live, and the values match GraphQL for the same window.
- [ ] A composed-server test covers both paths through the strict validator.
