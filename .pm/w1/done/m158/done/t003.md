---
id: w1/m158/t003
title: "Live: suspend, resume and scale a background worker over a failed pre-deploy step and a failed build"
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m158/t002]
---

## Objective

Run the m158 definition of done on production after the fix deploys.

## Context

- **Plan.** A background worker may require a paid plan. Never buy anything. Create a `qa-<yyyymmdd>-…` worker only if the QA workspace allows it without a purchase.
- **Otherwise.** Move this task and the milestone to `w1/blocked/` with the exact question.

## Steps

1. **Fixture.** Create a worker from `examples/hello-go` and wait for it to go live.
2. **Failed pre-deploy.** Set a failing pre-deploy command, then suspend, resume and scale. Record the instance count and phase after each step.
3. **Failed build.** Repeat over a failed build.
4. **Clean up.** Record the transcripts in the m158 README and delete the fixture.

## Files

- `.pm/w1/m158/README.md`

## Acceptance criteria

- [x] Each DoD bullet has a live transcript, or the milestone is blocked with the exact question.
- [x] All fixtures are deleted.

## Out of scope

- Buying a plan.
