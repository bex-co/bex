# Fix missing-image recovery in the local agent stack

Why: a fresh node must import required agent images so developers can reliably verify ADR047 agent sessions locally, supporting ADR008's agent capabilities.

Estimate: 45m. Worker: worker1. No dependencies. Priority: before 083. Approved 2026-09-08 pm-brainstorm handoff; sub-hour work stays an inbox note.

Source: `scripts/dev-env.sh`'s `agent_reconcile_node_images`, following `done/m137/README.md`. The branch `if ! observed=$(agent_node_image_identity ...); then rc=$?` captures the negation's successful status (0), not the inspection's missing-image status (2). The subsequent status check aborts instead of importing. A minimal Bash reproduction confirmed the status handling; this is not a claimed live cluster reproduction. Existing helper-level tests do not exercise this reconciliation branch.

- Preserve the original inspection exit status and distinguish missing images from inspection failures.
- Extend `scripts/dev-env.test.sh` to drive `agent_reconcile_node_images` itself: a missing image imports and verifies, a matching image skips import, and an inspection error aborts before rollout. Use behavioral assertions and prove the missing-image regression fails before the fix.
- Run the relevant script suite and shell syntax checks. Preserve m137's identity comparison and post-import verification.

Done when missing images are imported, matching images avoid import, inspection failures stop rollout, and the regression passes only with the fix.

Render parity does not apply: local development tooling only, with no REST/GraphQL/MCP/UI contract change. Source-freshness redesign, production image distribution, and w3/m79's GitHub draft-PR proof are out of scope.

Completed 2026-09-09: capture the inspection status in an `||` handler before testing for missing-image status 2. Added `scripts/dev-env-images.test.sh`, invoked by the existing suite, which runs the real reconciliation/import helpers against file-backed node images. It asserts missing-image import plus post-import inspection, matching-image skip, and refusal to roll out on inspection errors, import errors, or identity mismatch. The new test failed on the original code and passed after the fix. `bash scripts/dev-env.test.sh` passed, including its live interrupted-import convergence and mismatch checks on two local CAPD nodes; shell syntax checks passed.
