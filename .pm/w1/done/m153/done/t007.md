---
id: w1/m153/t007
title: Test coverage
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m153/t005]
---

## Objective

Meaningful tests for what m153 shipped. A poll or refetch over cached data must not unmount stateful UI, and the first load must still show the skeleton.

## Steps

1. **Component tests** (vitest with the Apollo mocks in `src/test/mocks/apollo.ts`, driving a refetch or poll over cached data with fake timers):
   1. `EnvironmentsPanel`: with the settings dialog open and `203.0.113.0/24` typed, a refetch keeps the dialog open and the value intact. With "Manage resources" open and a box ticked, it stays ticked. No `ProjectEnvironmentCardSkeleton` renders after first data.
   2. `EnvironmentsPanel`: a refetch that returns an error while data is cached keeps the card mounted and shows the error inline.
   3. `ServiceSourcePicker`: a focused "Search repositories…" input keeps focus and value across a refetch, and the disconnected empty state never flashes while the connection is cached.
   4. The env-group detail editor: a draft row keeps focus and value, and its controls stay enabled across a refetch.
   5. First load with no cache: each of the three renders its skeleton.
2. **Regression controls.** The service env-var editor draft and the already-guarded hooks named in t004 keep their behavior. The four explicit `notifyOnNetworkStatusChange: true` hooks still report loading as they did, if option A was chosen.

## Acceptance criteria

- [ ] Each new test is shown failing on the pre-fix code.
- [ ] `yarn typecheck && yarn lint && yarn test` are green in `dashboard/`.
