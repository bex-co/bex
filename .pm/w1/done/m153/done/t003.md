---
id: w1/m153/t003
title: "The env-group Environment editor keeps its rows mounted across EnvGroup polls (probe live first)"
worker: worker1
status: done
estimate: 25m
depends_on: []
---

## Objective

Confirm live whether an `EnvGroup` poll unmounts the env-group detail page's variable and secret-file rows. The code path says it does, but it was not observed in pass 25. Then make the editor receive first-load-only `loading`, as the service caller already does, so a draft row keeps focus, value and `touched` state, and its controls are not disabled by a poll.

## Context

- **The route.** `dashboard/src/routes/env-groups_.$groupId.tsx:186` gates the page skeleton on `!group` (correct). `:209-214` passes `useEnvGroup`'s raw `loading` to `<EnvGroupEditors loading={loading} …/>`.
- **The hook.** `features/env-groups/hooks/use-env-groups.ts:125-145`: `useEnvGroup` uses `cache-first` and polls at `:131`, then returns raw `loading` at `:142`. The list hook at `:109-112` is already guarded.
- **The editor.** `features/env-groups/components/env-group-editors.tsx:34` passes `loading={loading}` to `EnvironmentEditor`, which reaches `EnvironmentSection`'s `loading ? <RowsSkeleton /> : rows` at `features/services/components/service-environment-editor.tsx:991`.
- **The guarded service caller.** It passes `env.loading && env.keys.length === 0` (`service-environment-editor.tsx:121-124`).
- **Per the trace (verify):** the editor's `loading` also disables controls at `service-environment-editor.tsx:636`, `:669` and `:732`. `FileDraftItem` keeps `touched` state at `:1219`.
- **Pass-25 attempt.** After Edit → "Add variable" (menu) → "Add variable", `getByPlaceholder('Key')` found no input on the env-group page. Locate the row's inputs with `page.locator('main').ariaSnapshot()` first.

## Steps

1. **Probe live, before any fix.**
   1. `POST /v1/env-groups` `{"name":"qa-<yyyymmdd>-egpoll","ownerId":"<tea-…>","envVars":[{"key":"QA_K","value":"v"}]}`.
   2. Open `/env-groups/<evg-…>` and start an unsaved variable draft.
   3. Focus its key field, type, and wait 65 s while logging `EnvGroup` requests.
   4. Record whether rows unmount, focus drops or controls disable, then delete the group (`DELETE` → `204`, then `GET` → `404`).
2. **Record the outcome.** Paste the transcript into the m153 README § Evidence. If it reproduces, add a DoD bullet for it.
3. **Fix.** Pass first-load-only `loading` (`loading && !group`, or t004's convention) to `EnvGroupEditors`, and confirm the disabling uses follow the same flag.

## Files

- `dashboard/src/routes/env-groups_.$groupId.tsx`
- `dashboard/src/features/env-groups/hooks/use-env-groups.ts` (if `useEnvGroup` itself is fixed)

## Acceptance criteria

- [x] The pre-fix live transcript is in the m153 README, with a DoD bullet if it reproduced.
- [x] After the fix, a draft row on the env-group page keeps focus and value across two polls (live), and its controls stay enabled.
- [x] A vitest proves the same, and fails on the pre-fix code.

## Out of scope

- `LinkedServicesCard busy` and the new-group dialog flicker (t004).
