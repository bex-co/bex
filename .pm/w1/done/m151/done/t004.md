---
id: w1/m151/t004
title: Render parity
worker: worker1
status: done
estimate: 20m
depends_on: [w1/m151/t002, w1/m151/t003]
---

## Objective

Every surface that describes free-tier sleep says what the operator now does, and it matches Render's "15 minutes without receiving any inbound traffic (HTTP requests and WebSocket messages)", or diverges on the record.

## Steps

1. **Dashboard.** The idle-timeout hint (`dashboard/src/features/services/locales/en.ts:1092`, "Free services sleep after this idle window…") and the `IDLE_TIMEOUT_PRESETS` doc (`dashboard/src/features/services/lib/idle-timeout.ts:6-13`) say the window counts time **without inbound traffic**. Keep zh in step.
2. **MCP and the CRD.** The MCP `idleTTLSeconds` description and `kubectl explain app.spec.idleTTLSeconds` state the same activity rule.
3. **ADRs.** Record the rule in `docs/ADR003-control-plane.md` § free web tier, and add or extend a row in `docs/ADR018-render-parity.md` for free-tier spin-down, including any t002 WebSocket divergence.

## Acceptance criteria

- [ ] The dashboard copy (en + zh), the MCP description, the CRD doc, ADR003 and ADR018 all state the activity rule.
- [ ] Any remaining divergence is filed as `w1/NNN`.
