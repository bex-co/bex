---
id: w1/m145/t004
title: Render parity
worker: worker1
status: done
estimate: 30m
depends_on: [w1/m145/t001, w1/m145/t002, w1/m145/t003]
---

## Objective

Confirm the schedule contract and refusal shape are consistent across REST, GraphQL, MCP, Blueprint and the dashboard, and compare with render.com.

## Steps

1. Send `schedule: "0 0 * * 7"` and `"0 0 ? * *"` through REST `POST /v1/services` + `PATCH /v1/services/{id}`, GraphQL `createService`/`updateCronJob`, MCP `create_cron_job`/`update_service`, and a `render.yaml` Blueprint validate. Record each response. All must agree with t001's vector table.
2. Render: check the pinned Render OpenAPI schema (`schedule` field description/pattern) and, if a Render account is available, create a cron job with `0 0 * * 7`. `render.com/docs/cronjobs` (2026-09-14) does not document either case. If Render accepts `7`, file the normalize-on-write question as a follow-up; do not change the contract here.
3. Update the `docs/ADR018-render-parity.md` cron row with the settled acceptance set and the evidence.

## Acceptance criteria

- [x] A per-surface response table for both schedules is recorded in the m145 README.
- [x] Render's behavior is recorded, or explicitly noted as unobtainable, with the reason.
- [x] Any drift is filed as a `w1/NNN` note carrying `file:line`. (None found.)

## Result (2026-09-14)

- **Surface table:** m145 README § Render parity. It is backed by `lego/backend/internal/apps/cron_schedule_surfaces_test.go`, which runs 14 subtests (2 schedules × 7 surfaces), and every one agrees with `testdata/cron-schedule-vectors.json`. Every refusal carries the sentence the dashboard relays.
- **No bex drift.** Each write path reaches the single `validCronSchedule`. `TestSetCronJobRejectsInvalidSchedule` now iterates every invalid vector.
- **Render: unobtainable.** The pinned OpenAPI types `schedule` as a bare string, the docs say nothing about `7` or `?`, and this environment has no Render account. No follow-up was filed.
- **`docs/ADR018-render-parity.md` cron row** now records the settled acceptance set and this evidence.
