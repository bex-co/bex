---
id: w1/m145/t006
title: Test coverage
worker: worker1
status: done
estimate: 40m
depends_on: [w1/m145/t004]
---

## Objective

Meaningful tests for what m145 shipped. No tautologies or snapshot-everything tests.

## Steps

1. Shared cron vector table: read by both the backend and dashboard suites, and proven by flipping one vector.
2. Both arms (server refusal → server sentence; transport failure → generic) for `useCreateService`, `useCronJob`, and every hook t003 migrated, or a table-driven test over the shared helper plus one per-hook wiring test.
3. Regression tests on the controls that already behaved: a `useFieldMutation` hook, one conflict-dialog path, the plan-limit CTA, and the throttled-error path.
4. The t003 source-sweep test, shown red with a planted bare catch.

## Acceptance criteria

- [x] Each new test is shown failing on the pre-fix code (or with the fix reverted).
- [x] The full dashboard suite, the backend `internal/apps` package tests, and lint are green.

## Result (2026-09-14)

1. **Shared vector table.** `lego/backend/internal/apps/testdata/cron-schedule-vectors.json` has 63 vectors, with robfig's answers as the truth. It is read by `cron_schedule_vectors_test.go` (`validCronSchedule`), `cron_schedule_surfaces_test.go`, `cron_settings_test.go` (`SetCronJob` refuses every invalid vector) and `dashboard/.../lib/__tests__/cron.test.ts` (`isValidCron`). **Flip proof:** marking `0 0 * * 7` valid failed `TestValidCronScheduleVectors`, all 7 surface subtests for that schedule, and `isValidCron("0 0 * * 7")`. The file was then restored byte-for-byte.
2. **Both arms.**
   - `mutationErrorMessage` has a table of cases in `graphql-error.test.ts`: conflict, bad-request prefix stripped, transport failure, throttled, and an empty message.
   - Per-hook wiring tests assert the server sentence and the generic copy in `use-cron-job.test.ts` and `use-create-service.test.ts`.
   - The ~90 other migrated sites are pinned structurally by the invariant test rather than one test per hook, as step 2 allows.
3. **Controls, still green and unchanged:**
   - `use-field-mutation.test.ts`
   - `custom-domains-section.test.tsx` and `use-custom-domains.test.ts` (inline refusal)
   - `invite-member-dialog.test.tsx` (PLAN_LIMIT CTA and inline reason)
   - the `use-create-service.test.ts` name-conflict control
   - throttled: `isThrottledError` plus the new `mutationErrorMessage` throttled case
4. **Invariant test red proofs.** Planted cases fail on the bare catch, on a block that reads `err` elsewhere but toasts generic copy, and on an `error` binding. With `cron.ts`, `use-cron-job.ts` and `use-create-service.ts` reverted to `HEAD`, 20 tests failed across 4 files.
5. **Green:**
   - `yarn vitest run`: 413 files / 3237 tests
   - `yarn lint` (typecheck + ESLint + knip): exit 0
   - `go test ./internal/apps/` and `go vet`: ok, gofmt clean
   - `make lint-backend` reports one pre-existing finding outside this diff: `internal/api/scope_matrix.go:163 writeGraphQLErrors is unused`, from `36457c620`.
