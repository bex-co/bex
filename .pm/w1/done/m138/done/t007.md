---
id: w1/m138/t007
title: Test coverage
worker: worker1
status: done
estimate: 45m
depends_on: [w1/m138/t005]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — real assertions on failure modes, not snapshots.

## Context

- Backend: `lego/backend/internal/events/service_test.go`, `vocabulary_test.go`; adapter tests for REST/GraphQL/MCP.
- Operator: controller envtest for build registry Secrets.
- Dashboard: `services.$serviceId.events` route tests, `deploys-list-page.test.tsx`.

## Steps

1. Backend: table test across `build_failed` / `pre_deploy_failed` / `update_failed` / `canceled` / `live` deploy rows → expected `deployStatus`, extra status, `failureReason` on each adapter.
2. Operator: marker present/absent envtest (from t004) plus the merged-credential path.
3. Dashboard: badge + reason rendering per failure kind on both surfaces; absence on success.

## Files

- Test files listed above.

## Acceptance criteria

- [ ] Each failure kind is covered on each adapter and each UI surface.
- [ ] Removing the t001 fill lines makes the backend tests fail (mutation spot-check).

## Out of scope

- Coverage for unrelated event types.
