---
id: w1/m153/t006
title: Simplify
worker: worker1
status: done
estimate: 15m
depends_on: [w1/m153/t005]
---

## Objective

Run `/simplify` over the code m153 changed: the hooks, the Apollo client defaults, the panel, picker and editor gates, and the per-site `initialLoading` derivations. Keep it behavior-preserving. For example, delete now-redundant local `initialLoading` guards if the convention makes them tautological, and keep them only where they still express something.

## Acceptance criteria

- [ ] `/simplify` findings are applied, or recorded as declined with the reason.
- [ ] `yarn typecheck && yarn lint && yarn test` are green in `dashboard/`.
