---
id: w1/m153/t005
title: Render parity
worker: worker1
status: todo
estimate: 10m
depends_on: [w1/m153/t004]
---

## Objective

Confirm m153 changed no REST/GraphQL/MCP surface, and that the dashboard behavior it restores matches Render's dashboard. Record any drift instead of silently diverging.

## Steps

1. **No API change.** Confirm the m153 diff touches only `dashboard/`: no schema, REST, or MCP changes.
2. **Compare with Render.** In Render's dashboard, check that an environment's settings, moving resources between environments, and repo search on a new service keep their open state and typed input while the page refreshes in the background. Note what Render does, and when it was checked.
3. **Record drift.** File any divergence found as `w1/NNN`. A plain match needs no `docs/ADR018-render-parity.md` row.

## Acceptance criteria

- [ ] The diff scope is confirmed as dashboard-only.
- [ ] The Render comparison, with its date, is noted in the m153 README. Any drift is filed.
