---
id: w2/m164/t005
title: "ADR086 amendment: the disposition table becomes the census's source of truth"
worker: worker2
status: todo
estimate: 40m
depends_on: [w2/m164/t003, w2/m164/t004]
---

## Objective

Connect the written contract to the executable one. ADR086 already ends with the rule — "Future subject/provenance columns must extend this table and cleanup before shipping" — but nothing enforced it; after this milestone the censuses do, and the ADR should say so and point at them.

## Context

- `docs/ADR086-account-deletion.md` § Data disposition is the identity-side contract.
- ADR043 (tenant isolation) and ADR003 (control plane) are the right homes for a pointer to the tenant-scope census, since workspace teardown completeness is their concern.
- The ADR should state which artifact is authoritative when they disagree — the recommendation is: the ADR states policy, the census enforces coverage, and a census entry may never be looser than the ADR row.

## Steps

1. Add the new dispositions (t004's rows, and anything t003 decided that is identity-adjacent) to ADR086's table.
2. Add a short § naming both censuses, what each enforces, and the rule that a new table declares before it ships.
3. Cross-reference from ADR043/ADR003 for the workspace-teardown half.
4. Record the retention-class list (audit-class tables kept deliberately) so "retained" is never mistaken for "forgotten".
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR086-account-deletion.md`
- `docs/ADR043-tenant-namespace-isolation.md` or `docs/ADR003-control-plane.md` (cross-reference)

## Acceptance criteria

- [ ] Every disposition this milestone decided appears in the ADR.
- [ ] The ADR names the censuses and the precedence rule between policy and guard.
- [ ] The deliberate-retention class is listed as such.
- [ ] Markdown is prettier-clean.

## Out of scope

- Rewriting ADR086's existing decisions.
