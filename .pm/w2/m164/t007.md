---
id: w2/m164/t007
title: "Test coverage: both censuses fail on a seeded violation"
worker: worker2
status: todo
estimate: 40m
depends_on: [w2/m164/t005]
---

## Objective

A guard nobody has watched fail is not a guard. Prove both censuses reject the exact shapes they exist to catch, and that the dispositions they now cover actually run.

## Context

- The mutation-style proof is the standard this repo already applies elsewhere (`w5/m67`'s allowlist test was proven on a seeded violation; `alwayspublic_guard_test.go` enumerates and fails on an unclassified mount).
- Real Postgres is available in CI (`BEX_TEST_DB_URI`).
- Useless-test rules apply: a census test that only re-asserts its own inventory proves nothing — the seeded-violation case is the real assertion.

## Steps

1. Seed a table with a workspace id and no cascade/purger/retention; assert the tenant-scope census fails; drop it.
2. Seed a column of an evading identity shape (`foo_subject`, `bar_email`); assert the identity census fails; drop it.
3. Assert the newly-declared dispositions actually execute: post-deletion state for `workspace_creation_attempts`, post-workspace-delete state for each table t003 cascaded.
4. Confirm both censuses run in the normal backend suite (no build tag that hides them from CI).

## Files

- `lego/backend/internal/store/accounts_pg_test.go`
- `lego/backend/internal/store/tenant_scope_census_pg_test.go`

## Acceptance criteria

- [ ] Both seeded violations fail their census, demonstrated in the task's evidence.
- [ ] Post-deletion and post-workspace-delete states asserted for the newly covered tables.
- [ ] Both censuses run in CI's normal backend job.

## Out of scope

- Live cluster walk (closeout).
