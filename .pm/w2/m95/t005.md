---
id: w2/m95/t005
title: Simplify
worker: worker2
status: todo
estimate: 15m
depends_on: [w2/m95/t004]
---

## Objective

Run `/simplify` over the code m95 changed: the dotenv parser's escape decoding, the auto-growing textarea and its call sites, the shared reserved-key validator and every write path that now calls it, and the dashboard hint wiring. Keep it behavior-preserving. For example, collapse duplicated per-path `PORT` checks into the single validator if any crept in.

## Acceptance criteria

- [ ] `/simplify` findings are applied, or recorded as declined with the reason.
- [ ] `go test ./...` in `lego/backend` and `yarn typecheck && yarn lint && yarn test` in `dashboard/` are green.
