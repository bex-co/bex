---
id: w2/m95/t006
title: Test coverage
worker: worker2
status: done
estimate: 35m
depends_on: [w2/m95/t004]
---

## Objective

Meaningful tests for what m95 shipped. Values must survive export → import and paste → save byte-exact, and `PORT` must be refused identically on every write surface.

## Steps

1. **Round trip** (vitest): the t001 property test plus explicit cases for each JSON escape and a surrogate pair; a negative test that a single-quoted `\uXXXX` stays literal.
2. **Multi-line field** (vitest component tests with the Apollo mocks in `src/test/mocks/apollo.ts`): a three-line paste into the service Environment editor, the env-group editor and the new-env-group dialog reaches the mutation payload with its newlines; an imported multi-line draft renders on multiple lines; a single-line value renders at one row.
3. **`PORT` refusal** (Go): one table test through the real REST/GraphQL/MCP adapters for service and env-group writes, asserting the shared sentence and an unchanged store; a Blueprint validation test with `PORT` in `envVars` asserting the source location; the create-service path.
4. **Control.** The operator's `appEnv` shadowing test and the existing import/export unit tests stay unchanged and green.

## Acceptance criteria

- [ ] Each new test is shown failing on the pre-fix code.
- [ ] `go test ./...` in `lego/backend`, `make test` in `lego/operator`, and `yarn typecheck && yarn lint && yarn test` in `dashboard/` are green.
