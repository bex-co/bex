---
id: w2/m96/t006
title: Simplify
worker: worker2
status: todo
estimate: 15m
depends_on: [w2/m96/t005]
---

## Objective

Run `/simplify` over the code m96 changed: the retained-name store accessor and its writers, `usage/service.go` name resolution, the webhook delivery view, and the two dashboard cards. Keep it behavior-preserving; in particular fold the sandbox and datastore lookups into one batch path if the live-fallback branches duplicate each other.

## Acceptance criteria

- [ ] `/simplify` findings are applied, or recorded as declined with the reason.
- [ ] `cd lego/backend && go test ./...` and `yarn typecheck && yarn lint && yarn test` in `dashboard/` are green.
