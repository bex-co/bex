---
id: w2/m94/t007
title: Simplify
worker: worker2
status: done
estimate: 20m
depends_on: [w2/m94/t006]
---

## Objective

Run `/simplify` over the code m94 changed: the auto-deploy gate and its call sites in `envgroups`, the exported quota helper, the operator's `secretFileMounts` ordering, the service view's link order, and the panel's shadowing computation. Keep it behavior-preserving. For example, collapse the six hard-coded `SaveModeDeploy` call sites onto one helper if the gate makes them identical.

## Acceptance criteria

- [ ] `/simplify` findings are applied, or recorded as declined with the reason.
- [ ] `cd lego/backend && go test ./...`, `make test` in `lego/operator/`, and `yarn typecheck && yarn lint && yarn test` in `dashboard/` are green.
