---
id: w2/m94/t008
title: Test coverage
worker: worker2
status: todo
estimate: 45m
depends_on: [w2/m94/t006]
---

## Objective

Meaningful tests for what m94 shipped. Each must fail on the pre-fix code and assert the real behavior, not a mock echo.

## Steps

1. **Backend (`envgroups`).** Per Render-shaped verb, link, unlink, create-with-services and delete: an auto-deploy-on service gets a deploy row, an auto-deploy-off service gets none and its Secret is updated; the result lists the skipped ids on REST, GraphQL and MCP. Explicit `saveMode` callers unchanged (control).
2. **Backend (quota).** A group seeded over quota: delete-only patch succeeds, add refused with the unchanged sentence.
3. **Operator.** Projection order: service files Secret last whether it was first or last in `spec.filesFromSecrets`; groups keep relative order; removing the service file leaves the group file.
4. **Dashboard.** Panel: precedence ordering; earlier-linked group's colliding key and file marked with the later group named; service-override marker unchanged (control).
5. **Live DoD.** Re-run every m94 README DoD bullet on production and paste the transcript into the README under `## Evidence`.

## Acceptance criteria

- [ ] Each new test is shown failing on the pre-fix code.
- [ ] All three suites are green.
- [ ] The live DoD transcript is in the m94 README.
