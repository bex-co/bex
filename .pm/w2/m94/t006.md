---
id: w2/m94/t006
title: Render parity
worker: worker2
status: todo
estimate: 20m
depends_on: [w2/m94/t004, w2/m94/t005]
---

## Objective

Confirm the m94 changes are consistent across every surface they touch, REST, GraphQL, MCP and the dashboard, and match Render's documented behavior. Record any drift instead of silently diverging.

## Steps

1. **Three adapters agree.** For each changed verb (the Render-shaped group writes, link, unlink, create-with-services, delete, the batch patch, the service read's link order), drive REST, GraphQL and MCP in a composed test or on `dev-2` and compare fields, semantics and error shapes. The auto-deploy-off skip list and the quota refusal sentence must be identical.
2. **Compare with Render.** Against `render.com/docs/configure-environment-variables` (re-fetch and date it): auto-deploy gate, service-over-group for env vars, group-vs-group precedence, and Render's Blueprint behavior for env-group edits. Note where bex deliberately differs (last linked wins) and confirm t005 recorded it.
3. **Dashboard.** Check the Environment page marker and ordering against Render's Environment tab behavior for a linked-group collision, if Render shows one.
4. **Record drift.** File any new divergence as a `w2/NNN` inbox note. Update the m94 README with the comparison date.

## Acceptance criteria

- [ ] REST, GraphQL and MCP return the same result shapes and refusals for every changed verb.
- [ ] The Render comparison, with its date, is noted in the m94 README. Any drift is filed.
