---
id: w2/m94/t005
title: Record the rules in ADR013 and the ADR018 environment-groups row: service wins, then last linked wins, auto-deploy gating
worker: worker2
status: todo
estimate: 15m
depends_on: [w2/m94/t001, w2/m94/t003]
---

## Objective

Every rule this milestone enforces has a written home. `docs/ADR013-secrets.md` states the precedence for env vars and secret files (service over group; last linked wins between groups) and the auto-deploy gate. The environment-groups row of `docs/ADR018-render-parity.md` records where bex matches Render and the one deliberate divergence (last linked vs Render's most recently created group).

## Context

- `docs/ADR018-render-parity.md` row 114 ("Environment groups (+ link / unlink)") is ✅ on all four surfaces and records the save modes but neither the auto-deploy gate nor group precedence.
- `docs/ADR013-secrets.md` describes the Secret projection but not file precedence.
- Render's documented rules (fetched 2026-09-14, `render.com/docs/configure-environment-variables`): service value always wins; between groups precedence is not guaranteed, currently most recently created; group changes redeploy only linked services with autodeploys enabled; nothing stated for secret files.
- The `ApplyEnvGroup` (Blueprint sync) decision from t001 also belongs in the row.

## Steps

1. Add a short "Precedence and redeploy" paragraph to `docs/ADR013-secrets.md` with the three rules and the operator mechanism (`envFrom` order, projected volume order).
2. Update the ADR018 row 114 cell with the auto-deploy gate (matches Render), service-over-group for vars and files (matches for vars; files undocumented by Render, bex chooses service wins), last-linked-wins between groups (deliberate divergence, reason: changing it would flip running services), and the `ApplyEnvGroup` decision.
3. Cross-reference `w2/m94` from both.

## Files

- `docs/ADR013-secrets.md`
- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Both documents state every rule t001–t003 enforce, with the Render source and fetch date.
- [ ] The ADR018 row names the last-linked divergence explicitly.

## Out of scope

- Any code change.
