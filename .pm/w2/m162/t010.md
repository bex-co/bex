---
id: w2/m162/t010
title: Closeout
worker: worker2
status: todo
estimate: 20m
depends_on: [w2/m162/t009]
---

## Objective

Close the milestone once its observable end state is real — not merely once the code is written.

## Context

- The reported symptom is the acceptance test: one GitHub account must serve two workspaces, with no path that stops without saying why.
- Deploy ordering is **migration, then binary**. Un-deploy ordering is **disconnect the extra bindings, then binary, then migration** — the down migration refuses while any installation serves two workspaces ([ADR078 §2](../../../docs/ADR078-github-workspace-connections.md)). Confirm this is written somewhere an on-call reader will find it before closing.

## Steps

1. Walk the milestone's Definition of done on `dev-2`, all seven points.
2. Run the live walk: two real workspaces, one real installation, bind both, push once, confirm one deploy in each and that disconnecting one leaves the other working.
3. Confirm ADR078 §2/§3a/§4a, ADR026 §4, and ADR018's git rows match what shipped.
4. Set every remaining task `status: done`, move each `tNNN.md` to `w2/m162/done/`, mark every row `— **DONE**` and `**Status:** done` in the milestone README, move `w2/m162/` to `w2/done/m162/`, and check `- [x]` in `w2/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w2/m162/**`, `.pm/w2/README.md`

## Acceptance criteria

- [ ] All seven DoD points demonstrated, with evidence recorded here.
- [ ] Live walk done, or explicitly deferred with the blocker named (the way m94–m100 record their production-credential block).
- [ ] Board status consistent in all three places; no stub left behind.

## Out of scope

- The deferred items ADR078 records as future work: a per-connection repo allowlist, and periodic re-proof / binding TTL for proof staleness.
