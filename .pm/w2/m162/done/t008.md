---
id: w2/m162/t008
title: Simplify the code this milestone changed
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m162/t007]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, strictly behavior-preserving.

## Context

- Likely candidates: the candidate-filtering loop in `claimFromCallback` after the bound-elsewhere branch is deleted; the two scope call sites in `webhook.go` that now iterate; overlap between the connect and claim bind paths, which converge further under N:N; and the two quota errors, which may share one shape.
- `make lint` runs whole-program dead-code analysis across all four modules — the removed singular `GitConnectionByInstallation` and any newly-orphaned helper must actually be gone.

## Steps

1. Run `/simplify` over the diff.
2. Take only behavior-preserving changes; anything that alters a proof, a fail-closed branch, or a quota admission is out of scope here.
3. `make lint` from `lego/operator/`.

## Files

- Whatever this milestone touched.

## Acceptance criteria

- [x] No behavior change; all suites still green.
- [x] No dead code left from the singular lookup or the deleted filter.
- [x] `make lint` green across all four modules.

## Out of scope

- Refactors reaching beyond this milestone's diff.
