---
id: w2/m98/t003
title: Capture Render's actual suspended response and record the decision in the ADR018 Suspend/Resume row
worker: worker2
status: todo
estimate: 15m
depends_on: [w2/m98/t001, w2/m98/t002]
---

## Objective

`w1/094` could not find what Render serves for a suspended web service (`render.com/docs/service-actions` returned 404 during the hunt). Capture it from a real Render service or its current docs, then record bex's response and any divergence in the parity ledger instead of assuming.

## Context

- `docs/ADR018-render-parity.md` row 73 (Suspend / Resume) is ✅ on every surface but records no suspended-response decision.
- `.pm/DO_NOT_DO.md` has no suspend entry.
- bex's response after t001/t002: `503` + `Retry-After`, HTML / JSON / plain text by `Accept`, no wake.

## Steps

1. Fetch Render's current docs for suspending a service, or curl a suspended Render service if one is available, and note the status code, content type and body, with the date checked.
2. Add the bex behavior and the Render observation to the Suspend/Resume row. If Render's status or body differs, state the divergence and why bex keeps its shape.
3. Note in this milestone's README where the capture came from.

## Files

- `docs/ADR018-render-parity.md`
- `.pm/w2/m98/README.md`

## Acceptance criteria

- [ ] The Suspend/Resume row names bex's suspended response (status, negotiation, no wake) and Render's observed response with the date.
- [ ] Any divergence is stated explicitly, not implied.

## Out of scope

- Changing bex's response to match Render if it differs; file that as a follow-up note if wanted.
