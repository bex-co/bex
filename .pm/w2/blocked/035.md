# Phase 4: drop dual-read + delete legacy registry/static blobs (ADR055 F2/F3 close)

Why: w2/m92 executed phases 1–3 and armed the 14-day clean window; without a filed follow-up the destructive Phase 4 (and full F2/F3 closure) can be forgotten.

**STOP + window-gated.** Do not promote/execute until an evidenced 14-day clean window ends **and** an explicit change-window authorization is recorded.

## Evidence window (w2/m93)

| Marker | Value |
| --- | --- |
| m92 calendar lower bound | 2026-09-08 → **2026-09-22** (not proof of cleanliness) |
| Continuous evidence start | **pending GitOps roll of Loki 504h + zot/static-server shipper pipelines** — use the first hour those streams are live; do **not** backdate to 2026-09-08 |
| Earliest eligible cleanup | `evidence_start + 14d` **and** ≥ 2026-09-22; whichever is later |
| Readiness command | `KUBECONFIG=… bash scripts/registry-migration-readiness.sh` |
| Initial live assessment | **insufficient evidence** until collection is live for 14 continuous days (see `.pm/w2/m93/evidence/readiness.md`) |

Criteria: `registry-migration-readiness.sh` reports `clean` (zero dual-read hits — no kubelet pull of `<registry>/A:…`, no static-server GET under `A/<rev>/`) and every migrated App remains tombstoned on a scoped image ref. Keep this note unpromoted until its existing gates hold. Revalidated 2026-09-09; cleanup remains open.

## Scope

- Disable/remove `BEX_REGISTRY_DUAL_READ` migration window on the operator (currently `=1` for the window).
- Delete legacy Zot repos `A` and S3 prefixes `A/` (including `.bex-tombstone`) for tombstoned Apps only.
- Confirm no unlabeled sibling still owns `A`.
- Annotate ADR055 F2/F3 fully closed.

## Risk

Destructive; rollback = restore from registry/S3 backup. No in-place un-delete.

## Source

w2/m92 t005 arming; w2/m93 evidence; runbook `docs/runbooks/registry-static-identity-migration.md` § Phase 4; ADR055 F2/F3.

---

## Triage 2026-09-15 — still BLOCKED, and now for a second reason

`bash scripts/registry-migration-readiness.sh` run live against `hetzner-prod`:

```
== inventory (labeled Apps) ==
collection live: service=zot
collection live: service=static-server
RESULT status=insufficient_evidence reason=untombstoned_labeled name=tea-daif693dqjvc73e7as3g-hello-go
```

Two independent gates are open:

1. **The window.** Today is 2026-09-15; the calendar lower bound alone is 2026-09-22, and the real bound is `evidence_start + 14d` where `evidence_start` is the first hour the Loki/shipper streams were live. Both shipper streams now report `collection live`, so the clock has started — but 14 continuous days have not elapsed, and the readiness script is the authority on when they have.
2. **An untombstoned labeled App.** `tea-daif693dqjvc73e7as3g-hello-go` is labeled but not tombstoned on a scoped image ref, which is a `## Scope` precondition ("Confirm no unlabeled sibling still owns `A`", and every migrated App tombstoned). This is new information since the 2026-09-09 revalidation and must be resolved before Phase 4 can even be assessed, independent of the date.

Phase 4 is **destructive with no in-place un-delete**, and the note requires an explicitly recorded change-window authorization — a decision only the operator can make. Re-run the readiness command on or after the later bound; when it reports `clean`, promote this note and execute under an authorized window.
