---
id: w2/m98/t005
title: Simplify
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m98/t004]
---

## Objective

Run `/simplify` over the code m98 changed: the `ingressBackend` precedence, the suspended marker, and the activator's suspended branch. Keep it behavior-preserving. For example, fold the suspended, maintenance and sleep responders onto one negotiation helper if they now duplicate it.

## Acceptance criteria

- [ ] `/simplify` findings are applied, or recorded as declined with the reason.
- [ ] `make test` and `make lint` in `lego/operator/` are green.
