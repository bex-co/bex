---
id: w2/m98/t004
title: Render parity
worker: worker2
status: done
estimate: 10m
depends_on: [w2/m98/t003]
---

## Objective

Confirm m98 changed no REST/GraphQL/MCP shape, and that every surface that describes a suspended service agrees with what the public host now serves. Record any drift instead of silently diverging.

## Steps

1. **Wire shapes.** Confirm the diff touches only `lego/operator/` plus docs: `POST …/suspend` / `…/resume`, GraphQL `suspendService`/`resumeService` and MCP `suspend_service`/`resume_service` still return exactly what they did.
2. **Surfaces agree.** After a suspend, REST/GraphQL/MCP read `suspended=suspended`, the dashboard header reads Suspended, and the public host serves the m98 503. After a resume all four flip back together.
3. **Render comparison.** Reuse t003's capture; confirm the row is updated. File any residual divergence as a `w2/NNN` note.

## Acceptance criteria

- [ ] The diff is confirmed operator-only plus docs.
- [ ] The four-surface agreement after suspend and resume is noted in the m98 README, with any drift filed.
