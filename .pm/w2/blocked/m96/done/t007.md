---
id: w2/m96/t007
title: Test coverage
worker: worker2
status: done
estimate: 40m
depends_on: [w2/m96/t005]
---

## Objective

Meaningful tests for what m96 shipped: a charge line and a delivery row keep a resource's name after deletion, live resources show their current name, and workspace deletion purges the record.

## Steps

1. **Backend (real Postgres):**
   1. A usage row for a deleted service, Postgres and Key Value resolves its retained name with `deleted: true` on REST, GraphQL and MCP.
   2. A sandbox row resolves the session repo+branch label, or `plan · image` when no session exists.
   3. A live renamed service shows its current name; a same-name re-create stays a separate line.
   4. Workspace teardown removes every retained-name row for that tenant.
   5. A delivery for a deleted service still returns `serviceName` on REST and GraphQL.
2. **Dashboard (vitest):** charges card deleted/live/nameless rows; deliveries card name link and deleted case.
3. **Regression controls:** a pre-fix row with no retained name still falls back to the id and does not error.

## Acceptance criteria

- [ ] Each new test is shown failing on the pre-fix code.
- [ ] Backend and dashboard suites green.
