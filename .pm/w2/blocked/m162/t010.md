---
id: w2/m162/t010
title: Closeout
worker: worker2
status: blocked
estimate: 20m
depends_on: [w2/m162/t009]
---

## Objective

Close the milestone once its observable end state is real — not merely once the code is written.

## Context

**BLOCKED 2026-09-16 — the GitHub App's OAuth pair is not configured.** `BEX_GITHUB_APP_CLIENT_ID` and `BEX_GITHUB_APP_CLIENT_SECRET` are **absent from `.env`** (not merely empty; `.env.example` declares both at lines 633–634, so this is also a `.env`/`.env.example` name drift). Without them the installation-admin verifier is nil, and per [ADR078 §7](../../../../docs/ADR078-github-workspace-connections.md) `connectGit`/`claimGit` refuse **at start** — by design, so nobody walks the whole GitHub round trip only to 503 at the callback. That is the same half-configured state ADR078 §7 was written about after the 2026-08-20 production walk, still present locally.

Consequence: the live two-workspace walk cannot run here or anywhere else until the operator adds the pair (generate a client secret on the App's settings page, pair it with the client id, and confirm the Redirect URI + "Request user authorization during installation" per [ADR026 §2](../../../../docs/ADR026-github-integration.md)'s four required settings). Everything else in the Definition of done is already demonstrated — see below.

- The reported symptom is the acceptance test: one GitHub account must serve two workspaces, with no path that stops without saying why.
- Deploy ordering is **migration, then binary**. Un-deploy ordering is **disconnect the extra bindings, then binary, then migration** — the down migration refuses while any installation serves two workspaces ([ADR078 §2](../../../../docs/ADR078-github-workspace-connections.md)). Confirm this is written somewhere an on-call reader will find it before closing.

## Steps

1. Walk the milestone's Definition of done on `dev-2`, all seven points.
2. Run the live walk: two real workspaces, one real installation, bind both, push once, confirm one deploy in each and that disconnecting one leaves the other working.
3. Confirm ADR078 §2/§3a/§4a, ADR026 §4, and ADR018's git rows match what shipped.
4. Set every remaining task `status: done`, move each `tNNN.md` to `w2/m162/done/`, mark every row `— **DONE**` and `**Status:** done` in the milestone README, move `w2/m162/` to `w2/done/m162/`, and check `- [x]` in `w2/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w2/m162/**`, `.pm/w2/README.md`

## Evidence recorded 2026-09-16 (shipped as `c82108964`)

DoD points **3–7 are demonstrated and need no cluster**:

- **3 (fan-out + fail-closed):** `internal/apps/webhook_scope_test.go` — two proved bindings yield both scopes; unknown installation, absent installation id, unwired resolver and lookup error each act on nothing in multitenant operation; an empty workspace id is never emitted as a scope. `webhook_replay_test.go` proves claims are per-scope (same bytes, other workspace ⇒ still fresh) and that a hard failure releases every claim the fan-out took.
- **4 (both quotas, transactional):** `internal/store/git_connections_pg_test.go` races two binds at limit−1 against **real Postgres** through two pools, for each cap independently; the coded refusals are asserted at the service layer.
- **5 (selection grants no new authority):** `internal/github/m75_claim_flow_test.go` — replay, foreign subject, anonymous caller, out-of-set id, and a start-time selector naming an unadministered installation all bind nothing.
- **6 (rollback guard):** verified by hand against real Postgres — the down migration succeeds at one-workspace-per-installation, refuses with `installation(s) 5001 serve multiple workspaces` once two exist, and proceeds after the extra binding is disconnected.
- **7 (suites + docs):** backend `go test -p 1 ./...` green on a fresh Postgres (as CI runs it), operator `make test`, `make lint` across all four modules, CLI tests, dashboard `yarn test` (3471). ADR078 §2/§3a/§4a, ADR026 §4, ADR018 and ADR086 reconciled against the shipped code.

DoD points **1–2 (the live two-workspace walk and the no-dead-end walk) remain open** — they are browser ceremonies against real GitHub, blocked as above.

## Acceptance criteria

- [x] DoD points 3–7 demonstrated, with evidence recorded above.
- [ ] Live walk — **BLOCKED on `BEX_GITHUB_APP_CLIENT_ID`/`_SECRET`** (see Context), recorded the way m94–m100 record their production-credential block.
- [ ] Board status consistent in all three places; no stub left behind.

## Out of scope

- The deferred items ADR078 records as future work: a per-connection repo allowlist, and periodic re-proof / binding TTL for proof staleness.

## Drain verification — 2026-09-21

The configured local environment still has neither `BEX_GITHUB_APP_CLIENT_ID` nor `BEX_GITHUB_APP_CLIENT_SECRET`. Implementation and automated evidence remain shipped in `c82108964`; the remaining GitHub browser ceremony needs the operator to supply that pair and confirm the App callback settings. Parked the whole milestone with t001–t009 preserved in `done/`; no live acceptance is claimed.
