---
id: w2/m97/t005
title: Simplify
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m97/t004]
---

## Objective

Run `/simplify` over the code m97 changed: the classifier, the three call sites (preview, `runSync`, auto-sync worker), the GraphQL type, and the dashboard reason-to-copy mapping. Keep it behavior-preserving. Look especially for the classifier and the dashboard switch duplicating the reason list, and for now-dead error-string massaging in `PreviewBlueprint`.

## Acceptance criteria

- [ ] `/simplify` findings are applied, or recorded as declined with the reason.
- [ ] `cd lego/backend && go test ./internal/apps/...` and `yarn typecheck && yarn lint && yarn test` in `dashboard/` are green.
