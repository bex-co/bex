---
id: w2/m164/t006
title: Simplify the code this milestone changed
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m164/t005]
---

## Objective

Run `/simplify` over this milestone's diff — reuse, simplification, efficiency, behavior-preserving only.

## Context

- Candidate: the two censuses will share most of their machinery (migrate, introspect `information_schema`, compare against a declared inventory, report a readable diff). One helper, two inventories.
- Candidate: `CleanupAccountSubject`'s parallel `deletes`/`updates` statement slices (`store/accounts.go:376-404`) grow with each disposition; check whether they should be driven by the same inventory the census reads, so the contract has exactly one list.

## Steps

1. Run `/simplify` over the diff.
2. Apply behavior-preserving findings; defer semantic ones to a note.
3. Re-run backend tests and `make lint`.

## Files

- Whatever the milestone touched.

## Acceptance criteria

- [x] `/simplify` run; findings applied or declined with a reason.
- [x] Backend tests and lint green.

## Out of scope

- Behavior changes.

## Evidence — 2026-09-22

Three simplify reviewers completed reuse, quality, and efficiency passes over the full milestone diff. Applied the deploy-attribution index, requester/state-based webhook fixture selection, and corrected recovery-worker reference. Both censuses share enumeration and inventory validation. Kept runtime cleanup as explicit SQL: mixing external lifecycle rules, anonymization, and FK cascades into a generic inventory-driven executor would add indirection without reducing policy duplication. The focused real-PG suite passed (12.231s), then the full backend rerun and all-module lint passed on merged main.
