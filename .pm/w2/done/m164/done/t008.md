---
id: w2/m164/t008
title: Closeout
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m164/t006, w2/m164/t007]
---

## Objective

Close the milestone once its definition of done actually holds.

## Context

- DoD is in `.pm/w2/m164/README.md`; it is verifiable from the backend suite against real Postgres — no cluster needed, unlike most closeouts here.
- Board mechanics: `.pm/CLAUDE.md`, `/pm done`.

## Steps

1. Run the full backend suite with `BEX_TEST_DB_URI` set; capture both censuses passing and both seeded-violation demonstrations.
2. Record the final inventory (how many tables, how many cascade / purger / retention) in the milestone README — it is the artifact future work will read.
3. Mark tasks done, move them to `w2/m164/done/`, set `**Status:** done` and the `— **DONE**` markers.
4. Move `w2/m164/` to `w2/done/m164/`; check `- [x]` in `.pm/w2/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w2/m164/**`, `.pm/w2/README.md`

## Acceptance criteria

- [x] Both censuses green, both seeded violations demonstrated red.
- [x] The final inventory recorded in the milestone README.
- [x] Milestone moved to `w2/done/m164/` with no stub left behind.

## Out of scope

- Shipping.

## Evidence — 2026-09-22

The full backend suite passed with a fresh real PostgreSQL database and enforced OpenFGA: `GOWORK=off GOMAXPROCS=3 go test -p 1 -count=1 ./...`. The three new real OpenBao integration tests (including six surface/save-mode subtests) passed separately against native OpenBao 2.5.5. All-module make lint passed. Both live-schema censuses and seeded rejection demonstrations passed; final inventory is 32 identity/provenance columns and 48 workspace tables/columns: 43 cascades, zero SQL tables relying solely on purgers, five explicit retentions. The first full run encountered SSH test-role grants retained by the earlier m163 test database; those isolated local grants were cleared before the successful fresh-database rerun. No production state was involved.
