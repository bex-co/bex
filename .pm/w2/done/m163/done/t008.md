---
id: w2/m163/t008
title: Closeout
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m163/t006, w2/m163/t007]
---

## Objective

Close the milestone once its definition of done actually holds on a running environment.

## Context

- DoD is in `.pm/w2/m163/README.md`; the 401-after-removal check needs a real Hydra, so run it on `dev-2` (`bash scripts/dev-env.sh 2 up`).
- Board mechanics: `.pm/CLAUDE.md`, `/pm done`.

## Steps

1. On `dev-2`: mint a key as member B, remove B, then call the API with B's key and record the result.
2. Record the evidence in the milestone README.
3. Mark tasks done, move them to `w2/m163/done/`, set `**Status:** done` and the `— **DONE**` markers.
4. Move `w2/m163/` to `w2/done/m163/`; check `- [x]` in `.pm/w2/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w2/m163/**`, `.pm/w2/README.md`

## Acceptance criteria

- [x] The live credential check was actually run and its result recorded.
- [x] Milestone moved to `w2/done/m163/` with no stub left behind.
- [x] Workstream README in sync.

## Out of scope

- Shipping.

## Closeout — 2026-09-21

Passed the strict live walk against production API composition, real PostgreSQL, Hydra, Kratos, and enforced OpenFGA. The local CAPD preflight failed, so isolated native services provided the same credential dependencies; Kubernetes was an in-memory client with no resource fixtures. See [the environment, reproduction, and 235 sanitized observations](../evidence/README.md).

The first walk exposed cached machine tokens returning 403 after removal. Unbinding now atomically writes a permanent machine revocation, and both cached and freshly introspected identities consult it. Admin removal, self-leave, and account deletion then returned 401 before cache expiry; Hydra clients, SQL bindings, and FGA tuples disappeared, while remaining members retained access. Scoped exits preserved the departing member's other-workspace key.

Validation: full backend suite with real PostgreSQL and enforced OpenFGA; all-module lint; focused warm/cold, cross-replica, delayed-introspection, atomic-unbind, and idempotency regressions; dashboard Team panel's 20 tests. Three simplify reviewers completed and duplicate test fixtures were replaced with existing helpers.
