---
id: w2/m163/t004
title: "Surface the outcome: the removal response and the API-keys list tell the truth"
worker: worker2
status: done
estimate: 40m
depends_on: [w2/m163/t002]
---

## Objective

Make the consequence visible before and after the act: an admin removing a member should know that N keys will be affected, and the API-keys surface should never show a live key whose creator is gone without saying so.

## Context

- `APIKey.CreatedBy` is already carried on GraphQL (`lego/backend/internal/apikeys/graphql.go:35`) — check whether REST and MCP carry it too, and whether the dashboard renders it at all.
- If t001 chose revocation, the surfacing job is a **pre-removal warning** ("this also revokes 2 API keys") plus an audit row naming the keys.
- If t001 chose retention, the surfacing job is a **badge** on the API-keys list — the option is only defensible if that badge exists.
- Three-adapter parity applies either way (`lego/backend/internal/api/CLAUDE.md`).

## Steps

1. Carry the outcome on the removal response / audit row (which keys, how many).
2. Add the surfacing the decision requires (warning or badge), on all three surfaces plus the dashboard.
3. Add en/zh strings; keep any skeleton matching its ready geometry.

## Files

- `lego/backend/internal/apikeys/rest.go`, `graphql.go`, `mcp.go`
- `lego/backend/internal/members/` (audit/response)
- `dashboard/src/features/` (team + API keys surfaces), locale files

## Acceptance criteria

- [x] The removal outcome for keys is recorded in the audit trail.
- [x] The decision's required surfacing exists on all three API surfaces and in the dashboard.
- [x] Nothing claims a key is fine when its creator has been removed.

## Out of scope

- Changing how keys are minted.
