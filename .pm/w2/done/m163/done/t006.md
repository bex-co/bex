---
id: w2/m163/t006
title: Simplify the code this milestone changed
worker: worker2
status: done
estimate: 30m
depends_on: [w2/m163/t005]
---

## Objective

Run `/simplify` over this milestone's diff — reuse, simplification, efficiency, behavior-preserving only.

## Context

- Candidate: three exit paths now call the same disposition; check it lives in exactly one place with one scoping rule, not three call sites with three subtly different scopes.
- Candidate: the workspace-scoped key lookup (`Binding.TenantForKey` in a loop over all keys, `apikeys/service.go:349`) is O(keys) per call and now runs on member removal too — check whether a scoped store query is warranted.

## Steps

1. Run `/simplify` over the diff.
2. Apply behavior-preserving findings; defer semantic ones to a note.
3. Re-run backend tests, `make lint`, and `dashboard/yarn test`.

## Files

- Whatever the milestone touched.

## Acceptance criteria

- [x] `/simplify` run; findings applied or declined with a reason.
- [x] Backend, lint and dashboard suites green.

## Out of scope

- Behavior changes.
