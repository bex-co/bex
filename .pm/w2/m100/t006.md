---
id: w2/m100/t006
title: Closeout
worker: worker2
status: todo
estimate: 10m
depends_on: [w2/m100/t005]
---

## Objective

Close the milestone once t001–t005 are done **and every Definition-of-done bullet in the m100 README has been re-probed and passes, including the two live bullets against production** (`GET https://api.bex.co/v1/webhooks/event-types` → `200`, `GET https://api.bex.co/v1/notification-settings/overrides?ownerId=<tea-id>` → `200`) on the deployed build. Merged is not enough.

1. Set every task `status: done` and move the tasks to `w2/m100/done/`.
2. Mark every table row `— **DONE**` and set `**Status:** done` in the milestone README.
3. Move `w2/m100/` to `w2/done/m100/`.
4. Check `- [x]` in `.pm/w2/README.md`.
