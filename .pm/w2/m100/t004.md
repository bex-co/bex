---
id: w2/m100/t004
title: Simplify
worker: worker2
status: todo
estimate: 15m
depends_on: [w2/m100/t003]
---

## Objective

Run `/simplify` over the code m100 changed: the validator's pass-through decision in `render_openapi.go`, the reverse inventory test and any mux-walking helper it shares with the forward inventory test, and the overrides list handler in `apps/rest.go`. Keep it behavior-preserving. For example, if the forward and reverse inventory tests now walk the same route list, share one enumerator rather than two.

## Acceptance criteria

- [ ] `/simplify` findings are applied, or recorded as declined with the reason.
- [ ] `cd lego/backend && go test ./...` and `make lint` (from `lego/operator/`) are green.
