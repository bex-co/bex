---
id: w2/m99/t005
title: Simplify
worker: worker2
status: done
estimate: 15m
depends_on: [w2/m99/t004]
---

## Objective

Run `/simplify` over the code m99 changed: the progress follower's queued-reason mapping and re-emission key, any reconciler or migration change from the carrier decision, and the adapter test. Keep it behavior-preserving; for example, fold the reason→line table into the existing narration helpers rather than adding a parallel one.

## Acceptance criteria

- [ ] `/simplify` findings are applied, or recorded as declined with the reason.
- [ ] `cd lego/backend && go test ./...` and `make lint` (from `lego/operator/`) are green.
