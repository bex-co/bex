---
id: w5/m102/t004
title: REST / GraphQL / MCP adapters for the Leave verb
worker: worker5
status: todo
estimate: 45m
depends_on: [w5/m102/t002]
---

## Objective

Expose the one Leave verb identically on all three surfaces as thin fragments — no second implementation, no surface-only guard.

## Context

- Members adapters: `lego/backend/internal/members/rest.go`, `graphql.go`, `mcp.go`; the contract is one service verb, three registration fragments (`lego/backend/internal/api/CLAUDE.md`).
- Shape per t001's decision — expected: REST `DELETE /v1/workspaces/{id}/members/me` (a bex extension, like `POST /v1/invites/accept`), GraphQL `leaveWorkspace(workspaceId:)`, MCP `leave_workspace`.
- `TestSurfaceParityAndWiring` asserts every feature registers into the single schema/router/registry; `TestAuthzGuardsEveryVerb` sweeps for the authorization gate — a self-scoped gate must still satisfy it (see `AcceptInvite`'s precedent).

## Steps

1. Add the three fragments, each mapping straight onto `LeaveWorkspace`.
2. Carry the coded refusals (`OWNER_CANNOT_LEAVE`, last-admin) identically on all three.
3. Give the MCP tool a description that tells an agent it acts on itself only.
4. Confirm the parity and authz sweep tests pass without an exemption; if an exemption is needed, justify it in the test's excuse list.

## Files

- `lego/backend/internal/members/rest.go`, `graphql.go`, `mcp.go`
- `lego/backend/internal/api/server_test.go`

## Acceptance criteria

- [ ] All three surfaces expose Leave with identical semantics and codes.
- [ ] No surface accepts a subject argument.
- [ ] Parity and authz sweeps pass; any excuse entry is justified in place.

## Out of scope

- Dashboard (t005).
