---
id: w5/m99/t007
title: Render parity across REST/GraphQL/MCP and the dashboard
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m99/t001, w5/m99/t004, w5/m99/t005, w5/m99/t006]
---

## Objective

Check that this milestone's contract changes are identical on every surface that exposes them, and that any remaining gap against render.com is written down rather than silently diverging.

## Context

- Changed contract surface: the sandbox read shape's effective lifetime (`t001`) and the agent-session lifecycle's teardown guarantees (`t003`, `t004`).
- Sandbox surfaces: `lego/backend/internal/sandbox/{rest,graphql,mcp}.go`. Agent-session surfaces: `lego/backend/internal/agentsessions/{rest,graphql,mcp}.go` and the dashboard's Agents pages.
- `docs/ADR006-bex-api.md` is the cross-surface contract; `docs/ADR018-render-parity.md` holds the divergence ledger; `docs/cli-compatibility-checklist.md:270` grades `ea sandbox create`.
- Render's own sandbox surface is early-access; `docs/render-artifacts/ea-sandbox.md` is the captured reference.

## Steps

1. Compare field names, semantics, and error shapes for the changed sandbox and agent-session reads across REST, GraphQL, and MCP.
2. Check the dashboard renders the effective lifetime wherever it shows sandbox state, and that a reclaimed sandbox is reflected rather than shown as running.
3. Compare against render.com's documented sandbox timeout behavior and record any drift as a row in ADR018 and a line in the CLI checklist.

## Files

- `lego/backend/internal/sandbox/{rest,graphql,mcp}.go`
- `lego/backend/internal/agentsessions/{rest,graphql,mcp}.go`
- `dashboard/src/features/` agent-session and sandbox surfaces
- `docs/ADR018-render-parity.md`, `docs/cli-compatibility-checklist.md`

## Acceptance criteria

- [ ] A cross-surface test asserts the changed fields match on REST, GraphQL, and MCP.
- [ ] Any divergence from Render is a written ledger row, not an undocumented difference.

## Out of scope

- Adding sandbox surfaces Render does not have.
