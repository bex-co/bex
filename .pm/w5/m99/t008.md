---
id: w5/m99/t008
title: Simplify
worker: worker5
status: todo
estimate: 20m
depends_on: [w5/m99/t007]
---

## Objective

Run `/simplify` over the code this milestone changed: reuse, simplification, efficiency, altitude. Behavior-preserving only.

## Context

- The reconcile in `t003` and `sandbox.CleanupAgentDispatches` do overlapping work (both list the workspace's sandboxes and terminate a subset) — a prime candidate for unification rather than a second parallel scanner.
- The teardown-intent handling in `t004` overlaps the existing `agent_session_dispatches` machinery.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply the findings that are behavior-preserving; file anything larger as a separate note.

## Files

- the milestone's changed files

## Acceptance criteria

- [ ] `/simplify` findings applied or explicitly deferred with a reason.
- [ ] `make lint` and the backend suite stay green.

## Out of scope

- Refactors beyond the milestone's diff.
