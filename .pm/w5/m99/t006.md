---
id: w5/m99/t006
title: Reclaim the production orphan and record its cost (needs explicit user approval)
worker: worker5
status: todo
estimate: 20m
depends_on: [w5/m99/t003]
---

## Objective

Stop the live orphan and write down what it cost, so the milestone closes against a real number.

## Context

- Sandbox `271ec9ce-a32b-4128-bb43-02a9e57b01b6`, workspace `bex` / `tea-d98210cbbpdc73dcrkvg`, plan `starter`, `status: running` since `2026-08-30T02:20:56Z`, image `ghcr.io/bex-co/bex-agent-sandbox@sha256:e7adb8cb…`.
- Its session `ags-da9p720k98cs738k20c0` was canceled `2026-08-30T02:27:32Z`.
- As of 2026-09-15 it had accrued `866.53 vCPU-hr` = `$77.55` in period `2026-09` alone, against a workspace `billing.currentCost.amountUsd` of `$247.72` (fully absorbed by a $1000 credit grant).
- **This is a pre-existing production resource that no QA run owns.** Stopping it is the user's call, not an agent's. It may also still hold state worth capturing.

## Steps

1. Ask the user to confirm the stop, and whether anything on the sandbox should be captured first.
2. On approval, stop it through the supported path (`bex ea sandboxes stop <id>`, or the `t003` reconcile once deployed — preferring the reconcile proves the fix).
3. Confirm absence from `bex ea sandboxes list` and that the usage meter stops advancing for that `serviceId`.
4. Record the final accrued cost for August + September in this milestone's README, and check whether any other workspace holds the same shape.

## Files

- none (operational); evidence recorded in `.pm/w5/m99/README.md`

## Acceptance criteria

- [ ] The user explicitly approved the stop before anything was terminated.
- [ ] `bex ea sandboxes list` returns no `running` sandbox without a live owning session, in every accessible workspace.
- [ ] The total cost across both periods is recorded in the milestone README.

## Out of scope

- Any Stripe credit or invoice adjustment — ADR071 tenant credits are a `DO_NOT_DO.md` non-goal; grants stay a manual Stripe Dashboard action.
