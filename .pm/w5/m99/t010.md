---
id: w5/m99/t010
title: Closeout
worker: worker5
status: todo
estimate: 10m
depends_on: [w5/m99/t009]
---

## Objective

Close the milestone once its definition of done actually holds.

## Steps

1. Confirm every acceptance criterion in `README.md` § Definition of done is actually met — in particular that production holds no orphaned sandbox and the recorded cost is in the README.
2. Set every remaining task `status: done`, move each `tNNN.md` to `w5/m99/done/`, mark every row `— **DONE**`, set `**Status:** done`.
3. Move `w5/m99/` to `w5/done/m99/` and check `- [x]` in `.pm/w5/README.md`.

## Acceptance criteria

- [ ] Definition of done verified against live production, not just code.
- [ ] Status synced in the workstream README, the milestone README, and every task's frontmatter.

## Out of scope

- Shipping — `/ship` is a separate, explicitly authorized action.
