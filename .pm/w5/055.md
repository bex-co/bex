# 055 — Retire the stale FUTURE-MAYBE entry for datastore lifecycle webhook events

Why: `.pm/FUTURE-MAYBE.md` still lists "Datastore availability/health/completion webhook events" as deferred pending a trigger, but `w3/m82` shipped exactly that on 2026-09-08 — so the board describes landed work as future and the next `/pm-brainstorm` may re-propose it.

- **Entry:** `.pm/FUTURE-MAYBE.md` line 17 under `## Deferred` — trigger text: "a second feature needs durable operator status transitions (status history, uptime reporting), or a tenant asks for datastore health webhooks".
- **Landed:** [`.pm/w3/done/m82/README.md`](../w3/done/m82/README.md) — `datastore_event_facts` (migration 0107) gives the observed Postgres/Key Value lifecycle a durable source: eleven observed types (`postgres_unavailable`/`postgres_available`, `key_value_unhealthy`/`key_value_available`, backup completed/failed, restore succeeded/failed, upgrade started/succeeded/failed) plus four configuration-change types, hydrated through `GET /v1/events/{eventId}` and projected to the events feed, outbound webhooks, and push. `docs/ADR018-render-parity.md` "Outbound event webhooks" row and gap backlog were updated the same day.
- **Do:** move the entry to `## Done (trigger met, work landed)` (line 51) with a one-line "TRIGGERED and landed 2026-09-08 as `w3/m82`" lead, keeping the original text for history — matching the section's existing entries. `npx prettier@3.4.2 --write` the file.
- **Out of scope:** any code change; re-auditing m82's vocabulary.

Filed from `/pm-brainstorm for w5` 2026-09-10 proposal 7 (sub-hour, so an inbox note rather than a milestone).
