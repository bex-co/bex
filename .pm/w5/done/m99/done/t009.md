---
id: w5/m99/t009
title: Test coverage
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m99/t007]
---

## Objective

Lock in the three orphan paths with tests that fail on `main` today, so this class cannot regress silently.

## Context

- The production shape is: terminal session, `sandbox_id = ''`, sandbox alive, row far outside the reaper's `8h30m` window.
- Existing suites to extend: `lego/backend/internal/agentsessions/` (completion/reaper tests), `lego/backend/internal/sandbox/` (`agent_dispatch_test.go`, `service_test.go`), and the real-Postgres store tests (`internal/store/store_pg_test.go`).

## Steps

1. Write the three failing-first tests: blanked `sandbox_id` on a canceled session, a terminal row aged past the window, and a sandbox with no row.
2. Add a durable-retry test: repeated terminate failures still retry.
3. Add the lifetime-bound test from `t001` and the metric-label assertions from `t005`.
4. Confirm each new test fails on `main` before the fix and passes after.

## Files

- `lego/backend/internal/agentsessions/*_test.go`
- `lego/backend/internal/sandbox/*_test.go`
- `lego/backend/internal/store/store_pg_test.go`

## Acceptance criteria

- [ ] Every new test is shown failing on `main` and passing after the fix.
- [ ] No test asserts only that a function was called; each asserts the observable outcome (sandbox terminated, row converged, series incremented).
- [ ] `cd lego/backend && go test ./...` and `make lint` green.

## Out of scope

- A live production E2E, which `t006` covers.
