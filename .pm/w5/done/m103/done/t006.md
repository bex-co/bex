---
id: w5/m103/t006
title: Simplify the code this milestone changed
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m103/t005]
---

## Objective

Run `/simplify` over this milestone's diff — reuse, simplification, efficiency, behavior-preserving only.

## Context

- Candidate: the member/seat/owner projections are read in three places (`members.List`, `members.SeatUsage`, `workspaces.ListMembers`). After filtering, check whether they share one projection helper instead of three near-copies.
- Candidate: the machine-subject discriminator should exist exactly once, not inline at each call site.

## Steps

1. Run `/simplify` over the diff.
2. Apply behavior-preserving findings; defer semantic ones to notes.
3. Re-run backend tests, `make lint`, and `dashboard/yarn test`.

## Files

- Whatever the milestone touched.

## Acceptance criteria

- [ ] `/simplify` run; findings applied or declined with a reason.
- [ ] Backend, lint and dashboard suites green.

## Out of scope

- Behavior changes.
