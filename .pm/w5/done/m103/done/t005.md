---
id: w5/m103/t005
title: Render parity check for the member list, seat usage and owner fields
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m103/t003, w5/m103/t004]
---

## Objective

Check the reshaped member list, seat usage and owner attribution stay consistent across REST, GraphQL, MCP and the Team page, and match Render's shapes (`teamMember`, `owner.usage.users`, the owner object).

## Context

- Pinned evidence: `docs/render-artifacts/team-members.graphql`, `docs/render-artifacts/owners-api.md`, `docs/render-artifacts/workspace-family-walk.md`.
- ADR024 already records bex's deliberate divergences (no `name` trait, flatter GraphQL, `identityResolved` instead of Render's `status`) — extend that section rather than starting a new list.
- Render has no notion of machine members on the team surface, which is the parity argument for t002.

## Steps

1. Diff the four surfaces for identical member/seat/owner semantics.
2. Compare each against the pinned Render artifacts; record evidence limits honestly.
3. Update ADR024's divergence section and the ADR018 rows.
4. File drift as w5 inbox notes.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR024-members.md`, `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Surface diff recorded with no unexplained divergence.
- [ ] Divergences documented in ADR024 with reasons.
- [ ] Drift filed as notes.

## Out of scope

- Implementing newly found gaps.
