---
id: w5/m103/t008
title: Closeout
worker: worker5
status: todo
estimate: 15m
depends_on: [w5/m103/t006, w5/m103/t007]
---

## Objective

Close the milestone once its definition of done actually holds — including the already-satisfied branch, if t001's evidence took it there.

## Context

- DoD is in `.pm/w5/m103/README.md` and has two legitimate endings: the gap closed, or the gap disproved with evidence and the milestone re-scoped to owner attribution. The w5/m61 precedent (ALREADY-SATISFIED, no code) is the model for the second.
- Board mechanics: `.pm/CLAUDE.md`, `/pm done`.

## Steps

1. Walk the DoD on `dev-5` with humans + bound keys present.
2. Record evidence — or, on the already-satisfied branch, record the disproof and what was shipped instead.
3. Mark tasks done, move them to `w5/m103/done/`, set `**Status:** done` and the `— **DONE**` markers.
4. Move `w5/m103/` to `w5/done/m103/`; check `- [x]` in `.pm/w5/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w5/m103/**`, `.pm/w5/README.md`

## Acceptance criteria

- [ ] DoD verified with evidence, or the premise disproved with evidence and the outcome recorded.
- [ ] Milestone moved to `w5/done/m103/` with no stub left behind.
- [ ] Workstream README in sync.

## Out of scope

- Shipping.
