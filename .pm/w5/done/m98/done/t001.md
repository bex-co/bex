---
id: w5/m98/t001
title: Bring up dev-5, reprovisioning the shared kind/CAPD cluster if needed; record what was rebuilt
worker: worker5
status: done
estimate: 30m
depends_on: []
---

## Objective

Get a healthy `dev-5` stack on the shared local cluster without disturbing other workstreams' `dev-N` stacks, and record exactly what had to be rebuilt.

## Context

- `bash scripts/dev-env.sh 5 up` / `status` (idempotent, pre-approved); ports in `.pm/w5/dev-5/ports.env`; namespaces `dev-5`, `dev-5-auth`.
- Cluster state at filing: API refused on `127.0.0.1:55003` (2026-09-10 21:06 local). `w1/m139` Evidence: apiserver crash-looping from etcd read latency (09-09); `mock-cluster.sh` reran green on 09-10 reusing 13-day-old nodes. If `dev-env.sh` diagnostics point at the cluster, `bash scripts/mock-cluster.sh` (reprovision) is pre-approved; `dev-1` may be in use by `w1/m139` — coexist, do not tear it down.
- Never print `.env` or kubeconfigs.

## Steps

1. `kubectl --kubeconfig infra/local/bex.kubeconfig get nodes`; if refused, follow `dev-env.sh` diagnostics → `mock-cluster.sh` as needed.
2. `bash scripts/dev-env.sh 5 up && bash scripts/dev-env.sh 5 status` until the verification inventory passes.
3. Record in the milestone README: what was rebuilt, node ages, and whether any other `dev-N` was live concurrently.

## Files

- `.pm/w5/m98/README.md` (Evidence)

## Acceptance criteria

- [ ] `dev-env.sh 5 status` passes its inventory.
- [ ] Rebuild scope recorded; no other workstream's stack was modified.

## Out of scope

- Fixing cluster substrate bugs beyond what bring-up requires (file them).
