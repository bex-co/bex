---
id: w5/m102/t006
title: Render parity check for the Leave surface
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m102/t005]
---

## Objective

Check the Leave verb is consistent across REST, GraphQL, MCP and the dashboard, and compare it with render.com's leave-team behavior, recording drift as follow-up rather than diverging silently.

## Context

- Render manages members through dashboard GraphQL; pinned evidence in `docs/render-artifacts/team-members.graphql` and `workspace-family-walk.md`.
- bex's REST Leave route is a deliberate bex extension (Render has no public members REST) — record it as such, the way ADR024 records `POST /v1/invites/accept`.
- Ledger row: `docs/ADR018-render-parity.md`; the feature ADR is `docs/ADR024-members.md`.

## Steps

1. Diff the four surfaces for identical semantics, codes and refusals.
2. Compare against Render's leave-team flow from the pinned artifacts; state evidence limits honestly where the tier does not permit capture.
3. Update ADR024 (§ the membership matrix from `m101/t006`) and the ADR018 row.
4. File any drift as a w5 inbox note.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `docs/ADR024-members.md`, `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Surface diff recorded; no unexplained divergence.
- [ ] bex-extension surfaces labeled as such.
- [ ] Drift filed as notes, not silently accepted.

## Out of scope

- Implementing newly found parity gaps.
