---
id: w5/m102/t007
title: Simplify the code this milestone changed
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m102/t006]
---

## Objective

Run `/simplify` over this milestone's diff — reuse, simplification, efficiency, behavior-preserving only.

## Context

- The obvious candidate: `Remove` and `LeaveWorkspace` now share the revoke-then-delete sequence and the guard set; check whether one shared internal helper is clearer than two parallel bodies — without collapsing their different authorization gates.
- Dashboard side: the post-action "refresh list, wait for provider, select destination" sequence now exists for invite acceptance and for leaving; check for a shared hook.

## Steps

1. Run `/simplify` over the diff.
2. Apply behavior-preserving findings; defer anything semantic to a note.
3. Re-run backend tests, `make lint`, and `dashboard/yarn test`.

## Files

- Whatever the milestone touched.

## Acceptance criteria

- [ ] `/simplify` run; findings applied or declined with a reason.
- [ ] Backend, lint and dashboard suites green.

## Out of scope

- Behavior changes.
