---
id: w5/m95/t005
title: Test coverage
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m95/t003]
---

## Objective

Make every DoD bullet falsifiable by an automated check: outcome classification (each branch), nil-safety, keep-list admission, promtool fire/no-fire, coverage-check inclusion.

## Context

- `lego/backend/internal/sandbox/metrics_test.go`, `service_test.go` (existing fakes for the runtime client), `rules/alerts_test.yml`, `scripts/gitops-validate.sh` (a guard that the keep-list regex still contains `sandbox`).

## Steps

1. Add the missing negative cases (denied create increments nothing; capacity error → `capacity`; runtime error → `failed`).
2. Add a gitops-validate guard for the keep-list entry so a later regex edit cannot silently drop the family.

## Files

- as above

## Acceptance criteria

- [ ] Mutating the outcome classifier makes a test fail.
- [ ] Removing `sandbox` from the keep-list regex makes `gitops-validate.sh` fail.

## Out of scope

- Load or soak tests.
