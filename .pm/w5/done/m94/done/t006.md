---
id: w5/m94/t006
title: Render parity
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m94/t005]
---

## Objective

Confirm the telemetry surface stays Render-compatible after the extension: the exact upstream `CliTelemetryEventPOSTInput` body is still accepted unchanged, the header is optional, and the divergence is recorded rather than silently assumed.

## Context

- `docs/ADR006-bex-api.md` (one core, thin adapters); `docs/ADR018-render-parity.md`; `docs/cli-compatibility-checklist.md` "Usage analytics go to bex-api, never Render" bullet.
- Upstream exposes telemetry over REST only — m92 recorded GraphQL/MCP as intentionally out of scope; keep that.

## Steps

1. Replay the pinned upstream request body (from m92's fixtures) with and without the header; assert identical 2xx and the same stored fields except `bex_version`.
2. Confirm the unmodified upstream `render` binary against bex still ingests (no header → `''`).
3. Update the checklist bullet and the ADR018 CLI-analytics mention to name the header and field as a bex extension; flag any drift as a follow-up note rather than fixing here.

## Files

- `docs/cli-compatibility-checklist.md`, `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Fixture replay proves the upstream body is accepted unchanged.
- [ ] Checklist and ledger name the extension; no undocumented divergence remains.

## Out of scope

- GraphQL/MCP telemetry twins (deliberately absent, as upstream).
