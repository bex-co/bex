---
id: w5/m94/t009
title: Closeout
worker: worker5
status: done
estimate: 15m
depends_on: [w5/m94/t008]
---

## Objective

Close the milestone only when its definition of done actually holds: shipped to origin/main, deployed, and the live board shows the new dimension.

## Context

- `.claude/skills/pm/SKILL.md` closeout rule; `.pm/CLAUDE.md` done-folder rule.

## Steps

1. Verify each DoD bullet against production (read-only: the live board filters by release; one real post-deploy row carries both versions).
2. Set every task `status: done`, move task files to `done/`, mark rows `— **DONE**`, set `**Status:** done`, move `w5/m94/` to `w5/done/m94/`, tick the box in `w5/README.md`.

## Files

- `.pm/w5/m94/**`, `.pm/w5/README.md`

## Acceptance criteria

- [x] DoD verified with dated evidence in the README before the move.
- [x] Board status consistent in all three places.

## Out of scope

- Any further feature work.

## Evidence (2026-09-14)

Production closeout recorded in the milestone README § Production closeout: migration present; `cli_analytics.events` re-bootstrapped to expose `bex_version`; real tree-built launcher deliveries (`bex --version`, `bex workspaces`, `bex glm`) carry `cli_version=2.27.0` + `bex_version=0.0.0-m94-verify`; live dashboard ConfigMap has the Bex-release filter and adoption panel; reader-role panel queries return the new release. Grafana Argo annotation-size stall filed as `w5/058`.
