---
id: w5/m92/t006
title: Render parity cross-surface check
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m92/t005]
---

## Objective

Confirm the new telemetry surface matches render.com's wire shape and record the intentional surface boundaries.

## Context

- Upstream exposes telemetry over REST only (`POST /cli-telemetry-events`); there is no GraphQL/MCP equivalent to mirror.
- bex-api parity doctrine: `docs/ADR006-bex-api.md`; parity ledger: `docs/ADR018-render-parity.md`.

## Steps

1. Diff our accepted request/response shape against upstream `CliTelemetryEventPOSTInput` field-for-field (names, enums like `completion_kind`).
2. Verify dashboard needs nothing (telemetry is internal diagnostics, no UI surface).
3. File any drift as follow-up work; record GraphQL/MCP as intentionally out of scope with the reason (upstream has none).

## Files

- `docs/ADR018-render-parity.md` (telemetry row if the ledger tracks it)
- `docs/cli-compatibility-checklist.md` (confirm t005's row)

## Acceptance criteria

- [ ] Field-level parity statement recorded; drift, if any, filed as follow-up, not silently diverged.
- [ ] GraphQL/MCP exclusion noted with reason.

## Out of scope

- Building GraphQL/MCP telemetry surfaces.
