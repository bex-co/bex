---
id: w5/m92/t008
title: Test coverage
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m92/t007]
---

## Objective

Add meaningful tests for the behavior this milestone shipped — real behavior and failure modes, never trivial or tautological tests.

## Context

- Existing gates to extend: backend API conformance tests, `bridge_test.go` consent mapping, launcher tests in `lego/cli`.

## Steps

1. Handler tests: valid body persists, unknown fields accepted, malformed body rejected, unauthenticated rejected, flood throttled.
2. Store tests: attribution from auth identity, id-convention compliance, retention enforcement.
3. Bridge tests: default sends (no force-disable), both opt-outs suppress, explicit user settings preserved.

## Files

- Test files adjacent to t001–t005's implementation.

## Acceptance criteria

- [ ] Every acceptance bullet in t001–t005 has at least one automated test behind it.
- [ ] `cd lego/backend && go test ./...` (touched packages at minimum) and `cd lego/cli && go test ./...` green.

## Out of scope

- Snapshot-everything or coverage-gaming tests.
