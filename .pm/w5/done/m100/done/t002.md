---
id: w5/m100/t002
title: Document dual predeploy + build reachability
worker: worker5
status: done
estimate: 20m
depends_on: [w5/m100/t001]
---

## Objective

Update ADR010, ADR018 (pre-deploy / build-logs rows), and `docs/cli-compatibility-checklist.md` `--type` so the dual path is explicit: `type=predeploy` remains the precise live extension; `type=build` also carries pre-deploy stdout once shipped.

## Acceptance criteria

- [ ] Checklist no longer grades `--type` as unqualified support on app/request alone.
- [ ] ADRs state CLI clients use `type=build` to read pre-deploy output.
