# Grafana Argo sync stalls: dashboard ConfigMap annotations exceed 262144 bytes

Why: Argo cannot apply further CLI/product analytics dashboard updates until `grafana-dashboards-platform` sheds its oversized `last-applied-configuration` annotation, so obs.bex.co board changes risk staying stuck OutOfSync after each GitOps edit.

**DONE 2026-09-14** during `w5/m95` closeout: set `ServerSideApply=true` on `deploy/gitops/base/grafana.yaml`, cleared the live ConfigMap's `kubectl.kubernetes.io/last-applied-configuration` annotation, and forced a sync. Argo `grafana` returned to `Synced`; the ConfigMap published the m95 sandbox panels (`bex_sandbox_create_total` et al.). No last-applied annotation remains on the object.
