---
id: w5/m101/t001
title: "Failing test: removing a personal-workspace owner resurrects admin on their next request"
worker: worker5
status: done
estimate: 40m
depends_on: []
---

## Objective

Pin the defect down with a test before changing behavior: remove the owner of a personal workspace (another admin present, so `guardLastAdmin` passes), then drive the onboarding path the auth gate runs on every session request and assert what actually happens — the `tenant_members` row is gone, `tenants.owner_identity_id` still names the removed subject, and `EnsureTenant` both re-grants the workspace-admin tuple and resolves the removed owner's default workspace back to it.

## Context

- `members.Service.Remove` — `lego/backend/internal/members/service.go:956`; deletes the row and revokes the tuple, never touches the owner binding.
- `tenantService.EnsureTenant` — `lego/backend/internal/api/tenancy.go:156`; resolves by `TenantForOwner` (owner-keyed, `internal/store/workspaces.go:76`), so it never mints a replacement workspace, then calls `ensureGranted` (`tenancy.go:334`) which re-grants `admin`.
- The auth gate calls it per request: `lego/backend/internal/api/auth.go:341`.
- Default-workspace resolution skips the membership check: `lego/backend/internal/core/base.go:651-668`.
- Real-Postgres integration tests run in CI (`BEX_TEST_DB_URI`), see `lego/backend/internal/store/store_pg_test.go` for the pattern.

## Steps

1. Add a real-Postgres test in `lego/backend/internal/api` (or `members`) that: mints a personal workspace via `CreateTenantWithMember`, adds a second admin, removes the owner through `members.Remove`, then calls `EnsureTenant` for the removed owner.
2. Assert the current (wrong) outcome explicitly so the test documents the defect, with a comment naming the two failure branches (resurrection vs. bricked account).
3. Add the sibling assertion for `ChangeRole` self-demotion and `Remove` self-removal (both currently succeed).
4. Leave the test named for the invariant it will assert after t002/t003, and mark the wrong-outcome assertions with a TODO pointing at those tasks.

## Files

- `lego/backend/internal/api/tenancy_test.go` (or a new `members_owner_test.go` alongside it)
- `lego/backend/internal/members/members_test.go`

## Acceptance criteria

- [ ] A test exists that reproduces the owner-removal resurrection against real Postgres and fails (or is explicitly pinned to the wrong outcome) before t002 lands.
- [ ] The test covers all three subjects of the milestone: owner removal, self-removal, self role change.
- [ ] No production code changed in this task.

## Out of scope

- Fixing the behavior (t002/t003).
- Ownership transfer (deferred, `.pm/FUTURE-MAYBE.md`).
