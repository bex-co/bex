---
id: w5/m101/t010
title: Closeout
worker: worker5
status: todo
estimate: 15m
depends_on: [w5/m101/t008, w5/m101/t009]
---

## Objective

Close the milestone once its definition of done actually holds — the observable end state, not merely the code.

## Context

- DoD is in `.pm/w5/m101/README.md`; it requires a store-backed environment with OpenFGA enforced (`scripts/dev-env.sh 5 up`).
- Board mechanics: `.pm/CLAUDE.md` and the `/pm done` subcommand.

## Steps

1. Walk the DoD on `dev-5`: attempt owner removal, self-removal, self role change on REST, GraphQL, MCP and the Team page; confirm each refusal and that the rows survive.
2. Record the evidence (commands, codes, screenshots) in the milestone README.
3. Set every remaining task `status: done`, move each `tNNN.md` to `w5/m101/done/`, mark every row `— **DONE**`, set `**Status:** done`.
4. Move `w5/m101/` to `w5/done/m101/` and check `- [x]` in `.pm/w5/README.md`.
5. `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w5/m101/**`, `.pm/w5/README.md`

## Acceptance criteria

- [ ] Every DoD bullet verified against a running environment, with evidence recorded.
- [ ] Milestone moved to `w5/done/m101/` with no stub left behind.
- [ ] Workstream README checkbox and status line in sync.

## Out of scope

- Shipping (`/ship` is a separate, user-initiated action).
