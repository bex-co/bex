---
id: w5/m101/t009
title: "Test coverage: the invariant matrix end to end"
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m101/t007]
---

## Objective

Cover the behavior this milestone shipped with tests that assert real failure modes — the resurrection path above all, since it is invisible from the members surface alone.

## Context

- The t001 reproduction becomes the regression test once the guards land.
- Real Postgres is available in CI (`BEX_TEST_DB_URI`), and OpenFGA too (`BEX_TEST_OPENFGA_URL`) — the tuple assertion needs the latter.
- An enumeration test is the durable form of the rule (ADR024's contributor-boundary precedent): a member-mutating verb added later should fail the sweep if it skips the self/owner guards.

## Steps

1. Convert t001 into a passing regression test over the shipped guards.
2. Add the OpenFGA-backed assertion: after a refused owner removal, and after a successful ordinary removal, the tuples match the rows.
3. Add the concurrency test: two admins removing each other at the same instant cannot land the workspace at zero admins (the advisory-lock path) — extend it to the owner guard.
4. Add the enumeration sweep over `members.Service`'s member-mutating verbs asserting each runs the self and owner guards.
5. Dashboard tests from t005 count toward this; verify they run in `dashboard/yarn test`.

## Files

- `lego/backend/internal/members/members_test.go`
- `lego/backend/internal/store/store_pg_test.go`
- `lego/backend/internal/api/tenancy_test.go`

## Acceptance criteria

- [ ] Regression test proves the resurrection path is closed.
- [ ] Row↔tuple consistency asserted against real OpenFGA.
- [ ] Concurrency test covers the owner guard under the advisory lock.
- [ ] An enumeration test fails if a new member-mutating verb skips the guards.
- [ ] No tautological or assert-nothing tests added.

## Out of scope

- Live cluster verification (covered by the DoD walk at closeout).
