---
id: w5/m101/t007
title: Render parity check for the three refusals across every surface
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m101/t005, w5/m101/t006]
---

## Objective

Check this milestone's change is consistent across every surface it exposes — REST, GraphQL, MCP in `lego/backend/`, plus the dashboard Team UI — and compare the refusals against render.com's own behavior, flagging drift as follow-up rather than diverging silently.

## Context

- Render manages members through its dashboard GraphQL; the pinned capture is `docs/render-artifacts/team-members.graphql`, and `docs/ADR018-render-parity.md` holds the ledger row.
- Known Render behavior to verify or record as unverifiable from this tier: you cannot change your own role, and leaving a team is a separate action from removing a member.
- ADR024 already records honest "not capturable from this workspace's tier" notes — use that pattern rather than guessing.

## Steps

1. Diff the three refusals across REST/GraphQL/MCP for identical codes, semantics and messages.
2. Compare with Render's equivalents from the pinned artifacts; where the tier makes a behavior uncapturable, say so explicitly.
3. Update the ADR018 row (and `docs/render-artifacts/` evidence) with what was verified.
4. File any drift as a w5 inbox note rather than fixing it here.

## Files

- `docs/ADR018-render-parity.md`
- `docs/render-artifacts/team-members.graphql` (evidence notes)

## Acceptance criteria

- [ ] Surface diff done and recorded; no unexplained divergence between the three adapters and the UI.
- [ ] Render comparison recorded with explicit evidence limits.
- [ ] Any drift filed as a note, not silently accepted.

## Out of scope

- Implementing newly discovered parity gaps.
