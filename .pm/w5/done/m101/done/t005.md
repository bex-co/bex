---
id: w5/m101/t005
title: 'Team panel: "you" marker, owner badge, disabled remove/role controls with the reason'
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m101/t004]
---

## Objective

Stop the dashboard from offering actions the API now refuses. The caller's own row and the owner's row keep their controls visible but disabled (the m50 always-rendered-disabled pattern), each with a tooltip naming the reason, and the server refusal still renders inline if it is ever reached.

## Context

- `dashboard/src/features/team/components/team-panel.tsx` — `handleRemove` (`:84`) and `handleChangeRole` (`:81`) are offered for every row today; rows are keyed by `member.subject` (`:169`).
- The caller's own identity is available from the session/`currentUser` data the dashboard already loads — confirm the exact source before adding a query.
- The owner flag needs a field on the member view; coordinate with t004 (surface it as `isOwner` on `workspaceMembers`, or derive it — decide and keep all three surfaces consistent).
- Disabled-control precedent: `EditableFieldRow` / the m50 pattern (`dashboard/CLAUDE.md`).
- Skeleton rule: if the row gains chrome, the Team panel's loading skeleton must still match the ready geometry at desktop and narrow-mobile widths (root `CLAUDE.md`).

## Steps

1. Mark the caller's row ("You") and the owner's row (owner badge) in the members table.
2. Disable the Remove action and role picker on those rows with an explanatory tooltip; keep them rendered, not hidden.
3. Keep the inline server-error path working for the race (someone else changed roles mid-session).
4. Add en/zh locale strings.
5. Verify pending and ready states side by side at desktop and narrow-mobile widths.

## Files

- `dashboard/src/features/team/components/team-panel.tsx`
- `dashboard/src/features/team/components/__tests__/team-panel.test.tsx`
- dashboard locale files

## Acceptance criteria

- [ ] The caller's own row offers no enabled Remove or role control, and says why.
- [ ] The owner's row offers no enabled Remove, and says why.
- [ ] Tests cover both rows plus a normal teammate row (still fully actionable).
- [ ] Loading skeleton still matches the ready state at both widths.

## Out of scope

- The Leave workspace action (`w5/m102` ships it in workspace settings).
