---
id: w5/m97/t003
title: View column, "Creations by surface" and "Agent-driven share" panels, SQL tests, runbook definitions
worker: worker5
status: done
estimate: 45m
depends_on: [w5/m97/t002]
---

## Objective

Make the split readable on the existing board with the same honesty rules v1 established (gaps not zeros, legacy labeled, audience filters applied).

## Context

- `scripts/product-analytics.sql` (`product_analytics.events` view at line 19; reader column allowlist enforced by `scripts/test_product_analytics.py`).
- Dashboard `bex-product-adoption` (`deploy/gitops/base/values/grafana.values.yaml`, uid ~line 192); the collapsed **Adoption detail** row hosts creator/activation/deploy detail — add there, not to the main inventory layout.
- `docs/runbooks/product-analytics.md` § Adoption detail table and § Known gaps (line ~39).

## Steps

1. Expose `surface` on the events view; extend the allowlist and fixtures.
2. Panels: "Creations by surface" (stacked per-day bars, exclusive categories, respects audience/type filters and selected end) and "Agent-driven share" (MCP successful creations ÷ all successful creations in range; null when the denominator is zero, never 0%).
3. Runbook: add the `surface` definition + precedence rules to the Adoption detail table; remove the attribution sentence from Known gaps; keep "CLI-with-agent is not derivable here" as an explicit gap.
4. `scripts/test_product_analytics.py`: both queries against fixtures with mixed surfaces and legacy `unknown` rows.

## Files

- `scripts/product-analytics.sql`, `scripts/test_product_analytics.py`
- `deploy/gitops/base/values/grafana.values.yaml`, `docs/runbooks/product-analytics.md`

## Acceptance criteria

- [ ] Python suite green; `bash scripts/gitops-validate.sh` green; pinned render diff limited to the intended panels.
- [ ] After deploy, both panels return 200 through Grafana (recorded in the milestone README).

## Out of scope

- Any change to the main inventory panels.
