---
id: w5/m98/t003
title: "m88: agent-turn latency/outcome series via a repo-less session, or record the model-key blocker"
worker: worker5
status: todo
estimate: 45m
depends_on: [w5/m98/t001]
---

## Objective

Prove the m88 metrics contract live: after a real repo-less agent session on dev-5, the control-plane registry exposes `bex_agent_session_turn_duration_seconds{outcome}` and `bex_agent_session_turn_outcomes_total{outcome}` with the pin-timing semantics the unit tests asserted.

## Context

- Deferred line: `.pm/w5/done/m88/README.md:58`; contract in `lego/backend/internal/agentsessions/completion_metrics.go`.
- Repo-less sessions are green on `dev-N` per `w3/m79`; a model key is needed — use the operator's configured provider credentials if present in `.env` (never print), else record BLOCKED with the exact variable name.
- dev-5 has no Grafana; read `/metrics` on the dev-5 bex-api directly and compare the live production panels ("Agent turn running duration p95", "Agent turn outcomes (15m)") for shape only.

## Steps

1. Create a repo-less session with a trivial prompt; wait for terminal.
2. Scrape `/metrics`; assert one outcome increment and one duration sample under the right label; confirm a never-running turn adds no duration sample (force one if cheap).
3. Record PASS/FAIL or BLOCKED.

## Files

- `.pm/w5/m98/README.md` (Evidence)

## Acceptance criteria

- [ ] Dated line with the exact series/labels observed, or BLOCKED naming the missing credential.

## Out of scope

- First-token tracing / provider-token accounting (m88 deferral, explicitly out).
