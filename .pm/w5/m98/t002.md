---
id: w5/m98/t002
title: "m89–m91: multi-replica Metrics walk — instance selection, aggregation, percentages, selection persistence"
worker: worker5
status: todo
estimate: 45m
depends_on: [w5/m98/t001]
---

## Objective

Walk the Metrics card with a real multi-replica service — the walk m89 deferred — and, since m90/m91 changed the same card, confirm the CPU/memory percentages across a rollout and selection persistence through empty windows in the same session.

## Context

- Deferred lines: `.pm/w5/done/m89/README.md:59`; m90 (percentages across replicas/rollouts) and m91 (selection preserved through empty windows/refreshes) closed with unit evidence and a live walkthrough for m90 (`9e868c855`) — re-check them only where the multi-replica case differs.
- Dashboard: `dashboard/src/features/metrics/` (instance selector, aggregate mode); API: `GET /v1/metrics/*`, GraphQL/MCP twins via `MetricsWithQuantiles` (`w6/m110`).
- Playwright MCP writes to `.playwright-mcp/`; use bare filenames.

## Steps

1. Deploy a paid-tier web service on dev-5 and scale to 3 replicas; generate load; open the Metrics tab at desktop and narrow widths.
2. Verify: each instance selectable, "all" aggregates, percentages sane per replica and during a rollout, selection survives an empty window and a refresh.
3. Compare the same window over REST, GraphQL, and MCP — identical series/quantiles.
4. Record PASS/FAIL with screenshots; fix small FAILs in-task with a regression test; file larger ones.

## Files

- `.pm/w5/m98/README.md` (Evidence); possibly `dashboard/src/features/metrics/**`

## Acceptance criteria

- [ ] Dated PASS/FAIL lines for m89, m90, m91 aspects; screenshots referenced.
- [ ] Cross-surface comparison recorded.

## Out of scope

- New Metrics features.
