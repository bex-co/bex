# Grafana Argo sync stalls: dashboard ConfigMap annotations exceed 262144 bytes

Why: Argo cannot apply further CLI/product analytics dashboard updates until `grafana-dashboards-platform` sheds its oversized `last-applied-configuration` annotation, so obs.bex.co board changes risk staying stuck OutOfSync after each GitOps edit.

Observed 2026-09-14 during `w5/m94` closeout: Argo application `grafana` is `OutOfSync` with sync error `ConfigMap "grafana-dashboards-platform" is invalid: metadata.annotations: Too long: may not be more than 262144 bytes`. The live ConfigMap already carries the m94 Bex-release panels, so current board content is fine; the failure blocks the next dashboard patch. Fix by clearing/replacing the annotation (or switching the dashboard provision path away from a single giant annotated ConfigMap) and confirming `grafana` returns to Synced.
