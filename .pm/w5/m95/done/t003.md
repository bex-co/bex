---
id: w5/m95/t003
title: Panel beside the agent-session provisioning panels; coverage check green; ADR088 row flips to "alert"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m95/t002]
---

## Objective

Give the new alert its panel (the "every alert gets a panel" rule, mechanically enforced) and update ADR088 so the coverage table stops listing an unowned surface.

## Context

- Dashboard `bex-api-gateways` (`deploy/gitops/base/values/grafana.values.yaml`, uid ~line 3137) already hosts "Agent sandbox provision p95" (~3241) and "Agent sandbox provision outcomes + failure ratio (1h)" (~3392).
- `scripts/obs-coverage-check.sh` (w5/054) maps alert series → panels with matcher compatibility and `unless` awareness; waiver list should stay as-is.
- `docs/ADR088-platform-observability-ui.md:108` Sandboxes row; § per-dashboard SLI table (bex-api + gateways row).

## Steps

1. Add "Sandbox create outcomes + failure ratio (1h)" (verbatim rule expression mirrored) and "Sandbox create p95" next to the agent panels; keep the dashboard's existing sizing.
2. Run `scripts/obs-coverage-check.sh`; confirm `SandboxProvisionFailing` is covered without a waiver.
3. ADR088: flip the Sandboxes row to `alert` with the rule name and `w5/m95`; add the series to the SLI table; remove "Owner: unfiled".

## Files

- `deploy/gitops/base/values/grafana.values.yaml`, `docs/ADR088-platform-observability-ui.md`

## Acceptance criteria

- [ ] `scripts/obs-coverage-check.sh` exit 0, new alert listed as covered, waiver count unchanged.
- [ ] `bash scripts/gitops-validate.sh` passes; pinned Helm render diff is only the intended panels.
- [ ] ADR088 has no "unfiled" cell.

## Out of scope

- Reworking existing agent panels.
