---
id: w5/m95/t002
title: SandboxProvisionFailing rule with a traffic floor, promtool fire/no-fire tests, scrape keep-list
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m95/t001]
---

## Objective

Page when a meaningful share of sandbox creates fail, never on an idle hour or a single failure, and make sure the series actually reaches Prometheus.

## Context

- Shape to mirror: `deploy/gitops/base/prometheus.yaml` `AgentSessionProvisionFailing` (~line 1215): failed-ratio over `[1h]` `> 0.25` **and** total rate `> 0.0028` (≈10/h), `for: 15m`, `severity: warning`, no `vector(0)` coercion.
- Tests: `deploy/gitops/base/rules/alerts_test.yml` (`AgentSessionProvisionFailing` cases at ~1108/1126/1139: fire, below-floor silence, low-ratio silence).
- Keep-list: `prometheus.yaml:268` `regex: bex_(billing|agent_session|webhooks|push|api)_.*` — the m86 "death layer" lesson: a rule over a never-scraped series is worse than no rule.
- promtool is installed by the GitOps CI workflow; locally it may be absent — say so if you could not run it.

## Steps

1. Add `bex_sandbox_create_total` to the keep-list regex (`sandbox`).
2. Add `SandboxProvisionFailing` using `outcome=~"failed|capacity|unavailable"` over `bex_sandbox_create_total`, same thresholds and `for` as the sibling; summary/description name the tenant impact.
3. Add three promtool cases mirroring the sibling's.

## Files

- `deploy/gitops/base/prometheus.yaml`, `deploy/gitops/base/rules/alerts_test.yml`

## Acceptance criteria

- [ ] `promtool test rules deploy/gitops/base/rules/alerts_test.yml` passes (or CI's run is cited if local promtool is unavailable).
- [ ] `bash scripts/gitops-validate.sh` passes.
- [ ] The keep-list admits the family (pinned render shows the regex).

## Out of scope

- Terminate-failure alerting (paneled only).
