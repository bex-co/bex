---
id: w5/m101/t008
title: Simplify the code this milestone changed
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m101/t007]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, behavior-preserving only.

## Context

- The likely finding: four guards now sit at the head of `Remove`/`ChangeRole` (`AuthorizeOnTarget`, `AuthorizeFreshOn`, `guardCallerRoleSettled`, `guardSelf`, `guardOwner`, `guardLastAdmin`) — check whether they read as one coherent preamble or want a single `guardMemberMutation` seam.
- Also check the store side for a duplicated owner/last-admin recheck that could share one helper inside the advisory-lock transaction.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply the behavior-preserving findings; leave anything that changes semantics to a follow-up note.
3. Re-run backend tests + `make lint`.

## Files

- Whatever the milestone touched.

## Acceptance criteria

- [ ] `/simplify` run and its findings applied or explicitly declined with a reason.
- [ ] `cd lego/backend && go test ./...` and `make lint` green.

## Out of scope

- Behavior changes.
