---
id: w5/m94/t008
title: Test coverage
worker: worker5
status: todo
estimate: 45m
depends_on: [w5/m94/t006]
---

## Objective

Add meaningful tests for the shipped behavior and its failure modes — real assertions, not tautologies.

## Context

- Launcher: header scoping, idempotent install, one-event-per-native-command, opt-out silence, no argv leakage.
- Backend: header sanitization (charset, length), absent header, migration down/up, view allowlist.
- SQL: bex-release filter and installations-by-release query against fixtures with `unknown` and mixed releases.

## Steps

1. Fill the gaps left by t001–t004's tests; include at least one negative case per boundary (garbage header, opted-out run, third-party host).

## Files

- `lego/cli/**/*_test.go`, `lego/backend/internal/clitelemetry/*_test.go`, `lego/backend/internal/store/clitelemetry_pg_test.go`, `scripts/test_cli_analytics.py`

## Acceptance criteria

- [ ] Every DoD bullet has at least one automated assertion behind it.
- [ ] No test passes trivially (spot-check by mutating the code under test).

## Out of scope

- Coverage-percentage games.
