---
id: w5/m94/t004
title: "CLI usage board: bex-release filter + installations-by-release panel; label provider launches; retire the m93 caveat"
worker: worker5
status: done
estimate: 30m
depends_on: [w5/m94/t003]
---

## Objective

Surface the new dimension on the existing ops board so release adoption and agent-launch usage are read, not inferred, and remove the wording that labeled them as gaps.

## Context

- Dashboard `bex-cli-usage` in `deploy/gitops/base/values/grafana.values.yaml` (uid at ~line 4974; "Agent and CI signals per day" ~5523, "Detected agent families" ~5577); datasource is the restricted `cli_analytics.events` view.
- m93 t004 documented metric definitions and coverage boundaries — find the exact doc via `.pm/w5/done/m93/done/t004.md` and `docs/ADR088-platform-observability-ui.md` § per-dashboard SLI table (CLI usage row).
- `scripts/test_cli_analytics.py` executes every committed panel query against real Postgres; `scripts/gitops-validate.sh` pins the datasource contract.

## Steps

1. Add a `bex_release` template variable (distinct `bex_version`, `unknown` included) and thread it into the product panels the same way the upstream-version variable is threaded; collection-health panels stay global.
2. Add "Installations by bex release" (distinct installation ids per release, last value) and make sure `code` / `glm` / `upgrade` / `version` rows are labeled in the command table.
3. Replace the coverage-boundary sentences (board descriptions, m93 definitions doc, ADR088 row) with the measured definitions; keep the remaining honest gaps (logged-out early failures).
4. Extend the Python suite with the new panel queries and a `bex_version` filter case.

## Files

- `deploy/gitops/base/values/grafana.values.yaml`
- `scripts/test_cli_analytics.py`
- `docs/ADR088-platform-observability-ui.md`, the m93 definitions doc

## Acceptance criteria

- [ ] `bash scripts/gitops-validate.sh` and the Python suite green; pinned Helm rendering unchanged except the intended dashboard JSON.
- [ ] After deploy, the live board filters by bex release and every new panel query returns 200 through Grafana (recorded in the milestone README).

## Out of scope

- New alert rules (telemetry is product analytics, not an SLI).
