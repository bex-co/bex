---
id: w5/m96/t002
title: "Wire the repository: fixture ids as variables, key via .env + scripts/gh-secrets.sh"
worker: worker5
status: todo
estimate: 15m
depends_on: [w5/m96/t001]
---

## Objective

Publish the fixture to GitHub Actions with the secrecy split ADR088 prescribes: ids as repository variables (legible in run logs), the key as a secret.

## Context

- `.github/workflows/ssh-edge-liveness.yml:388-421` (job `tenant-view-liveness`: needs `BEX_CANARY_SERVICE_ID`, `BEX_CANARY_URL` vars + `BEX_CANARY_API_KEY` secret); `deploy-canary.yml:48-78` (needs `BEX_CANARY_WORKSPACE_ID` var + key; optional `BEX_CANARY_STATIC_REPO`); `isolation-matrix.yml`.
- `scripts/gh-secrets.sh` pushes `.env` → GitHub secrets; `.env.example:476` already mirrors the name.
- ADR019 credential inventory names the custody.

## Steps

1. `gh variable set BEX_CANARY_WORKSPACE_ID/BEX_CANARY_SERVICE_ID/BEX_CANARY_URL`.
2. `bash scripts/gh-secrets.sh` with the key in `.env`; confirm via `gh secret list` (name only).
3. Update the ADR019 inventory row if it still says owed.

## Files

- `docs/ADR019-infra-credentials.md`

## Acceptance criteria

- [ ] `gh variable list` shows the three ids; `gh secret list` shows the key name.
- [ ] `.env.example` unchanged (name already present) and `git status` shows no `.env`.

## Out of scope

- Any runner or pool change (`#RUNNER-HOSTS`).
