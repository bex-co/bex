---
id: w5/m96/t007
title: Test coverage
worker: worker5
status: todo
estimate: 20m
depends_on: [w5/m96/t004, w5/m96/t005]
---

## Objective

Guard the wiring so it cannot silently drift back to "owed": a mechanical check that every `BEX_CANARY_*` name referenced by a workflow is declared where ADR088 says it lives (`.env.example` for the secret, the ADR088 settings table for variables), and that the soft-skip branch still exists for a missing configuration so an unset value never false-reds.

## Context

- `scripts/github-actions-validate.sh` already fails closed on runner-label shapes; extend it (or add a sibling script it calls) with the name-parity assertion and fixture-prove both red paths.

## Steps

1. Add the assertion + a fixture test (a workflow referencing an undeclared `BEX_CANARY_FOO` fails; a removed soft-skip guard fails).

## Files

- `scripts/github-actions-validate.sh` (+ test fixture)

## Acceptance criteria

- [ ] Both red paths fixture-proven; current tree passes.

## Out of scope

- Testing the probes' own assertions (they are their own tests).
