---
id: w5/m96/t003
title: Dispatch the three credentialed workflows once; record green run ids in ADR088 and the m83 evidence
worker: worker5
status: todo
estimate: 30m
depends_on: [w5/m96/t002]
---

## Objective

Turn "owed" into evidence: one green `workflow_dispatch` per credentialed workflow, with no soft-skip notice, recorded where the claim lives.

## Context

- Workflows: "Production edge liveness" (`ssh-edge-liveness.yml`, job `tenant-view-liveness`), "Deploy canary (weekly)" (`deploy-canary.yml`), "Isolation matrix (weekly)" (`isolation-matrix.yml`, covers both `verify-tenant-isolation.sh` and `verify-sandbox-isolation-live.sh`; the model-key legs stay default-off per ADR088).
- Evidence homes: `docs/ADR088-platform-observability-ui.md` settings table (replace `_owed_`), `.pm/w3/done/m83/README.md` § Evidence (annotate; do not reopen the milestone).

## Steps

1. `gh workflow run` each; `gh run watch`; confirm the log has no `::notice::…skipped — not configured` line.
2. Record run ids + dates in ADR088 and m83's Evidence.

## Files

- `docs/ADR088-platform-observability-ui.md`, `.pm/w3/done/m83/README.md`

## Acceptance criteria

- [ ] Three green run ids recorded; the deploy canary's teardown converged on every read surface (its own assertion).
- [ ] No `_owed_` cell remains except the optional static repo.

## Out of scope

- Changing probe cadence or thresholds.
