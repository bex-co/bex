---
id: w5/m96/t005
title: Classify the canary workspace as canary for product analytics; confirm the ops boards exclude it
worker: worker5
status: todo
estimate: 15m
depends_on: [w5/m96/t003]
---

## Objective

Keep first-party probe activity out of customer analytics: label the canary workspace explicitly (never inferred from billing exclusion, per the runbook) and check both ops boards treat it as non-customer.

## Context

- `docs/runbooks/product-analytics.md` § Audience classification: admin upserts exact workspace ids into `product_analytics_audiences` with `audience ∈ {customer, internal, qa, canary}` (migration `0114_product_analytics.up.sql:26-28`). That schema is part of the still-uncommitted product-adoption work; if it is not live in production yet, record the id in the runbook's classification list as pending instead.
- The CLI usage board (`bex-cli-usage`) sees only CLI telemetry; the probes use curl, so no exclusion is needed there — confirm and note.

## Steps

1. If `product_analytics_audiences` exists in production: insert `('<tea-…>', 'canary')` via the admin path; verify the Product adoption board's default audience filter drops the workspace and the unclassified tile decrements.
2. Else: add the id to the runbook's pending-classification list with the date.

## Files

- `docs/runbooks/product-analytics.md`

## Acceptance criteria

- [ ] Either the classification row exists in production, or the pending entry is recorded with the exact id.
- [ ] The deploy canary's weekly create/delete does not appear as customer growth once classified.

## Out of scope

- Classifying other internal/QA workspaces (belongs to the product-adoption rollout).
