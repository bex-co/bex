# w7 — project workstream (worker7)

**Worker:** worker7. This is a general-purpose bex workstream. It may take work anywhere in the project; the milestones below are scheduled work and historical records, not a permanent purpose, specialty, or ownership boundary.

## Local dev environment

Develop against `.pm/w7/dev-7/`, this worker's own isolated stack on the shared local kind/CAPD cluster — never the shared cluster's default `auth`/`bex-system` namespaces or standard ports (5173/4433/4445/8090/8091/5432), which any other worker's session may also be using. `dev-7` gets its own Kratos + Hydra + Mailpit (namespace `dev-7-auth`) and app namespace (`dev-7`), reusing the shared cluster's CNPG operator and bex operator, plus a locally-built `bex-api` on dedicated ports derived from N=7 (`dev-7/ports.env`) so it never collides with any other workstream's `dev-N`.

- `bash scripts/dev-env.sh 7 up` — bring it up (idempotent — safe to re-run)
- `bash scripts/dev-env.sh 7 status` — health check (processes, pods, HTTP) + a pass/fail verification inventory
- `bash scripts/dev-env.sh 7 down` — tear it down (leaves the shared cluster and every other workstream's `dev-N` untouched)
- `bash scripts/dev-env.sh 7 clean` — reclaim `logs/` and `bin/` (refuses while the environment is up)

`up` prints the dashboard command to point at it once bex-api is running. One shared implementation serves every workstream since `w1/m72`; `.pm/w7/dev-7/` keeps only `ports.env` (a generated record of the derivation), this README, and `.gitignore`.

## Milestones

- [ ] **m151** — [Coherent datastore placement after moves and deletion](m151/README.md) (8 tasks, ~4h55m) ← approved additional w7 brainstorm
- [ ] **m152** — [Validate grouping mutations before changing members](m152/README.md) (8 tasks, ~4h25m) ← approved additional w7 brainstorm

- [x] **m148** — [Reliable local cluster bring-up](done/m148/README.md) (7 tasks) ← approved five-item brainstorm. Local CAPD cluster reprovisioned 2026-09-17; the rebuild also fixed a pre-existing helm `--set` comma bug that had been aborting every bring-up at metrics-server.
- [ ] **m149** — [Correct cold deep-link initialization](m149/README.md) (8 tasks) ← promotes 049. **Unblocked 2026-09-17** — its gate `w7/m148/t007` is done and the local cluster is healthy. t001 remains a live measurement the DoD forbids substituting reasoning for; the falsified/confirmed candidates from the `049` triage are recorded in the milestone README to aim it.
- [ ] **m150** — [Sandbox file copy through the pinned CLI](m150/README.md) (9 tasks) ← promotes 046

- [x] **m89** — [Finish production build-cache enablement](done/m89/README.md) (8 tasks; ~4–6h) ← promoted from `043`; `/pm-brainstorm for w7 for top 3 customer-impactfully work` 2026-09-09 #1. **DONE 2026-09-15 on the hold branch of its own DoD:** the live image contains both correctness fixes (m87 + m88), and capacity passes the trial's admission budget — Zot 41.49% used, retained peak 44.62%, 20.03 GiB reserve above peak to the 65% ceiling against an expected 6–9 GiB of cache growth (analytic; `BEX_BUILD_CACHE` is manager-wide, so no per-App switch exists to measure one production shape with). The 48–72h trial is **explicitly held with `BEX_BUILD_CACHE` unset in production**: its one remaining start condition is human — a named observer plus an independently verified armed rollback — so t004/t005 are deferred rather than marked done and carried as [`047`](blocked/047.md). ADR060 D3 and the drill closeout both carry the dated hold.
- [x] **m90** — [CNPG `enablePDB: false` for single-instance clusters](done/m90/README.md) (7 tasks; ~3h) ← same brainstorm #2; ADR060 D8 residual. **DONE 2026-09-15:** single-instance Clusters project `enablePDB: false`, HA Clusters keep CNPG's default, the existing estate converges on reconcile (no one-shot); unit + envtest pins; ADR060 D8 residual closed.
- [x] **m147** — [Restore `bex ea sandboxes exec` under the pinned Render CLI: run connect-token handshake + SSE exit/error shapes](done/m147/README.md) (10 tasks; ~6h) ← continuous `/qa-find-bugs-cli` for w7, 2026-09-14; source-proven, live repro is t001. Since the v2.24 pin the client first mints `POST /v1/sandboxes/{id}/runs/stream/token`, a route bex never shipped (`w3/m33/t002` deferred). Every pin also decodes `exit_code`/`{status,message}` while the gateway emits `exitCode`/`{error,code}`, so failing sandbox commands exit 0. **Code shipped 2026-09-15 (t002–t009):** handshake routes, pinned SSE shapes, two-sided contract test, ledger re-grade. **DONE 2026-09-15:** live DoD verified against production with the pinned launcher (exit 7 / exit 0 / `SANDBOX_NOT_FOUND` message; token single-use and binding refusals) after deploy run 34950840699.

- [x] **m87** — [Rebuild native output when build-time environment changes](done/m87/README.md) (8 tasks; 3h implementation, ~5h total) ← approved 2026-09-08 pm-brainstorm proposal 1. **DONE 2026-09-08:** opaque native env revision busts BuildKit cache on environment-only changes; local BuildKit A→B proof + unit coverage; ADR018/ADR060 updated. Unblocks m88/t001.
- [x] **m88** — [Make clear-cache deploys effective](done/m88/README.md) (9 tasks; 4h implementation, ~6h total) ← approved proposal 2. **DONE 2026-09-08:** clearCache=clear skips registry import for that release, still exports fresh cache; sibling Job fence; ADR018/ADR060 + drill evidence.

- [x] **m1** — East-west tenant isolation: default-deny network for tenant workloads (8 tasks) ← from `/pm-brainstorm for w7` 2026-07-09
- [x] **m2** — Tenant workload hardening: Pod Security baseline + quotas + token hygiene (7 tasks) ← from `/pm-brainstorm for w7` 2026-07-09
- [x] **m3** — bex-api abuse hardening: Render-shaped rate limits + request caps (8 tasks) ← from `/pm-brainstorm for w7` 2026-07-09
- [x] **m4** — Tenant egress hardening: block cloud metadata + node-local endpoints (6 tasks) ← from `/pm-brainstorm for w7` 2026-07-11
- [x] **m5** — Managed Key Value network access controls (ipAllowList parity) (8 tasks) ← from `/pm-brainstorm for w7` 2026-07-11
- [x] **m6** — Custom domain collision + reserved-host guard (Render "already in use" parity) (7 tasks) ← from `/pm-brainstorm for w7` 2026-07-12
- [x] **m7** — Least-privilege platform RBAC (operator + bex-api secret scoping) (6 tasks) ← from `/pm-brainstorm for w7` 2026-07-12
- [x] **m8** — Tenant registry authn/z (close the unauthenticated Zot hole) (8 tasks) ← from `/pm-brainstorm more for w7` 2026-07-12
- [x] **m9** — Per-workspace abuse limits (creation caps + build concurrency) (7 tasks) ← from `/pm-brainstorm more for w7` round 2, 2026-07-12
- [x] **m10** — Security hygiene: image CVE scanning in CI + HTTP hardening headers (7 tasks) ← from `/pm-brainstorm more milestones to work on` 2026-07-13, groups `001`, `002` (each sub-hour)
- [x] **m11** — Admission-time tenant-image signature verification (8 tasks) ← from `/pm-brainstorm more milestones to work on` 2026-07-13 (`w6/006` shipped signing, verification was deferred and never picked back up — flagged three times across two workstreams)
- [x] **m27** — Blueprints dashboard surface (list · manifest · validate · sync) (10 tasks) ← from `/pm-brainstorm more tasks for w5` 2026-07-13; closes last ✖ UI cell in parity ledger
- [x] **m12** — Delete really deletes: purge orphaned tenant artifacts on service/Postgres/Key Value deletion (12 tasks) ← from `/pm service deletion across all service, db, key value types` 2026-07-14 (verbs shipped in w2/m4 + w5/m14; this closes the delete-time teardown gaps)
- [x] **m28** — Build logs: ship `type=build` into the log store (9 tasks) ← from `/pm-brainstorm more milestones for each worker` 2026-07-14 (ADR018 §Logs "`type=build` stays empty by design"; unblocks `w5/m29`); placed here for capacity — topical owner w3 has 4 open milestones (the m27 precedent)
- [x] **m29** — Execute and record the ADR031 restore drills (7 tasks) ← from `/pm-brainstorm more milestones for each worker` 2026-07-14 (`w2/done/m27`'s "operational drills require live cluster execution" residual — etcd, OpenBao, and bex-db restores have never actually been run)
- [x] **m30** — Render OpenAPI contract-conformance suite in CI (8 tasks) ← from `/pm-brainstorm more milestones for each worker` round 4, 2026-07-14 (the parity ledger's manual "verified vs Render's OpenAPI" method has no automated guard; the gap-well is dry so the risk is now drift, not absence). Mechanizes ADR018's central claim; w7's CI-guard charter (gitleaks/trivy/structural guards). Render-parity closing task omitted — the milestone IS the parity check
- [x] **m31** — `renderSubdomainPolicy`: disable the platform subdomain (9 tasks) ← from `/pm-brainstorm` round 6, 2026-07-14 (field-level spec-grep: enum enabled|disabled on webServiceDetails + POST/PATCH, zero hits in `lego/`; bex mirror = drop `<slug>.onbex.co` from `effectiveHosts` while custom hosts serve); host policy is the m6 domain-guard territory
- [x] **m32** — Service inbound `ipAllowList`: web services + static sites (10 tasks) ← from `/pm-brainstorm` round 7, 2026-07-14 (systematic field-diff: `[{cidrBlock, description}]` on webServiceDetails + staticSiteDetails POST/PATCH, zero hits in apps; the m5 Traefik-middleware mechanism, HTTP flavor)
- [x] **m33** — Fix CNPG bootstrap vs. the tenant egress deny (6 tasks) ← promotes `004` 2026-07-15 (production defect from the `w9/m3` rollout verification: new managed Postgres on tenant nodes stalls in CNPG init — the m4 egress-deny policy selects the workspace label the operator propagates onto CNPG pods, blocking their k8s-API traffic; deny-overrides-allow ⇒ the selectors must be split)
- [x] **m34** — Rate-limit response headers (verify-first) (7 tasks) ← from `/pm-brainstorm` round 10, 2026-07-15 (polishes `m3`: bex sends only `Retry-After` on the 429 itself — verified in `internal/api/ratelimit.go`; t001 captures Render's actual header contract from the live API + pinned spec, and a Render-ships-nothing finding closes the milestone as parity-by-absence)
- [x] **m35** — Outbound webhook SSRF guard (shared dial-time IP block with the activator) (5 tasks) ← from `/pm-brainstorm` round 11, 2026-07-15 (the one **open** ADR028 follow-up-register finding: `webhooks/worker.go`'s `defaultClient` POSTs to tenant-supplied URLs with no SSRF protection — a tenant can target `169.254.169.254`/internal IPs; `w1/m37` built the exact dial-time guard next door and left this pre-existing gap to stay scoped, recommending a shared helper)
- [x] **m36** — Per-App registry pull credentials (8 tasks) ← from `/pm-brainstorm` round 12, 2026-07-15 (ADR022:204's recorded shared-pull-credential blast radius, "before real tenants" sequencing); after `w6/m29` — composes with its imagePullSecret attach
- [x] **m37** — Credential lifecycle hardening chores (7 tasks) ← from `/pm-brainstorm` round 12, 2026-07-15, groups ADR019:80-82's three `_Follow-up:_` markers (scoped non-`system:masters` operator kubeconfig, admin-cert expiry alert, CA-rotation runbook) + ADR013:111's OpenBao rekey runbook (each ~1h, the m10 chores pattern)
- [x] **m38** — Custom-domains list: honor cursor/limit + verificationStatus/domainType filters (7 tasks) ← from `/pm-brainstorm` round 13, 2026-07-15 (mechanical-consistency mining: `listDomains` never reads the query string while emitting per-item cursors — the projects-style loop hazard plus two silently-ignored documented Render filters; w7 per the m34 API-parity-chore precedent)
- [x] **m39** — Per-App registry credentials: production hardening (7 tasks) ← from `/pm-brainstorm` round 15, 2026-07-15 (consistency-mine over the day-old m36 code, `internal/registry/creds.go`: no rotation path, a GC comment claiming impossible owner-reference collection while reclamation rides an explicit delete, singleton-Secret write contention failing reconciles after 5 attempts, and the hardcoded `baseZotConfig` literal owning retention policy + contract values unguarded)
- [x] **m40** — Custom-domains list parity: GraphQL + MCP filters & pagination (6 tasks) ← from `/pm-brainstorm` round 19, 2026-07-15 (shipped-diff mine over `436fd9c2..bb9cc60b`: m38's cursor/limit + verificationStatus/domainType shipped REST-only — GraphQL `customDomains` still takes only `IDArg()`, MCP `list_custom_domains` only `serviceArgs`; third round of the w8/m13 / w2/m43 list-consistency pattern, w7 as m38's direct continuation) — **DONE 2026-07-16**
- [x] **m41** — Static site create: Build Command + Build Filters (8 tasks) ← from user Playwright research 2026-07-16 (Render's `/static/new` shows Build Command prominently + Build Filters in Advanced; bex's create wizard hides Build Command for static sites via `!isStaticType` guard and has no Build Filters in any create wizard; settings also lacks a Build Command editor — backend already supports all fields) — done 2026-07-16: Build Command field in create wizard + settings InlineEditSetting, Build Filters (Included/Ignored Paths) in Advanced section, setBuildCommand mutation + MCP tool + server query, buildFilter in createService; moved to `done/m41/`
- [x] **m42** — Logs UI: URL-shareable filters + Render-simple filter bar (7 tasks) ← from user Playwright research 2026-07-16 (Render's logs URL encodes filter state as `?t=app&r=1h` and keeps a 3-control primary toolbar; bex URL-backs only `range` — the other 8 filters reset on reload — and shows 9 always-visible controls; backend already honors every filter, so this is dashboard-only URL-shareability + progressive-disclosure polish) — done 2026-07-16: full filter set URL-backed (defaults omitted, live-verified round-trip/reload/share), minimal primary toolbar + Filters popover with count badge + removable chips, Render `t`/`r`/`application` deep-link aliases (incl. Render's 15m/24h/7d range grammar) self-healing to canonical keys, ADR018 Logs rows updated; root-caused a dev reload storm to a duplicate vite server regenerating routeTree.gen.ts; moved to `done/m42/`
- [x] **m43** — Scaling page parity: Manual Scaling card + Recent Metrics (7 tasks) ← from user Playwright research 2026-07-16 (live comparison of Render's `/web/srv-…/scaling` vs bex's Scaling tab: Render pairs the autoscaling card with a mutually-exclusive Manual Scaling card (slider 1–100 + Save) and a Recent Metrics section (48h avg CPU/memory utilization + total instances); bex shows only the autoscaling card — the manual stepper sits on Settings per the docs-fallback guess in `docs/render-artifacts/manual-scaling.md`, the en locale credits "Render" for scaling, the slider caps at 25, and the default target is 75 vs Render's 60; API surface already at full parity (w2/m12 + w1/m20) so this is dashboard-only) — done 2026-07-17: Manual Scaling card on the Scaling tab (slider 1–100 + Save via scaleService, hidden while autoscaling on, w5/m16 Settings stepper removed), Recent Metrics (48h avg CPU/memory utilization + total instances over the existing metrics query, honest empty states), autoscaling deltas corrected (en copy, slider cap 100, default 60, disable-confirm dialog); live-verified round-trips on dev-7; ADR018 + manual-scaling.md updated; moved to `done/m43/`
- [x] **m44** — Enrich deploy-failure email: commit message + View Logs link + Render-shaped framing (6 tasks) ← from user request 2026-07-17 (Render's deploy-failure email frames the impact, names the failing commit message, and links to the logs; bex sends one terse line `A deploy of "x" failed.` — the `Deploy` row already carries `Commit`/`CommitMessage` and `BEX_DASHBOARD_URL` is wired, just not threaded to the mailer; email-content-only, no surface change, parity-ledger Notifications row stays ✅) — done 2026-07-17: failure email now frames impact ("we encountered an error … may not be live"), names the commit message, and links to the deploy-detail logs page; commit + link threaded through DeployNotification (reconcile path) with honest-omit for image-backed/no-dashboard-URL; email composed once (not per recipient); notify-on-fail.md + ADR018 updated; started-path enrichment on the deploys-trigger seam noted as a follow-up; moved to `done/m44/`
- [x] **m46** — Render CLI `psql [id|name]` end-to-end acceptance: flip the IP-allow-list-gated `[~]` to `[x]` (6 tasks) ← from `docs/cli-compatibility-checklist.md` `psql` row + 2026-07-18 `render-oss/cli` research (dev-9 environment gate, not a bex code gap — mirror the proven `pgcli` acceptance)
- [x] **m45** — Key Value update parity: honor memory-policy + IP allow-list on the update PATCH (6 tasks) ← from `docs/cli-compatibility-checklist.md` verification pass 2026-07-18 (six-agent CLI-compat sweep: `keyvalues update --memory-policy | --ip-allow-list | --clear-ip-allow-list` return 200 + empty diff and change nothing — `handleUpdateKeyValue`/`KeyValuePatch` decode only name/plan; the `/pm` request said "create" but create verified fully working, so scoped to the real `update` defect); closes the update path of m5's KV ipAllowList parity — done 2026-07-18: `KeyValuePatch` gained `MaxmemoryPolicy` + `IPAllowList` (nil=unchanged, empty list clears), `handleUpdateKeyValue` decodes them; programmatic parity via new GraphQL `setKeyValueMaxmemoryPolicy` + MCP `set_key_value_maxmemory_policy`/`set_key_value_ip_allow_list` (the latter closed a pre-existing MCP gap); shared `maxmemoryPolicyKnown` helper (simplify); REST+GraphQL+MCP regression tests assert real read-back mutation; live-verified with unmodified official CLI v2.21.0 in dev-9; ADR018/ADR021/cli-compat checklist updated; dashboard eviction-policy editor filed as follow-up `w7/007.md`; moved to `done/m45/`
- [x] **m47** — Shadow export: emit sealed usage to Metronome (10 tasks) ← from `/pm-brainstorm` billing/ADR040 2026-07-19; **moved from w8 per user** (billing placed in w7 for capacity — the m27/m28/m41–m46 off-theme-absorption precedent). Fires `.pm/FUTURE-MAYBE.md`'s "Subscription/invoices/payments" trigger — the billing half of `w8/m7`'s pricing estimate; env-gated shadow export via `usage_hourly`→`/v1/ingest`, byte-identical when off, no charging risk
- [x] **m48** — Billing surface: real invoices and cost from Metronome (8 tasks) ← from `/pm-brainstorm` billing/ADR040 2026-07-19, **moved from w8**, needs m47 (per-customer contracts + real invoiced/current spend across REST · GraphQL · MCP + dashboard, beside the `w8/m7` estimate)
- [x] **m50** — Pivot the billing sink: Metronome → Stripe Billing (11 tasks) ← from user 2026-07-20 (Stripe **acquired Metronome** Dec 2025; Stripe Billing now natively does usage meters + tiered prices + native collection, so bex → Metronome → Stripe collapses to bex → Stripe). Re-targets the m47/m48 pipeline (outbox/seal/reader seams reused) to Stripe meter events + invoice reads; Phase-3 collection becomes native Stripe. Supersedes ADR040 §1–2 — done 2026-07-26
- [x] **m49** — Strict Render REST request validation with kin-openapi (9 tasks) ← from user research + `$pm` handoff 2026-07-20 — done 2026-07-20: one SHA-pinned complete Render spec validates the authenticated 119-operation bex intersection; strict unknown query/body/trailing input rejection, reviewed bex extensions, shared kin response conformance, refresh runbook, full backend tests, and lint are green
- [x] **m51** — Stripe customer billing: Checkout payment setup + Tax + Customer Portal (13 tasks) ← from user `$pm` handoff 2026-07-27; continues m50 in **test mode only** and makes a production workspace payment-ready without exposing Stripe credentials — done 2026-07-27: production REST/GraphQL/MCP/dashboard readiness parity, hosted Checkout payment binding, signed replay safety, fail-closed Tax state, invoice preview, scoped Portal, and full disposable-fixture cleanup evidenced in `docs/drills/2026-07-27-stripe-billing-onboarding.md`
- [x] **m52** — Stripe dunning lifecycle: trusted events → grace → reversible enforcement → recovery (15 tasks) ← from user `$pm` handoff 2026-07-27; needs m51 and closes ADR040's explicitly deferred payment-failure reaction in **test mode only** — done 2026-07-28: durable/reorder-safe webhook + polling convergence, notification delivery, reversible enforcement, audited controls, exact App recovery, cross-surface parity, and full production-hosted test-clock evidence in `docs/drills/2026-07-28-stripe-billing-dunning.md`
- [x] **m53** — Production Stripe test-mode billing acceptance + operations handoff (12 tasks) ← from user `$pm` handoff 2026-07-27; needs m51–m52 and proves the whole billing lifecycle in prod without creating or using live-mode objects — done 2026-07-28: runtime-RAK reconciliation of 434 emitted rows, eight loaded/inactive alerts, exact rejection repair, paid/excluded/comp fixtures, disable/recovery, webhook rotation, and complete non-secret evidence in `docs/drills/2026-07-28-stripe-billing-operations.md`
- [x] **m54** — Static-site multi-tenant trust boundaries: accepted browser divergence · alias authority · least-privilege S3 (9 tasks) ← from user `$ship` → `$pm` security handoff after the 2026-07-28 production static-site repair; done 2026-07-30 after the owner explicitly waived PSL inclusion
- [x] **m55** — Cross-workspace negative-authorization matrix: REST · GraphQL · MCP non-member deny guard (7 tasks) ← promotes `009` 2026-07-30 (`/pm-brainstorm for w7` coverage sweep confirmed spot-checked, not systematic: deny-all sweep can't catch confused-deputy authz, 3/186 REST routes E2E-checked, zero MCP cross-workspace tests) — done 2026-07-30: enumerated non-member deny matrix across 252 service verbs · 146 resource REST routes · 207 GraphQL fields · 186 MCP tools, each completeness-guarded; real-OpenFGA per-object E2E; anti-tautology self-tests prove all 5 regression classes turn CI red
- [x] **m56** — SSH gateway least-privilege DB credential (ADR035 §116) (6 tasks) ← from `/pm-brainstorm for w7` 2026-07-30 (the only unowned security follow-up left in `docs/`; gateway exposure grew twice since it was written — web shell `w2/m55`, sandbox exec `w3/m33` — while the credential stayed full-privilege) — done 2026-07-30: gateway rotated to a scoped `bex_ssh_gateway` Postgres role (key/session/nonce/audit only, denies billing/creds/app rows — Postgres 42501), single-sourced DDL + CI-durable allow/deny test, `Migrate`/`CheckOwnership` removed; prod role provisioned + negatively verified live, gateway rolled off `bex-db-app` via Argo. Corrected 3 wrong DoD assumptions (needs tenant reads; no `api_keys` table; Migrate needed a Go change)
- [x] **m57** — Security audit round 3: post-2026-07-20 surfaces (billing · static plane · tenant namespaces) (9 tasks) ← promotes `010` 2026-07-30 (the trigger-gated audit follow-on fired when `w3/m35` closed the same day; scoped to what m35 did _not_ cover, produces a fresh ADR028-format register) — done 2026-07-30: [`docs/ADR045`](../../docs/ADR045-security-review-round3.md), 10 findings (no critical); **[High] cross-tenant host-hijack on the create paths fixed in place** (+ 2 medium + 1 low fixes, all tested/guarded); follow-ups filed (w3/m34, w1/m57, `011`, `012`)
- [x] **m58** — Platform-pod hardening parity + universal CI guard (6 tasks) ← from `/pm-brainstorm more for w7` 2026-07-30 (code sweep: static-server is the one platform Deployment with no securityContext, running internet-facing in the privileged-PSS `bex-system` ns; extends m2's tenant-pod hardening to bex's own pods + adds the missing universal guard, the m7/m30 CI-structural-guard pattern) — done 2026-07-30: static-server + both sni-proxies hardened to full parity; universal fail-closed `gitops-validate` guard (single-sourced `.yq`) + durable self-test wired into CI; egress-meter the sole documented exemption
- [x] **m59** — Per-workspace storage & object quota hardening (ADR045 Finding 4) (6 tasks) ← from `/pm-brainstorm more for w7` round 2, 2026-07-30 (ownership check: the ADR's "cross-referenced to w3/m34" filed no m34 task and m34 is gate-blocked, so the Medium finding was unowned; verified live — `quotaForPlan` has no storage/PVC/LB dimensions while the CNPG disk-autoscaler grows tenant PVCs automatically) — **DONE 2026-07-31**: `quotaForPlan` gains `requests.storage`/`persistentvolumeclaims` + zeroed LB/NodePort caps (free 20Gi·4, paid 5Ti·200); disk-autoscaler pre-flights the namespace quota and sets a self-clearing `DiskGrowthBlockedByQuota` condition (Postgres) / `StorageBlockedByQuota` state (KeyValue) instead of a silent PVC-resize failure or hot-loop; operator gains read-only `resourcequotas` RBAC; ADR043 §D3 documents the per-plan values, ADR045 Finding 4 fixed-in-place
- [x] **m60** — Outside-gate public route hardening: webhook intake metering + completeness guard (6 tasks) ← from `/pm-brainstorm more for w7` round 2, 2026-07-30 (code sweep: `/v1/webhooks/git` + `/v1/webhooks/stripe` are the last unmetered unauthenticated POST routes — neither limiter reaches them; the device-flow-limiter class of gap); absorbs `012` — **DONE 2026-07-31**: IP-keyed `WebhookRateLimiter` (reuses `api.RateLimiter`, generous 600/min default, sheds pre-HMAC, `BEX_WEBHOOK_RATE_LIMIT`/`_BURST`, `0` disables); CI completeness census `TestComposedMuxAlwaysPublicInventory` over the composed `rootMux` (+ anti-tautology both directions) retires ADR045 Finding 8 / inbox `012`; env-table + `.env.example` + `internal/api/CLAUDE.md` inventory synced
- [x] **m61** — Insider role-ladder deny matrix: every mutating verb × under-privileged role (6 tasks) ← from `/pm-brainstorm more for w7` round 2, 2026-07-30 (authz sweep: role mechanism covered, matrix coverage partial — only a viewer sample in one E2E; m55's insider-axis sibling, reusing its sweep/completeness/real-FGA tooling) — **DONE 2026-07-31**: `roleGrants` table pinned to `model.fga` (parse+compare), completeness-guarded role-ladder matrix over {viewer,contributor,billing} × every sweepable verb, an **independent** representative-relation guard that catches a too-weak relation regression, contributor+billing real-OpenFGA E2E (validated live locally), and anti-tautology self-tests; surfaced finding `014` (billing verbs gate on `can_manage`, not `can_manage_billing`)
- [x] **m62** — Security-chores round 2: govulncheck CI + sandbox-prune keying + publish-credential scope (6 tasks) ← from `/pm-brainstorm more for w7` round 2, 2026-07-30 — groups `011` + `013` + ADR045 Finding 7 (whose "folded into w1/m57" was checked and found not executed — m57 codified the wildcard credential as-is); the m10/m37 sub-hour-chores grouping precedent — **DONE 2026-07-31**: pinned `govulncheck` CI job over the Go workspace (`1.25` line, gate-on, baseline triaged clean via x/text v0.39.0 bump); `<ws>-sandbox` prune now keys on workspace existence not the toggle (ADR045 Finding 6 fixed); ADR045 Finding 7 found already-resolved by w3/m35 and documented (publish Job repo-scoped when `BEX_REGISTRY_NS` set)
- [x] **m65** — CI supply-chain hardening: SHA-pin actions + pinning guard + dashboard dependency scanning (7 tasks) ← from `/pm-brainstorm more for w7` round 3, 2026-07-31 (supply-chain sweep: 0/42 third-party `uses:` refs SHA-pinned while `deploy.yml` holds write+signing credentials; dashboard npm tree unscanned — Go has govulncheck m62, images have Trivy m10, JS has nothing; m63/m64 numbers reserved for that round's unmaterialized candidates 1–2) — **DONE 2026-07-31**: 42 refs SHA-pinned (each re-verified a real commit) + fail-closed 40-hex guard with red/green self-test, `dependabot.yml` (github-actions + npm/dashboard), and a `yarn npm audit` CRITICAL-gate/HIGH-warn step in `dashboard-test.yml` that transitively gates deploys
- [x] **m66** — Service events parity: surface Render's build/pre-deploy/job lifecycle event types that bex folds away (10 tasks) ← from user request 2026-07-31 (`learn from bex + Render live /events pages, make events consistent, update /pm for w7`; both dashboards login-gated so the gap was derived from Render's list-events OpenAPI + webhooks reference diffed against bex's verified `internal/events` + `internal/store/reconciler.go` — no `build_started`/`build_ended` (folded into deploy; `service.go:88` calls them source-less, a claim ADR034's BuildKit Jobs now falsify), no standalone `pre_deploy_started`/`_ended` (only embedded `preDeployStatus`, w1/m33), no `job_run_ended`; metrics-graph event annotation already shipped); the m41–m44 live-parity-research precedent — **DONE 2026-07-31**: six new observed-lifecycle facts (`build_started`/`build_ended`, `pre_deploy_started`/`pre_deploy_ended`, `job_run_ended`, `branch_deleted`) on the existing `service_event_facts` path + new closed `status` column (migration 0056), surfaced across REST/GraphQL/MCP + dashboard (catalog/icons/badges/metrics-markers, en+zh); `service.go:88` "no source" claim corrected; `delete`-event + push-`--delete` branch-delete handling disables auto-deploy
- [x] **m67** — Auth-DB off-cluster backups: Barman Cloud plugin for kratos/hydra/openfga (8 tasks) ← from the 2026-07-31 persistent-store backup audit (`/pm` handoff; ADR012 §Consequences records the gap — identity/OAuth/authz data is PVC-only while `bex-db`'s plugin path was PITR-drilled 2026-07-28 in the same cluster; the cheapest fix on the backup-gap list) — **DONE 2026-07-31**: all three PostgreSQL-18 auth clusters continuously archive WAL + take staggered nightly plugin backups under unique `auth-dbs/*-pg18` identities; fresh base backups landed in S3, Kratos restored a known identity into a throwaway cluster in 62s with zero leftovers, and namespaced >26h alerts + a fail-closed all-Cluster CI guard are live
- [x] **m68** — Tenant KeyValue off-cluster backups: nightly RDB snapshots to S3 for paid plans (8 tasks) ← from the same 2026-07-31 audit (ADR021 §Consequences defers backup; last tenant-data store with zero off-cluster copy; rides w7/m12's teardown-purge and the `BEX_DB_BACKUP_*` env-gating patterns); lower-priority residuals filed as `015` (cross-region replication) + `016` (RPO/RTO + re-drill cadence) — **DONE 2026-07-31**: every paid KeyValue now gets a staggered coherent nightly RDB in S3 with keep-seven retention and fail-closed delete purge; production created eight snapshots, restored a marker into a fresh PVC through the AOF transition/restart, and proved zero Kubernetes/S3 leftovers
- [x] **m69** — Scripted backup restore: executable recovery for every ADR031 store, verified end to end (9 tasks) ← from user request 2026-07-31 (`/pm m69` — restore scripts + ADR + a real backup→restore→verify exercise); closes ADR031's "all restores manual (runbook-driven)" consequence; disjoint from m67/m68's ship-time drills (those prove the backup mechanisms once; m69 makes recovery executable and proves the full loop with scripts only), and its timings seed `016`'s RPO/RTO table — **DONE 2026-07-31**: four safety-gated scripts shipped; fresh production etcd/OpenBao/bex-db/tenant-Postgres/Kratos/KeyValue backups restored and verified; all throwaways, drill sources, and deleted-store object prefixes reached zero leftovers

- [x] **m70** — OpenBao cross-workspace secret isolation: tenant-key env/secret-file paths + fix purge-name mismatch (6 tasks) ← from codex-security scan `codex-security-bex-azfRGv` 2026-08-01 (finding #1 **high** + #15 med — `baoTenant="default"` hard-coded, storage key omits workspace identity; the scan's only HIGH, a real authenticated cross-workspace secret read/overwrite) — **DONE 2026-08-01**: ctx-threaded tenant keying + lazy migration + public-name purge (`68a1d297`, deployed `405f284a`→`f0f5ebf9`); test-backend green incl. 4 new isolation/migration/purge tests
- [x] **m71** — Shared build-namespace Secret ownership: ownership-check the clone/registry mirror + the finalizer delete loop (5 tasks) ← from codex-security scan (#5 + #14 med — App-controlled names overwrite/destroy foreign Secrets in `bex-build` with no ownership check) — **DONE 2026-08-01**: `copyCloneSecret` refuses foreign-destination overwrite + `deleteOwnedSecret` gates the finalizer (`b00b40a4`, deployed `49c67eeb`); reuses `execution.ArtifactIdentity`
- [x] **m72** — Public-surface availability & resource-exhaustion hardening: pg-sni handshake deadline, activator host→App cache, static-server LimitReader, exports cap, activator HTTP timeouts, build ephemeral-storage (9 tasks) ← from codex-security scan (#2,#7,#10,#13,#21,#3) — **DONE 2026-08-01**: 6 bounds shipped (`b0642583`, deployed `f633316c`); activator cache + server timeouts, pg-sni deadline, static LimitReader, export concurrency cap, build/publish ephemeral limits
- [x] **m73** — Tenant-image signature & digest integrity: bind cosign payload to digest/repository + fail-closed digest resolution (5 tasks) ← from codex-security scan (#8 med + #29 low — undermines the w7/m11 admission gate) — **DONE 2026-08-01**: `verifyPayloadBinding` binds digest+repo + Always-pull mutable tags (`ece39b92`, deployed `93876eec`); 2 new binding-negative tests
- [x] **m74** — Dashboard auth & session-boundary hardening: device-grant confirmation, consent body cap, Apollo clear on logout, sandbox-exec cross-replica nonce (7 tasks) ← from codex-security scan (#9,#11,#24,#30) — **DONE 2026-08-01**: device confirmation gate + consent body cap + Apollo clearStore + sandbox-exec DB nonce (`c7ed18e2`+`0efe6e55`, deployed `efb9ec07`); 3 new device-confirmation tests
- [x] **m75** — CI & infrastructure supply-chain pinning: snapshot.yml input-injection fix + pin gVisor/Argo-manifest/Argo-charts/k3s (8 tasks) ← from codex-security scan (#16 med — the immediately exploitable one — + #18,#19,#20,#27 low) — **DONE 2026-08-01**: env-funneled + regex-guarded kubernetes_version, Argo v3.4.6, exact chart versions, pinned+checksummed gVisor, pinned k3s (`29031c59`, deployed `804d0fce`)
- [x] **m76** — Sandbox compute metering: `sandbox_compute_seconds` (8 tasks; DONE 2026-08-01) ← from [docs/ADR047](../../docs/ADR047-cloud-coding-agent-sessions.md) D6 + `/pm-brainstorm` 2026-08-01 (ADR047 wave 1; closes "sandboxes are the one resource not yet metered" — valuable regardless of later waves)

- [x] **m77** — Finish ADR043: namespace managed Postgres/Key Value under `<ws>` and repair every broken datastore link (13 tasks) — **done 2026-08-21, including the live hetzner-prod cutover.** All four of the multisite forum tenant's datastores now live in `<ws>`, all three forums (`beancount.io`, `tianpan.co`, `blockeden.xyz`) serve real content, every hand-made Secret copy / CiliumNetworkPolicy / hostname patch is gone, and `count/databases.app.bex.co` + `count/keyvalues.app.bex.co` are actually charged — **superseding `.pm/w3/010.md`**. Executing it found three silent production defects on the backup/restore path: `036` (no managed Postgres could provision anywhere), `039` (no tenant Postgres could be restored at all), and `040` (a same-name migration silently stops backing up, and the retire step would have erased the live database's archive). Carried forward: retiring the old `default` instances after a soak, which also reverts the raised `bex-tenant-0` autoscaler ceiling.
- [x] **m78** — A failed reconcile step must not strand a service with no Service and no Ingress (6 tasks) — **FOLDED into m79 2026-08-09**: t001 disproved the premise. Nothing failed — production runs `BEX_BASE_DOMAIN` unset by deliberate security decision (`m54`), so a service with no custom domain has no platform host and `reconcileIngress` correctly writes nothing. Proven by a deterministic envtest reproduction covering all three configurations (kept as the routing regression guard for the tenant-namespace shape, which had none). The residual — nobody is told their service has no public URL — is `m79/t003`, whose premise was corrected rather than left to rediscover it
- [x] **m79** — Surface unsatisfiable service dependencies instead of letting them time out silently (7 tasks) — **DONE 2026-08-09**. Two silences closed, both by carrying a diagnosis the platform already had: `CreateContainerConfigError` (the pod never starts, so neither existing diagnosis applied) now reaches the deploy's `failure_reason` and from there REST/GraphQL/MCP + the dashboard + the failure email, replacing the literal "did not become healthy within the health-gate window"; and a new `PublicRouting` condition on the CRD contract surfaces as `publicRoutingNotice`, rendered in the header slot the URL would occupy. Absorbed **m78** and corrected its premise (nothing failed — `BEX_BASE_DOMAIN` is unset by design). No migration: the event vocabulary stayed closed

- [x] **m80** — Render-parity health checks: probe semantics and the three disagreeing deploy timers (9 tasks) — **DONE 2026-08-10**: unset `healthCheckPath` now yields a `TCPSocket` probe (Render's default) instead of a forced `GET /`, so an API whose root is a legitimate 404 is deployable at all; every probe carries Render's 5s budget in place of Kubernetes' inherited 1s; a `startupProbe` owns boot with the full 15-minute budget. The parity sweep caught that the change was **inert** — all four surfaces coerced an empty path back to `"/"`, so nothing could reach the new default; fixed in the shared service verb, the dashboard row, the MCP tool description, and both locales. t002's own premise was also wrong and its new test caught it: the three timers are not three equal numbers but two mechanism timers (startup budget, `progressDeadlineSeconds`) at 15m with the control-plane observer (`DeployGateTimeout`) deliberately above at 18m, matching the rule the build/pre-deploy gates already follow ← designed 2026-08-10 while diagnosing why `beancount-cms-v2` would not deploy. bex is stricter than Render on two independent axes at once: the readiness probe inherits Kubernetes' `timeoutSeconds: 1` against Render's documented 5s, and `+kubebuilder:default=/` forces an HTTP check on every service where Render defaults to a TCP socket probe. Their intersection makes an ordinary API service — one whose `/` is a legitimate 404 — **undeployable**, since Kubernetes scores HTTP probes healthy only on `200 ≤ code < 400`. Separately, three timers bound a rollout (`startupProbe` budget, `progressDeadlineSeconds`, bex-api's `DeployGateTimeout`) at ∞/600s/**180s**, so the effective budget is the accidental minimum of three unrelated choices against Render's 15 minutes. **Complements m79**, which made the failure message honest; this fixes what the message reports

- [x] **m81** — livenessProbe: restart a wedged instance after 60s (Render steady-state stage 2) (7 tasks) ← promotes `027` 2026-08-17 — **DONE 2026-08-17**: t001 chose unconditional parity (the spiral hazard does not exist in TCP mode; in HTTP mode it is Render's own documented behavior, bounded by m80's `startupProbe`); every non-worker service now carries a `livenessProbe` on the shared handler (5s budget, 10s × 6 = 60s), startup-coexistence and worker-absence pinned in both unit and envtest (the old "no liveness" pins inverted); the t004 sweep caught a stale `AppView.HealthCheckPath` "Empty means '/'" comment m80 missed and updated MCP/GraphQL/dashboard-locale descriptions to state the restart; `/simplify` surfaced a pre-existing perpetual Deployment `CreateOrUpdate` PUT loop, filed as `028`
- [x] **m82** — Build failure classification: retry only what retrying can fix (ADR060 D2 + D4) (9 tasks) — **DONE 2026-08-17**: a `clone` exit 128 or `buildkit` exit 1 now fails **once** and is attributed to the tenant, while a node drain is absorbed by `podFailurePolicy` (`Ignore` on `DisruptionTarget`) instead of failing the deploy — retiring the `safe-to-evict` pin that blocked node consolidation. Reserved exit codes 90/91 classify at the two phases that can carry tenant input; the class reaches the App condition (`BuildFailedUserError`/`BuildFailedInfrastructure` via a shared `lego/types` contract), the deploy record, and a `user_failed`/`infra_failed` metrics split that makes a 99.5% infra-success SLO definable. Push gains `--retry-times 3`, TLS verification becomes conditional (**strikes `w1/046` F11**), and Zot gains `scrub` + a guarded `gcDelay >= BuildTimeout` invariant. **The parity sweep found bex-api matched only the flat `"BuildFailed"` literal, so classified failures were being recorded as generic `update_failed` deploys** — fixed, with a test proven red against the pre-fix tree; the eleven-value wire vocabulary is unchanged, so no migration. **`/simplify` found four real bugs**: `set -e` silently discarded the captured exit code (a second `bex_run` call would have reported a failed phase as success), the classification log was unbounded on a disk that shares the build's ephemeral limit (an eviction there is `Ignore`d, so it looped until the deadline), a tenant `RUN echo "connection refused"` could forge an infra classification and move the platform SLO, and the counters fired **per reconcile rather than per build**, which would have inflated the SLO and false-paged the critical alert. All four fixed and regression-tested. `bex_build_push_seconds`/`_errors_total`/`retries_total` deferred to m83 where the pod read that feeds them lands, rather than shipping always-zero series ← from [`builder-issues.md`](builder-issues.md) §3.1/§3.2/§3.8 (P0/P1/P7)
- [x] **m83** — Build-plane observability + the two structural guards (9 tasks) — **DONE 2026-08-18**: queue time is now its own series. `bex_build_queue_seconds` (admission → first placement) splits the platform's SLI from the tenant's run time, with `bex_builds_active`/`_queued`/`bex_build_queue_oldest_seconds` for the live picture and the push/retry series m82 deferred here; `BuildQueuedTooLong` fires at ~10 minutes of queueing, eight before bex-api's 18m deploy gate closes the deploy that the 2026-08-11 incident lost. **The bigger find was that none of it was readable**: the manager's metrics endpoint was disabled by default, the checked-in metrics Service pointed at a port nothing bound, and Prometheus had no operator scrape job — so **w2/m72's build-outcome alerts had never been able to fire**. Fixed end to end. `build-image-prewarm` regains the build-taint toleration D8 stranded it behind, and two fail-closed guards land in `clusterapi-validate.sh` with a red/green self-test: prewarm completeness, and the arithmetic floor (CA hint ≥ build request read from `build.go` + every DaemonSet that tolerates the taint) that keeps the measured 305 MiB cx33 margin from eroding invisibly. Admission gains the cluster-wide `BEX_MAX_ACTIVE_BUILDS` ceiling (production `4` = D8's physical concurrency) plus re-count-after-create shedding the **newest** build, and all three concurrency variables become readable from git. **`/simplify` found a real metering bug**: the failure branch's once-per-build gate never closed (`r.fail` deliberately never stamps `ObservedGeneration`), so one failed build re-observed its queue wait ~20×/hour into the histogram the new p95 alert pages on — fixed with a durable marker, pinned by an envtest proven red against the pre-fix tree; the same pass caught warm-node (sub-second) queue times being silently dropped, biasing p95 upward. **No hardware change** — measurement closed that thread ← from [`builder-issues.md`](builder-issues.md) §3.3/§3.4/§3.5 (P2/P3/P4) + ADR060 D5/D6/D7. **Retires `024`**
- [x] **m84** — Operator reconcile convergence: no `CreateOrUpdate` may PUT on a no-op (7 tasks) — **DONE 2026-08-19**: a converged reconcile of an App, KeyValue or Database now issues **zero writes**, proven by an invariant that counts the client's requests rather than watching `resourceVersion` — and that distinction is the finding. **`028` was right about the PUT and wrong about the loop**: the reproduction measured _no_ `resourceVersion` movement anywhere, because the API server re-defaults the incoming object, finds it byte-identical to what is stored, and skips etcd — so the redundant PUT fires no watch event and the self-sustaining `ResourceVersionChangedPredicate` requeue loop never actually closed. The obvious test, the one `028`'s fix sketch proposed, would have passed against the very bug it was written for. Real cost: one decode/validate/admission round trip per object per pass on the mutating path, scaling with tenant count. **The blind deletes turned out to be the larger half by count and nobody had predicted them** — every App issued _three_ DELETEs per pass for Traefik middlewares it does not have, against one PUT; the t005 efficiency review then corrected what fixing them is worth, and only one of the six actually loses its round trip (the rest are unstructured or ride the uncached secret client, so they become reads instead of writes — off the mutating path, out of the write audit stream). Fixed via one shared `server_defaults.go` (Deployment, CronJob, Valkey StatefulSet, backup CronJob, plus the Service `Protocol` w9/m57's fix never reached), an empty annotations map on the CNPG Cluster, and existence checks before six blind deletes. **No rollout, proven by the same fact**: unchanged stored bytes ⇒ unchanged pod-template hash, asserted by storing a pre-fix Deployment and showing today's projection does not even move its `resourceVersion`. `CreateOrPatch` and server-side apply were evaluated and rejected in writing. One named exemption (the CR's own status subresource → `033`); `032`/`034`/`035` filed. **Retires `028`** ← from [`builder-issues.md`](builder-issues.md) §3.6 (P5)
- [x] **m85** — Close the digest-pinning inventory + fail-closed CI guard (8 tasks) — **DONE 2026-08-21**: the ADR055 F7 lineage is retired, six reports in (→ ADR061 #1 → ADR063 #12 → ADR066 #7 → ADR067 #8 → ADR068 #9, both ADRs now marked resolved). The register implied a sweep; measured against the tree, **t001 and t002 were already fully done** by `9081fbdb`, `w1/m73` and round-14 #5 — recorded as such rather than claimed. What was genuinely floating and is now pinned: the `BEX_SANDBOX_IMAGE` default (`alpine:3` — the one that actually runs, since no deployment sets that variable), the `BEX_AGENT_SESSION_IMAGE` `:latest` floor, CI's `postgres:17` + `openfga/openfga:latest`, and `axllent/mailpit` twice. **ADR068 #9's named fix — a separate `valkey-cli`+`gzip`+`age` helper image — was deliberately not built**: the bex image is already the signed, SBOM-attested, digest-pinned first-party artifact, so `filippo.io/age` was compiled into it as `/backup-encrypt` and the encrypt stage execs that. No shell, no package manager, no download in the stage holding the plaintext RDB; the operator resolves its own image off its Pod so the stage always matches the digest it was rolled out with, and encryption-configured-but-unresolved **fails the reconcile closed** rather than uploading plaintext. Interop proved against the upstream `age` v1.3.1 CLI the restore script actually invokes, so no restore change. **Verifying the pins found a live defect**: `quay.io/skopeo/stable` was pinned to a digest quay.io does not serve — it reports the _converted_ manifest-list digest for an index it stores only as OCI — so the build plane's push stage named an image the registry would not return. Repinned across three files; the lesson (resolving from `Docker-Content-Digest` is not sufficient) is recorded. The durable half is `scripts/image-pin-validate.sh`: 78 references re-derived every CI run across **four** syntaxes (the fourth, workflow `docker run`, added because the three the task named would have missed `openfga:latest`), failing closed on anything unclassifiable, with enumerated per-reference exemptions carrying a reason each and a red/green self-test that is itself meta-verified. It refuses to run on bash 3.2, which silently returned a third of the inventory. `041` files the three residuals left open on purpose ← from [`builder-issues.md`](builder-issues.md) §3.9 (P8)
- [x] **m86** — Per-App registry build cache (9 tasks): on-cluster D5 dashboard **179s → 38s**, native Node API **75s → 64s**; storage quantified, gate remains opt-in. See `done/m86/`.

## Approved queue (priority order)

Approved by `$pm all for w7` following the five-item brainstorm. All items belong to worker7. Estimates below include milestone closing tasks.

1. [m148 — Reliable local cluster bring-up](m148/README.md) — 7 tasks, ~4h25m; first actionable task t001.
2. [m149 — Correct cold deep-link initialization](m149/README.md) — 8 tasks, ~4h55m; t001 depends on m148/t007.
3. [m150 — Sandbox file copy through the pinned CLI](m150/README.md) — 9 tasks, ~5h55m; first actionable task t001, scope decision approved.
4. [055 — Honest liveness detection wording](055.md) — 45m; no dependency, decision approved.
5. [057 — Consistent destructive confirmations](057.md) — 45–60m; no dependency, decision approved.

The existing [047 cache trial](blocked/047.md) remains blocked on its measurement window and retain-or-rollback decision; it is not duplicated or closed by this queue.

## Additional approved queue (2026-09-17)

Approved by `$pm all for w7 and $ship` after the four-item additional brainstorm. Existing approved work remains scheduled; these four items are new and are not duplicates of m148–m150 or 055/057. Priority within this batch:

1. [058 — CLI-only commits must not suppress platform deployment](058.md) — 45m, no dependency.
2. [m151 — Coherent datastore placement after moves and deletion](m151/README.md) — 8 tasks, ~4h55m total; implementation starts at t001, live acceptance requires m148/t007.
3. [m152 — Validate grouping mutations before changing members](m152/README.md) — 8 tasks, ~4h25m total; implementation starts at t001; coordinate shared files with m151, live acceptance requires m148/t007.
4. [059 — Resolve the legacy Blueprint ownership question](059.md) — 60m diagnostic; requires read access to the named control-plane records and cluster metadata.

- [ ] **058** — platform supersession filter correction, scoped above.
- [ ] **059** — legacy Blueprint ownership diagnosis, no resource mutation authorized.

## Inbox

**Approved execution order** (`$pm all for w7`): **054 → 053 → 055**, approximately **2h05m** total. Each was scoped under an hour with no dependency on the others. Remote main has since completed 054 and 053 and moved 055 to blocked after investigation; their approved scopes are preserved at those current paths.

- [x] **044** — [Reject empty registry tokens before saving metadata](done/044.md) (45m) ← transferred from `w4/061`; `/pm-brainstorm for w7 for top 3 customer-impactfully work` 2026-09-09 #3. **DONE 2026-09-15:** whole-request validation precedes the metadata write; refusal leaves row, `updatedAt`, and token untouched (regression test with mutation check).
- [x] **045** — [Brand the remaining Render command references and docs link in supported nested help](done/045.md) (40m) ← continuous `/qa-find-bugs-cli` for w7, 2026-09-14; `ea sandboxes*` help's double-quoted `"render …"` references + "your Render workspace", and `jobs create`'s `render.com/docs` link — the remainder `w4/done/063` did not reach. **DONE 2026-09-15:** double-quoted `"render …"` and `your Render` rewrites + a narrow `jobs create` example override; fresh-process help assertions for the sandbox family and `jobs create`.
  > **046** promoted to [m150](m150/README.md); source evidence archived in [done/046.md](done/046.md). Sandbox copy approved; sandbox groups remain a non-goal.
- [ ] **047** — **BLOCKED (needs the 48–72h trial window, then a measured retain-or-rollback decision from the user/operator)** — [Bounded production build-cache trial](blocked/047.md). Technical admission passed; withdrawal alert repaired and installed. Production overlay deadline: **2026-09-20T02:34:30Z**; initial review around **2026-09-19T02:41:04Z**. Deployment/build verification is recorded in the note.
- [x] **048** — [The Scaling page offers autoscaling on a free service, then the save fails](done/048.md) (40m) ← live `/qa-find-bugs` of `dashboard.bex.co` 2026-09-15, journey 6; the autoscaling card takes no `plan` prop, so its range runs to 100 while the sibling Manual Scaling card already caps free at 1 — `setAutoscaling` refuses with "the free plan is limited to 1 instance(s)". **Audited sweep 36 — the reasoning held.** Both universals verified exact (`AutoscalingSection` really has one caller; the card really takes no `plan` prop — its only `plan` match in 441 lines is a comment), and the `maintenance-mode-section.tsx:20,43` reference is precisely right, including all three gating moves. The errors were six drifted line numbers, none in `dashboard/`: `autoscalingSpec` → `:4439`, `checkInstanceCap` → `:3246`, `planDowngradeError` → `:3264`, `errWorkerFreePlan` → `:2855`, `ADR018:77` → `:79`, and the `manual-scaling-section.tsx` reuse citation. Four of those sit in one 4,500-line file, so grep the symbol rather than jumping to the line.
  > **049** promoted to [m149](m149/README.md); source evidence archived in [done/049.md](done/049.md). Diagnosis and fix are scheduled after local substrate verification.
- [x] **050** — [Coverage record: journeys 1, 2, 3, 4, 5, 7, 9, 10, 11, 12, 13 and 15 pass end to end (8 refuses honestly on free; 14 is already owned elsewhere); the look-alikes are already-filed issues](done/050.md) (record only) ← live `/qa-find-bugs` of `dashboard.bex.co` 2026-09-15/17 sweeps 3–40; adds the cadence measurement behind `055` (the liveness cron running 3–5.5h late, breaking `w3/m83`'s six-hour guarantee) and the investigation behind `054` (a failing production canary whose root cause sweep 32 pinned to cpu being a `rate()` whose 60 s lookback a just-woken pod cannot fill — falsifying this record's own earlier timing explanation and withdrawing the owed 0–3 s test), the Postgres SQL console keeping both limits its copy advertises (1000 rows requested → "Result capped at 500 rows"; `pg_sleep(15)` → `query exceeded the statement timeout`), the webhook UI delete journey (its control is on the **Settings** tab, which sweep 24 never opened) and a second inconsistency axis recorded against `053` — the webhook confirm requires `delete webhook <name>` with no `sudo`, unlike every `SudoCommandField` sibling — plus the webhook delivery retry lifecycle verified against an intentionally unreachable receiver (14 attempts over 7 event chains, each retrying at exactly the documented +30 s then +10 m, failing with an honest DNS `transportError`), `053`'s dated live re-confirmation (completing the set — every open note now re-observed on the post-deploy build) and a recorded event-feed asymmetry where a cascaded domain pair-delete reports one removal against two additions, dated live re-confirmations of `048` and `049` on the post-deploy build (both still reproduce; 049's second symptom only shows on the page the note names, which the first probe got wrong), the live measurement of `052`'s fifth field (same object, same instants, GraphQL Go-format vs REST RFC3339) and a `400` raised against freshly-deployed code then disproved as the probe's own fault, a re-verification of this board's own open notes against the caught-up build (`048`, `049`, `053` stand unchanged; `052` was **widened** to five fields by `w4/m109`, and two of its own claims corrected), **`w4/092` verified fixed on production** after its deploy landed (the ownership TXT Host now renders the bare `_bex-challenge` on the dashboard _and_ the API, closing the deploy-lag thread opened in sweep 11) plus the served-bundle probe method that caught the flip, the per-service notification override round trip (GraphQL and the Render-shaped REST override route agree through `all` and back to `default`), the reason blueprint **apply** cannot be run under the `qa-` naming contract (a sync names resources from the manifest) plus a verified-honest blueprint sync-error surface, maintenance mode's correct paid-only gate (the live control case now appended to `048`), the API-key show-once round trip (OAuth2 `client_credentials`, honest revoke warning, revoked and verified), deletion reaching the **usage view** (Services 5/100 matching the five pre-existing services) and the SSH public-key round trip (comment stripped as promised, fingerprint identical, deleted again), journey 2 end to end (Docker build 1m13s → Live → `curl` 200 `OK` outside the browser) and journey 8's honest paid-only refusal, the custom-domain journey (apex + auto-paired `www`, shared-host TXT with sibling preservation, pair-delete cascade) and a copy-proven deploy-lag record showing production predates `w4/092`, project→environment→delete, notification preferences, env-group round trip, the Agents composer-by-design verdict, the webhook endpoint round trip (`eventTypes: []` means _all events_), billing/usage reads, a Chinese-locale i18n scan, the service-settings edit journey, and a transient `auth.bex.co` 525 recorded for pattern-watching; also logs + filters, the ⌘K palette, workspace settings, and free-tier sleep→wake (slept at 15m, `503 {"error":"service hibernated","retryAfter":5}`, serving again ~10–15s after the first request); static site (create/serve/redirect rule, `/index.html` settled clean), cron (`*/5` run fired and its line reached Logs; Trigger Run lives on Events), Postgres and Key Value (create → Available → reveal-gated connections → delete), deploys (config-change deploy then `rollbackService` replaying deploy 1's image digest), and the blueprint plan preview (`valid:true`, 1 create action, apply deliberately not run) are all clean; the datastore label symptom is `w1/085`, fixed on `main` and awaiting deploy.
- [x] **051** — [A rollback deploy's trigger reads "Rollback To Dep-Dakvlk8…": CSS title-casing mangles the deploy id](done/051.md) — **WITHDRAWN 2026-09-16, duplicate of `w4/m108/t005`** (filed by another session in `879ba2aa9`, same two render sites); kept as an independent reproduction only ← live `/qa-find-bugs` of `dashboard.bex.co` 2026-09-15 sweep 5, journey 3; `deploys.triggerRollback` is the only trigger label carrying an id, and all three render sites wrap it in Tailwind `capitalize`, so the restored deploy's id no longer matches the one in the same row.
- [x] **052** — [GraphQL serializes Project, Environment and Blueprint timestamps in Go's `time.String()` format, while REST returns RFC3339](done/052.md) (35m) ← live `/qa-find-bugs` of `dashboard.bex.co` 2026-09-16 sweep 7, journey 1; the same project reads `2026-09-16 01:51:44.60528 +0000 UTC` over GraphQL and `2026-09-16T01:51:44.60528Z` over REST, because `StrField` resolvers are handed a `time.Time` instead of a pre-formatted string. **Re-checked 2026-09-16 (sweep 20): now five fields, not four** — `w4/m109` (`b3cd31e3f`, deployed 09:19:31Z) added `Project.updatedAt` on the same path after filing, which is the argument for a shared `gqlutil.TimeField` over five one-off format calls. **Re-verified exhaustively 2026-09-16 (sweep 33): still five** — all 77 `*At` GraphQL fields enumerated by name and cross-referenced to their backing Go type, which is the opposite method from the original `StrField`-shaped search. That earlier method misses 15 date fields resolved through multi-line `gqlutil.Field`/`Typed` blocks; all 15 happen to be correct, so it got the right answer for the wrong reason. Two suspicions raised and cleared (`registrycreds` and `apikeys` both declare `string` — the `time.Time` near `APIKey` belongs to `hydraClient`), one invented type name fixed (the note's `ServiceView` **does not exist**; the correct-today control is `AppView`, `apps/service.go:443-444`), and one new constraint on the fix: three private formatters exist at two precisions (RFC3339 vs RFC3339Nano), so a shared `TimeField` must choose one and cannot be a pure refactor.

- [x] **053** — [Destructive typed-phrase delete confirms announce as `role=dialog`, not `alertdialog`](done/053.md) (50m) ← live `/qa-find-bugs` of `dashboard.bex.co` 2026-09-16 sweeps 11 and 34; on one Settings page the custom-domain delete resolves `alertdialog` while **Delete Service** resolves `dialog`, because call sites render `DialogContent` directly instead of the shared `ConfirmDialog`, whose `phrase` prop already covers this exact interaction. **Sweep 34 corrected two claims that failed in opposite directions**: it is **four** call sites, not three — Key Value's _delete_ is a plain `Dialog` and was wrongly cited as the control (its _suspend_ is the `ConfirmDialog` one, in the same file) — and the webhook confirm this note filed as "the single outlier on both axes" actually renders `ConfirmDialog` and is already correct on role, its phrase convention being the only real divergence. Cause of both errors: enumerating by the `SudoCommandField` helper rather than by the behaviour, so every confirm reaching it through `ConfirmDialog` (webhooks, both disk confirms) fell outside the search — the same failure mode sweep 33 found in `052`.

- [x] **054** — [The tenant-view canary's metrics stage is the only stage with no retry budget](done/054.md) (45m) ← live `/qa-find-bugs` of `dashboard.bex.co` 2026-09-16 sweeps 27 and 32, from two **failing** `Production edge liveness` runs (`35093330199`, `35127061795`); stage 1 waits 60s and stage 2 waits 180s, but stage 3 queries `/v1/metrics/cpu` **once** seconds after waking an always-hibernated free canary, and has opened four "Tenant view is broken" issues in five days across three different stages (one auto-closing after six minutes). **Sweep 32 pinned the root cause and falsified the note's original timing argument**: cpu is built as `rate(container_cpu_usage_seconds_total{…}[60s])` (`metrics/source.go:282`) while memory is a plain gauge (`:273`), and cAdvisor scrapes every 15s (`prometheus.yaml:256`) — so a pod alive ~12s holds 0–1 samples where `rate()` needs 2, and cpu is structurally empty while memory would pass. The probe never finds out, because `for metric in cpu memory` exits on cpu first. Wake duration does not discriminate (a 9s wake produced both passes and failures), the owed 0–3s fixture test is **withdrawn as unnecessary**, and `w6/m110` is excluded for this fixture (`hello-go` is far under the 58-char truncation threshold that bug needed). **Sweep 37: the next run passed and #71 auto-closed** at 21:41:21Z after standing 9h41m — which exposed a hole in sweep 32's own correction. Seven runs now show a 9s wake producing **three passes and two failures** (both 12s wakes failed), so wake duration still does not discriminate; but the rate-lookback mechanism predicts a 9s-wake run should **fail**, and this one passed cleanly. The mechanism therefore explains every failure and **none of the passes** — something started the pod before the probe's request in at least three runs, and the decisive datum (pod age at stage-3 query time) is recorded by no run. The fix is unaffected either way, but the note no longer reads as solved.

- [x] **055** — [Honest liveness detection wording](done/055.md) (45m). Approved: retain best-effort scheduling, remove the unsupported detection guarantee, qualify timestamp attribution. The advisory cadence checker remains separately tracked in [w3/042](../w3/042.md); an in-cluster trigger is deferred.

- [x] **057** — [Consistent webhook deletion confirmation with disk-restore protection](done/057.md) (45–60m). Approved: standard webhook delete phrase; preserve destructive restore protection.
- [x] **056** — [The `gitops (render)` gate has been red for 5.5 days because its own assertion miscounts under the yq version CI pins](done/056.md) (20m) ← live `/qa-find-bugs` of `dashboard.bex.co` 2026-09-17 sweep 39, found from CI when `main` moved for the first time in fourteen sweeps; the guard that stops broken manifests reaching Argo CD has failed every run since `d82cc98cd` (2026-09-12), and **the manifest it guards is correct**. `scripts/gitops-validate.sh:2628` uses `(.tolerations // [])` after a document-level `select`, and under the pinned **yq 4.44.6** the `//` operator materializes for the filtered-out document, so a two-document file yields `"0\n0"` which never compares equal to `0` — measured under both 4.44.6 (FAIL) and 4.47.1 (PASS) on byte-identical files. The three sibling assertions in the same block pass, because only this one uses `// []`. **The fix is `head -1`, not the in-tree `tr -d '\n'` idiom** — that concatenates to `"00"` and still fails, verified. Blast radius counted: 1 of 10 `// []` sites. 22 commits touching the guarded paths merged unvalidated in the window, and `main` is not branch-protected so nothing was blocked.

> **043** promoted to **m89** 2026-09-09 (Sep 9 approval expanded recommend-only scope past the inbox sizing rule); archived in `done/043.md`.

> **2026-09-06:** `023` deferred to `.pm/FUTURE-MAYBE.md` and archived in `done/023.md`; the proxy already exists, while token billing awaits an explicit product/provider contract. `038` closed after three verified database restores, offline clean-image preservation, and exact old ReplicaSet/manifest/blob retirement; see `done/038.md` and the 2026-09-06 drill record. `042` investigation closed — historical upload routing is `nvme4tbfish` / prior Render disks; no attached recovery source today; restore + durable disk deferred to `.pm/FUTURE-MAYBE.md` (`done/042.md`).

> `032` **fixed 2026-09-05**: unstructured child projections preserve admission defaults while removing withdrawn bex-owned fields. CNPG and backup-store request-count tests, full operator suite, lint, and dead-code analysis passed. Moved to `done/`.

> `036` and `039` **closed 2026-09-05**: reusable managed-Database provisioning/recovery drill passed twice on production, including exact-marker recovery and full cleanup. Backup delegation review and ADR031 cadence updated; evidence in `docs/drills/2026-09-05-managed-postgres-recovery.md`.

> `033` **fixed 2026-09-05**: all owner-status update sites now skip unchanged persisted status. Request-counting convergence tests cover cron and provisioning/Ready/suspended datastores, while existing transition and conflict-durability tests stay green. Moved to `done/`; operator tests, lint, and dead-code analysis verified.

> `041` **reviewed and retired 2026-09-05**: credential-custody scripts already pin images and reject unpinned overrides; the JSON inventory has a source-pin test; checksum-verified backup binaries and local fixture images remain documented exclusions. Guard header corrected to describe all four syntaxes and the separate protections.

> **Verified closeout 2026-09-05:** `029` documented the intended serving-meter placement; `031` measured production serving headroom with no capacity change warranted; `034` fixed mutable-image pulls for cron/manual runs; `035` fixed KeyValue and cron fixture isolation and enabled sibling randomization; `040` verified old default-namespace databases are gone and current archives are healthy. All five notes moved to `done/`.

> **Review 2026-09-05:** `030` (builder monitoring and weekly digest refresh) and `037` (CLI granular scopes) verified implemented and moved to `done/`. The stale active listing for `024` was removed; it was already retired by m83.

> `027.md` promoted to **m81** 2026-08-17; `028.md` retired by **m84** 2026-08-19; both moved to `done/`.

> **Board review 2026-08-09** — every open w7 item dispositioned (implement, hold, or block-with-reason; nothing removed — each survivor is a real defect or a deliberate hold):
>
> - `025` **done** (was on disk but never listed here — index gap fixed by this entry): both App-side metrics fallback paths now resolve the App's `<ws>` namespace (`AppNamespaceByName` in the snapshot fallback, `app.Namespace` in filter-value discovery), pinned by `TestMetricsSnapshotAndFilterDiscoveryUseAppNamespace`. Honest impact recorded in the note: dev/degraded-mode only — prod sets `BEX_PROM_URL`, and the production filter-values source ignores its namespace argument.
> - `026` **done** — the structural-guard candidate shipped: `gitops-validate.sh` asserts operator + bex-api + static-server render an identical effective `BEX_BASE_DOMAIN` (absent ≡ explicit `""`) in both prod and default renders; red-tested against a diverged manifest. Live SSA-tombstone verify-once folded into the next prod-access session (m77/t007).

> **Inbox cleanup 2026-08-07** — `008`/`015`–`022` all retired to `done/` (dispositions appended in each note; `023` is the sole open note):
>
> - `016` **done** — ADR031 gained the per-store RPO/RTO objectives table (incl. the honest Free-KeyValue statement) + a re-drill owner and concrete next-due date (2027-07-31).
> - `022` **done** — dev-tooling npx pinned in place: `.mcp.json` → `@playwright/mcp@0.0.79`, mermaid skill → `@mermaid-js/mermaid-cli@11.16.0` (scan #17/#22 closed).
> - `018` + `019` **obsolete** — both fail-closed flips had already shipped via `w1/m65` (F16: OpenFGA-absent refuses to start, `BEX_ALLOW_INSECURE_AUTHZ` override; F13: `BEX_REQUIRE_VERIFIED_INVITE_EMAIL` secure-by-default).
> - `008`, `017`, `020`, `021` **parked in `.pm/FUTURE-MAYBE.md`** with their promote-triggers intact (Zot in-cluster TLS · ssh-gateway RLS · operator `jobs.create` narrowing · `onbex.co` PSL track-to-closure).
> - `015` **merged** into FUTURE-MAYBE's existing "Off-provider/cross-region backup replica" entry (updated with the Wasabi single-region facts; trigger: first paying tenant or compliance conversation).
> - (#28 invite-link auto-redeem stays token-capability **by design** — Render/GitHub parity — no action, never filed.)

> `014.md` (billing verbs gate on `can_manage`, not `can_manage_billing` — the m61 sweep's finding) promoted to **`w1/m60`** 2026-07-31 (user picked Option 1, align to Render; w7 drained, w1 adopted it m57-style); note moved to `done/`.

> `011.md` + `013.md` grouped into **m62** and `012.md` absorbed into **m60** (its completeness-guard task) 2026-07-30; notes moved to `done/`.

> `009.md` promoted to **m55** 2026-07-30 (the investigate-first branch fired: gap confirmed by the coverage sweep); note moved to `done/`.

> `010.md` promoted to **m57** 2026-07-30 (the trigger fired: `w3/m35` closed the same day, so the audit round-3 follow-on no longer double-covers m35's owned sandbox/NamespaceReconciler work); note moved to `done/`.

_(`001.md` — fresh tenant nodes could not pull authenticated Zot images — promoted to `w6/m29` and closed 2026-07-15; the source incident had already been archived as `done/005.md` by the earlier cross-workstream note-retirement pass.)_

> `004.md` promoted to **m33** 2026-07-15; note moved to `done/`.

> `007.md` (dashboard Key Value eviction-policy editor, the m45 follow-up) implemented 2026-07-19 — a Maxmemory Policy section on the KV detail page over the existing `setKeyValueMaxmemoryPolicy` mutation (`use-set-key-value-maxmemory-policy` hook + component test); note moved to `done/`.

> `006.md` (the unrecoverable `default/biliblilitest` Postgres): **retired 2026-07-19** — user authorized retire; executed against the prod app cluster (`kubectl delete database biliblilitest`, operator finalizer tore down the CNPG Cluster + owned PVC/Secret/Services in ~5s), verified zero leftovers cluster-wide, no data lost (never left `Provisioning`). Note in `done/006.md`. **Inbox cleared 2026-07-19** — then reopened the same day with `008.md`/`009.md` (residuals from `/pm-brainstorm for w7`; see the active list above).

## Not in w7 (deliberate)

- **microVM runtime tier** (Kata/gVisor `RuntimeClass`) — the isolation ladder's next rung; parked in [`.pm/FUTURE-MAYBE.md`](../FUTURE-MAYBE.md) with a public-GA trigger.
- **Dependabot triage** — stays `w1/006` per that note's own instruction (sub-hour triage; promote only if fixes need breaking upgrades); w7 cross-references it as adjacent hygiene.
- **Static-CIDR firewalls / vcluster / sandboxes** — DO_NOT_DO items. m1's pod-level east-west policies are a different layer from the banned `:22`/`:6443` source-IP allowlist (no source-IP allowlists anywhere in w7).
