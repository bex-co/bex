# Start the approved 48–72h production build-cache trial (needs a named observer)

Why: `w7/m89` closed on the hold branch of its definition of done — every technical start condition is met and the only thing left is human. The shipped cache feature (ADR060 D3, m87 + m88 correctness fixes) stays permanently dark until someone owns an observation window, and the measured win is the largest customer-visible build-latency improvement on the board (Dockerfile 179s→38s class).

**Worker:** worker7. **Estimate:** 45m to start + a 48–72h window + 30m to record the decision. **Status:** todo — blocked on a user decision, not on engineering.

## What is already true (m89, 2026-09-15)

- Production runs an operator image containing both `1343b7f17070` (m87 native env invalidation) and `3aea3310212f` (m88 clear-cache).
- Zot 41.49% used of 98.31 GiB; retained-window peak 44.62%; **20.03 GiB reserve above that peak to the 65% admission ceiling**.
- Expected steady-state cache growth **6–9 GiB** — an analytic projection from the live catalog, not a measurement (`BEX_BUILD_CACHE` is manager-wide, so no per-App switch exists to isolate one production shape). Worst case at 3× for every App ≈ 17.9 GiB, which fits only without a concurrent rebuild transient and otherwise relies on the 70%-for-10-minutes stop rule.
- Push p95 baseline ~27 s, so the stop rule's 60-second floor governs. `BEX_BUILD_CACHE` is unset in production.

## What this note needs from the user

1. A **named observer** for the 48–72 hour window, and a **rollback armed at 72 hours** that someone other than the agent session has verified.
2. Then: add `BEX_BUILD_CACHE=registry` to the `controller-manager` JSON patch in `lego/operator/config/prod/kustomization.yaml`, ship, wait for the manager rollout, trigger one build (a disposable QA App is enough) and confirm the Job carries `cache-restore`/`cache-save` phases. Bounce Zot if the per-App cache ACL grants need a process restart.
3. Record start / 48h / 72h times and the rollback owner in the drill before observation begins; close with a dated retain-or-rollback backed by PVC use, push p95 and stale/clear correctness spot checks.

Stop rules, the emergency `set env` rollback, and the full procedure are unchanged in [docs/drills/2026-09-09-build-cache-enablement.md](../../docs/drills/2026-09-09-build-cache-enablement.md).

**Goal linkage:** [ADR008](../../docs/ADR008-vision.md) dependable push-to-deploy; [.pm/GOAL.md](../GOAL.md) #3; ADR060 D3 estate enablement.

**Render parity:** none — ops enablement / infra gate only; `clearCache` semantics were parity-fixed in m88. No REST/GraphQL/MCP/UI contract change.
