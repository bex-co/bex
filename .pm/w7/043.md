# Decide production build-cache enablement

Why: w7/m86 measured faster cached builds but left estate-wide enablement as a capacity decision; the cache-correctness fixes make that decision reviewable.

**Worker:** worker7. **Estimate:** 45m; keep as an inbox note. **Priority:** 3 in the approved 2026-09-08 handoff.

**Depends on:** `w7/m87/t008` and `w7/m88/t009` (both verified closeouts). These are canonical task IDs; look under `done/` after archival.

**Status (2026-09-09):** in progress — production preflight completed; conditional trial on hold. The user approved implementing the recommended correctness/capacity checks followed by a bounded 48–72 hour trial. This approval expands the original recommendation-only scope; it does not waive the start conditions.

## Source and goal

User-approved pm-brainstorm proposal 3, 2026-09-08. [w7/m86](done/m86/README.md) and its [2026-09-05 drill](../../docs/drills/2026-09-05-build-cache.md) measured Dockerfile builds at 179s → 38s and native Node builds at 75s → 64s; the recorded production gate remained unset. Goal: [ADR008](../../docs/ADR008-vision.md) dependable push-to-deploy and [.pm/GOAL.md](../GOAL.md) #3, making a shipped performance feature usable within measured storage/transfer capacity.

## Work

1. Reuse m86 and m87/m88 evidence. Read the current effective operator cache setting, Zot PVC capacity/free space and storage growth, and build queue/run/push metrics; do not assume the September 5 setting is still current.
2. Estimate added cache storage and transfer cost against observed build volume and concurrency, accounting for cache retention/GC, image storage needs, and incomplete measurement windows.
3. Record a dated **enable or hold** recommendation, the expected benefit, concrete headroom/transfer thresholds, and thresholds for reversing enablement. If data is insufficient, state the missing measurement and keep the recommendation conditional.
4. Identify the exact existing config setting and rollback action. Under the September 9 approval, start a bounded 48–72 hour production trial only after the correctness and capacity conditions pass; record the trial window, observations, and retain-or-rollback decision.

**Files/systems:** `lego/operator/config/manager/manager.yaml`, `deploy/gitops/base/zot.yaml`, `deploy/gitops/base/prometheus.yaml`, existing `bex_build_*` and Zot PVC metrics, `lego/operator/internal/controller/build_cache_drill_test.go`, and this note's dated outcome.

**Done when:** both correctness milestones are closed, a measured enable-or-hold decision records storage/transfer cost, expected improvement, evidence time window, and rollback thresholds, and the approved trial has a recorded outcome or an explicit disposition of its remaining start conditions. Keep this note open while the approved trial remains pending.

**Render parity:** omitted; this is an internal capacity/enablement operation with no surface change.

**Out of scope:** unconditional or permanent enablement, new hardware or cache services, persistent cache volumes, and rerunning m86's full benchmark without new evidence requiring it. Trial elapsed time does not itself justify a new milestone.

## September 9 implementation and decision

- [Live preflight, acceptance evidence, capacity budget, and trial/rollback procedure](../../docs/drills/2026-09-09-build-cache-enablement.md).
- Production cache flag is unset. Zot has 36.76 GiB used of 98.31 GiB measured filesystem capacity (37.39%); the retained-window peak is 46.20 GiB. Reserve to the 65% admission ceiling above that peak is 17.70 GiB.
- Monitoring retains about 75 hours, not a full seven days. The last 24 hours contain about 54 build-run samples. Production-size cache growth and transfer remain unmeasured; the tiny correctness fixture is not a fleet-size forecast.
- The deployed operator/API image predates both cache-correctness fixes. [Deployment run 34329225161](https://github.com/bex-co/bex/actions/runs/34329225161) failed in backend OpenFGA startup and Dashboard typechecking, so its build/deploy jobs were skipped.
- Both real build-plane acceptance runs passed (7 builds): native env A → B → B and random-artifact cold → warm → clear → warm. The harness verifies actual image contents and uses UID-guarded fixture cleanup. All fixture Jobs/Pods/Secrets and image/cache tags are retired. Full operator `make test` passed; the repository-wide lint gate has existing toolchain/backend failures detailed in the report.
- **HOLD:** deploy an image containing both fixes through passing CI and confirm the representative cache-size budget before setting `BEX_BUILD_CACHE=registry`. No observation window has started; production configuration is unchanged.
