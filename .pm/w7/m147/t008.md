---
id: w7/m147/t008
title: Simplify
worker: worker7
status: todo
estimate: 20m
depends_on: [w7/m147/t007]
---

## Objective

Run `/simplify` over the code this milestone changed. Look for reuse, simplification, and efficiency improvements, keeping behavior identical.

## Context

- Changed code in `lego/backend/internal/sandbox/`, `internal/sshgateway/sandboxsse/`, and `internal/api/`. Watch for the connect-token signer duplicating the existing exec ticket signer, and for a second hand-rolled rate limiter (use `core.KeyedRateLimiter`).

## Steps

1. Run `/simplify` and apply behavior-preserving fixes.
2. Re-run the backend tests.

## Files

- Files changed by t002–t004.

## Acceptance criteria

- [ ] Simplify findings applied or explicitly declined; tests green.

## Out of scope

- Refactors outside the changed code.
