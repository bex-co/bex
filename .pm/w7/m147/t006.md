---
id: w7/m147/t006
title: Re-grade the sandbox rows in the CLI ledger, including pinned-but-ungraded commands
worker: worker7
status: todo
estimate: 25m
depends_on: [w7/m147/t005]
---

## Objective

Make the compatibility ledger match the pinned client's actual sandbox wire, and give every sandbox command in the pinned help tree an explicit grade.

## Context

- `docs/cli-compatibility-checklist.md:265-273`: the `ea sandbox exec` row describes the v2.21 single-step `POST /exec`, and its evidence contains no non-zero exit.
- `docs/render-artifacts/ea-sandbox.md:10-22` has the same stale route table and claims.
- The pinned help tree contains `ea sandboxes copy` (uses `/sandboxes/{id}/files/{operation}/token`), `ea sandbox-groups list`, and `ea sandboxes snapshots {create,get,list}`. Only snapshots is graded (round-4 bullet). `copy` and `sandbox-groups` have no row.
- The re-baselines in w8/m33 and w6/m142 were help-graded only. Record that a transport change hid behind an unchanged help surface.

## Steps

1. Rewrite the exec row with the handshake, the `exit_code`/`error` shapes, and t001's plus the post-fix live evidence (including a non-zero exit).
2. Add rows for `copy` and `sandbox-groups list` with a truthful grade (`[ ]` gap or `[-]` non-goal, with reason). File a board note for any gap the user wants closed; do not implement it here.
3. Add a step to `lego/cli/UPSTREAM_RENDER_CLI.md`'s pin-update procedure: diff `pkg/sandbox`, `pkg/client` operation paths, and SSE structs, not just the help tree.

## Files

- `docs/cli-compatibility-checklist.md`, `docs/render-artifacts/ea-sandbox.md`, `lego/cli/UPSTREAM_RENDER_CLI.md`.

## Acceptance criteria

- [ ] Every `ea sandbox*` command in `bex ea --help` has exactly one graded row that matches the live behavior.
- [ ] The pin-update procedure names the transport and SSE diff step.
- [ ] `npx prettier@3.4.2 --write` on the changed Markdown.

## Out of scope

- Implementing copy, sandbox groups, or snapshots.
