---
id: w7/m147/t001
title: Reproduce both failures live with the pinned CLI and capture the redacted wire
worker: worker7
status: todo
estimate: 30m
depends_on: []
---

## Objective

Turn the milestone's source-proven defects into a live reproduction before changing code. Use the pinned launcher, human device login, and one free disposable sandbox.

## Context

- Build: `bash scripts/bex-cli-build.sh <tmp>` (Render v2.27.0 ldflags). Use an isolated `BEX_CLI_CONFIG_PATH`, remove inherited `RENDER_*`, set `BEX_HOST=https://api.bex.co/v1/`.
- Consumer: `pkg/sandbox/repo.go:105-133,298-324`, `pkg/sandbox/sse.go:23-31`, `cmd/sandboxexec.go:106-114` in the pinned module.
- Producer: `lego/backend/internal/sandbox/rest.go:97`, `internal/sshgateway/sandboxsse/sandboxsse.go:257-263`.

## Steps

1. `bex ea sandboxes create --plan <free/smallest> -o json`. Record the id in a private ledger immediately.
2. `bex ea sandboxes exec <id> -- true`. Record stdout, stderr, exit code, and duration. Expected today: failure before output, because `POST /v1/sandboxes/<id>/runs/stream/token` is not routed.
3. Replay that exact request (placeholder token, never on argv) and record status, headers, and body.
4. Defect B on the legacy route: `POST /v1/sandboxes/<id>/exec` with `{"command":"sh -c 'exit 7'"}`. Record the raw `event: exit` data (`{"exitCode":7}`), then show that the pinned `readSandboxExecStream` decodes it to 0. A unit harness over the pinned package is acceptable evidence.
5. Terminate the sandbox and confirm it no longer appears in `ea sandboxes list`.

## Files

- `.pm/w7/m147/README.md`: append the sanitized evidence under `## Evidence`.

## Acceptance criteria

- [ ] Redacted request and response for the missing handshake route, plus the CLI's stdout, stderr, exit, and duration.
- [ ] Raw legacy-route `exit` and `error` events with the pinned decoder's result.
- [ ] Sandbox deleted and verified absent. No token, device code, or config content in the record.

## Out of scope

- Any code change.

## Status 2026-09-15

Not run 2026-09-15: live testing needs human device login against production; this session had no QA credentials (`.env` unreadable in the harness). The static evidence in the README stands; run this task before t010 in a session with production access. The code (t002–t004) is shipped, so the expected result now is exit 7 / exit 0 rather than the failure the steps describe — record both the pre-fix production behaviour (if the old image is still live) and the post-fix one.
