---
id: w7/m147/t005
title: Blast-radius verification across exec callers, legacy route, and route guards
worker: worker7
status: done
estimate: 40m
depends_on: [w7/m147/t002, w7/m147/t004]
---

## Objective

Prove that the shared exec changes did not regress any other consumer of the gateway SSE stream or the sandbox route set.

## Context

- Buffered readers: `ExecBuffered` (MCP `sandbox_exec`, `internal/sandbox/mcp.go:91`), `systemBufferedExec` via `clientSuspend` (`service.go:965`; scrub-before-suspend, see `snapshot_test.go:34-75`), and the direct `bufferExec` calls in `AgentSessionLifecycle.ReadSessionStatus` (`service.go:659`) and `HibernateAgentSessionSandbox` (`service.go:796`).
- Streaming reader: legacy `POST /v1/sandboxes/{id}/exec` (`rest.go:97`) used by older Render CLIs (≤ v2.21).
- Guards: `TestScopeMatrixCoversLiveOperations`, `TestComposedMuxAlwaysPublicInventory`, and the OpenAPI boundary.

## Steps

1. Enumerate every non-test caller of `bufferExec`, `ExecBuffered`, `systemBufferedExec`, and `StreamExec`, and record the count in the milestone README.
2. For each caller, run or add a behavioral check with a non-zero exit and a terminated-target error, over both the new payload and the transitional old one.
3. Replay a v2.21-shaped `POST /exec` request (single-step) and confirm it still streams.
4. Run `cd lego/backend && go test ./...` and `make lint` from `lego/operator/`.

## Files

- Tests under `lego/backend/internal/sandbox/` and `internal/sshgateway/sandboxsse/`.

## Acceptance criteria

- [x] Caller census recorded, and each caller has a passing non-zero-exit plus error-path check.
- [x] Legacy `/exec` still serves a v2.21-shaped request.
- [x] Backend suite and lint green.

## Out of scope

- New sandbox features.

## Done 2026-09-15

Caller census (non-test): `StreamExec` ← `rest.go` legacy `/exec`, `connect.go` redeem; `ExecBuffered` ← `mcp.go` `sandbox_exec`; `systemBufferedExec` ← `service.go:965` `clientSuspend` scrub; `bufferExec` direct ← `service.go:659` `ReadSessionStatus`, `service.go:796` `HibernateAgentSessionSandbox`. All four buffered callers share `bufferExecWithLimit`, which `TestBufferExecReadsPinnedAndTransitionalShapes` exercises with non-zero exit and terminated/unavailable errors over both payload shapes; the pre-existing per-caller tests (`TestExecBufferedParsesSSE` exit 7, `TestReadSessionStatusFailsClosedOnTerminalSandbox`, `TestSuspendRunsPlatformScrubUnderContributor`, hibernate coverage) now run over the pinned gateway shape. Legacy v2.21-shaped `POST /exec` still streams (`TestStreamExecAuthorizesMintsTicketAndRelaysSSE`). `cd lego/backend && go test ./...`: every package green except `internal/apps` `TestShippedExampleBlueprintsValidate`, which fails only because a stale July agent worktree under `.claude/worktrees/` (with its own uncommitted edits, left untouched) is walked for `examples/**/bex.yml` — unrelated to this change and absent in CI. `make lint` stays red on two pre-existing hits (`publish.go` modernize, backend `writeGraphQLErrors` unused) that predate this milestone.
