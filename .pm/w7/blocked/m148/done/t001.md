---
id: w7/m148/t001
title: "Serialize local cluster mutations"
worker: worker7
status: done
estimate: 40m
depends_on: []
---

## Objective

Add an ownership-aware local lock around bring-up and scale, with bounded contention diagnostics and safe cleanup. Do not break an active lock or race another session rebuilding the shared cluster.

## Context

- See [milestone scope and acceptance](README.md).
- Source: Approved w7 brainstorm item 1 via `$pm all for w7`; `.pm/w9/blocked/060.md`, `.pm/w10/003.md`, and current `scripts/mock-cluster.sh` / `scripts/dev-env.sh`.

## Steps

1. Read the relevant current implementation and applicable repository instructions.
2. Add an ownership-aware local lock around bring-up and scale, with bounded contention diagnostics and safe cleanup. Do not break an active lock or race another session rebuilding the shared cluster.
3. Record the behavioral evidence and update task status only when acceptance holds.

## Files

- `scripts/mock-cluster.sh`

## Acceptance criteria

- [ ] A second invocation performs no provisioning or scale mutation while the first owns the lock.
- [ ] Normal exit and interruption release only the invoking process's lock; stale ownership has a safe recovery path.

## Out of scope

Production provisioning, new runner hosts, and unrelated workstream mutations.

## Done 2026-09-17

Lock implemented in `scripts/mock-cluster.sh` (mkdir-based; flock is absent on macOS), covering bring-up and `scale`. Verified live: a live owner was refused, a SIGKILL'd owner left the lock, and the next run reclaimed it by liveness and released cleanly. `scripts/mock-cluster.test.sh` pins all seven ownership behaviours.
