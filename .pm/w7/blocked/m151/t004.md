---
id: w7/m151/t004
title: "Reconcile existing drift and verify read surfaces"
worker: worker7
status: blocked
estimate: 40m
depends_on: [w7/m151/t002, w7/m151/t003]
---

## Objective

Repair only provably stale placement and verify REST, GraphQL, MCP and dashboard views on isolated dev-7; preserve ambiguous ownership and report it.

## Context

- Environment deletion in environments/service.go explicitly retains datastore labels; keyvalue.Service.SetProjectID and its Postgres counterpart change the project without clearing an incompatible environment. Views expose these labels directly.
- See [milestone scope](README.md).

## Steps

1. Inspect the relevant implementation, existing tests and repository instructions.
2. Repair only provably stale placement and verify REST, GraphQL, MCP and dashboard views on isolated dev-7; preserve ambiguous ownership and report it.
3. Record evidence against the acceptance criteria.

## Files

- `lego/backend/internal/projects/service.go`
- `lego/backend/internal/environments/service.go`
- `lego/backend/internal/postgres/service.go`
- `lego/backend/internal/keyvalue/service.go`

## Acceptance criteria

- [x] Existing provably stale references are repaired idempotently.
- [ ] All read surfaces agree on final membership and inherited rules.
- [ ] A dated live walkthrough records successful moves, deletion and recovery.

## Out of scope

No datastore deletion, ownership transfer, new grouping API, automatic foreign-resource adoption, or broad architecture refactor.

## Blocked verdict — 2026-09-21

Implemented idempotent stale-reference repair in the existing datastore reconciliation pass. It reuses CR inventories, caches authoritative grouping reads within a bounded pass, resumes fairly after cancellation, and uses optimistic-lock patches. Only datastores in their canonical workspace namespace with matching ownership labels, a local tenant and this control plane's managed hosting namespace qualify. Missing project/environment references and incompatible same-workspace environment placement are repaired; foreign/ambiguous references and lookup errors are preserved and reported. Resource-owned allowlists are untouched.

Local get/list and grouping views agree across REST/GraphQL/MCP in the composed 27-case matrix, and the dashboard's grouping consumer suites pass. The task explicitly requires the isolated dev-7 walkthrough, including live dashboard state; those remaining criteria are not checked off. The earlier m148 bring-up gate is no longer a missing cluster: the shared 16 GiB OrbStack VM now suffers global OOM, leaving dev-7 database/auth/API dependencies unstable (see blocked/m149 evidence). The user/operator must restore sufficient shared capacity, then run the live move/delete/recovery walkthrough and record its date before closing this task.
