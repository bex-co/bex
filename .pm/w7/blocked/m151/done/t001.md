---
id: w7/m151/t001
title: "Pin datastore move and delete behavior"
worker: worker7
status: done
estimate: 40m
depends_on: []
---

## Objective

Capture Postgres and Key Value project/environment transitions, resource-owned versus inherited rules, and interruption cases. Establish the invariant that an environment belongs to the resource’s current project.

## Context

- Environment deletion in environments/service.go explicitly retains datastore labels; keyvalue.Service.SetProjectID and its Postgres counterpart change the project without clearing an incompatible environment. Views expose these labels directly.
- See [milestone scope](README.md).

## Steps

1. Inspect the relevant implementation, existing tests and repository instructions.
2. Capture Postgres and Key Value project/environment transitions, resource-owned versus inherited rules, and interruption cases. Establish the invariant that an environment belongs to the resource’s current project.
3. Record evidence against the acceptance criteria.

## Files

- `lego/backend/internal/projects/service.go`
- `lego/backend/internal/environments/service.go`
- `lego/backend/internal/postgres/service.go`
- `lego/backend/internal/keyvalue/service.go`

## Acceptance criteria

- [x] Reproductions distinguish deleted references from incompatible live-environment membership.
- [x] The acceptance matrix covers both datastore kinds and preserves resource-owned allowlists.

## Out of scope

No datastore deletion, ownership transfer, new grouping API, automatic foreign-resource adoption, or broad architecture refactor.

## Completion evidence — 2026-09-21

Pinned failing regressions for both datastore kinds before implementation: project moves/detaches retained env-a plus inherited rules; same membership wrote resourceVersion; environment deletion retained deleted labels and a list-to-reassignment interleave cleared the new environment policy. Project deletion omitted datastore cleanup even when a member would fail. The composed REST/GraphQL/MCP matrix also reproduced 12 failing move/delete cases. The tests separately cover missing grouping references, incompatible live environments and ambiguous/foreign ownership; every case checks resource-owned allowlist preservation.
