---
id: w7/m152/t003
title: "Preflight project-cascade permissions before member clearing"
worker: worker7
status: todo
estimate: 40m
depends_on: [w7/m152/t001]
---

## Objective

Authorize all affected child environments before clearing the first member, preserving fresh mutation-time checks. Prevent preflight-detectable refusal from partially changing earlier children.

## Context

- environments/service.go SetDatabases/SetKeyValues, setResourceMembers, clearMembersForProject and clearEnvironmentMembers; projects/service.go setResourceMembers and Delete.
- See [milestone scope](README.md).

## Steps

1. Inspect the relevant implementation, existing tests and repository instructions.
2. Authorize all affected child environments before clearing the first member, preserving fresh mutation-time checks. Prevent preflight-detectable refusal from partially changing earlier children.
3. Record evidence against the acceptance criteria.

## Files

- `lego/backend/internal/projects/service.go`
- `lego/backend/internal/environments/service.go`
- `lego/backend/internal/postgres/service.go`
- `lego/backend/internal/keyvalue/service.go`

## Acceptance criteria

- [ ] A project delete refused on any child leaves every child and member unchanged.
- [ ] Authorized cascade still succeeds and protected-environment checks remain enforced.

## Out of scope

No distributed transaction guarantee across Kubernetes and Postgres, new roles, weakened authorization, or lifecycle-cleanup duplication with m151.
