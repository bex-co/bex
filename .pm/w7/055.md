# 055 — The production liveness schedule runs 3–5.5 hours late, so `w3/m83`'s "within six hours" guarantee is not delivered

Why: `w3/m83` exists so that "a silent regression … surfaces as a GitHub issue **within six hours**". Measured over nine consecutive scheduled runs, the workflow fires **3h04m–5h27m** after its cron slot and the gap between runs reaches **7h28m** — so a regression can go unreported for longer than the guarantee promises, and nobody would notice because each individual run looks fine.

## UNBLOCKED 2026-09-16 — the owner decided: restate honestly

The decision this note was blocked on was made during `/pm-brainstorm for w3`, presented with both options and the measured evidence: **restate `w3/m83`'s six-hour guarantee to the cadence GitHub actually delivers.** Moving the trigger to something bex controls (an in-cluster scheduler or `repository_dispatch`) was weighed and **deferred**, with drift beyond the restated window as its escalation trigger.

That makes the remaining work concrete and unblocked:

1. Pick a window every measured gap fits inside — the observed worst is 7h28m, so ~8h is the honest ceiling. State it as the schedule's best-effort ceiling, not a target.
2. Update `docs/ADR088-platform-observability-ui.md` §6's coverage table and the header comments of `.github/workflows/ssh-edge-liveness.yml` + `scripts/tenant-view-liveness.sh` to that window, with one sentence naming GitHub-side dispatch (settled below: queue wait is zero on all ten runs) as the reason it is not six hours.
3. Add a dated correction line to `.pm/w3/done/m83/README.md` — the six-hour claim was made there and a reader landing on it deserves the correction. Annotate; do not rewrite a closed milestone's history.

The cadence checker this note's closing section asked for is **not** part of this scope — it is filed separately as [`w3/042`](../w3/042.md) and depends on step 1 producing a number.

## Approved scope — priority 3

Approved by `$pm all for w7` following the w7 brainstorm. **Worker:** worker7. **Status:** todo — unblocked (investigation done; cadence decision made, see above). **Estimate:** 30m investigation _(done)_ + ~30m to apply the restatement. **Depends on:** none; scheduled after 053.

Compare scheduled slots, workflow creation/start times, and job queue/start times for `.github/workflows/ssh-edge-liveness.yml` to distinguish dispatch delay from runner wait. Record the evidence and recommend supported detection-window wording for `w3/m83` and `docs/ADR088-platform-observability-ui.md`, or a concretely scoped scheduling follow-up. If timestamps cannot settle attribution, state that limit rather than inventing a cause or a guaranteed interval.

**Acceptance:** the note records measured dispatch/queue delays (or the missing evidence), an evidence-based cadence recommendation, and any specific follow-up needed. This approval covers diagnosis and recommendation, not provisioning a scheduler or runner hosts; the broader alternatives below remain conditional.

**Goal / why now:** ADR008 operational reliability; resolve the unsupported six-hour detection guarantee before expanding monitoring. No Render-parity task: operational scheduling only. Keep this as an inbox note, not a milestone.

**Estimate:** 30m to decide · **Severity:** minor (detection latency, not a tenant-facing defect) · **Attribution:** CI/synthetics · **Worker:** worker7

## Measured

`.github/workflows/ssh-edge-liveness.yml` line 52 declares `cron: "37 */6 * * *"` — 00:37, 06:37, 12:37, 18:37 UTC. Actual start times of the runs GitHub reports with `event: schedule`, read 2026-09-16T14:03:51Z:

| cron slot     | actual start (UTC)     | late by |
| ------------- | ---------------------- | ------- |
| 18:37 (09-14) | 2026-09-14T22:00:54Z   | 3h24m   |
| 00:37 (09-15) | 2026-09-15T05:17:16Z   | 4h40m   |
| 06:37 (09-15) | 2026-09-15T12:04:16Z   | 5h27m   |
| 12:37 (09-15) | 2026-09-15T17:14:50Z   | 4h38m   |
| 18:37 (09-15) | 2026-09-15T21:43:39Z   | 3h07m   |
| 00:37 (09-16) | 2026-09-16T05:11:45Z   | 4h35m   |
| 06:37 (09-16) | 2026-09-16T11:59:59Z   | 5h23m   |
| 12:37 (09-16) | 2026-09-16T17:15:38Z   | 4h38m   |
| 18:37 (09-16) | 2026-09-16T21:40:48Z   | 3h04m   |

Consecutive gaps, all eight between the nine runs above: **7h16m** · 6h47m · 5h10m · 4h29m · 7h28m · 6h48m · 5h16m · 4h25m.

**Corrected sweep 37.** This list previously began at 6h47m and silently omitted the first gap (2026-09-14T22:00:54 → 2026-09-15T05:17:16 = **7h16m**), which is itself over the six-hour guarantee. The omission understated the note's own case.

**The 12:37 (09-16) slot was delayed, not dropped.** At 14:03:51Z it had not fired; it landed at 17:15:38Z, 4h38m late. This matters because GitHub may either defer *or* skip a scheduled run, and the two have different fixes — a skipped slot means a missed detection window outright, a delayed one means a late detection. Every slot measured so far has eventually run, so the observed failure mode is **latency, not loss**. That keeps the eight rows above a drift measurement rather than a gap measurement.

Every row above is `event: schedule`; the one `workflow_dispatch` run in the window (2026-09-14T22:01:48Z) is excluded.

## Why it matters

- `w3/m83`'s goal sentence is the guarantee: _"surfaces as a GitHub issue **within six hours**"_, and its definition of done lists the tenant-view canary as `(6h)`.
- **Four of the eight** observed gaps exceed six hours (7h16m, 6h47m, 7h28m, 6h48m) — half of them, not the "two of five" this note originally claimed. A regression appearing just after a run therefore waits up to ~7.5 hours for its issue, not ≤6.
- The drift is invisible from any single run: each one passes or fails on its own merits, and only the sequence shows the guarantee is not being met. That is the same shape as the lineage this canary came from — a guard that exists and does not do what it claims.

## Cause is NOT established, deliberately

GitHub documents scheduled workflows as best-effort: they can be delayed under load and skipped entirely, and the delay is not part of any SLA. So the cause may be entirely outside bex. Two candidates, neither probed here:

1. **GitHub-side dispatch delay** — the documented best-effort behaviour, which would make this a wrong *claim* rather than a broken mechanism.
2. **Runner availability** — the fleet runs on a single self-hosted Mac ([`DO_NOT_DO.md` `#RUNNER-HOSTS`](../DO_NOT_DO.md)), so a queued or offline runner would also defer a dispatched run. Worth checking queue wait times on these runs before concluding.

Distinguishing them is the first step of any fix; do not assume either.

## Fix (decide, then do one)

- **Restate the guarantee honestly.** If GitHub-side delay is the cause and is accepted, change `w3/m83`'s six-hour wording (and ADR088 §6's coverage table, if it repeats it) to the cadence actually delivered — a guarantee nobody can keep is worse than a slower one that is true.
- **Or move the trigger to something bex controls.** A scheduler inside the cluster invoking the same scripts, or a `repository_dispatch` from one, removes the dependency on best-effort cron.
- Either way, add the cheap check that would have caught this: assert the gap between consecutive runs of each liveness workflow stays under its advertised window, and open an issue when it does not. The canaries watch production; nothing watches the canaries.

## Blast radius

- The same cron governs all three jobs in this workflow (`public-edge-liveness`, `request-logs-liveness`, `tenant-view-liveness`), so the latency applies to every surface they cover, not just the tenant view.
- Other scheduled probes on the same fleet plausibly drift too (the isolation matrix, the deploy canary's weekly run); this note measures only this workflow, and the others are **unverified**.

## Dedupe

Distinct from [`054`](054.md), which is about the tenant-view canary's stage 3 having no retry budget (false positives). This note is about detection latency (delayed reporting) — different mechanism, different fix, same workflow. `w3/m83` is **done** and its DoD does not mention schedule drift. No open board item mentions scheduled-workflow latency or the six-hour guarantee. Nothing in `.pm/DO_NOT_DO.md` applies; `#RUNNER-HOSTS` forbids splitting the runner fleet across hosts, which this note does not propose.

## Unverified

- Queue wait time per run was not read, so runner contention versus GitHub dispatch delay is undecided.
- Only this workflow was measured. Whether the other scheduled probes drift equally is not checked.
- Whether any regression was actually missed because of the drift is not established — this records that the guarantee is unmet, not that it has already cost something.

## Cause settled 2026-09-17 (`/loopx w7`) — GitHub dispatch, not the runners

The note deliberately left two candidates open and named reading queue wait times as the first step of any fix. Done, and it is decisive:

```text
gh run list --workflow "Production edge liveness" --json event,createdAt,startedAt
  (event: schedule, ten most recent)

created=2026-09-17T05:19:08Z  started=2026-09-17T05:19:08Z
created=2026-09-16T21:40:48Z  started=2026-09-16T21:40:48Z
created=2026-09-16T17:15:38Z  started=2026-09-16T17:15:38Z
created=2026-09-16T11:59:59Z  started=2026-09-16T11:59:59Z
created=2026-09-16T05:11:45Z  started=2026-09-16T05:11:45Z
created=2026-09-15T21:43:39Z  started=2026-09-15T21:43:39Z
created=2026-09-15T17:14:50Z  started=2026-09-15T17:14:50Z
created=2026-09-15T12:04:16Z  started=2026-09-15T12:04:16Z
```

**`createdAt` equals `startedAt` to the second on every run — queue wait is zero.** A runner picks each job up the instant GitHub creates it, so candidate 2 (runner availability on the single self-hosted Mac, `#RUNNER-HOSTS`) is **falsified**. The lateness is entirely candidate 1: GitHub creates the scheduled run hours after the cron slot, which is the documented best-effort behaviour.

This matters for the decision, because it removes one of the two fixes: adding runner capacity would change nothing. The real choice is now binary — **restate the guarantee** to the cadence GitHub actually delivers, or **move the trigger** to something bex controls (an in-cluster scheduler, or a `repository_dispatch` from one). The third bullet, asserting the gap between consecutive runs, is worth doing under either.

A tenth scheduled run has since landed: 00:37 (09-17) started 2026-09-17T05:19:08Z, **4h42m late** — inside the measured band, which now holds across ten consecutive runs.

## BLOCKED 2026-09-17

Everything diagnosable is diagnosed; what remains is a decision only the owner can make — accept GitHub's best-effort cron and rewrite `w3/m83`'s "within six hours" wording (and ADR088 §6 if it repeats it), or invest in a trigger bex controls. Implementing either without that call would be picking a product guarantee on the owner's behalf.
