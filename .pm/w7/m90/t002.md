---
id: w7/m90/t002
title: Envtest pins single-instance vs HA enablePDB
worker: worker7
status: todo
estimate: 45m
depends_on: [w7/m90/t001]
---

## Objective

Prove in envtest that single-instance Clusters get `enablePDB: false` and HA Clusters keep PDB enabled.

## Context

- Convergence tests already touch `enablePDB: true` as a live default to preserve (`reconcile_convergence_envtest_test.go`).
- Invert the single-instance expectation carefully so HA preservation tests stay green.

## Steps

1. Add/adjust envtest covering create+reconcile for instances=1 → `enablePDB=false`.
2. Add/adjust HA path (≥2) asserting PDB remains enabled (default or explicit true).
3. Run `make test` for the operator package.

## Files

- `lego/operator/internal/controller/database_test.go`
- `lego/operator/internal/controller/reconcile_convergence_envtest_test.go`
- `lego/operator/internal/controller/spec_projection_test.go` if needed

## Acceptance criteria

- [ ] Tests fail on the pre-fix tree and pass after t001.
- [ ] HA path not regressed.

## Out of scope

- Live cluster drain drill (optional evidence in t003/t004 docs only).
