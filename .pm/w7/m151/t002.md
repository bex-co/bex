---
id: w7/m151/t002
title: "Clear incompatible environment state on project departure"
worker: worker7
status: todo
estimate: 50m
depends_on: [w7/m151/t001]
---

## Objective

Make project departure clear incompatible environment membership and inherited IP rules through the shared feature seams, preserving resource-owned configuration and authorization.

## Context

- Environment deletion in environments/service.go explicitly retains datastore labels; keyvalue.Service.SetProjectID and its Postgres counterpart change the project without clearing an incompatible environment. Views expose these labels directly.
- See [milestone scope](README.md).

## Steps

1. Inspect the relevant implementation, existing tests and repository instructions.
2. Make project departure clear incompatible environment membership and inherited IP rules through the shared feature seams, preserving resource-owned configuration and authorization.
3. Record evidence against the acceptance criteria.

## Files

- `lego/backend/internal/projects/service.go`
- `lego/backend/internal/environments/service.go`
- `lego/backend/internal/postgres/service.go`
- `lego/backend/internal/keyvalue/service.go`

## Acceptance criteria

- [ ] A datastore moved to another project has no membership or inherited rules from the former project’s environment.
- [ ] Authorized no-op reassignment does not churn state; refusal does not weaken protection.

## Out of scope

No datastore deletion, ownership transfer, new grouping API, automatic foreign-resource adoption, or broad architecture refactor.
