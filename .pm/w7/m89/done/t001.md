---
id: w7/m89/t001
title: Unblock deploy.yml so an image containing m87+m88 ships
worker: worker7
status: done
estimate: 1.5h
depends_on: []
---

## Objective

Get a green `deploy.yml` path that builds and deploys an operator/API image containing both cache-correctness fixes, so production is no longer stuck on a pre-m87/m88 digest.

## Context

- Preflight ([docs/drills/2026-09-09-build-cache-enablement.md](../../../../docs/drills/2026-09-09-build-cache-enablement.md)): live image predates `1343b7f17070` (native env) and `3aea3310212f` (clear-cache).
- Recent failures skipped build/deploy after backend OpenFGA in-memory exit 125 and dashboard typecheck fixture gaps (e.g. `outboundIps` on `ServiceView` / `EnvGroupView`).
- Trial must not start until the corrected image is live.

## Steps

1. Reproduce the failing gates on tip; fix only what blocks backend/dashboard CI for deploy.
2. Confirm a `deploy.yml` run reaches build+deploy (or the next green run after the fix).
3. Record the new image digest and that it contains both fixes.

## Files

- `.github/workflows/deploy.yml` (and jobs it `needs`)
- Backend OpenFGA test/startup path under `lego/backend/`
- Dashboard fixtures/types under `dashboard/src/` as required by typecheck

## Acceptance criteria

- [x] A deploy pipeline run builds and deploys (not skipped) from a commit that includes m87+m88.
- [x] Production (or Argo-tracked) image digest is recorded in the drill or this task's closeout note.
- [x] No secrets printed; fixes are minimal and regression-tested where practical.

## Out of scope

- Setting `BEX_BUILD_CACHE` (t004).
- Unrelated flaky suites unrelated to the deploy gate.

## Closeout 2026-09-15

Done without new code in this milestone: the gates named in Context were repaired by other workstreams between September 9 and 10, and `deploy.yml` has built and deployed on every green run since.

- First green build+deploy after the two fixes: [run 34444812690](https://github.com/bex-co/bex/actions/runs/34444812690) (2026-09-10 06:21 UTC) from `46e16ec38835`, which contains both `1343b7f17070` (m87) and `3aea3310212f` (m88) by `git merge-base --is-ancestor`. Built operator/API image digest `sha256:9b5162498e5b4be820dd95159b06bed35734b8e2a6157bca3419dd1d5e89e777`.
- Latest green run at triage time: [run 34936503315](https://github.com/bex-co/bex/actions/runs/34936503315) (2026-09-15 06:20 UTC) from `c4212ec71909`; digest `sha256:1f3648adb9598bd2d73304b2ebc346bfffe04542d3db13a55e4d198168711a46`; the job log shows `deployment "bex-controller-manager" successfully rolled out` after the restart onto that image, and `deploy/gitops/base/bex.yaml:29` pins the same digest (write-back commit `0eb2036e2`).
- Every run between 2026-09-09 03:37 UTC and 2026-09-10 03:17 UTC failed (12 runs); none of the repairs live in this milestone, so no drill file changed here beyond the dated note in `docs/drills/2026-09-09-build-cache-enablement.md`.
