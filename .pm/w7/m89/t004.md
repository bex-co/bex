---
id: w7/m89/t004
title: Start bounded production cache trial
worker: worker7
status: todo
estimate: 45m
depends_on: [w7/m89/t003]
---

## Objective

Enable `BEX_BUILD_CACHE=registry` for a bounded 48–72h production trial with rollback armed, only after t002/t003 pass.

## Context

- Procedure already drafted in the Sep 9 drill: prod overlay JSON patch, Argo self-heal, 48h observe / 72h hard stop, rollback thresholds (PVC >70% 10m, push p95, stale/clear faults).
- Zot may need ACL bounce for cache grants before first warm build.

## Steps

1. Add `BEX_BUILD_CACHE=registry` via `lego/operator/config/prod/kustomization.yaml` (durable GitOps path).
2. Record start time, 48h checkpoint, 72h hard stop, observer, and scheduled rollback.
3. Verify a newly created build Job carries cache phases; bounce Zot if ACLs require process restart.
4. Do not leave the switch on past 72h without t005's retain decision.

## Files

- `lego/operator/config/prod/kustomization.yaml`
- `docs/drills/2026-09-09-build-cache-enablement.md`

## Acceptance criteria

- [ ] Gate live only after t002/t003 go.
- [ ] Start/48h/72h and rollback procedure recorded before observation begins.
- [ ] At least one build Job observed with cache phases.

## Out of scope

- Permanent enablement without t005.
- New hardware or persistent cache volumes.

## Status 2026-09-15

Start conditions are met (t002 go, t003 fit on the expected footprint). Not started in this session: the procedure requires a named observer for the 48–72 h window and an independently verified scheduled rollback at 72 h, and an unattended agent session cannot be either. To start: add `BEX_BUILD_CACHE=registry` to the `controller-manager` JSON patch in `lego/operator/config/prod/kustomization.yaml`, ship, wait for the manager rollout, trigger one build (a disposable QA App is enough) and confirm the Job carries the `cache-restore`/`cache-save` phases, then record start/48 h/72 h times and the rollback owner here.
