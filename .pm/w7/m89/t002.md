---
id: w7/m89/t002
title: Confirm production digest and Zot PVC budget
worker: worker7
status: todo
estimate: 30m
depends_on: [w7/m89/t001]
---

## Objective

Verify production runs the corrected image and re-measure Zot capacity against the 60%/65% trial thresholds before admitting cache growth.

## Context

- Sep 9 snapshot: 36.76 GiB used / 98.31 GiB FS; peak 46.20 GiB; 17.70 GiB reserve above peak to 65%.
- Do not assume the snapshot is still current after t001's deploy.

## Steps

1. Read manager env and image digest; confirm m87+m88 commits are present in that image's provenance.
2. Re-read Zot PVC used/capacity and recent peak; compare to 60% target and 65% admission ceiling.
3. Update the drill record with the new numbers and go/no-go for t003/t004.

## Files

- `docs/drills/2026-09-09-build-cache-enablement.md` (or a dated follow-on)
- `deploy/gitops/base/zot.yaml`, Prometheus/`bex_build_*` as needed

## Acceptance criteria

- [x] Dated confirmation that production runs a post-m87/m88 image (2026-09-15, from CI: see `done/t001.md`).
- [ ] Dated PVC used/peak/headroom vs 60%/65% recorded.
- [ ] Explicit go/no-go for starting the trial after t003.

## Out of scope

- Enabling the cache flag.
- Provisioning more PVC capacity unless the budget fails closed.

## Attempted 2026-09-15 — BLOCKED on production cluster access

Step 1 is satisfied from CI evidence (`done/t001.md`): the Argo-tracked digest `sha256:1f3648adb9…` was built from `c4212ec71909`, which contains both fixes. Steps 2–3 need a live read of the Zot PVC (`bex-registry/zot-pvc-zot-0`) and Prometheus. The production kubeconfig is fetched with `scripts/fetch-app-kubeconfig.sh`, which requires `HCLOUD_TOKEN` and `~/.ssh/id_bex`; this session's harness refused every read of `.env`, no `HCLOUD_TOKEN`/`KUBECONFIG` was exported, and the only kube contexts present are the local `kind-bex-mgmt`/`orbstack` ones. The September 9 PVC numbers must not be reused as current (see Context), so no go/no-go is recorded. To resume: run this task in a session with the production kubeconfig, then t003–t005 follow unchanged.
