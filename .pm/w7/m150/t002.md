---
id: w7/m150/t002
title: "Mint and redeem authorized file-connect tokens"
worker: worker7
status: todo
estimate: 50m
depends_on: [w7/m150/t001]
---

## Objective

Add file operation/path-bound connect tokens using existing sandbox owner checks and durable single-use redemption. Keep file and run claims distinct, reauthorize at redemption, and register scope policy for new routes.

## Context

- See [milestone scope and acceptance](README.md).
- Source: Approved w7 brainstorm item 3 via `$pm all for w7`; promoted `w7/046`, archived at `.pm/w7/done/046.md`; w7/m147 file-copy exclusion and `docs/cli-compatibility-checklist.md` copy gap.

## Steps

1. Read the relevant current implementation and applicable repository instructions.
2. Add file operation/path-bound connect tokens using existing sandbox owner checks and durable single-use redemption. Keep file and run claims distinct, reauthorize at redemption, and register scope policy for new routes.
3. Record the behavioral evidence and update task status only when acceptance holds.

## Files

- `lego/backend/internal/sandbox/connect.go`
- `lego/backend/internal/sandbox/rest.go`
- `lego/backend/internal/api/scope_matrix_ops.go`

## Acceptance criteria

- [ ] Tokens bind caller, workspace, sandbox, operation and destination/source path.
- [ ] Expired, replayed, foreign and wrong-operation tokens are refused across replicas.
- [ ] OAuth and connect-token routes preserve their intended authentication boundaries.

## Out of scope

Sandbox groups, snapshots, App-instance SFTP/SCP/forwarding, a forked CLI, and new GraphQL/MCP/UI file-transfer surfaces.
