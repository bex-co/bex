# 058 — CLI-only commits must not suppress platform deployment

Why: A CLI-only push can suppress an in-flight platform build without scheduling a replacement platform deployment.

**Worker:** worker7. **Status:** done. **Estimate:** 45m.

## Source and goal

Approved by `$pm all for w7 and $ship` after the additional w7 brainstorm (2026-09-17); source review at d7ea2ac52. These are source-backed findings, not newly reproduced production failures.

`.github/workflows/deploy.yml` excludes `lego/cli/**` from triggers, but `scripts/deploy-superseded.sh` compares all of `lego`. w1/m59 shipped supersession hygiene; this is the current filter disagreement, not a policy redesign.

**Goal linkage:** ADR008 dependable hosting delivery and deterministic, agent-readable deployment state. **Why now:** A CLI-only push can suppress an in-flight platform build without scheduling a replacement platform deployment.

## Scope

Align the supersession filter with the workflow trigger; extend `scripts/deploy-superseded.test.sh` for CLI-only, mixed platform/CLI and generated-digest cases.

## Acceptance

- [x] CLI-only changes do not supersede platform builds; real platform and substantive manifest changes still do; generated digest changes remain excluded.

## Dependencies and bounds

No Render parity: internal delivery tooling. Preserve latest-input safety and runner topology. This mechanism is source-proven; do not claim it caused prior production pin delays without evidence.

Keep as an inbox note under the ≤1h sizing rule.

## Completion — 2026-09-21

The shared supersession script now excludes `lego/cli/**`, matching deploy.yml's push filter. Both pre-build and write-back consumers retain the same latest-input guard and generated-field normalization.

The pre-fix real-Git fixtures reproduced four false supersessions (root/nested CLI-only files and CLI plus generated platform/agent digests). All 19 final cases pass, including CLI deletion, platform↔CLI renames, mixed backend/dashboard changes, generated-only changes, and substantive manifest drift. `bash -n`, `git diff --check`, and `scripts/github-actions-validate.sh` pass. Logs: `/tmp/bex7-058-supersession-red.log`, `/tmp/bex7-058-supersession-green.log`, `/tmp/bex-w7-058-actions.log`.

This establishes the source-level defect and fix; no claim is made that it caused a past production pin delay. Runner topology and deployment policy are unchanged.
