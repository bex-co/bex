---
id: w7/m90/t003
title: Existing single-instance Clusters reach enablePDB false
worker: worker7
status: done
estimate: 30m
depends_on: [w7/m90/t001]
---

## Objective

Ensure already-provisioned single-instance tenant Postgres Clusters converge to `enablePDB: false`, not only new creates.

## Context

- Unstructured projection must update the field without thrashing unrelated defaults.
- If reconcile cannot safely flip live objects, document and execute a one-shot with evidence.

## Steps

1. Confirm reconcile writes `enablePDB: false` when live is true and instances=1.
2. If blocked by ownership/SSA rules, document the one-shot and prove it on one non-prod or disposable cluster first.
3. Record how production estate will be covered (reconcile roll vs script).

## Files

- `lego/operator/internal/controller/database_controller.go`
- Drill/note under `docs/drills/` if a one-shot is required

## Acceptance criteria

- [x] Path for existing single-instance Clusters is real and tested or drilled.
- [x] HA live objects not flipped incorrectly.

## Out of scope

- Mass HA topology changes.

## Done 2026-09-15

Reconcile path, no one-shot: `mergeProjectedMap` overlays desired keys onto the live spec, so a pre-m90 Cluster (annotation without `enablePDB`, live `true`) is written to `false` on its next reconcile and unrelated admission defaults survive. Pinned by `TestSpecProjectionSingleInstancePDBFlip` (unit) and by the envtest that strips the key from the recorded projection, sets live `true`, and expects exactly one Cluster update then convergence. Growing to HA withdraws the key (previous-but-not-desired keys are deleted) so CNPG's default returns.
