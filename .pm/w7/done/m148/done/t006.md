---
id: w7/m148/t006
title: "Test coverage"
worker: worker7
status: done
estimate: 45m
depends_on: [w7/m148/t001, w7/m148/t002, w7/m148/t003, w7/m148/t004]
---

## Objective

Add or strengthen behavioral regressions for this milestone's success and failure cases. Prove the tests detect the pre-fix defect or deliberately broken implementation; run the relevant suites and required checks.

## Context

- See [milestone scope and acceptance](README.md).
- Source: Approved w7 brainstorm item 1 via `$pm all for w7`; `.pm/w9/blocked/060.md`, `.pm/w10/003.md`, and current `scripts/mock-cluster.sh` / `scripts/dev-env.sh`.

## Steps

1. Read the relevant current implementation and applicable repository instructions.
2. Add or strengthen behavioral regressions for this milestone's success and failure cases. Prove the tests detect the pre-fix defect or deliberately broken implementation; run the relevant suites and required checks.
3. Record the behavioral evidence and update task status only when acceptance holds.

## Files

- `.pm/w7/m148/README.md`
- `scripts/dev-env.sh`
- `scripts/mock-cluster.sh`

## Acceptance criteria

- [ ] Meaningful success and failure coverage passes and catches the targeted failure.
- [ ] Relevant checks pass; any environmental limitation is recorded without claiming live acceptance.

## Out of scope

Production provisioning, new runner hosts, and unrelated workstream mutations.

## Done 2026-09-17

`scripts/mock-cluster.test.sh` — 16 checks over the lock lifecycle and every kubeconfig publication path (clusterctl failure, TLS-skip refusal, unreachable apiserver, happy path, and that credential material never reaches stdout/stderr). Wired into CI in `.github/workflows/scripts.yml` (shellcheck list + a run step); both scripts are shellcheck-clean at `--severity=warning`, which also cleared two pre-existing warnings.
