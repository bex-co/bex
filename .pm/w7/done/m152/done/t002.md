---
id: w7/m152/t002
title: "Validate requested datastore IDs before membership writes"
worker: worker7
status: done
estimate: 40m
depends_on: [w7/m152/t001]
---

## Objective

Prevalidate the full requested datastore set for project and environment membership. Reject unknown and foreign ids without disclosing whether another tenant owns them; retain explicit empty-list clear semantics.

## Context

- environments/service.go SetDatabases/SetKeyValues, setResourceMembers, clearMembersForProject and clearEnvironmentMembers; projects/service.go setResourceMembers and Delete.
- See [milestone scope](README.md).

## Steps

1. Inspect the relevant implementation, existing tests and repository instructions.
2. Prevalidate the full requested datastore set for project and environment membership. Reject unknown and foreign ids without disclosing whether another tenant owns them; retain explicit empty-list clear semantics.
3. Record evidence against the acceptance criteria.

## Files

- `lego/backend/internal/projects/service.go`
- `lego/backend/internal/environments/service.go`
- `lego/backend/internal/postgres/service.go`
- `lego/backend/internal/keyvalue/service.go`

## Acceptance criteria

- [x] Invalid replacement sets leave every existing membership unchanged.
- [x] Unknown and foreign ids have equivalent non-disclosing refusal behavior.
- [x] An authorized empty list still clears membership.

## Out of scope

No distributed transaction guarantee across Kubernetes and Postgres, new roles, weakened authorization, or lifecycle-cleanup duplication with m151.

## Completion — 2026-09-21

Project and environment datastore replacement now validates the entire requested set against the existing workspace inventory before writing. Both kinds reject unknown/foreign/empty ids and mixed sets identically; authorized empty clears pass. Removed obsolete ignoreUnknownIDs policy; real-CR tests preserve labels, own rules, inherited rules, and unrelated annotations.
