---
id: w4/m103/t005
title: Test coverage for the shipped behavior
worker: worker4
status: todo
estimate: 40m
depends_on: [w4/m103/t003]
---

## Objective

Close what t002 did not cover, asserting real behavior and failure modes.

## Context

t002 covers the seven taxonomy cases. What remains:

- **The contradiction itself as a test**: a single assertion that for any App, a terminal deploy status and the projected phase are never mutually contradictory (deploy failed + phase claiming progress). This is the invariant `w6/m52` and `w6/m124` both existed to protect and neither expressed as a standing test.
- **The dashboard header**: a test that a service with a failed latest deploy and the settled phase renders one coherent pair of badges.
- **Timing**: an envtest that the settle happens within one reconcile of the deadline, so the pair cannot disagree for minutes.

## Steps

1. Write the tests above.
2. Confirm each fails against the pre-fix behavior.

## Files

- `lego/operator/internal/controller/*_test.go`
- `dashboard/src/features/services/components/__tests__/`

## Acceptance criteria

- [ ] Each new test recorded here with the pre-fix behavior it fails against.
- [ ] The never-contradictory invariant exists as a standing test.
- [ ] Header coherence asserted in the dashboard suite.
- [ ] All suites green: `make test`, `cd lego/backend && go test ./...`, `make lint`, `dashboard/ yarn test`.

## Out of scope

- Surfaces this milestone did not change.
