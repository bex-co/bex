---
id: w4/m103/t006
title: Closeout
worker: worker4
status: todo
estimate: 15m
depends_on: [w4/m103/t005]
---

## Objective

Close the milestone once its definition of done holds against deployed production.

## Context

The live probe costs ~20 minutes of waiting, most of it unattended: create the crash-looping fixture, wait out the 900s rollout budget plus a margin, then read `phase` on REST, GraphQL and the dashboard header. Budget for it rather than deferring — `w4/m92`–`m96` all closed with the live re-probe deferred, and this milestone's whole subject is a state that only appears after a timeout.

Use a fixture named `qa-<yyyymmdd>-bad` (≤19 characters for the purge-Job limit per `w4/064`) and delete it when done.

## Steps

1. Re-run every DoD bullet against deployed production; paste the timeline and responses into this task.
2. Then: set every remaining task `status: done`, move each `tNNN.md` into `w4/m103/done/`, mark every row `— **DONE**`, set `**Status:** done`, move `w4/m103/` to `w4/done/m103/`, and check `- [x]` in `.pm/w4/README.md`.
3. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w4/m103/**`, `.pm/w4/README.md`

## Acceptance criteria

- [ ] Every DoD bullet re-probed against deployed production, with the full timeline pasted — no deferral.
- [ ] The fixture is deleted and its absence confirmed (detail 404, public URL 404, workspace counts at their pre-run values).
- [ ] Status synced in all three places; milestone moved to `w4/done/m103/`.
- [ ] Prettier run.

## Out of scope

- Anything not in the definition of done.
