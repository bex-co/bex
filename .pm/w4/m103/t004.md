---
id: w4/m103/t004
title: Simplify pass over this milestone's changes
worker: worker4
status: todo
estimate: 25m
depends_on: [w4/m103/t003]
---

## Objective

Run `/simplify` over the code this milestone changed — behavior-preserving only.

## Context

Likely targets:

- The phase-settling paths after t001: `fail`, `settleFailedBuildOverPriorRelease`, and whatever t001 adds are three places that decide "what phase truthfully describes this App now". If they can become one decision function over (has-prior-release, parked, failure-kind), that is the pass's main prize.
- `deployment_projection.go`'s three-timer comment block (`:105-127`) is excellent documentation; if t001 adds a fourth observer, that comment must absorb it rather than being duplicated nearby.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply behavior-preserving findings; file design-laden ones as a w4 inbox note.

## Files

- Whatever the milestone touched.

## Acceptance criteria

- [ ] `/simplify` run and each finding dispositioned, recorded here.
- [ ] `make test`, `make lint`, `cd lego/backend && go test ./...` green after the pass.
- [ ] t001/t002's assertions still hold unchanged.

## Out of scope

- New fixes surfaced by the pass — file them.
