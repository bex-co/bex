---
id: w4/m113/t005
title: Render parity + docs
worker: worker4
status: todo
estimate: 20m
depends_on: [t003, t004]
---

## Objective

Record what changed (and what deliberately didn't) in the parity ledger and the metrics docs, so the next hunt reads the ledger instead of rediscovering the gap.

## Context

ADR018 row 84 already promises static request attribution ("requests stay — the static site's Ingress routers attribute Traefik series per App") — the code never delivered it. This task closes the ledger-to-code gap from the docs side once t001–t004 land, or records a partial state honestly if any leg can't ship.

## Fix shape

- ADR018 static-site row (row 84): note the fix (router-attributed requests/latency/filters; request-line attribution) with the milestone ref, keeping the accepted-divergence notes (SPA fallback, rooted destinations) untouched.
- `docs/render-artifacts/metrics-page.md`: add the static case (which series exist per type, the router-vs-service selector split) if the doc enumerates sources.
- ADR010 § Log filters if t004 changes the request-log pipeline's documented attribution (shipper regex / new producer).
- No new divergence rows unless a leg genuinely can't match compute (e.g. INSTANCE stays empty — already covered by t003's code comment; only ledger it if the ledger enumerates filter fields).

## Acceptance

- A reader starting from ADR018 row 84 can find the static request-observability behavior and its evidence without opening code.

## Files

- `docs/ADR018-render-parity.md` (row 84)
- `docs/render-artifacts/metrics-page.md`, `docs/ADR010-observability.md` (as needed)

## Acceptance criteria

- [ ] Ledger + metrics/log docs describe the shipped static behavior
- [ ] `npx prettier@3.4.2 --write "**/*.md"` applied to touched docs

## Out of scope

- Code changes (t001–t004); live verification (t007)
