---
id: w4/m141/t004
title: Render parity across REST / GraphQL / MCP / UI
worker: worker4
status: todo
estimate: 20m
depends_on: [w4/m141/t001, w4/m141/t002, w4/m141/t003]
---

## Objective

Confirm that REST `POST /v1/services/{id}/rollback`, GraphQL `rollbackService`, MCP's rollback tool, and `deployActions` agree for static sites (eligible or named refusal). Compare with render.com's static-site rollback and file drift.
