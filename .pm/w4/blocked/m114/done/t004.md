---
id: w4/m114/t004
title: Render parity + docs (cron-runs.md single-execution guarantee)
worker: worker4
status: done
estimate: 20m
depends_on: [t001, t002, t003]
---

## Objective

Make `docs/render-artifacts/cron-runs.md` (and the ADR018 cron row, if it enumerates run semantics) describe the shipped behavior: what trigger-during-active does, what cancel-then-cancel-then-resume does, and the convergence budget for terminal states.

## Context

The w6/039-era doc promises single execution via the suspend-mitigation; this milestone found two holes around it (resurrection via overwritten CancelRun, twin-pending via late tick) plus a UI/API split. The doc must match the fixed code, not the aspiration.

## Fix shape

- `docs/render-artifacts/cron-runs.md`: trigger-during-active behavior (per t003's chosen direction), manual-run lifecycle vs `runAt`/`CancelRun` (per t001's fix), terminal-state convergence budget (per t002's evidence: polls, not minutes).
- ADR018 cron row: touch only if it enumerates these semantics; no new divergence rows for behavior that now matches.
- Keep the m96 log-boundary note intact (re-probed live and holding — see milestone README).

## Acceptance

- A reader can predict the t001/t002/t003 behaviors from the doc alone.

## Files

- `docs/render-artifacts/cron-runs.md`, `docs/ADR018-render-parity.md` (as needed)

## Acceptance criteria

- [x] Docs describe shipped trigger/cancel/convergence behavior
- [x] `npx prettier@3.4.2 --write "**/*.md"` applied to touched docs

## Out of scope

- Code changes (t001–t003); live verification (t006)

## Result, 2026-09-18 — partial, scoped to what shipped

`docs/render-artifacts/cron-runs.md` updated for the two defects that were
fixed; the twin-pending/terminal-delay contract is deliberately NOT restated,
because t002 is blocked and writing a guarantee the code does not yet make
would be worse than the current silence.

- **Single-execution section** gains "A canceled run stays canceled
  (w4/m114 t001)": why the single-slot `spec.cancelRun` guard was disarmed by
  the next unrelated cancel, that the guard now reads terminal history from
  `status.runs`, and that a fresh trigger still runs (new `runAt`, new Job
  name).
- **Trigger Run row (73)** rewritten. The row had claimed bex "rejects while
  one is active" and called that a deliberate divergence from Render's
  cancel-then-replace — **that was never the shipped behavior**:
  `TriggerCronRun` has always written `spec.cancelRun` + `spec.runAt` together
  (`apps/service.go:3019-3024`), i.e. Render's own cancel-then-replace, and
  only the dashboard's disabled button enforced the documented rejection. The
  row now describes preemption on every surface, cites the live 2026-09-17
  contradiction, and quotes the new confirm copy.
- No ADR018 change: the parity position did not move (bex matches Render on
  cancel-then-replace, and now says so).
- `npx prettier@3.4.2 --write` applied.
