---
id: w4/m114/t006
title: Closeout (live re-probe with a failing cron)
worker: worker4
status: todo
estimate: 15m
depends_on: [t005]
---

## Objective

Prove the milestone's Definition of Done on production with a throwaway failing cron, then leave zero residue.

## Context

Filing pass drove schedule `* * * * *` + `echo <marker>; exit 3` on `srv-dam3habs0ils73bgp0v0` (deleted). Re-run that shape post-deploy.

## Fix shape

- Throwaway cron (repo `bex-co/bex`, rootDir `examples/cron-demo`, runtime docker, `* * * * *`, command `echo <fresh-marker>; exit 3`).
- Assert, in order: trigger during active behaves per t003 on BOTH surfaces; cancel manual → cancel scheduled → suspend → resume produces ZERO resurrected attempts (t001); at no instant do two runs show active together, and any failing run is terminal within ~2 polls of its last attempt (t002); m96's 3-record live/history convergence still holds on a `seq`-command run (regression control from the filing pass).
- Delete the fixture (service + project/env) and re-run the residue check (services/projects GraphQL must show no QA ids; no platform URL to check — crons have none).
- Sync board state per `.pm/CLAUDE.md` and move the milestone to `done/` only when every DoD bullet is live-proven.

## Acceptance

- Every Definition-of-Done bullet reproduced live post-fix, with run ids/timestamps quoted in the closeout note.
- Zero QA residue; tree clean except the board sync.

## Files

- `.pm/w4/m114/README.md`, `.pm/w4/README.md` (status sync)

## Acceptance criteria

- [ ] DoD proven live; fixture deleted; board synced

## Out of scope

- Any further behavior change — new findings file as new milestones
