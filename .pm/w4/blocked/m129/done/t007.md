---
id: w4/m129/t007
title: Simplify — /simplify over the code this milestone changed
worker: worker4
status: done
estimate: 25m
depends_on: [w4/m129/t006]
---

## Objective

Run `/simplify` over the code this milestone touched, behavior-preserving, looking for reuse and duplication.

## Context

- The two queries (`serviceEventsQuery`, `webhookEventsQuery`) are deliberately near-twins with documented differences. If this milestone's change is duplicated verbatim across both, that is the first candidate for a shared fragment — but the comment at `webhooks.go:708-712` explains why they are separate, so read it before merging anything.
- The effect recorder added in t004 may duplicate `RecordPlanChanged`/`RecordAutoDeployChanged`.

## Steps

1. Run `/simplify` over the changed files.
2. Apply only behavior-preserving changes; anything that alters a row's content belongs in its own task.

## Files

- `lego/backend/internal/store/events.go`, `webhooks.go`
- `lego/backend/internal/apps/service.go`, `lego/backend/internal/events/service.go`

## Acceptance criteria

- [x] `/simplify` run and its findings either applied or recorded with a reason for declining.
- [x] No behavior change, proven by the t008 suite passing unchanged.

## Out of scope

- Merging the two queries into one.

## Completion evidence (2026-09-21)

Three simplify reviewers checked reuse, quality, and efficiency. Applied shared patch-test helper, shorter contract comments, and primary-key lookup for typed audit targets; lint and final Postgres regressions pass.
