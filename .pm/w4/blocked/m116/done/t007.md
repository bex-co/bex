---
id: w4/m116/t007
title: Render parity across the touched surfaces
worker: worker4
status: done
estimate: 45m
depends_on: [w4/m116/t001, w4/m116/t002, w4/m116/t003, w4/m116/t004, w4/m116/t005, w4/m116/t006]
---

## Objective

Every surface this milestone changes stays consistent across REST, GraphQL, MCP, the CLI, and the dashboard, and matches render.com behavior except where bex deliberately diverges (recorded, not silent).

## Context

- Touched surfaces: Postgres connection strings + launcher psql/pgcli (t001), KeyValue PATCH/PUT/GraphQL/MCP + dashboard hint (t002), jobs REST + RBAC (t003), Postgres status/data-plane agreement (t004), dashboard login/device/consent + e2e helper (t005), list envelopes (t006).
- Contract docs: `docs/ADR006-bex-api.md`, `docs/ADR018-render-parity.md`, `docs/ADR021-keyvalue-management.md`, `docs/ADR009-postgresql-management.md`, `docs/cli-compatibility-checklist.md`.

## Steps

1. For each shipped behavior change, verify the same fields/semantics/error shapes on REST, GraphQL, and MCP (same inputs, same outcomes; error IDs identical).
2. Verify the dashboard renders the new states (KV publish control/hint, job reason, resume honesty, single-login flow) without contradicting the API.
3. Compare each against the equivalent render.com behavior/API; record deliberate divergences in ADR018/ADR021/ADR009 (with reason) and file anything unintentional as follow-up — never silently diverge.
4. Re-grade every touched row in `docs/cli-compatibility-checklist.md` with fresh live evidence.

## Files

- The ADRs and checklist above; whichever surface files t001–t006 changed.

## Acceptance criteria

- [ ] A parity table exists (in this task's summary or the milestone README) mapping each behavior to REST ≡ GraphQL ≡ MCP ≡ CLI ≡ dashboard, with Render's behavior noted.
- [ ] No new divergence is undocumented; no checklist row touched by this milestone still cites stale evidence.

## Out of scope

- New features beyond the six findings.
