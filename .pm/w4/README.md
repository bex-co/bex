# w4 — project workstream (worker4)

**Worker:** worker4. This is a general-purpose bex workstream. It may take work anywhere in the project; the milestones below are scheduled work and historical records, not a permanent purpose, specialty, or ownership boundary.

## Local dev environment

Develop against `.pm/w4/dev-4/`, this worker's own isolated stack on the shared local kind/CAPD cluster — never the shared cluster's default `auth`/`bex-system` namespaces or standard ports (5173/4433/4445/8090/8091/5432), which any other worker's session may also be using. `dev-4` gets its own Kratos + Hydra + Mailpit (namespace `dev-4-auth`) and app namespace (`dev-4`), reusing the shared cluster's CNPG operator and bex operator, plus a locally-built `bex-api` on dedicated ports derived from N=4 (`dev-4/ports.env`) so it never collides with any other workstream's `dev-N`.

- `bash scripts/dev-env.sh 4 up` — bring it up (idempotent — safe to re-run)
- `bash scripts/dev-env.sh 4 status` — health check (processes, pods, HTTP) + a pass/fail verification inventory
- `bash scripts/dev-env.sh 4 down` — tear it down (leaves the shared cluster and every other workstream's `dev-N` untouched)
- `bash scripts/dev-env.sh 4 clean` — reclaim `logs/` and `bin/` (refuses while the environment is up)

`up` prints the dashboard command to point at it once bex-api is running. One shared implementation serves every workstream since `w1/m72`; `.pm/w4/dev-4/` keeps only `ports.env` (a generated record of the derivation), this README, and `.gitignore`.

## Milestones

- [ ] **m138** — [Blueprint detail: a sync never appears in Sync History, and the plan hides create vs update vs no-change](m138/README.md) (7 tasks, ~2h 35m) ← live `/qa-find-bugs` 2026-09-25 pass 161 (w4-targeted, `muse.env` credentials), journey 13. `useBlueprintSyncs` neither polls nor is refetched by the sync mutation, so a recorded successful sync stayed invisible for a 60s watch. `BlueprintPlanSummary` discards the `operation`/`changedFields` that `w6/m125` made correct, and says "1 resource to sync" for a backend `noop` that changed nothing.
- [ ] **m137** — [A restarting Key Value or Postgres reports "creating", as if it were brand new](m137/README.md) (7 tasks, ~2h 45m) ← promoted from w4/141 (pass 159, Key Value) and live `/qa-find-bugs` 2026-09-25 pass 160 (Postgres manual restart, journey 11). `kvStatus`/`dbStatus` default every non-Ready phase to `creating`, where Render's `databaseStatus` has `config_restart`. Key Value also reads `available` for ~15s after its restart begins.
- [ ] **m136** — [Datastore and deploy log viewers stop at the newest 100 lines with no warning](m136/README.md) (7 tasks, ~3h 10m) ← live `/qa-find-bugs` 2026-09-25 pass 159 (w4-targeted, `muse.env` credentials), journeys 12 + 7. `w4/m107` gave only the service Logs tab paging and a truncation notice. The Key Value and Postgres Logs tabs still issue one `limit:100` query and ignore `hasMore`. On `beancount-forum-db`, "Last 24 hours" reaches only 01:05Z–05:00Z (API `hasMore:true`), with no notice and no load-older control, and a Key Value's creation logs become unreachable once the hour holds >100 lines. Deploy logs are reasoned to share the gap (t002 verifies).
- [x] **m133** — [Report resources detached by blueprint sync](done/m133/README.md) (6 tasks) ← promoted from w4/119; m125 fixed claim release, detach notices remain.
- [x] **m132** — [Make unapplied Postgres overrides visible](done/m132/README.md) (6 tasks) ← promoted from w4/115; saved configuration gains runtime diagnostics across API and dashboard.
- [x] **m131** — [Service placement reads reflect committed moves](done/m131/README.md) (7 tasks) ← promoted from w4/109. Managed Get/List reads committed placement in one bounded query; three-surface, authorization and real-Postgres regressions pass. REST omission remains compatible with the pinned Render schema.

- [ ] **m130** — [Service environment CAS outcomes and returned revisions](blocked/m130/README.md) — **BLOCKED (release pipeline deployment, then production QA must identify the original failure and pass the deployed save/stale/concurrency probes)**. t002–t007 done: post-write compensation has a distinct error, successful CAS returns a revision on all three APIs, real OpenBao and adapter regressions added. t001 production attribution and t008 live closeout remain open.
- [ ] **m129** — **BLOCKED (release pipeline must deploy the fix; production QA must replay deletion/name reuse, idle-timeout refusals/no-ops, by-id reads, and webhook deliveries)** — [A re-created service inherits the deleted service's Activity feed](blocked/m129/README.md) (9 tasks, ~5h 5m) ← live `/qa-find-bugs` 2026-09-21 pass 156 (w4-targeted, `muse.env` credentials), journey 15 (free-tier sleep → wake), which this loop had never swept. Audit-derived service events are joined to a service by **name** while deploy-derived ones are joined by `app_id` (`store/events.go:464-467` states the split in its own doc comment), so deleting a service and creating a new one with the same name inherits the dead one's history: the re-created `qa-20260921-sleep` got a different `srv-` id and still reported **seven** `idle_timeout_changed` events older than its own `createdAt`, the oldest from a service of that name 4h45m earlier — while the control, a never-used name, reported zero. Only the audit-derived events crossed over; `deploy_*`/`service_hibernated`/`service_woken` correctly did not, and that asymmetry is both the proof and the fix's specification. `webhookEventsQuery` shares the join (`store/webhooks.go:708-720`) across three name spellings, so the wrong attribution is already being **delivered** to subscribers, not merely displayed. Carries two smaller defects found with it: a refused `setIdleTimeout` mints an `idle_timeout_changed` event (one refused call took the feed 6 → 7, value unchanged) because `w4/m122/t004`'s classification asserts `apps.SetIdleTTL` has "no validation, type gate or no-op path at all" when it has both — a misapplication of m122's own correctly-written doctrine, which names "an out-of-range value" as routine; and the inherited row is the only one of eleven that `serviceEvent(id:)` answers `EVENT_NOT_FOUND` for, while the API's default time window hides it and the dashboard's wider query shows it — **t001–t008 done 2026-09-21**; immutable event ownership, deferred success audits, regression/mutation checks, full Postgres + OpenFGA backend suite, lint, and simplify complete; **t009 blocked** on the live replay.
- [x] **m128** — [Deploy-notification preferences are stored and applied per workspace but can only be read and written for one of them](done/m128/README.md) (8 tasks, ~4h 25m) ← live `/qa-find-bugs` 2026-09-21 pass 145 (w4-targeted run, `muse.env` credentials), read-only, on an account in three workspaces. Four parts of the product disagree about the same data: the schema is `UNIQUE (tenant_id, subject)` and its own comment says "per-workspace" (`0013_notification_settings`); the mail fan-out `ListNotifyRecipients(tenantID)` joins `notification_settings` on `(tenant_id, subject)` (`store/notifications.go:122-130`), so **workspace B's row decides whether you are emailed about a deploy in B**; the API takes no workspace at all (`notifications/service.go:249-296` keys on `Base.Tenant(ctx)`, which falls back to the caller's default workspace) — verified live: GraphQL `notificationSettings` has no `ownerId` argument, `GET /v1/notification-settings/owners/{tea-id}` is **404** on all three workspace ids while the unscoped route is 200, and the sibling `…/overrides/services/{srv-id}` **is** at Render's exact path; and MCP lists both notification tools in `mcpCallerScopedTools` (`api/mcp_workspace.go:38-47`) as operating "on the caller rather than on a workspace". Net effect: a member can read/write exactly one of their rows, the rest are unreachable yet still govern real mail — turning deploy-failure email off does not stop it from other workspaces. Invisible to single-workspace accounts, and the audit that last blessed ADR018's ✅ Notifications row (`w5/m59`) was explicitly code-based with live Render captures blocked — done 2026-09-21 (**per workspace**, keeping the schema and the mail fan-out and changing the two halves that disagreed with them: the API and MCP. Per-user was rejected because it discards data — it needs a migration collapsing `(tenant_id, subject)` and a rule for members holding different values in different workspaces, which is a preference change made on the user's behalf. `ownerId` is now optional on REST `?ownerId=`, GraphQL `notificationSettings(ownerId:)`/`updateNotificationSettings(ownerId:)`, and MCP — both notification tools left `mcpCallerScopedTools` and take the shared `workspaceId` like every other resource tool; omitting it keeps the previous default-workspace behavior. The dashboard panel reads and writes the workspace it is showing, with the mutation's cache write keyed by the same `ownerId`. **Render's `/v1/notification-settings/owners/{ownerId}` is deliberately NOT implemented, and that is the parity finding:** the pinned schema shows the operation there returns the OWNER-level object (`{ownerId, slackEnabled, emailEnabled, previewNotificationsEnabled, notificationsToSend}`), a workspace-wide policy rather than one member's preferences, with Slack a recorded non-goal — mounting a member-preferences body there was tried and reverted because the route-intersection inventory then claimed two Render operations bex does not implement. ADR018's Notifications row and ADR024 say so. **Recorded gap, not fixed here:** the native-push half writes the same `(tenant_id, subject)` row and still keys on the default workspace only — t001 scoped it out. Backend `go test ./...`, dashboard `yarn test` (433 files / 3578 tests), `typecheck` and `lint` green. Live re-probe deferred to the next QA pass — no production access this session)
- [x] **m127** — [A protected environment refuses to pause a database and allows `DROP TABLE` inside it](done/m127/README.md) (7 tasks, ~4h 5m) ← live `/qa-find-bugs` 2026-09-21 pass 142 (w4-targeted run, `muse.env` credentials); a free-plan Postgres and Key Value created directly into a fresh `protectedStatus: protected` environment refused `suspendDatabase`/`suspendKeyValue`/`deleteDatabase`/`deleteKeyValue` with their `sudo …` phrases, and in the same session **accepted** `executeDatabaseQuery(allowWrites:true)` for `CREATE TABLE`, `INSERT` **and `DROP TABLE`**, plus `setKeyValuePersistenceMode("off")`, `setKeyValueMaxmemoryPolicy("allkeys_lru")`, `failoverDatabase` and `renameDatabase` — two of which lose data rather than availability, on an instance whose suspend (which loses nothing) is refused. `requireUnprotected` has exactly two call sites per package (`postgres/protection.go:32` → delete+suspend; `keyvalue/protection.go:32` → delete+suspend) and none of the accepted verbs carries a `confirm` argument, so the guard is unreachable rather than merely absent. Raises a question `w6/m19`/`w6/m37` never had to answer — whether protection covers the **data plane** at all — and the note is fair that the SQL console is not unguarded but guarded by something else: `can_create` **and** `can_view_sensitive`, re-asserted uncached at the sink (`postgres/query.go:124-180`, codex rounds 9/18), with the only write confirmation being dashboard-side and absent for REST/GraphQL/MCP callers. Sibling of **m126** (same shape for service verbs); t002 reuses m126/t001's rule rather than inventing a second definition of "protected". All fixtures removed via the guard with its phrases — done 2026-09-21 (**protection covers the data plane**, decided by the check the task named: it does NOT reach the connection string, and cannot, so option two's "an API confirmation is theatre against anyone determined" is correct on its own terms — and answers the wrong question, since the dominant failure is a mis-aimed console tab. ADR032 therefore records the guard AND that the data-plane confirmation is an accident guard, not a security boundary, rather than implying a containment the mechanism cannot deliver. The guarded set goes from two verbs to eight: delete, suspend, writable SQL, failover, rename, major-version upgrade, durability, eviction — read-only queries are never gated and resume stays exempt. Two verbs beyond the DoD are included with reasons: a version upgrade takes the DB offline irreversibly, and a Key Value rename is the identity twin of the Postgres one. ADR032 states the rule that decides membership so the next field needs no live matrix. Guards sit in the service verbs so all three surfaces are covered by construction; `confirm` is optional everywhere and no verb gained a required argument. **Gap filed rather than half-built:** the dashboard SQL console does not yet send the phrase, so a protected database will refuse its writable queries until it surfaces the refusal the way the delete/suspend dialogs do)
- [x] **m126** — [A protected environment blocks suspend and delete but lets anyone swap the image a service runs, with no confirmation and no way to give one](done/m126/README.md) (8 tasks, ~4h 20m) ← live `/qa-find-bugs` 2026-09-21 pass 141 (w4-targeted run, `muse.env` credentials), journey 1; a `qa-` service in a freshly created `protectedStatus: protected` environment refused `suspendService` with `retry with confirm="sudo suspend service qa-20260921-pe"` and in the same session **accepted** `setImage(image:"…:35")` — the service's stored image moved from `:latest` to `:35` with no confirmation asked; `setEnvVar`, `setEnvVars`, `scaleService` and `setIdleTimeout` were accepted too. **Corrected pass 147:** `setImage` stages the image and mints no deploy of its own (the rollout in that pass belonged to the `restartServer` issued alongside it) — the staged executable applies on the next deploy, which makes the gap quieter rather than smaller. `grep requireUnprotected` finds three call sites (`service.go:2301` delete, `service.go:4098` suspend, `deploy.go:2716` stack-apply) and the ungated verbs have **no `confirm` argument at all**, so the guard is unreachable rather than merely absent. The code already calls the verb create-like (`service.go:3562-3564`, gated on `can_create` because "repointing a service at a new repo or image chooses the executable the operator runs with the service identity"), and ADR032's own rationale for guarding Suspend — "blocks taking availability away" — plus its stack-apply guard for redefining a service both point at this verb. Same extension shape as `w6/m37`, which widened `w6/m19`'s guard from services to datastores; delete-with-phrase was verified as the control and all fixtures removed — done 2026-09-21 (**rule 1 chosen**: a protected environment guards what can lose data, change what the resource IS, or take it offline — the same rule m127 applied to datastores, and the one the pass-147 correction demands, since the rejected "guard every verb that mints a release" rule would have missed `setImage` entirely while gating a health-check-path edit. Three new confirmation verbs join delete/suspend/direct-deploy: `repoint` (image, repo, branch, registry credential), `redefine` (build/start/pre-deploy/cron commands, Dockerfile path, root dir) and `take offline` (enabling maintenance mode — disabling it is exempt for the same reason `Resume` is). One phrase per class, not per setter. Guards sit in the service verbs so REST, GraphQL and MCP are covered by construction; `confirm` is optional everywhere — GraphQL mutations, REST `PATCH /v1/services/{id}?confirm=`, MCP `update_service.confirm` — and the guard fails **closed** on a protection-lookup outage. Every exclusion is written down rather than left silent: scale (the minimum is 1 replica, so it cannot take the service offline), plan, idle timeout, health-check path, port, allowlist, subdomain policy, auto-deploy, build filter, display name, notifications — and, the honest borderline, **environment variables and secret files**, deliberately out because they are configuration rather than identity and gating the most-edited control in the product trains people to type the phrase without reading it. Dashboard: one `ProtectedRetryProvider` above the routes that every single-field Settings save reaches through `useFieldMutation`, plus the four own-bodied hooks; the phrase is still only ever parsed out of the server's error, never precomputed. Backend `go test ./...`, dashboard `yarn test` (432 files / 3571 tests), `typecheck` and `lint` green. Live re-probe of the deployed fix deferred to the next QA pass — no production access this session, and maintenance mode additionally needs a paid fixture)
- [x] **m125** — [Creating a second Blueprint for a repo+branch silently destroys the first one, and the resources it leaves behind stay claimed but invisible](done/m125/README.md) (7 tasks, ~4h) ← live `/qa-find-bugs` 2026-09-21 pass 133 (w4-targeted run, `muse.env` credentials), journey 13; two `createBlueprint` calls for `bex-co/bex`@`main` with different manifest paths returned the **same** id and left one row named after the second, because `AdmitBlueprintCreate` is an `INSERT … ON CONFLICT (tenant_id, repo, branch) DO UPDATE SET name/path/auto_sync/manifest` (`store/blueprint_lifecycle.go:186-201`) and `CreateBlueprint` (`apps/blueprint.go:689`) never checks whether the row exists — the first blueprint is destroyed and reported as a successful create, and its `static-site` is left Running, dropped from `resources[]`, and still holding the destroyed blueprint's ownership claim (the dashboard's own preview says `managed by blueprint blp-dan9…` about a blueprint whose `resources[]` disclaims it). Claims are released in exactly one place — `DisconnectBlueprint` (`apps/blueprint.go:1226`) — so every `w4/119` orphan is a _locked_ orphan. This is the row-level half of the guarantee [`w8/m23`](../w8/done/m23/README.md) shipped for resources, and the one ADR018 already advertises as bex's deliberate improvement over Render's silent clobber — done 2026-09-21 (`BLUEPRINT_CONNECTION_CONFLICT` refuses a create that would repoint a live blueprint, with the same 409 shape and `takeover blueprint <id>` phrase the resource guard uses, on REST/GraphQL/MCP and in `blueprintPreview`; the message names `updateBlueprint`, which is what a second connect usually wants. **Path, not name, decides what counts as a replacement** — re-connecting the same path proceeds, because that is also the retry path after a create whose apply failed, and one `confirm` cannot carry both a resource phrase and a connection phrase; refusing it would deadlock. A confirmed replacement is recorded on the sync run's new `note` column (migration 0131), since the row is reused and the replaced blueprint otherwise leaves no trace anywhere. `blueprintPreview` gained an optional `blueprintId` so the detail page's pre-sync preview does not conflict with itself, and `/blueprints/new` keeps Deploy reachable when every problem is takeover-able — a disabled button would have meant reading about a takeover you can never perform. t002: a sync now releases the claims for resources its manifest stopped declaring, inside the same fenced (generation, runID) window as the ownership stamps, clearing the CR marker with them and nothing else — the orphan keeps running, per `w4/119`. t003: `blueprintId` is readable on every service, database and Key Value read, on lists as well as single reads, projected from the label rather than queried per resource; a dashboard cell was deliberately deferred. Backend `go test ./...`, dashboard `yarn test` (433 files / 3578 tests), `typecheck` and `lint` green. Live re-probe deferred to the next QA pass — no production access this session)
- [x] **m124** — [A blueprint bex exported from a service never re-plans as a no-op, because `domains:` round-trips into the wrong spec field](done/m124/README.md) (8 tasks, ~4h 30m) ← live `/qa-find-bugs` 2026-09-21 pass 110 (w4-targeted run, read-only `validateBlueprint` probes), journey 13; Generate Blueprint's own output, re-planned against the resources it was generated from, reports `update` for the web service and the Key Value while the Postgres correctly reports `noop` — removing the single line `domains: [blockeden.xyz]` flips it to `noop`, because the exporter flattens `Spec.Host`+`Hosts` into one list (`blueprint_generate.go:469`), `CreateRequest` has no `Host` (`service.go:1687`), and the applier then writes `dst.Host = ""` (`blueprint_plan.go:153-161`), moving the domain between fields on every re-plan. Separately `changedFields` is not a diff at all — it is the manifest's declared field set (`blueprint_state_plan.go:188,206,217,225`), so two manifests differing in two fields return byte-identical plans. Not a regression of `w6/m125` and not deploy lag: 0 commits have touched the plan files since it landed 2026-09-03 — done 2026-09-21 (**three** round-trip defects, not two, all the same shape — the exporter and the planner disagreeing about how one value is represented. `domains` is one flat list while the spec splits it across `Host`/`Hosts` and the create path has no `Host` field; a declared `ipAllowList: []` clones to an empty non-nil slice while the live CR reads back nil, which is the Key Value cause the filing could only list as a candidate — traced, not guessed, and generalized into one `canonicalSlice` helper across every list applier; and a cron's command lives in `Spec.Command`, which the exporter knew and the applier did not — **found by this milestone's own guard on its first run**, predicted by nothing in the filing. `changedFields` is now a real diff, computed by applying each declared field alone so it comes from the same appliers a sync runs and cannot disagree with them; env groups keep their deliberate always-`update`. A generate→validate guard covers every exportable kind, with both host shapes, so the two sides cannot drift apart field by field again. The DoD's open question is answered: a spurious `update` is noisy, not destructive. Live re-probe deferred; the dashboard plan panel still needs opening — `w6/m125` recorded it renders neither field)

- [x] **m123** — [Every tenant container gets 174 Kubernetes service-link env vars nobody asked for, naming every sibling resource in the workspace](done/m123/README.md) (8 tasks, ~4h 15m) ← live `/qa-find-bugs` 2026-09-21 pass 109 (w4-targeted run, browser-driven, `muse.env` credentials), journey 4; a container's own environment read over HTTP held **185** variables of which **5** were the user's or bex's — the other 174 are kubelet's legacy Docker links, naming all 25 sibling prefixes in the workspace including managed Postgres primaries by IP and cert-manager's ACME solver, because `EnableServiceLinks` appears nowhere in the codebase (`grep -rl` → 0) and the API server defaults it to `true`. Contradicts `appEnv`'s own "the one env invariant the CRD contract promises"; the fix belongs in the Deployment projection, **not** `applyPodSpecServerDefaults`, and rolls every tenant pod once — done 2026-09-21 (`enableServiceLinks: false` on all **11** pod-producing projections — app, build, pre-deploy, cron, backups, exports, publish. Platform pods are disabled too, and the stated reason is that an exhaustive grep proves nothing in bex reads a service-link variable, so it is pure surface removal with no dependency to break; a source-level guard with an anti-vacuity floor fails if a new PodSpec site forgets. **One DoD bullet is corrected:** `KUBERNETES_SERVICE_HOST`/`KUBERNETES_SERVICE_PORT` do NOT disappear — the kubelet adds the master Service's pair regardless of the setting — so the delivered set is the user's variables + `PORT` + those two, and ADR004 says exactly that. The cert-manager ACME solver var does go, because that Service lives in the tenant's own namespace. Writing the field moves the pod template, so every tenant pod rolls once on deploy — recorded rather than announced, as every other pod-template change here has been. Live re-probe deferred; expect 7 vars, not 5)

- [x] **m122** — [The events feed reports failed operations as accomplished facts: five "Custom domain verified" rows for a domain that never verified](done/m122/README.md) (8 tasks, ~4h 45m) ← live `/qa-find-bugs` 2026-09-21 pass 107 (w4-targeted run, browser-driven, `muse.env` credentials), journey 5; every Re-check on a pending domain answers `409 DOMAIN_OWNERSHIP_PENDING` and still writes a `custom_domain_verified` event, because `core.Base.emit` (`audit.go:918`) records at **authorize** time with `Outcome` meaning allowed/denied, and `eventTypes` (`events/service.go:341`) projects that intent verb onto a past-tense fact — proven twice: a refused `addCustomDomain` for another service's reserved platform host also minted `custom_domain_added`. Same mechanism `.pm/w7/done/050.md:259` recorded but did not file; same doctrine `w4/m118` set for cron verbs, applied to a case it did not reach. No domain facts exist in `allFactTypes`, so excusing the verbs must add them in the same change — done 2026-09-21 (the mechanism was already in the repo: `core.WithDeferredAllowedWriteAudit` records a denial but nothing for an allowed write until the verb confirms its mutation, which `SetPlan`/`Scale`/maintenance-mode already used — so no new audit column, no migration, and no domain fact types. The audit reframed the milestone: this was never a domain bug. bex authorizes BEFORE it validates, so a type gate, range check, quota or no-op sits after the audit row on nearly every verb — **24 gated** (3 domain, 11 `apps.Set…`, suspend/resume, source, 5 environment, 3 env-group link, 2 job), **6 deliberately left alone** (they validate before authorizing, or have no failure path at all), and `apps.SetSource` has no implementation — a retired name kept so old rows still project. **Secondary defect fixed in the same change:** the four disk verbs had deferred their audit since the feature landed with no recorder ever written, so `disk_created`/`disk_updated`/`disk_deleted`/`disk_restored` could never appear in ANY feed — the mirror image of the same bug. `apps.Restart`'s mapping turned out correct-but-mode-specific and is documented rather than changed. Scope stated: the failed/refused half is closed everywhere, the no-op half wherever the verb already knows what changed. Live re-probe deferred to the next QA pass)

- [x] **m121** — [A service's port is create-only and unreachable from the dashboard, so bex's own `PORT` refusal names a control that does not exist](done/m121/README.md) (9 tasks, ~5h 10m) ← live `/qa-find-bugs` 2026-09-21 pass 106 (w4-targeted run, browser-driven, `muse.env` credentials); the dashboard create wizard never sends `port` and no dashboard surface renders a port control, while `port` is accepted at create by REST/GraphQL/MCP and by no update verb anywhere (`SetPort` does not exist among 23 `Set…` mutators; `PATCH /v1/services/{id}` answers `unknown field "port"`) and cannot be read back at all (`Cannot query field "port" on type "Service"`) — so `nginx:alpine` created through the wizard served `503` while the same workspace's API-created `whoami` at `port: 8080` served `200`, and `core.ReservedEnvKeySentence`'s "change the service port instead" points every surface at a setting that exists only at create. Follow-up to `w2/m95`, whose goal line promised exactly this "where" — done 2026-09-21 (`SetPort` joins the shared `servicePatchTable`, so REST `PATCH` + `serviceDetails.port`, GraphQL `setPort` and MCP `update_service(port:)` move together; `serviceDetails.port` and GraphQL `Service.port` publish the number beside the `internalAddress` suffix that used to be the only place it appeared. The dashboard gains a wizard field and a `#port` Settings card behind one `servesHttp` predicate that also gates the create payload, and the reserved-PORT refusal now links to it. **Tightened beyond the filing:** a sub-1024 port is refused at create as well as update — ADR022 drops `NET_BIND_SERVICE` from every tenant container, so accepting `:80` would hand back a service that reports created and then crash-loops, which is the same failure shape this milestone exists to remove. Blueprint deliberately keeps no `port` key: a manifest re-applies on every sync and would fight the control, recreating `w4/m120`'s two-writers problem. A port change is now a `port_changed` Activity event, and an operator test pins the container port, Service, Ingress, probe and release identity all moving from the one `EffectivePort()` derivation. Every DoD bullet is a live probe and none has been run — deferred to the next QA pass)

- [x] **m120** — [Blueprint manifest env is invisible to the env API and silently shadows dashboard writes](done/m120/README.md) (8 tasks, ~4h 35m) ← live `/qa-find-bugs` 2026-09-21 pass 105 (w4-targeted run, API-only, `muse.env` credentials); manifest `MESSAGE` reaches the process via `spec.Env` but `GET env-vars` reads `[]` and single-key GET 404s, because reads are store-only (`readAuthorizedEnvMap`) while the `w6/m45` carve-out keeps blueprint literals manifest-owned — and `SetEnvVar` has no `spec.Env` check, so a same-key write would 200 while k8s `env` keeps beating `envFrom` — done 2026-09-21 (env reads are now the union of the store and the App's manifest literals, each manifest entry flagged `managedBy: "blueprint"` on REST/GraphQL/MCP — and on the key list too, because the dashboard has to know a row is read-only before anyone reveals its value; the manifest wins a collision on read because the runtime does, so reporting the store's value would describe a process that does not exist. All four write verbs refuse a manifest-owned key with the stable `ENV_VAR_MANIFEST_MANAGED` conflict code naming the remedy, bulk writes all-or-nothing and renames refused on their source key. The Environment tab enforces it in four layers rather than by hiding buttons — row model, state guard, patch derivation, dotenv import — fails closed while the flag is loading, and surfaces the server's refusal verbatim. ADR013 and ADR018 record the union rule, the refusal code and the two deliberate consequences; no Render divergence, since Render's Blueprint-managed vars are read-only in its dashboard too. A simplify pass folded away the read wrapper lint found dead. Live re-probe deferred to the next QA pass)
- [x] **m119** — [`http_requests` lies about bursty traffic: 25 requests read back 27.8 or 6.67 by query alignment](done/m119/README.md) (6 tasks, ~4h 15m) ← live `/qa-find-bugs` 2026-09-20 pass 71 (w4-targeted run, API-only, `muse.env` credentials); 25 curls reconcile exactly with the access log (`nlogs: 25`) but the metrics API returns 27.8 or 6.67 depending on window alignment, because m108's `sum(increase(…))` fix extrapolates partial-window counters — follow-up to `w4/m108`, whose DoD bullet 1 is broken live — done 2026-09-21 (there is no extrapolation-free counter-increase in PromQL — `increase`/`rate`/`delta` all extrapolate by design — so the unfiltered `http_requests` read now goes through the same `count_over_time` primitive the host/path-filtered read already used, making the chart and the Logs tab reconcile by construction because they are the same records; a five-line routing change, since both backends were already drop-in compatible on labels, group-by and unit. `http_latency` stays on the histogram and bandwidth stays on the counter. An unwired or failing log store falls back to the counter rather than blanking a working chart — that approximation is exactly the pre-fix behavior, so the fallback is never worse than what it replaces, unlike the host/path branch where Prometheus cannot answer at all. The bandwidth sibling is settled as a documented, evidenced divergence: money never touches the chart helper (metering reads `Increase` over the whole billing window, where the same edge error is <0.01%), and bandwidth is a composed figure the HTTP-only access log cannot rebuild without silently dropping every WebSocket and direct-egress byte. Live re-probe of the deployed fix deferred to the next QA pass)
- [x] **m118** — [Scheduled cron runs never reach the events feed: a failed run is invisible in Activity](done/m118/README.md) (6 tasks, ~3h 5m) ← live `/qa-find-bugs` 2026-09-19 pass 26 (w4-targeted run, `muse.env` credentials); an every-minute cron's first run failed after 10m 54s of crash-loop backoff — Recent Runs showed Failed with duration, but Activity still showed only the 2 deploy events, because `allFactTypes` (`lego/backend/internal/events/service.go:351`) lists `job_run_ended` but neither cron type and `pushDown` has no fact case for them, so the fallback returns only the manual trigger/cancel audit verbs — while the reconciler records both facts, webhooks map them, push lists "Cron run failed", and the dashboard already renders both types — done 2026-09-21 (the fix is a vocabulary correction, not the union the task sketched: because `recordCronRunFacts` "does not special-case how a run was requested", a manual run already leaves BOTH a fact and an audit row, so mapping both would have shown every manual run twice. Two pieces of repo evidence settled it the other way — `eventTypes` already excuses `deploys.Trigger` on exactly this argument, and outbound webhooks had already pinned intent-verbs-are-not-events in a test — so the feed now matches its sibling surface: both cron fact types joined `allFactTypes` and the fact-only push-down, and the three intent verbs left `eventTypes` with the reason recorded in the doc comment, in `excusedVerbs` and in ADR038. No store or dashboard change was needed, as the filing predicted; the ended rows carry `succeeded|failed|canceled` for free. Live re-probe of the deployed fix deferred to the next QA pass)
- [x] **m117** — [Service ipAllowList denies allow-listed clients](done/m117/README.md) (5 tasks, ~3h 10m) ← live `/qa-find-bugs-cli` infinite loop, sweep 3, 2026-09-17; listed v4 `/32` + v6 `/128` both get Traefik `403` — enforcement matches no client — **closed 2026-09-21 as a duplicate of [`w1/m150`](../w1/blocked/m150/README.md)**, which this milestone's own Source section told us to merge into rather than fix twice. Same bug, already diagnosed to object level there: `cidrMiddlewareSpec` (`database_controller.go:563-569`) emits Traefik `ipAllowList.sourceRange` with no `ipStrategy`, and `infra/terraform/main.tf:190,208,226` keep `proxyprotocol = false`, so the match runs against the Hetzner load balancer's `10.10.0.7` instead of the client — every listed CIDR of either family is wrong by construction. `w1/m150` t001 (Traefik trusting PROXY protocol from the LB only) and t003 (blast radius) have shipped; what remains is the Terraform listener flip, which is parked because committing to `infra/` is `terraform apply -auto-approve` and flipping before Traefik expects the headers is a full HTTP(S) outage. This milestone's unique evidence — the CLI write path proven innocent, Traefik identified as the denier by its bare 9-byte `Forbidden`, and the **IPv6 `/128` half that rules v6 handling out as a separate cause** (w1/m150's repro was v4 only) — is merged into that milestone's README as an Independent corroboration section, so nothing is lost
- [ ] **m116** — **BLOCKED (needs four things from you: (1) the one-off-jobs scope decision — `.pm/DO_NOT_DO.md` line 22 excludes them while line 18's 2026-07-27 pillar-5 re-open never amended it, so grant-the-RBAC vs gate-the-endpoint is yours; (2) go-ahead for one throwaway suspend/resume reproduction on production — or a local `mock-cluster` + `dev-4` stack — since the post-resume dead data path needs pg-sni-proxy handshake verdicts and CNPG wake state, not inference; (3) a live device-login ceremony against production auth, the same disposable human identity `.pm/w9/blocked/062.md` is already waiting on; (4) a live QA re-probe once the shipped fixes deploy)** — [CLI hunt sweep 1: psql trust, KV publish, one-off jobs, resume path](blocked/m116/README.md) (10 tasks, ~8h 15m) ← live `/qa-find-bugs-cli` infinite loop, sweep 1, 2026-09-17 — **t001/t002/t006 + the closing tasks done 2026-09-20**: `bex psql`/`pgcli` now provision their own TLS trust from the w4/m95 `serverCaCertificate` in a new `lego/cli/internal/pgtrust` launcher interception (0600 temp CA, `PGSSLROOTCERT` for the child, never a downgrade, user choice always wins); a nonempty allow-list on **update** now publishes a Key Value — Render's enabling event, which bex honored at create only — with every entry point converged on one patch, clearing deliberately kept one-way, and explicit `public` controls added on both datastores; a job the platform cannot submit now returns the Kubernetes cause as 503 instead of a 201 carrying an already-`failed` record; and `null`-where-an-array-is-declared is fixed once at `core.WriteJSON` plus nine nested-field helpers, with a 41-route table test and a drift guard. **Premise correction:** the two `null` repros that opened t006 are **not** bex-api — `/v1/sandboxes` and `/v1/workflows` have returned `[]` all along; the `null` is the pinned upstream CLI's own nil-slice marshaling, identical against render.com, and DO_NOT_DO forbids forking it. **t003/t004/t005 blocked**; t005's helper half was already fixed by `6cf17cb12` (w9/062, same day)
- [x] **m115** — [Database Insights is blind: Active processes + Top queries render 34 `<insufficient privilege>` cells](done/m115/README.md) (6 tasks, ~3h 50m) ← live `/qa-find-bugs` 2026-09-19 (w4-targeted run, `muse.env` credentials); a live database with recent SQL-console activity shows every Query cell masked (9 process rows + 25 top-query rows, unchanged after Refresh) while Storage/Table scans/Parameters on the same page render fine, because `runInsight` dials the tenant DB with the unprivileged app-user URI so Postgres itself masks every other role's query text — fix is a `pg_monitor` provisioning grant plus a dashboard honest-degradation guard — done 2026-09-20 (the operator now always projects the database **owner** into CNPG `spec.managed.roles` as a `pg_monitor` member, declarative so it converges on existing databases at the next reconcile with no backfill and no `passwordSecret` to make it depend on a Secret the recovery bootstrap may not create; the read-only envelope is asserted, not assumed. `<insufficient privilege>` is folded into a typed `masked` flag with an empty `query` at the mapping site, so REST, GraphQL, MCP and the dashboard share one signal — the Insights panel renders a "Hidden" cell plus one per-section explanation, keeps every unmasked row and real statistic, and still catches the bare literal from an older bex-api; ADR009 + ADR018 record the grant; dashboard guard verified failing against the pre-fix panel by stash-run; live re-probe of the deployed fix deferred to the next QA pass)
- [ ] **m114** — **BLOCKED (needs your go-ahead to run one throwaway cron reproduction on production — an every-minute failing cron plus a manual trigger during an active run, watched ~15 min with `kubectl`, then delete; or a decision to stand up the full local `mock-cluster` + `dev-4` stack)** — [Cron runs can't be trusted: resurrected cancels, twin pendings, 11-minute terminal delay](blocked/m114/README.md) (6 tasks, ~4h) ← live `/qa-find-bugs` 2026-09-17 (w4-targeted run, `muse.env` credentials); failing every-minute cron: canceled manual run `run-2207ebf1` resurrected executing after resume (single-slot `CancelRun` overwritten by a later cancel drops the no-recreate guard while `runAt` persists — mechanism nailed in code), scheduled runs `29827872`+`29827874` pending together 6+ min with the elder failing 11 min after its last attempt (successor creation mid-flight contradicts ForbidConcurrent — needs cluster truth), and Trigger Run disabled in UI while the server preempts-by-design (200 + tested) — **t001/t003/t004/t005 done 2026-09-18** (the no-recreate guard now reads terminal history from `status.runs` instead of the single-slot `spec.cancelRun` the next cancel overwrites, which also stops a canceled run's deleted Job from pausing the recurring schedule forever; Trigger Run stays enabled during an active run and its confirm dialog names the preemption, and `cron-runs.md` row 73 is corrected — it had documented a rejection the backend never performed); **t002/t006 blocked** — the fixture and its Jobs and Events are gone, so the required cluster forensics are unrecoverable, and the leading hypothesis (the 11-minute delay is Kubernetes' own default backoff, ~10.5 min before `JobFailed`) means fixing on inference would mark live retrying runs as failed
- [x] **m113** — [Static sites are dark on request observability: no request counts, latency, or request logs](done/m113/README.md) (7 tasks, ~4h 50m) ← live `/qa-find-bugs` 2026-09-17 (w4-targeted run, `muse.env` credentials); ~25 driven requests read back empty on every request-observability surface — dashboard Total Requests + Response Times "No data", GraphQL `metrics(HTTP_REQUESTS|HTTP_LATENCY)` + `metricsFilters` + `logs(type:request)` all `[]`, REST `http-requests` `200 []` — while router-attributed bandwidth on the same service shows 5 nonzero buckets, because the PromQL selectors target a per-App Service static sites don't have and the shipper drops static access lines as `not_a_tenant_app`; contradicts ADR018 row 84's "requests stay" promise — done 2026-09-18 (a static site's request count, latency and status-code discovery now select Traefik's per-ROUTER counters over the App's own resolved Ingress routers — the attribution bandwidth already used and row 84 already promised — with an unresolvable router set yielding no query rather than an unbounded cross-tenant match, and discovery sharing the chart's one selector so they cannot drift; the log shipper's static misattribution — the ServiceName names the `bex-static-<app>` alias, not the App — is fixed by preferring Traefik's own `KubernetesIngressName`, gated so platform edge lines keep their bounded host allowlist; both Traefik access-line shapes and both router metric families were verified read-only against production before writing the fix; live re-probe of the deployed fix deferred to the next QA pass)
- [x] **m112** — [Health-gated rollouts stall silently: no UI signal names the failing probe](done/m112/README.md) (7 tasks, ~4h 10m) ← live `/qa-find-bugs` 2026-09-17 (w4-targeted run, `muse.env` credentials); a 404ing health-check path gated a rollout 15 silent minutes — deploy page bare "In Progress", events bare, nothing names the probe — because `stuckPodMessage` only inspects Waiting containers while probe-failing pods are Running-but-unready, and the Ready condition never reaches UI-readable fields; cancel half is w1/m152's mechanism (extended as m152/t010), settle-at-deadline verified live — done 2026-09-18 (`stuckPodMessage` gained a last branch for Running-but-unready pods that names the probe type, its path/port and any liveness restarts; the control plane projects that diagnosis onto the OPEN deploy row as a new `stall_reason` column — deliberately a column, not an event fact, so the w3/m78 outage/webhook/push exclusion is structurally untouched — cleared by `TransitionDeploy` at terminal; REST/GraphQL/MCP and the events feed all read the one column, and the deploy header/list/events rows render it through the existing `DeployFailureReason`; a failed deploy now also inherits the probe diagnosis instead of the generic health-gate line; live re-probe of the deployed fix deferred to the next QA pass)
- [x] **m111** — [Environment-group creation offers scope-incompatible links and cannot create in-env groups](done/m111/README.md) (6 tasks, ~3h 55m) ← live `/qa-find-bugs` 2026-09-17 (w4-targeted run, `muse.env` credentials); the create dialog lists every workspace service with no scope filter and no scope picker, so checking any in-environment service deterministically fails with `ENV_GROUP_SERVICE_ENVIRONMENT_MISMATCH` — and the service-page path pre-checks the failing link, making success unclickable; backend already accepts `environmentId` on create, the detail page already filters correctly, the dialog just never threads either — done 2026-09-18 (an Environment scope picker, shared with Move/Clone as one `ScopeSelect`, filters the candidate list through one `serviceMatchesScope` helper the detail page now also uses; the picked scope rides `$environmentId` on `createEnvGroup`, and switching scope drops now-incompatible checks; the service page opens the dialog in the service's own Environment so its pre-checked link is compatible by construction; explicit-null is pinned equal to omitted on the wire; live re-probe of the deployed fix deferred to the next QA pass)
- [ ] **m110** — **BLOCKED (needs your go-ahead to run one throwaway reproduction on production — a native web service from a Public Git URL, one start-command edit, then delete; or a decision to build out a full local `mock-cluster` + `dev-4` native-build stack)** — [Settings config changes roll the stale image; instance counts, rollback dialog, and build narration mislead](blocked/m110/README.md) (4 tasks, ~6h 15m) ← live `/qa-find-bugs` 2026-09-17 (w4-targeted run, `muse.env` credentials); build/start command edits open config-change deploys that go live on the previous image (no build events, pods run the old baked CMD) while hook/manual deploys of the same spec rebuild; INSTANCES counts terminated pods via the 5m lookback (headline 4 decaying to 1 with zero changes); the rollback dialog never got m108/t004's commit naming; the deploy log narrates builds that never ran — **t002/t003/t004 done 2026-09-18** (instance count is liveness-gated PromQL; the rollback dialog names `<short-sha> <subject>`; build narration now rides `build_started` evidence, so a reuse rollout narrates the rollout instead of a build); **t001 blocked** — prod runs the HEAD-pinned operator image, HEAD's identity code reproduces a live prod App's fingerprints byte-for-byte and both command edits flip the artifact, so the remaining cause is a runtime path that must be reproduced live

- [x] **m109** — [Projects `updatedAt` + `owner` value fix](done/m109/README.md) (7 tasks) — REST reads real `projects.updated_at`, batch-resolves owner via `resourcemeta.OwnerResolver` like apps/postgres/kv; value-level conformance guards both; live prod probe awaits deploy

- [x] **m108** — [Make deploy provenance and request metrics honest: public-repo commits, rate-vs-count charts, rollback label](done/m108/README.md) (9 tasks, ~6h 25m) ← live `/qa-find-bugs` 2026-09-15/16 (w4-targeted run); `http_requests` is `sum(rate(…))` on both sources but declared `unit: "count"`, so the Metrics page reads 0 for a service that just served 12 requests and under-reports a busy one by a factor of the resolution; a service created from **Public Git URL** never records a commit on any deploy because resolution requires a GitHub App installation; and a rollback's trigger label title-cases the deploy id it names (`Rollback To Dep-Dal3f2…`), the second member of the class `w4/036` fixed

- [x] **m106** — [Close the GraphQL scope gap that lets a read-classified root field return env-var and secret-file values](done/m106/README.md) (7 tasks, ~4h 10m) ← live `/qa-find-bugs` 2026-09-14 pass 25; nested `envVar`/`secretFile` under a read root returned plaintext while REST/MCP were sensitive — done 2026-09-14 (selection-set escalation; keys-only stay out of the nested registry; human OAuth `bex.read` enforcement tests; ADR012/ADR018)

- [x] **m105** — [A dashboard-minted API key cannot be used from the dashboard alone: no `client_id`, and the advertised scopes are refused](done/m105/README.md) (7 tasks, ~3h 40m) ← continuous live `/qa-find-bugs`, 2026-09-13 pass 13; the mint dialog shows only the secret and the table has no id column, so the `client_id` the `client_credentials` exchange requires appears nowhere in the UI — the credential works once it is fetched from GraphQL by hand; separately, discovery advertises `bex.read/write/sensitive` but the minted Hydra client is created with no `Scope`, so requesting one is refused `invalid_scope`. Closes the mint→authenticate round trip `w4/m8` recorded as unverified

- [x] **m104** — [`PUT /v1/services/{id}/autoscaling` is unusable: the pinned contract and the handler want different bodies](done/m104/README.md) (8 tasks, ~5h 10m) ← continuous live `/qa-find-bugs`, 2026-09-13 pass 9; every body shape 400s on every service type — bex's own `{minInstances,maxInstances}` is refused by the pinned OpenAPI validator, Render's exact `{enabled,min,max,criteria}` is refused by the handler, and the union of both is refused too, while the same intent through GraphQL `setAutoscaling` succeeds on the same service seconds later; third recorded instance of the `w7/m49` validator-vs-decoder class after `w4/038` and `w4/041`, so it audits the surface rather than patching one route — done 2026-09-14 (REST adopts Render wire; DecodeBody keeps field detail; gated-write inventory test)

- [x] **m103** — [A first deploy whose rollout fails leaves the service reading "Deploying" forever, while its deploy reads "Failed"](done/m103/README.md) (7 tasks, ~4h 15m) ← continuous live `/qa-find-bugs`, 2026-09-13 pass 5, journey 14; the deploy row settles to `update_failed` at the documented 900s budget with an excellent `failureReason`, but `status.phase` had no writer for a rollout failure (`PhaseFailed`'s only writer was `r.fail` on reconcile errors), so the header contradicted the deploy row on REST, GraphQL and the UI — the third case in the taxonomy `w6/m52` + `w6/m124` built — done 2026-09-14 (ProgressDeadlineExceeded settles Failed / prior-release Running|Hibernated)

- [x] **m102** — [Blueprint validation names the field that is actually wrong, and the shipped static-site example validates](done/m102/README.md) (8 tasks, ~5h 5m) ← continuous live `/qa-find-bugs`, 2026-09-13 pass 5, journey 13; `examples/static-site/render.yaml` is the 1 of 7 shipped manifests bex's own validator rejects, and the `anyOf` "closest branch" heuristic blames `staticPublishPath` (legal) instead of `plan` (the offender), then sends the author round a three-message loop that never names it — done 2026-09-14 (discriminator-aware anyOf; example drops `plan`; blueprint create errors say `staticPublishPath`; CI walk of shipped manifests)

- [x] **m101** — [Refuse autoscaling the runtime cannot honor, and apply static header rules to error responses](done/m101/README.md) (14 tasks, ~8h 5m) ← continuous live `/qa-find-bugs`, 2026-09-13 passes 2–3 + 7; a cron job accepts and stores `autoscaling.enabled=true` that the reconciler never reads (it returns before the replica path), the dashboard offers the editor above its own type gate, and a static site's `/*` header rule reaches 200/301 but none of the six error classes

- [x] **m100** — [Stop bex-api shedding a legitimate dashboard page load, and record the running commit on config-change deploys](done/m100/README.md) (9 tasks, ~6h 15m) ← continuous live `/qa-find-bugs`, 2026-09-13; a single Metrics page load draws 2–8 × `429 RATE_LIMITED` from the per-credential auth in-flight bound (not `BEX_RATE_LIMIT`), silently, and overturns `w6/028`'s "efficiency, not correctness" disposition; rides along with `config_change` deploy rows carrying no commit — done (per-cred knob + `AUTH_OVERLOADED`; GraphQL batch + `BatchHttpLink`; throttle UI; commit carry via `LatestDeployCommit`), moved to `done/m100/`

- [x] **m99** — [Restore image-create compatibility and long-name alias admission](done/m99/README.md) (7 tasks, ~3h 30m) ← continuous CLI QA, 2026-09-09

- [x] **m97** — [Preserve environment-group metadata during concurrent edits](done/m97/README.md) (8 tasks, ~5h 30m) ← approved w4 brainstorm, 2026-09-08; done 2026-09-08 (metadata `PutCAS` for name/scope/links/`updatedAt`; content compensation no longer restores stale meta; delete tombstone fence; `ENV_GROUP_METADATA_CONFLICT`; Core race tests + ADR/drill evidence; live `dev-4` OpenBao drill deferred), moved to `done/m97/`
- [x] **m98** — [Recover interrupted environment-group saves](done/m98/README.md) (8 tasks, ~6h) ← approved w4 brainstorm, 2026-09-08; done 2026-09-09 (durable op recovery + list/detail `availability` on REST/GraphQL/dashboard; Core race tests; live drill deferred), moved to `done/m98/`

- [x] **m96** — Use the same log message boundary in history and live streaming (6 tasks) ← continuous live `$qa-find-bugs w4`, 2026-09-06 pass 6; three cron records render six through newline drift — done 2026-09-07 (historical container records now match the live/fallback reader's message bytes: `parseLokiStreams` passes every message through `canonicalHistoryMessage`, which strips only the single kubelet transport terminator `bufio.Scanner`'s `ScanLines` already removes — one trailing `\r?\n`, never data whitespace, an escaped newline, or an empty record; source-aware — all `loki.source.kubernetes` streams (app/request/platform/postgres/keyvalue) are canonicalized while file-sourced `type=build` (stage.cri, already terminator-free) is left byte-for-byte untouched; id convergence is automatic since `logID` hashes the message, so history/live now derive one id per record; no frontend change — `mergeLogLines` was correct given its inputs; ADR010 records the boundary + build distinction, Render wire contract unchanged; new backend canonicalization table + `parseLokiStreams` integration + cross-source id-convergence tests, and frontend real-wire merge + regression tests; backend `go test ./...` 61 pkg green + golangci-lint 0 issues + dashboard typecheck/lint/tests green; live re-probe of the deployed fix deferred to the next QA pass), moved to `done/m96/`

- [x] **m95** — Complete the trust setup for external PostgreSQL connections (7 tasks) ← continuous live `$qa-find-bugs w4`, 2026-09-06 pass 4; private server CA is required but unavailable through the product — done 2026-09-07 (connection-info gains an additive `serverCaCertificate` PEM field on REST + GraphQL, sourced from the CNPG-default `<cluster>-ca` Secret via `certificateOnlyPEM` — certificate blocks only, never the key; only for public DBs, a missing CA answers an actionable 503 and malformed material is refused rather than downgrading TLS; the dashboard Connections panel offers a per-database `<id>-ca.pem` download + `PGSSLROOTCERT` verify-full instructions via a new shared `downloadTextFile` helper that env-export/Generate-Blueprint now reuse; ADR009 + ADR018 record it as a deliberate bex extension vs Render's public-CA hosts, MCP deliberately still has no connection-info tool; new `server_ca_test.go` + panel tests prove REST≡GraphQL delivery, no key leak, 503/refusal/internal-only classes; backend `go test ./...` 61 pkg green + golangci-lint 0 issues + dashboard typecheck/lint/tests green; live re-probe of the deployed fix deferred to the next QA pass), moved to `done/m95/`

- [x] **m94** — Preserve static assets and request-path headers under rewrites (7 tasks) ← continuous live `$qa-find-bugs w4`, 2026-09-06 pass 3; existing files masked and path headers omitted — done 2026-09-06 (an existing published object now wins over redirect/rewrite rules — only a genuine miss advances to ordered rules then SPA fallback, and origin denial/timeout/oversize/capacity keep their 502/413/503 classes; custom headers match the visitor's immutable request path on every response while the served path drives content-type/cache; the simplify pass caught and fixed the lookup-first origin amplification with a revision-immutable negative cache for confirmed misses; 10 new handler tests; ADR029 + ADR018 updated; operator suite + lint green; live re-probe of the deployed fix deferred to the next QA pass), moved to `done/m94/`

- [x] **m93** — Include linked environment groups in native builds (6 tasks) ← continuous live `$qa-find-bugs w4`, 2026-09-06 pass 2; build omits a group value that runtime receives — done 2026-09-06 (`projectNativeBuildEnv` merges the exact runtime `envFromSources` list — groups in order, own Secret last-wins — into an App-owned `<name>-native-env` Secret riding the existing single BuildKit mount, m92 codec untouched, literals still win; deterministic per-App destination + `checkOwnedArtifact` keep shared groups un-adopted and same-named foreign Apps refused; `EnvFromSecrets` joined the native artifact fingerprint so direct CR link edits rebuild; inventory + finalizer cleanup extended; 8 new controller tests incl. the group-only regression; operator + backend suites + lint green; live re-probe of the deployed fix deferred to the next QA pass), moved to `done/m93/`

- [x] **m92** — Preserve exact environment values in native builds (6 tasks) ← continuous live `$qa-find-bugs w4`, 2026-09-06; build/runtime byte mismatch — done 2026-09-06 (whole-record read + sentinel-guarded decode preserve Base64 padding and trailing newlines; literal producer strips exactly printenv's one terminator; invalid Base64 fails the build closed with no partial export; executed 17/17 against the real pinned busybox preparer digest + bookworm bash across both env paths, old decoder re-run as a failing control; operator suite + lint green; live prod re-probe of the deployed fix deferred to the next QA pass), moved to `done/m92/`
- [x] **m90** — Workspace-scoped billing email + payment method at creation (9 tasks) ← user request 2026-08-31 after live authenticated comparison of `dashboard.bex.co/new/workspace` and `dashboard.render.com/new/workspace`; closes the shape-only/current-workspace shortcut left by w9/m88 — done 2026-08-31 (account-prefilled billing email plus workspace-local Stripe Payment Element/SetupIntent, resumable subject-bound attempts, server-verified idempotent finalization, bounded cleanup, authenticated Render parity evidence, and real Stripe test-mode success/cancel/retry), moved to `done/m90/`
- [x] **m89** — Honest agent-session states and a real error taxonomy (8 tasks) ← from `/pm-brainstorm` 2026-08-28 findings 3+4: 17 `ErrAgentSessionsUnavailable` sites collapse into one "aren't configured — ask your operator" alert (hit live in dev-1 from a transient etcd timeout), and a normally-starting session renders failure copy.
- [x] **m1** — Platform auth: Ory Kratos + Hydra on the cluster (+ ADR) (9 tasks) ← from brainstorm 2026-07-05
- [x] **m2** — bex-api auth: Hydra introspection + Kratos sessions (6 tasks) ← from brainstorm 2026-07-05, needs m1
- [x] **m3** — API keys replace the static token (7 tasks) ← user decision 2026-07-06, needs m2
- [x] **m4** — Authorization: OpenFGA over the auth substrate (8 tasks) ← promoted from w4/001, needs m3
- [x] **m5** — Platform secrets: OpenBao on the cluster (+ ADR) (8 tasks) ← from brainstorm 2026-07-06, parallel with m4
- [x] **m6** — Tenant secrets: env-vars API backed by OpenBao, injected into Apps (9 tasks) ← from brainstorm 2026-07-06, needs m4 + m5
- [x] **m6.5** — Env vars in the dashboard: Render-style Environment tab wired to the m6 API (7 tasks) ← user request 2026-07-07, needs m6 + w5/m5 (service-detail IA)
- [x] **m7** — Email flows: Kratos courier + recovery/verification live (8 tasks) ← from brainstorm 2026-07-06, independent of m4–m6 (closing tasks retrofitted 2026-07-09) — done 2026-07-12 (recovery + verification proven e2e via `scripts/auth-mail-e2e.sh`; courier/relay wired prod + local; `auth.verification` dashboard page filed as inbox `w4/008`), moved to `done/m7/`
- [x] **m8** — API keys in the dashboard (settings surface) (6 tasks) ← from brainstorm 2026-07-06, coordinates with m4's checker; inbox `004`'s key-metadata half was subsequently completed in m13
- [x] **m9** — OAuth 2.1 provider for agents: one dashboard login, first-party sessions + third-party clients (7 tasks) ← promoted from w4/001 (user request 2026-07-07), needs m1 + m2 — done 2026-07-08 (e2e-verified incl. real-browser login pass), moved to `done/m9/`
- [x] **m10** — Audit log: every Core write recorded with caller identity (8 tasks) ← from `/pm-brainstorm tasks for w4` 2026-07-08 (closing tasks retrofitted 2026-07-09) — done 2026-07-11 (verified via unit + real-Postgres integration tests; live mock-cluster smoke not run — no cluster in this environment), moved to `done/m10/`
- [x] **m11** — MFA: TOTP + passkeys via Kratos (9 tasks) ← promoted from `002` 2026-07-08 (its w1/m2 revisit condition met; closing tasks retrofitted 2026-07-09) — done 2026-07-14 (TOTP + recovery codes + WebAuthn/passkey all shipped and live-verified against prod, including the full enroll → logout → aal2-challenge ceremony in a real browser via Playwright + a CDP virtual authenticator; `t009` found+fixed two real bugs blocking WebAuthn — a dashboard CSP missing the Kratos origin in `script-src`, and Kratos's identity schema never marking `email` as a webauthn identifier — surfaced only once someone actually drove a browser through the ceremony against the real stack), moved to `done/m11/`
- [x] **m12** — Workspace members & roles (Render team surface) (9 tasks) ← from `/pm-brainstorm tasks for w4` 2026-07-08, needs w1/m9 + w4/m7 (closing tasks retrofitted 2026-07-09)
- [x] **m13** — API-key hygiene: token TTL + key metadata (7 tasks) ← promoted from `004` 2026-07-09 (its m8 pairing plan didn't happen); done 2026-07-09
- [x] **m14** — Audit log in the dashboard: Settings → Audit Log panel over the m10 API (10 tasks) ← user request 2026-07-11, needs m10 (done) — done 2026-07-11 (`yarn test`/`yarn lint` green; IA-placement drift vs Render found in parity check, filed as `007`), moved to `done/m14/`
- [x] **m15** — Settings → Security & Compliance grouping (move Audit Log) (6 tasks) ← promoted from `007` 2026-07-12 (user resolved the IA-placement decision as "yes, move it"), coordinates with `006` (session mgmt) + m11 (MFA) on the same Settings→Security surface — done 2026-07-12 (real-app verified: Audit Log card renders inside the Security & Compliance region; `yarn test` 613 green), moved to `done/m15/`
- [x] **m16** — OAuth consent screen for third-party agent clients (9 tasks) ← from `/pm-brainstorm more for w4` 2026-07-12 (ADR012 §7 "consent UI is future work" + ADR025's operator-blessing dead end), follow-on to m9; anti-goal tension (DO_NOT_DO "headless consent acceptor") resolved by user acceptance 2026-07-12 — done 2026-07-12 (`scripts/auth-oauth21-e2e.sh` exit 0 over 12 legs: unblessed DCR client → consent page → approve → working `/mcp` token, deny → `access_denied`, remembered re-authorize skips the page, trusted client still headless; `yarn test` 675 green; a login-path gap found on the way filed as `010`), moved to `done/m16/`
- [x] **m17** — Login-flow correctness: signed-in OAuth authorize + aal2 step-up altitude (8 tasks) ← promoted from `010` (+ `009` folded in) 2026-07-12, follow-on to m16; fixes the ADR025 signed-in connect path — done 2026-07-12 (`010` confirmed: Kratos v1.3.1 answers a satisfied login challenge with `200 null`, which the SDK resolves as a null flow ⇒ the login page sat on its skeleton forever; fixed by handing the challenge back to Kratos as a browser navigation. `009`'s aal2 probe replaced by `fetchSession`'s `session_aal2_required` → `requireAuth` → `?aal=aal2`. `scripts/auth-oauth21-e2e.sh` exit 0 over 13 legs incl. a reused signed-in jar; the whole ADR025 recipe driven in a real browser through to a working Bearer token; `yarn test` 692 green. A consent-page bug that broke **every** real-browser approve/deny — `disabled` on the submit buttons dropped the submitter's `decision` field, invisible to curl — was found in the browser check and fixed), moved to `done/m17/`
- [x] **m18** — Account access control: connected agents + active sessions (8 tasks) ← from `/pm-brainstorm for w4` 2026-07-12, m16 follow-on (remembered consent shipped with no revocation surface); folds `006`; independent of m17 (do m17 first — live user-facing break) — done 2026-07-13 (Settings → Security & Compliance gained **Connected agents** — list/revoke Hydra consent sessions, `revokeOAuth2ConsentSessions(subject, client)` — and **Active sessions** — Kratos `listMySessions`/`disableMySession`/`disableMyOtherSessions` — both dashboard-SSR only, no bex-api surface added; `auth-oauth21-e2e.sh` leg 14 written but not run this session — the local ports it needs were already bound by a live dev/cluster session; dashboard suite green, 838 tests), moved to `done/m18/`
- [x] **m19** — Duplicate service names: workspace-unique names + globally-unique subdomains, Render-consistent (11 tasks) ← user request 2026-07-12 (`/pm`); fixes the cross-tenant name-existence 403 leak + conflict-500 on the way (tenancy-correct naming = w4's multi-tenant-security mandate) — done 2026-07-14 (workspace-unique names reject with 409 across REST/GraphQL/MCP, never upsert/500; two workspaces both own the same name via a globally-unique `apps.slug` with a random suffix on collision; dashboard debounced availability + inline suggestion; proven live via `scripts/name-conflict-e2e.sh`, two green runs against the real CAPD mock cluster; backend/operator/dashboard suites all green; a real name-leak bug — the create response echoing the tenant-prefixed CR name instead of the public service name — found and fixed in the t009 simplify pass, with a regression test), moved to `done/m19/`
- [x] **m20** — IAM & service-payload parity chores (7 tasks) ← groups `001`(→renumbered `012`, duplicate of the done original), `011` 2026-07-14 (each sub-hour, grouped per the sizing rule — the w1/m23 / w6/m15 / w7/m10 pattern): read-verb denials into the audit log, `slug` on the service object, stale ADR018 identity rows — done 2026-07-14 (a denied `can_view`/`can_view_logs`/`can_view_sensitive` check now leaves a `status: denied` `audit_events` row, allowed reads still don't; `slug` reads correctly on REST/GraphQL/MCP + the dashboard service header, incl. the cross-tenant-collision suffixed case; ADR018's MFA row now reflects m11's live-verified WebAuthn close, its email-recovery row ◐ → ✅ for m7; backend + dashboard suites green. One `/simplify`-pass efficiency finding filed as a follow-up rather than fixed inline: `w4/014.md`), moved to `done/m20/`
- [x] **m21** — Per-service notification override: `notifyOnFail` (8 tasks) ← from `/pm-brainstorm more milestones for each worker` round 3, 2026-07-14 (zero `notifyOnFail` hits in `lego/backend/` — Render's service object carries the override; w3/m9's notifications are workspace/member-level only). Capacity placement — topical owner w3 has four open milestones; continues m20's payload-parity thread — done 2026-07-14 (`App.spec.notifyOnFail` on the CRD + REST/GraphQL/MCP create+PATCH; `DeployNotifier` honors ignore/notify/default against each member's w3/m9 preference for failure mail only; dashboard Settings → Notifications card, live-verified in a real browser; `/simplify` reshaped `DeployNotifier.NotifyDeploy` into a struct), moved to `done/m21/`
- [x] **m22** — Close the ADR032-flagged `internal/projects` authz + error-mapping gaps (8 tasks) ← done 2026-07-14: Projects + Environments pin cross-tenant-existing 403 vs nonexistent 404; Projects joined the shared authz/audit/relation sweep; store-off Projects maps to REST 503 and native GraphQL/MCP errors; the official CLI's real-Project Environments success path + unknown-id 404 passed live after m22 found/fixed the missing `{environment,cursor}` list envelope. Remaining Render `ipAllowList` wire drift filed as `017.md`; moved to `done/m22/`
- [x] **m23** — Audit & payload-parity chores round 2 (8 tasks) ← groups `013` + `014` + `015` + `017` 2026-07-15 (the m20 chores pattern): audit-log `direction` honored-or-400, `suspenders` on the service object, the denied-read audit fan-out latency fix, Environment `ipAllowList` `{cidrBlock, description}` wire shape — done 2026-07-15 (t001–t005 + t007 implemented by a parallel session working the notes directly, resolutions in `done/013`–`017`; t006's simplify pass over `cc2156b8` applied five behavior-preserving cleanups — shared `core.ParseDirection` for logs+audit, one `pageKeyset` helper, a shared `auditsDenial` predicate, zero-I/O aggregate-denial workspace object, no redundant environments-PATCH `Get` after Rename — and filed the declined-depth findings as `022.md`; backend suite + lint green), moved to `done/m23/`
- [x] **m24** — ipAllowList descriptions: persist what we accept (7 tasks) ← from `/pm-brainstorm` round 10, 2026-07-15 (RC12/RC14's recorded "accepted but dropped" subset for Postgres/Key Value + Environments' bare strings; descriptions persist and round-trip end to end, CRD → surfaces → dashboard → CLI verify)
- [x] **m25** — Identity completeness: user `name` + machine-caller resolution (8 tasks) ← from `/pm-brainstorm` round 12, 2026-07-15 (`workspaces/service.go:285` — Name "is always ''"; ADR018:217's left-open API-key-caller half) — done 2026-07-15 (optional Kratos `name` trait + settings/registration; `core.Identity.Name`/`ClientID`; API-key callers resolve via the `created-by` binding; official CLI `render whoami` live-verified for both caller types; dashboard user menu + profile settings; GitHub OIDC `name` claim mapped; `mfaEnabled` stub false-positive filed as `020.md`), moved to `done/m25/`
- [x] **m26** — Audit-log Render-shape verification, evidence-first (7 tasks) ← from `/pm-brainstorm` round 13, 2026-07-15 (parity-ledger mining: ADR018:134 is the only substantive ◐ with no owner — "schema wasn't resolvable from public docs at authoring time"; the pinned OpenAPI, CLI source, and real-account capture workflows postdate that assessment) — done 2026-07-15 (the blockage had expired: Render's public OpenAPI now publishes the full `auditLog` schema, corroborated by the official CLI's generated client types at pinned `72b3fbd`; REST aligned exactly — `event` with a guarded 1:1 verb mapping, `{type,id}` actor object, closed `success|error` status with the denial preserved as `metadata.outcome`, required string-map metadata keyed by target kind via new `core.SplitTarget`, limit cap 1000 — GraphQL deliberately keeps bex's flat dialect for the dashboard; capture in `docs/render-artifacts/audit-logs-api.md`, ADR018 audit row ◐→✅; live real-account capture not run, no `RENDER_API_KEY` here), moved to `done/m26/`
- [x] **m27** — Official Render CLI browser login via Hydra device flow (9 tasks) ← user request 2026-07-15 + w9/m2 auth-gap investigation; needs m9 + m17 — done 2026-07-15 (implementation `441d6040` was already deployed; the outstanding production gate passed live: fixed public client verified in prod Hydra, and `scripts/render-cli-auth-e2e.sh` exit 0 against `api.bex.co` with the pinned unmodified CLI in a real Chrome — browser device login, human tenancy, forced refresh rotation, scoped logout incl. the ≤30s multi-replica cache window, two-user isolation, shared secretless client; two verifier fixes shipped — password-method selector vs prod's GitHub social login, logout probe polling past `core.PositiveTTL`; CLI checklist login/logout rows ◐→✅; simplify pass cleanups + `023.md` filed), moved to `done/m27/`
- [x] **m28** — Environment inbound-IP rules: real enforcement semantics (8 tasks) ← promotes `018` (filed by w5/m31's closeout), `/pm-brainstorm` round 14, 2026-07-15; Render applies environment rules to eligible public web services/static sites + datastores, composes workspace/env/service layers, and treats empty as deny-all (seeded `0.0.0.0/0`) — bex fans only to member datastores and treats empty as open; coordinate with m24 — done 2026-07-17 (design + full enforcement implementation + tests + docs shipped; five consistency-review gaps fixed pre-ship, cross-feature edges filed as `w4/025.md` and later closed by m32; layered composition table-driven test + migration no-lockout invariant pass; `docs/render-artifacts/protected-environments.md` drift paragraph rewritten as shipped behavior), moved to `done/m28/`
- [x] **m29** — Retire the ipAllowList legacy-string shim: one-shot CR normalization + schema tightening (7 tasks) ← promotes `019` (filed by w4/m24's simplify pass) via `/pm-brainstorm` round 15, 2026-07-15 — done 2026-07-15 (two-deploy sequence honored: `fbfee9a7` shipped `scripts/ipallowlist-normalize.sh` + its 16-assertion clusterless harness and recorded the prod fleet already clean of string entries; a separate later push restored the structural CRD schema — `required: [cidr]`, bare strings + cidr-less objects rejected at a real apiserver in envtest — deleted `v1alpha1.IPAllowEntry.UnmarshalJSON` (legacy shape survives only as a must-fail test fixture), kept `core.IPAllowListEntry`'s wire-side union deliberately unchanged, and swept the docs the retirement falsified: ADR009/ADR021 "still decode" clauses, cidr.go's dead twin instruction, plus adjacent m24/w9-era staleness in ADR018/ADR006), moved to `done/m29/`
- [x] **m30** — Environments partial-update core verb + AuthorizeApp loop hoist (7 tasks) ← promotes `022` (filed by w4/m23's simplify pass) via `/pm-brainstorm` round 18, 2026-07-15, re-scoped after w6/m33 shipped the create-with-ACL half same-day: the PATCH merge moves into a core `EnvironmentPatch`/`Update` verb (GraphQL `updateEnvironment` + MCP `update_environment` ride it), and `AuthorizeApp`'s collision loops stop re-resolving the loop-invariant acting workspace per candidate — done 2026-07-16 (the merge — pointer overrides + the `""→unprotected` pre-migration default — now lives exactly once in `environments.Service.Update`, one authorize + one fetch, with REST PATCH/GraphQL `updateEnvironment`/MCP `update_environment` all riding it and proven byte-identical by `TestUpdateAcrossRESTGraphQLAndMCPProduceIdenticalMerges` + the pre-migration-default cross-surface test; `AuthorizeApp`'s three collision loops now thread a pre-resolved acting workspace through `resourceWorkspaceFor`, asserted at exactly one `Workspace.Tenant` call for N candidates by `TestAuthorizeAppCollisionFallbackResolvesActingWorkspaceOnce`; OpenFGA BatchCheck assessed and NOT adopted, rationale recorded inline in `base.go`; `/simplify` graduated the duplicated `gqlBoolPtr` into `gqlutil.BoolPtr` (apps + environments both now call it), factored a shared `validateEnvironmentName` for `CreateWithACL`/`Update`, and deduped test setup boilerplate; the identical redundant-resolution shape in `GetApp`/`AuthorizeLabeled` was found but is out of this milestone's scope, filed as `027.md`), moved to `done/m30/`
- [x] **m31** — Auth-gate hardening: revocation epoch + device-route rate limiting (6 tasks) ← promotes `023` (filed by w4/m27/t007's simplify pass) via `/pm-brainstorm` round 19, 2026-07-15, both findings re-verified live at promotion: `introspectUpstream` holds `revocationMu.RLock()` across the Hydra RTT (`auth.go:227-240` — one logout stalls all cache-miss auth), and the three credential-less device-flow routes are unmetered Hydra amplification — done 2026-07-16 (`introspectUpstream` now runs the Hydra RTT under no lock at all, snapshotting an atomic revocation epoch beforehand; a narrow `epochMu` — held only for the epoch-bump in `invalidate` and the epoch-recheck-plus-cache-`Put` in `introspectUpstream`, never across any I/O — keeps those two atomic with each other, closing a real race a first-cut bare-atomic design had (confirmed by a dedicated concurrency-review pass: a stale positive result could land in the cache strictly between the epoch check and the `Put`, after a concurrent `invalidate` had already completed); proven by `TestInvalidateDoesNotBlockDuringInFlightUpstreamRTT` + `TestEpochBumpDuringRTTDiscardsStalePut`, `go test -race` green including repeated runs. The three device-flow routes now sit behind a new IP-keyed `cliauth.DeviceRateLimiter`, shedding floods before any Hydra call with OAuth-dialect `slow_down` 429s, env-tunable via `BEX_DEVICE_RATE_LIMIT`/`BEX_DEVICE_RATE_BURST`; a `/simplify`-pass finding — three near-identical hand-rolled rate-limiter implementations, this one plus `api.RateLimiter` plus the pre-existing `deploys.DeployHookRateLimiter` — filed as `028.md` rather than fixed inline (touches `deploys/`, outside scope). Live CLI device-flow ceremony not run — no cluster access this session, same constraint as m28's open t008), moved to `done/m31/`
- [x] **m33** — Codex round-4 security remediation: authorize the sink, finish the revoke (10 tasks) ← from the `codex-security-bex-JOJ6g5` triage 2026-08-09 (scan of `e815388e`); the six P0/P1 findings, three of them regressions the previous remediation pass (`18cd7871`) left half-fixed. P2/P3 remainder filed as `029.md` — done 2026-08-15: t001–t003 landed in the original pass; t004 (device-code phish), t005 (postgres drop tombstone), t006 (workflow_dispatch main-gate) were re-found and re-fixed by later codex rounds (#8/#9/#11) and verified against HEAD; two missing t005 test assertions backfilled, moved to `done/m33/`
- [x] **m35** — bex CLI distribution: first release (`bex-cli/v0.1.0`), install script, signed checksums, Homebrew tap (7 tasks) ← from the 2026-08-15 distribution research; user approved v0.1.0 + tap-repo creation + `BEX_TAP_PUSH_TOKEN` provisioning; GoReleaser evaluated and not adopted (monorepo tag prefixes are Pro-only; hand-rolled chain already covers its core outputs). Activates m34's update-notice channel and gives `w4/030`'s future `bex upgrade` its artifacts
- [x] **m34** — bex CLI self-version + bex-owned update notice (7 tasks) ← from the 2026-08-14 update-channel research session: upstream `cfg.RepoURL` (a const) points `bex -v`'s update check at render-oss/cli releases and render.com upgrade docs — armed to misdirect on Render's next release; intercept the version path, inject a bex-native version in `cli-release.yml`, add a gh-style passive notice against `bex-cli/v*`, and PR upstream to make the const overridable (the `login.go` second call site). Phases 2–3 filed as `030.md` (`bex upgrade` self-update) and `031.md` (remote provider catalog + drift probe + `bex code doctor`)
- [x] **m36** — Universal `@` mention search in the /agents composer (Devin/Cursor-style) (7 tasks) ← user request 2026-08-14, from the Devin `@`-mention research session: bex's picker gated entities behind a literal `@repos:`/`@sessions:` token while Devin/Cursor/Linear/Notion all match entities directly after `@` — done 2026-08-14 (universal top level in `dashboard/src/features/agent-sessions/lib/mention.ts` scores categories + repos + sessions against one query, grouped section headers in the picker, `@repos:`/`@sessions:` kept as optional narrowing; `@be` now surfaces `bex-co/bex` directly; wire contract byte-identical, dashboard suite green 2102 + lint clean; simplify pass swapped two hand-rolled session fixtures onto the shared `agentSessionView` factory, killing a phantom-`evidence` rot; future mention types filed as `033.md`; live `/agents` browser check not run — no cluster/local repos this session), moved to `done/m36/`
- [x] **m32** — Environment IP-layer lifecycle correctness: cross-feature edges + backfill (7 tasks) ← promotes `025` (filed by w4/m28's consistency review) via `/pm-brainstorm for w4 focusing on polishing existing features`, 2026-07-15: `SetProjectServices` departure strands a frozen layer, `projects.Delete` cascades environments away without the member clear, and pre-m28 members carry no layer until an incidental write — all three enforcement-bearing since m28 shipped — done 2026-07-16 (`SetProjectServices` now NULLs `environment_id` alongside `project_id` for departing rows in one transaction, returning the names for a k8s-side clear; `projects.Service.SetServices`/`Delete` fan that clear out via a new narrow `EnvironmentIndex` interface satisfied by `environments.ProjectMemberClearer`, mirroring `apps.WorkspacePurger`'s "internal system operation, never call Authorize" shape — a `/simplify` pass restructured the first cut, which had bolted a mis-scoped `Authorize` onto the new methods, into unexported `Service` methods behind thin wrapper types (`ProjectMemberClearer`, `Backfiller`), and in the process factored `Delete`'s and the cascade path's duplicated member-clear logic into one helper, closing a real bug where the cascade path silently skipped env-group-membership clearing; a one-shot idempotent `environments.Backfiller` sweep (the `api environments-backfill` subcommand) repairs pre-fix drifted rows and re-stamps environments whose rules predate m28, every per-resource patch equality-gated so a second run is a true no-op — real-Postgres-verified (`store_pg_test.go`) plus CR-level fake-client tests per member kind (App/DB/KV); the residue-drop t003 optionally proposed was declined as out of scope; the sweep itself was not run against a live prod/dev cluster this session — no cluster access — filed as follow-up work for whoever has it), moved to `done/m32/`
- [x] **m81** — Stripe live-mode graduation readiness (9 tasks) ← from the 2026-08-15 live-cutover deep-research session (three-agent sweep over mode-tainted billing state, Stripe Tax activation, and runbook §6 gaps) — done 2026-08-16 (a livemode flip on `billing_provider_mappings` now clears `payment_method_bound_at` + the cached subscription id in the upsert itself, PG-verified, so a test-bound workspace can't keep paid access or usage export in live without a live card — plus the §6 one-time reset SQL as belt-and-braces; the w7/m52 test-only dunning fence is lifted in both the installer and `wireStripeBilling` (live `Lifecycle` threads the key's mode; `dunningGate` pins the contract); the installer refuses a live key without the scoped portal `bpc_*`; `stripe-billing-setup.py --webhook-url` provisions the pinned-version ten-event endpoint idempotently with drift refusal and a 0600 secret hand-off; `stripe-billing-reconcile.sh report`/`issues` admit a live key under `BEX_STRIPE_ALLOW_LIVE=1` while `repair` refuses live regardless — a gap found mid-milestone: repair had NO key gate at all; runbook §6 rewritten as the executable go-live checklist (out-of-band prerequisites incl. the tax recommendation `txcd_10102000`/`exclusive`, epoch semantics + the raw-kubectl footgun, cycle-anchor note, marker reset, staffed-window steps, post-cutover verification) and both stale "13 items" facts fixed to 14; 36 offline script tests + backend suite + targeted PG suite + lint all green. The cutover execution itself remains an operator-staffed §6 run gated on Stripe account activation, tax counsel, and the epoch decision)
- [x] **m38** — Render-CLI token-refresh idempotency + 7d lifespan (6 tasks) ← from `/pm-brainstorm more work for w4` 2026-08-17, promotes `FUTURE-MAYBE.md:33` — done 2026-08-17 (migration 0082 + a SHA-256-keyed, one-minute Postgres response cache; transaction advisory lock collapses same-token refreshes across replicas to one Hydra mint and one live access token; distinct tokens remain concurrent, OAuth errors stay verbatim/uncached, expiry is swept; the Hydra client bootstrap now writes and round-trip-checks `168h` on both grants; real-Postgres concurrency/restart/migration tests + full backend suite + lint green; production bootstrap execution deferred to an operator), moved to `done/m38/`
- [x] **m37** — `bex upgrade` self-update command (update-channel phase 2) (7 tasks) ← promotes `030` 2026-08-16 (user decision); phase-2 follow-on to m34 (passive notice) + m35 (signed release assets + tap) — done 2026-08-16 (`cli/internal/upgrade/`: resolves newest `bex-cli/v*`, in-process cosign/sigstore signature verification over `checksums.txt` pinned to the release-workflow Fulcio identity + SHA-256 match, atomic self-replace with implicit rollback, Homebrew-path deflection to `brew upgrade`; `update` package extended with asset-bearing `LatestRelease`; 18 tests, live `bex upgrade -n` proven against real GitHub; full live download→replace deferred until a release newer than v0.1.0 exists), moved to `done/m37/`
- [x] **m82** — Codex round-4 P2/P3 remediation + CLI release-staleness guardrails (10 tasks) ← promotes `029` (codex-security round-4 P2/P3 register) + `032` (release-engineering guardrails) via a full-board `/goal` triage session, 2026-08-16 — done 2026-08-16 (static-server `ReadTimeout` added; mobile push unregister now confirms the remote call before flipping local state; registry-credential `Delete` refuses with 409 while an App still resolves the id, explicitly or via host match; SSH gateway gained PROXY protocol support — a new `internal/proxyproto` leaf package (backend can't import operator's `sniproxy`) + `BEX_SSH_PROXY_PROTOCOL_TRUSTED_CIDRS` + the IngressRouteTCP's `proxyProtocol.version: 2` — so `ssh_sessions.remote_address` records the real client instead of Traefik's pod IP; `029`'s #10 HTTP half/#13/#6 confirmed already fixed by earlier rounds, #11 confirmed accepted risk (ADR072); a weekly CLI release-staleness cron (`scripts/cli-release-staleness.sh` + its workflow) now opens/closes a tracking issue instead of relying on memory; a live GitHub ruleset restricts `bex-cli/v*` tag creation/update/deletion to Admin/Maintain roles), moved to `done/m82/`
- [x] **m83** — Webhook delivery fairness and bounded per-workspace backlog (7 tasks) ← from `/pm-brainstorm for w4` 2026-08-17 + `w1/048` finding #3 — done 2026-08-17 (transactional 10,000-open-notification workspace bound with `0` disable; fair workspace-ranked `SKIP LOCKED` claims; low-cardinality overflow metrics + sustained-pressure alert; real-Postgres concurrency/race coverage), moved to `done/m83/`
- [x] **m84** — Durable pending custom-domain claims and DNS-TXT ownership (8 tasks) ← from `/pm-brainstorm for w4` 2026-08-17, promotes `w1/045` — done, moved to `done/m84/`
- [x] **m85** — Role-aware write controls across remaining dashboard editors (7 tasks) ← from `/pm-brainstorm for w4` 2026-08-18, promoting the remainder in `w9/048`; the dashboard work shipped as **w9/m87**, this folder is board closeout only — done, moved to `done/m85/`
- [x] **m86** — Build-toolchain freshness monitoring and digest refresh cadence (7 tasks) ← from `/pm-brainstorm for w4` 2026-08-18 + `w7/030`, after `w7/m85`
- [x] **m87** — Durable timing for replayed agent transcripts (7 tasks) ← from `/pm-brainstorm for w4` 2026-08-18, materializing `w3/015`; sequenced after `w5/m66`
- [x] **m88** — Platform dashboard observability: error logs + Loki ship (6 tasks) ← from live prod k9s/log investigation 2026-08-19 (silent `dashboard.bex.co` pod + shipper drops)
- [x] **m91** — Managed-datastore disk metrics show the logical (billed) capacity, not the physical PVC floor (7 tasks) ← from a live `/qa-find-bugs` hunt of `dashboard.bex.co`, 2026-09-02, journey 11. Two managed Postgres DBs with logical disks of 1 GB and 5 GB both showed Metrics → Disk → "Capacity 10 GiB" (the physical PVC — Hetzner's 10 GiB volume floor, corroborated by `w2/m86/t004`), so an 828 MiB/1 GB database (~81% full, near its autoscale/billing trigger) is drawn as ~8% of 10 GiB. Root cause `lego/backend/internal/metrics/source.go:441-442` sources `disk_capacity` from `kubelet_volume_stats_capacity_bytes` while Details, autoscaling, and billing use the logical `diskSizeGB`/`StorageGB`. Fix: the user-facing `disk_capacity` (REST/GraphQL/MCP + dashboard panel) reports the logical size; the ops `PersistentVolumeFillingUp` alert stays physical. Applies to Postgres + KeyValue. Shipped the metric in w3/m10 but never considered the logical-vs-physical split. — done 2026-09-02 (one source of truth via `tiers.{Postgres,Valkey}.EffectiveStorageGB` = `max(plan, spec.storageGB, status.allocatedStorageGB)`, which `DatabaseStorageHighWater`/Details also use so metric and page can't drift; flat config-shaped `disk_capacity` series like cpu_limit, no Prometheus source; compute-service disks folded in; REST/GraphQL/MCP pinned byte-identical; `disk` used-series unchanged; `PersistentVolumeFillingUp` kept physical with a new promtool guard proving a 1 GB DB on the 10 GiB floor doesn't fire it; ADR018 parity note; operator+backend+dashboard suites + `make lint` + promtool all green — live prod-datastore spot-checks not run this session, no cluster access), moved to `done/m91/`

## Historical execution order (2026-07-09 brainstorm)

Completed: **m7 → (m10 ∥ m11) → m13 → m12.** m7 gates m12 (invites need the courier) and closes a live lockout risk; m10's one-interception-point argument strengthens as w2/m4–m5 add write verbs; m11 before real tenants means no forced-enrollment migration; m12 stays gated on w1/m9 + m7.

- [x] **m107** — [Log history past the newest 100 lines is unreachable on GraphQL and MCP](done/m107/README.md) (8 tasks) ← from live `/qa-find-bugs` 2026-09-14 pass 37; done 2026-09-16 (REST next-page cursors followable; GraphQL/MCP return the Render envelope; dashboard pages on scroll-up + truncation notice; `get_postgres_logs`/`get_key_value_logs` stay bare by design — use `list_logs`)

## Inbox

- [x] **114** — [MCP cannot create an image-backed service at all: the schema requires two fields the server refuses](done/114.md) ← live `/qa-find-bugs` 2026-09-21 passes 118/119; `create_web_service` and `create_cron_job` required `buildCommand`/`startCommand` — inherited verbatim from Render's git-only tool — while the handler refuses both for a prebuilt image, so no value satisfied both and **all four image-backed service types** were unreachable over MCP while REST and GraphQL created them happily — done 2026-09-21 (`omitempty` on the two fields, on both tools; `runtime` stays required, so the divergence is exactly as wide as the image path needs. **The note's warning against this did not apply, and why is the point:** the requirement was never the schema's — `resolveBuildStrategy` refuses a native runtime without both commands, per runtime, on every surface, which is the conditional an `if/then` schema would have duplicated and then drifted from. The misleading "ignored for docker" descriptions now say "required for a native runtime, refused for runtime image", and both `runtime` descriptions list `image`. Pass-119's table is the test matrix, split where each half lives: four types create with no commands and still refuse them when supplied, the two tools no longer require the pair while still requiring `name`/`schedule`, and the git control fails closed in three shapes with a complete native create still succeeding. MCP parity classification is unaffected — the guard flags _added_ required fields, and relaxing one is not that)\n- [x] **120** — [`disconnectBlueprint` is a soft-detach, and re-creating the same repo+path revives the old row with its history](done/120.md) ← live `/qa-find-bugs` 2026-09-21 pass 132; three create→disconnect cycles returned the same id, and the third blueprint — seconds old, carrying the name just passed — reported `createdAt` two days earlier with nine inherited syncs, which the dashboard rendered as "Created 1d" — done 2026-09-21 (**decided on evidence: reviving is intended**, so `createdAt` becomes the connection's. `w8/m37` states the soft-detach deliberately, for a fencing reason, so hard-deleting the row would reverse that decision and destroy sync history doing it. `created_at` was absent from the upsert's `DO UPDATE SET` — the note had already pinned it to the line — and is now conditional on the prior row having been disconnected, because the same arm also serves a re-apply of a live row, which is not a new connection and keeps its date; telling the two apart needs the pre-upsert state, which a CTE reads in the same statement. The inherited history is **marked rather than presented as its own**: the admitting run records the reconnection on w4/m125's `note` column, putting the boundary where the inherited entries are read, and a first connection records nothing. Postgres-backed test for the date semantics plus a service-layer test for the note)\n- [x] **111** — [Deferred-verification record: production predates 2026-09-20, so `w4/m115` and `w4/m118` re-probes fail as deploy lag, not defects](done/111.md) ← live `/qa-find-bugs` passes 108 and 114, 2026-09-21; a record rather than work — two hunts had already spent a pass re-diagnosing behavior that is simply the old build, and `w4/m118`'s in particular reads as a major regression until you check the timestamps — closed 2026-09-21 as a record (no code change was requested by it). Two observations inside it are carried forward rather than lost: the `auditLogs`-reports-refusals-as-`success` question is a **product decision, not a bug** — the field is a binary authorization outcome by design, and the note's real point is that a forensic surface saying "the caller was allowed to try" in the words of "the thing happened", on a row whose `targetName` is null, is a weaker claim stated as a stronger one; and log indexing lags 60–90s, which will fool a tight probe into calling a log surface empty\n- [x] **118** — [`validateBlueprint` passes a manifest whose `fromDatabase` reference cannot resolve, and it has the workspace context to know better](done/118.md) ← live `/qa-find-bugs` 2026-09-21 pass 129, read-only; a manifest whose `fromDatabase` named a database existing nowhere returned `valid: true` with a clean plan, while the same manifest's dangling `fromService` — a same-file reference — was rejected with a precise message. The resolvers, their wording and their ambiguous-name case all existed already; they ran one step too late — done 2026-09-21 (`fromDatabase` and `fromService`→Key Value now resolve at validate through the exact call the apply path makes, so both errors arrive in apply's own wording and the two steps cannot develop two vocabularies for one refusal; a reference to a database that _does_ exist still validates, the deliberate superset over Render, pinned by its own test. **`fromGroup` stays deferred on a real constraint**, not an oversight: its check sits behind an uncached `can_view_sensitive` re-assert because a group link materializes secret values to workload code, and `validateBlueprint` is a `can_view` verb — running it there would refuse a viewer's validate or weaken the gate. The split is stated at the site and in ADR018, which the note required either way)\n- [x] **127** — [Every read of a service's activity feed errored: `w4/m112` added `stall_reason` to the outer SELECT and not to the CTE it reads from](done/127.md) ← found 2026-09-21 while running the Postgres-backed suite with a live database to verify `w4/117`; `ListServiceEvents` — the only query behind the Activity tab — failed outright with `column "stall_reason" does not exist` for every service on the new build, reproduced on unmodified `main` against a clean database. `serviceEventsQuery`'s `feed` CTE is four positional `UNION ALL` branches whose outer `SELECT` reads them by name, and m112 added the column to the outer SELECT and the by-id query but not to the CTE — done 2026-09-21 (`d.stall_reason` on the deploy-started branch, the only one that can carry it under m112's own rule that an ended row is terminal, and an empty string in the other three. `TestServiceEventsFeedBranchesAgree` is the database-free guard: every branch must select the same number of expressions and every name the outer SELECT reads must be an alias the first branch declares — verified by removing the fix, which reports both symptoms and names `stall_reason` exactly. **Nothing in `go test ./...` executes this SQL**, which is how it shipped; recorded in the note as a CI question rather than acted on)
- [x] **117** — [Reading one webhook endpoint returns blank latest-delivery fields that reading all of them populates](done/117.md) ← live `/qa-find-bugs` 2026-09-21 pass 124; `webhookEndpoint(id:)` reported `latestStatus ""` / `latestSentAt ""` on an endpoint with four successful deliveries while `webhookEndpoints()` returned `delivered` for the same endpoint **in the same request** — the latest-delivery columns came from a `LEFT JOIN LATERAL` the list query had inline and the three single-row reads did not — done 2026-09-21 (fixed structurally rather than three times: the lateral and its column expressions are named constants every endpoint read composes, and because a `RETURNING` clause cannot join, the enable/disable and update verbs feed a CTE the same lateral joins. The latest-less scanner had no callers left and was deleted, so a new read cannot pick the shape that started this. Proven in a Postgres-backed test — the divergence is entirely in SQL, `toView` was never wrong — verified both ways against a local Postgres: it reproduces the exact reported blanks from all three sites pre-fix and passes post-fix, and asserts the list answer is non-empty first so it cannot pass vacuously. The note's single-vs-list sweep found no second instance, so the fix stayed local)\n- [x] **116** — [Router key writes ignore their own availability gate and fail as `internal error`](done/116.md) ← live `/qa-find-bugs` 2026-09-21; `routerAvailable` returns false for a non-beta workspace and `createRouterKey` in the same workspace answered `internal error` — the three write verbs never consulted `Available`, so the upstream call failed and `ErrUnavailable` (an unwrapped sentinel) surfaced as a 500 where a named refusal belongs — done 2026-09-21 (one `requireEnabled` gate on the three writes **and on `Overview`**, which had the same hole the note suspected. The class was decided, not defaulted: `ErrNotEnabled` wraps `core.ErrForbidden` because this is a capability the caller does not have, while `ErrUnavailable` stays unwrapped and keeps reaching the caller as a 5xx, since an upstream outage genuinely is one — the two now say different things. The refusal names no workspace: a message that named the beta one, or that differed between "not you" and "no such feature", would be an existence oracle for it, and the test asserts both that and that the upstream is never called)\n- [x] **110** — [`deployActions` reports `rollback: allowed` on a service with nothing to roll back to](done/110.md) ← live `/qa-find-bugs` 2026-09-21 pass 112, journey 3; on a service whose only deploy was its live first one the projection returned `rollback: allowed` with no precondition while `POST …/rollback` answered `409 … already live — nothing to roll back to` for the only candidate — the dashboard gates the control a second way, so the exposure was to the CLI, MCP and anything binding affordances straight to the capability response — done 2026-09-21 (the vocabulary was not missing: `no_eligible_rollback_target` already existed and was already wired. The projection computed it with `RollbackEligible`, which the verb does not use alone — `Rollback` also refuses the live deploy when it is the running image, w4/051, which is exactly what a fresh service looks like. One shared `RollbackActionable(d, currentImage)` now answers for both, so the projection and the verb cannot drift. The narrowness is preserved and tested: once `spec.image` drifts off the still-live deploy, as a failed rollout leaves it, rolling back is a legitimate recovery and both sides report ready again)\n- [x] **125** — [GraphQL reports `maintenanceMode` on cron jobs and private services, and its `NonNull` wrapper means the `w6/m130` remedy cannot be applied as-is](done/125.md) ← live `/qa-find-bugs` 2026-09-21 pass 138, the `AppView` type-gate sweep `w4/124` called for; REST omits the key for every type but `web_service` (Render declares it solely on `webServiceDetails`) while GraphQL returned `{enabled:false, uri:""}` for a cron job and a private service — not "unset" but a claim about a mode the service cannot have — done 2026-09-21 (**decided: make the field nullable**, the choice the note left open, after checking the input it named — the dashboard already types `maintenanceMode` as `MaintenanceModeView | null`, reads it with `?.`, and renders the section only behind `isWebService`, so the breaking half breaks nobody here and the regenerated types loosen to `Maybe<MaintenanceMode>` with no source or test change. The w6/m130 value-emptying remedy genuinely cannot reach an object — emptying it still yields `{enabled:false, uri:""}` — so the schema is what had to change. Reasoning recorded at the resolver and in ADR018's Maintenance Mode row; covered across all five types in `public_url_test.go` alongside `w4/124`, as the note asked)
- [x] **124** — [GraphQL still returns a private service's cluster-internal address in `url` — the fourth instance of the family `w6/m130` closed for `renderSubdomainPolicy`](done/124.md) ← live `/qa-find-bugs` 2026-09-21 pass 137, journey 10; GraphQL `service.url` returned `http://qa-20260921-priv:8080` — the same value the payload already carried correctly in `internalAddress` — because REST gates `url` at its emission site while GraphQL reads `AppView`, which `view()` filled unconditionally from `status.URL` — done 2026-09-21 (fixed in `view()` like `renderSubdomainPolicy` before it, but gated on the **scheme**, not the type: every public URL the operator publishes is `"https://"+host` and every internal fallback is `http://`, in two shapes. The audit the note asked for is why — a type gate would have fixed the reported case and missed the one beside it, since an **exposed** service whose platform subdomain is off with no custom domain takes the same no-host branch, so REST was leaking the internal URL for a `web_service` too. `internalAddress` still carries the address and `publicRoutingNotice` explains the absence; `Status.URLs` needed no change. `public_url_test.go` pins REST-omits/MCP-omits/GraphQL-null across all three shapes plus a control that a routed service still reports its URL)
- [x] **113** — [bex's MCP tells every agent it is operating a "Render workspace" — 180 of 188 tools, one constant](done/113.md) ← live `/qa-find-bugs` 2026-09-21 pass 117; `mcpWorkspaceIDDescription` (one unannotated constant, applied by `mcpWorkspaceMiddleware` to every tool not in `mcpCallerScopedTools`) described the caller's own workspace as "the Render workspace to use", so the only product-identity string in 180 of 188 tool schemas named a competitor's platform as the caller's own — done 2026-09-21 (the constant says "bex workspace" and now carries the comment it never had, stating the line: bex may **refer** to Render to explain compatibility and must never **call itself** Render. Checked rather than assumed that nothing held the wording in place — the pinned `render-mcp-tools.json` records which tools upstream registers, not their descriptions, and the string appears nowhere in it. Two guards in `internal/api/mcp_identity_test.go`: one fails if any tool names the caller's own resource as Render's, the other fails if the deliberate "Render's …" compatibility descriptions ever disappear — so the first cannot be satisfied by purging the word. Rule recorded in ADR018 beside the MCP parity paragraph)
- [x] **112** — [A machine token is granted `scope: "bex.read"` and then writes: the scope is echoed but never applies](done/112.md) ← live `/qa-find-bugs` 2026-09-21 pass 115; `client_credentials` against a workspace API key returned `{"scope":"bex.read"}` and that token performed a persisted write (a service's `maxShutdownDelaySeconds` 137 → 61, read back through a different credential) — not an authorization bypass, since a machine key is `CapabilityExempt` by the recorded design, but a credential advertising a restriction the platform never promised to honor — done 2026-09-21 (minted API-key clients now carry **no** scope allowlist, so asking for `bex.read` is refused naming the scope instead of granting an inert one; omitting `scope` stays the supported call. **This reverses `w4/m105`'s choice and had to be reconciled, not ignored:** m105 set the allowlist because discovery advertised scopes the client could not request, and its own DoD permitted either resolution — "requesting the advertised scopes succeeds, **or** discovery stops advertising them to this client class" — so this is m105's second branch, taken once the first turned out to issue a promise nothing keeps. Blast radius checked first as the note required: no first-party client requests `bex.*` on the machine path. Migration gap stated in ADR012 — keys minted earlier keep their allowlist until reminted, the `w4/076` shape)
- [x] **100** — [Events feed calls a Settings-edit deploy "Environment updated" while the deploys list calls it "Config Change"](done/100.md) ← live `/qa-find-bugs` 2026-09-19 pass 20; same `config_change` deploy, two trigger labels — the backend maps every config write onto the `envUpdated` flag (`events/service.go:727-731`) and the events feed renders the flag literally — done 2026-09-21 (events now says "Config Change", matching the deploys list; the backend flag was deliberately not split)
- [x] **104** — [Events feed calls a deploy-hook deploy "Manual deploy" while the deploys list calls it "Deploy Hook"](done/104.md) ← live `/qa-find-bugs` 2026-09-19 pass 40; same `deploy_hook` deploy, two trigger labels — the backend maps hook triggers onto the `manual` flag (`events/service.go:732`) and the events feed renders the flag literally; sibling of `100`, different trigger + flag — done 2026-09-21 (Render's vocabulary has no hook flag, confirmed against its OpenAPI, so bex added an additive `deployHook` one; `manual` is now exclusively the API trigger rather than true alongside, since leaving it set would move the false statement rather than remove it)
- [x] **101** — [Blueprint detail's Managed Resources names are plain text — nothing links to the resource](done/101.md) ← live `/qa-find-bugs` 2026-09-19 pass 28; the name cell renders `{r.name}` (`blueprints.$blueprintId.tsx:467-471`) while `r.id` sits unused beside it and no type→route helper exists — done 2026-09-21 (a `blueprintResourceHref` helper maps the backend's closed type set onto the routes, deferring the five service types to the existing `serviceBaseForType` rather than re-stating the static-site rule; unknown types and id-less rows render plain text, never a broken href)
- [x] **102** — [Any unknown `?type=` crashes `/services/new` to the error boundary instead of falling back](done/102.md) ← live `/qa-find-bugs` 2026-09-19; `serviceTypeCreateCopy` defaults only on nullish (`create-context.ts:74-79`) and the route `head` resolver reads the raw unvalidated `match.search?.type` (`services.new.tsx:63-67`), so `?type=worker` throws on `.titleKey` while `?type=background_worker` renders fine — done 2026-09-21 (the helper is now total — any non-member string resolves to the default through the existing type guard — so the "unknown values dropped" contract holds at both call sites and the router's raw-vs-validated question stops mattering)
- [x] **103** — [Cancel dialog promises "the last successful deploy remains live" when nothing is live yet](done/103.md) ← live `/qa-find-bugs` 2026-09-19 pass 30; `deploy-actions.tsx:218-222` renders the single `services.eventsCancelConfirmBody` string for every cancel with no first-deploy branch — done 2026-09-21 (a first deploy now gets its own sentence naming that nothing is serving; threaded from the deploy's trigger at all three call sites, with an absent trigger falling back to the original, less alarming copy. A later deploy on a service whose first build failed still gets the generic sentence — named in the note, since no call site holds a has-live-release signal)
- [x] **105** — [Service create with an inaccessible repo URL succeeds; the build then fails with git's misleading auth error](done/105.md) ← live `/qa-find-bugs` 2026-09-20 pass 86 (w4-targeted run, API-only, `muse.env` credentials); `ValidateRepo`'s only production caller is the source-edit path (`apps/service.go:3556-3560`), so create accepts a 404 repo and the clone fails 2/2 with `could not read Username` instead of the edit path's clear 400 — done 2026-09-21 (`create` now calls `ValidateRepo` under the edit path's own guard shape, before any write and after the duplicate-name check, so one gate covers REST/GraphQL/MCP. **The note's open question is answered:** the blueprint path reaches `createFromStack`, not `create()`, so it does NOT inherit the gate — left that way deliberately, since failing a whole multi-service sync on one GitHub hiccup is a different blast radius from failing one interactive create)
- [x] **106** — [Activity calls an image deploy a "First build"; the Deploys surfaces call the same deploy "First Deploy"](done/106.md) ← live `/qa-find-bugs` 2026-09-21 pass 106; an `image`-runtime service has no build stage, so the events label asserted a step that never ran — done 2026-09-21 (label only, the Render-named `firstBuild` flag stays on the wire; shipped alongside `100`/`104`, which corrected the two other labels in the very same `triggerKey` function, so its whole vocabulary now matches the Deploys tab)
- [x] **107** — [The Postgres page tells you to "Enable a pooler" and the dashboard cannot enable one](done/107.md) ← live `/qa-find-bugs` 2026-09-21 pass 108; the pooler write exists on REST alone (`w2/024` excluded it from GraphQL on parity grounds without weighing that the dashboard IS a GraphQL client), so the panel's only content was an instruction it could not carry out — done 2026-09-21 taking **option (b)**, the note's own recommendation: the copy now names `PATCH /v1/postgres/{id} {"pooler": true}`, the shape `databases.recoveryDisabled` already uses for a feature the page cannot turn on. **Option (a), a real pooler toggle, is a product call and is yours to ask for** — ~2h across GraphQL + dashboard + tests, milestone-sized, and additive (the `updateDatabase*` siblings already establish the per-field mutation shape). Same class as `m121`; a third instance would justify sweeping the pattern
- [x] **108** — [A revealed multi-line secret file renders as one line](done/108.md) ← live `/qa-find-bugs` 2026-09-21 pass 109; `white-space: normal` collapsed every newline, so a 20-line PEM revealed as one run-on line on the single screen where a user can check a secret they cannot see anywhere else — done 2026-09-21 (`whitespace-pre-wrap` on the shared `SensitiveViewItem`, which the env-group editors import, so both secret-file surfaces and multi-line env values are covered by the one change; bounded `max-h-48 overflow-auto` so a 40-line key scrolls inside its row instead of pushing the page)
- [x] **076** — [Pre-`w4/m105` API keys cannot request advertised scopes until reminted](done/076.md) ← residual from `w4/m105/t004`; keys minted before m105 have an empty Hydra `scope` and get `invalid_scope` if the caller passes `scope=`. Omitting `scope` still works; remint or admin-patch to get a scoped exchange
  > Inbox `099` transferred to [w9/061](../w9/061.md) by user direction; scope includes oversized and non-divisor cron steps.
- [x] **098** — [Journey 10's cron half works end to end, and the one thing that looked broken was a modal I never confirmed](done/098.md) ← live `/qa-find-bugs` 2026-09-14 pass 46; a record, not work to do. Schedule validation, "Every 10 minutes · runs in UTC", confirm dialog, run `pending`→`successful` in 39s, and the job's own output in the logs. Scaling still offered on a cron job is `w4/m101` **deploy lag** (`de4945aec` postdates the build) — a fourth discriminator. **Carries a trap worth reading**: zero network requests after a click is not evidence the control is dead — check for an open modal first
- [x] **097** — [A private service gets no public URL and is not served at its slug host, verified against a web-service control](done/097.md) ← live `/qa-find-bugs` 2026-09-14 pass 45; a record, not work to do. Private `url` and `sshAddress` are both `""` while a web service read seconds later has both; the slug host 404s exactly as a never-created name does. **States its own weakness**: the service never reached Live (nginx:alpine on 80 vs the platform's `:3000` convention — my bad input), so the edge 404 is not by itself proof about a _running_ private service. Open question: no port control exists on the create form or in Settings
- [x] **096** — [The datastore reveal is fetch-gated end to end: no DOM residue, no cache shortcut, no web-storage persistence](done/096.md) ← live `/qa-find-bugs` 2026-09-14 pass 44; a record, not work to do. Closes `w4/095`'s open question. Reveal → **in-app** nav away → back re-gates, with Apollo's cache alive; a second reveal issues `DatabaseConnectionInfo` over the network again, so no cache shortcut past the server's fresh re-assertion; value absent from localStorage and sessionStorage. Same on Key Value. `m106`/`082`/`088` audited the server only — this is the client half
- [x] **095** — [The datastore reveal gate is real at the DOM level, and backups/PITR are healthy — plus three near-misses this pass caused by sampling too early](done/095.md) ← live `/qa-find-bugs` 2026-09-14 pass 43; a record, not work to do. The client-side half of the sensitive-scope work: the 64-char password and the credentialed URI are **absent from the DOM** before reveal, so the gate is fetch-on-demand not masking. Backups 24/24 daily, newest today, PITR enabled on all three DBs. **Carries a measurement lesson** — the dashboard renders progressively and two near-filings came from reading `innerText` once; a third was a UTC-vs-local misread
- [x] **094** — [Journey 9's second half is clean: first-match-wins holds on the wire, and ADR018's accepted extensionless-200 divergence is confirmed live with a proper control](done/094.md) ← live `/qa-find-bugs` 2026-09-14 pass 42; a record, not work to do. A rewrite listed before a redirect on the same source wins (200, no 301). The extensionless-miss 200 looked like rule over-matching; removing **every** route and re-testing proved it unconditional — it is `ADR018:82`'s accepted `w3/m46` divergence, **not a bug**. Third foreign-`qa-` sighting (`qa-20260914-priv`, `qa-20260914-probe`)
- [x] **093** — [Journey 9 verified against the served response: redirects and header rules work, the 404 gap is undeployed `w4/m101`, and the stall covers the operator image too](done/093.md) ← live `/qa-find-bugs` 2026-09-14 pass 41; a record, not work to do. `curl` shows `x-qa-marker` on the 200 and on the 301 and absent on the 404 — a three-way match with `m101`'s own pre-fix sentence, and `de4945aec` postdates the deployed build, so **deploy lag, not a regression**. Key new fact: that fix is in the **operator/static-server** image, so the stall is the whole pipeline — "nothing has redeployed since before `e6419af0b`" — which is why the three bex-api verifications are equally blocked
- [x] **092** — [The custom-domain DNS panel labels both records "Host" but gives one a relative label and the other a fully-qualified name](done/092.md) ← closed 2026-09-16: ownership Host is zone-relative `_bex-challenge` on REST/GraphQL/MCP + dashboard; verifier still LookupTXT FQDN; panel names the zone ← live `/qa-find-bugs` 2026-09-14 pass 40, journey 5 end to end on a `qa-` static site since deleted. TXT Host renders `_bex-challenge.example.com` (FQDN, `domains.go:84-86`) beside CNAME Host `qa-20260914` (relative, `dnsRecordFor:269-279`, whose own doc comment defines Name as "the subdomain label(s)"). Pasted verbatim into a provider that appends the zone, the TXT lands at `_bex-challenge.example.com.example.com` — traffic works, ownership never verifies, and Pending cannot be told from slow propagation. **Contains a correction to `w4/091`'s "no service has a custom domain"** — that was my bad GraphQL argument, not the product
- [x] **091** — [Journey 1 (project → environment → env group → delete) is clean; a second deploy-lag discriminator, and a `qa-` resource in the workspace that is not this harness's](done/091.md) ← live `/qa-find-bugs` 2026-09-14 pass 39; a record, not work to do. **`qa-20260914-cron` (`srv-dak1vca6m8ac739r6210`) was not created by this harness** — verify cleanup by **id**, never by `qa-` name match, and do not remove it. Adds a cheaper second deploy check: if the env-group page says "this service", the build predates `99126d5ad`. Journey 5 (custom domains) still uncovered and why
- [x] **090** — [When the month-to-date bandwidth query fails, the figure silently vanishes: `network-metrics-card.tsx:375` gates on nullity and never reads the error it is handed](done/090.md) (~30m) ← live `/qa-find-bugs` 2026-09-14 pass 38. Proven by failing only that one operation: control renders "133 GiB used this month", failing case renders nothing and no error word appears anywhere on the page. The hook returns `?? null` (not `0`) and `error` on purpose; the consumer discards it, so "unknown" and "none" look identical. The same card's **chart** half was fixed twice (`w1/m50`, `w9/m86`) — this is the text figure beside it. Blast radius **1 of 1**; `range-select.tsx:201` next door shows the correct convention
- [x] **089** — [A deploy superseded by a newer commit is reported as "Canceled" with no cause, on all three surfaces, while its build logs show it succeeding](done/089.md) (~1h) ← live `/qa-find-bugs` 2026-09-14 pass 36, on `beancount-cms-v2` (read-only). Proven by timestamps: the canceled row finishes **76 ms** before its successor starts (another pair, 25 ms), and `failureReason` is `""` on all. `reconciler.go:1166` names the cause in a comment and returns bare `DeployCanceled`; `deployCloseFailureReason:770-787` has no case for it. **Do not fix by stamping `failureReason`** — `w6/m52` deliberately removed that for user cancels, and `deploy-failure-reason.tsx:23` is hardcoded `text-destructive`, so a supersede would render as an error — **done**: `cancelReason` + `reasonCode=superseded`, neutral UI tone
- [x] **088** — [Per-surface authorization parity closes clean: no adapter reaches a laxer path than its peers, and the layering makes that divergence structurally impossible](done/088.md) ← live `/qa-find-bugs` 2026-09-14 pass 35; a record, not work to do. Closes `w4/087`'s largest named limit. Three probes: no adapter touches store/client/secret/authz directly (all **84** adapter files, including the **7** not named `rest.go`/`graphql.go`/`mcp.go`); all 60 unexported call targets are registrars, renderers, delegators or self-authorizing; `apps/disks.go` authorizes every verb before the store and orders the workspace check ahead of the id lookup, avoiding an existence oracle. Names the cheap follow-on: make the layering invariant a CI grep, since the guarantee rests on it
- [x] **087** — [`TestAuthzGuardsEveryVerb` walks 325 verbs but its sanity floor is 19, and it skips verbs by name across 62 `core.Base` method names](done/087.md) (~20m) ← live `/qa-find-bugs` 2026-09-14 pass 34. Measured by a reverted `t.Logf` on the sweep: the floor both reflection sweeps share sits 17× below the real walk, so a filter regression dropping 94% of verbs still passes — and `isVerbMethod` skips by name, so a service verb colliding with any of 62 `core.Base` names would vanish from the authz **and** audit guarantees (no collision today; checked). Fix is a pinned exact count, not a higher floor. Also records the audit's structural conclusion: bex's MCP is hash-pinned against upstream Render, but bex's own three surfaces are pinned against **each other** by nothing — the hole that let `w4/083` ship
- [x] **086** — [Verb-parity closure for `w4/083`: across 15 resource families, `resume_keyvalue` is the sole genuine gap, and five look-alikes are not](done/086.md) ← live `/qa-find-bugs` 2026-09-14 pass 33; a record, not work to do. Settles the completeness question 083 left open and raises its priority (single exception, not one of many). Documents the four mechanisms that manufacture false gaps — consolidation into `update_*`/create verbs, nested GraphQL fields, data returned by a sibling tool, and case-sensitive regex misses — so the next audit does not re-file them
- [x] **085** — [`scripts/qa-login.sh` never revokes the session it mints, and the QA loop has buried Active Sessions under 122 live `curl/8.7.1` sessions](done/085.md) (~30m) ← live `/qa-find-bugs` 2026-09-14 pass 32. Closed 2026-09-15: `--logout` + companion jar + Phase 8 cleanup; existing 122 left for the operator
- [x] **084** — [All five `RevokeIconButton` callers name their destructive control with a bare verb, so every multi-row list exposes N identical "Sign out" / "Revoke" buttons](done/084.md) (~45m) ← live `/qa-find-bugs` 2026-09-14 pass 32; the same shape `w4/m101/t014` fixed for `CopyButton` and `t013` for env rows, now at `revoke-icon-button.tsx:36` with **5 of 5** callers passing a bare verb and no correct in-repo sibling to copy. Live on `/settings`: 122 × `button "Sign out"` and 2 × `button "Revoke"` in the accessibility tree, with device/location/last-active already rendered beside each one. Closed 2026-09-15: named `aria-label`s at all five call sites.
- [x] **083** — [MCP can suspend a Key Value but cannot resume one: `suspend_keyvalue` ships without a `resume_keyvalue`](done/083.md) (~20m) ← live `/qa-find-bugs` 2026-09-14 pass 31, verified against the deployed `tools/list` (186 tools; no `/^resume_key/` match). REST and GraphQL both have the pair, as do `suspend_postgres`/`resume_postgres` and `suspend_service`/`resume_service` on MCP itself; `keyvalue/service.go:522` `Resume` already exists. Landed via `cb052ca07` (`w6/m37`), a confirmation-phrase milestone that only framed the destructive direction. KV **delete** stays deliberately absent per m37 — do not widen this
- [x] **082** — [Half the scope-matrix's Sensitive/Mint overrides have no fail-closed heuristic: 29 of 59 downgrade silently with the whole guard suite green](done/082.md) (~45m interim fix) ← live `/qa-find-bugs` 2026-09-14 pass 30. Every Sensitive/Mint class is a hand-maintained `scopeClassOverrides` entry; only `TestMintAndSensitiveHeuristics` fails closed, and its literals cover the REST spellings far better than GraphQL camelCase or MCP snake_case. Verified by a reverted mutation sweep over all 59 entries — `GQL Query.envVars` and `MCP get_env_var` go Sensitive→Read, and `Query.deployHook`/`get_deploy_hook` go Mint→Read, with all three guard tests green
- [x] **081** — [Resolver-level closure of `w4/m106`'s audit: the nested-sensitive registry is complete, and its depth cap is unreachable](done/081.md) ← live `/qa-find-bugs` 2026-09-14 pass 29; a record, not work to do. Only three context-attached data readers exist and exactly two nested resolvers return a secret, matching `36457c620`'s registry; the walk resists aliasing and fragments; and its fail-open depth cap of 64 is unreachable behind `gqlMaxDepth = 15`
- [x] **080** — [Coverage record: the credential-word sweep pass 26's audit could not do — tickets are clean, closing that audit's named limit](done/080.md) ← live `/qa-find-bugs` 2026-09-14 pass 28; a record, not work to do. `AgentSession.ticket` sits under read-classified roots but is empty on reads (minted only by `attachAgentSession`), and `ShellSession.ticket` is already sensitive — so `w4/m106`'s "exactly two fields" blast radius survives a second, differently-shaped sweep
- [x] **079** — [`createWebhookEndpoint` mints a show-once signing secret but is classified as an ordinary write](done/079.md) (~30m) ← live `/qa-find-bugs` 2026-09-14 pass 27, from the Mutation-side audit `w4/m106` asked for; the milestone's own fix (`36457c620`) closed the nested-field gap and did not touch this. `createApiKey`/`createDatabaseUser`/`regenerateDeployHook`/`Query.deployHook` are all `OpClassMint`; this one is `OpClassWrite` while returning a 38-char secret shown once
- [x] **078** — [`w4/m100` and `w4/m105` are closed, but production still serves the pre-fix build, so m100's load-bearing bullet is still unverified live](done/078.md) (~20m re-probe) ← live `/qa-find-bugs` 2026-09-14 pass 24; a post-merge probe reproduces the pre-fix shed exactly (5 × 429 of 25, identical to pass 8) because the deploy has not landed — diagnosed from the old `maxInflight/8` arithmetic plus the sibling m105 dashboard fix also being absent. Carries the re-probe steps, including the anti-amplification and bearer controls that must be re-checked alongside
- [x] **077** — [Coverage record: metrics swept clean; MCP exercised for the first time and confirms `w4/070` and `w4/071` on the third surface](done/077.md) ← live `/qa-find-bugs` 2026-09-14 pass 23; a record, not work to do. MCP is reachable from a browser session (186 tools) and agrees with REST/GraphQL on both the unattributable members and the bare-SQLSTATE collapse, so those notes' "traced in code" lines are now measurements; metrics correct across ranges, surface parity, empty states, aggregation and percentage gating
- [x] **075** — [Coverage record: the env-group half of journey 4 verified end to end across four state transitions, including service-over-group precedence — no findings](done/075.md) ← live `/qa-find-bugs` 2026-09-13 pass 22; a record, not work to do. Link → value in the container, service var overrides the group, removing the override restores it, unlink removes it — each with a fresh pod
- [x] **074** — [Deferred-verification record: `w4/050`'s memory-percentage fix is live on prod, and `dev-4` is unreachable here so the paid-gated journeys stay blocked](done/074.md) ← live `/qa-find-bugs` 2026-09-13 pass 21; a record, not work to do. Closes `w4/050`'s deferred re-probe (no memory series exceeds 100% over 12h, vs 153% before) and states plainly that Docker is absent so `mock-cluster.sh`/`dev-4` cannot run — which blocks Shell, background workers, private services and blueprint apply
- [x] **073** — [The Deploys tab never shows a deploy that starts while you are watching: the header polls `Deploys(limit:1)`, the list's `limit:20` array never refetches](done/073.md) (~45m) ← live `/qa-find-bugs` 2026-09-13 pass 17, journey 3; a hook-fired deploy was narrated Building → Deploying → Live by the header while the list stayed at 4 rows for 96s, because `use-deploys.ts:92-96` has no `pollInterval` and Apollo keys the same document's result per variable set. The mirror image of `w6/m46/t005`, which fixed the header; existing rows do still advance, so statuses update and arrivals do not
- [x] **072** — [A deploy event can never say who triggered it: the `deploys` row has no caller, so the service Events feed drops the actor the audit log keeps](done/072.md) (~1h, sequence with 069) ← live `/qa-find-bugs` 2026-09-13 pass 16, journey 3; three deploys triggered three different ways all report `actor: ""`/`triggeredByUser: ""` because `EventSourceDeploy` never sets them and `deploys` has no caller column, while `audit_events` does. Also the first live end-to-end rollback in the loop's record — it works, and `w4/051`'s fix is confirmed in production
- [x] **071** — [The SQL console answers every query mistake with a bare SQLSTATE code, and the redaction it inherits has no sink left to protect](done/071.md) (~1h) ← live `/qa-find-bugs` 2026-09-13 pass 15, journey 11; `postgres/query.go:380` collapses every unclassified Postgres error while the two classified classes return clear prose, and `audit_events` has no column the message could leak into. Also closes `w4/m95` t007's and `w4/m91`'s deferred live re-probes with evidence
- [x] **070** — [Two active workspace members are unattributable on every surface, because the Kratos lookup miss is silently discarded](done/070.md) (~1h) ← live `/qa-find-bugs` 2026-09-13 pass 14; `members/service.go:426` drops the `ok` bool, `status` is hardcoded `"active"`, and the Team row renders the `own-` fallback in the same typography as a resolved email
- [x] **069** — [The audit log and API-keys table still show a raw Kratos subject where webhooks show an email](done/069.md) (~45m) ← live `/qa-find-bugs` 2026-09-13 pass 12; `use-webhook-creator.ts` is already generic and used on one of three surfaces
- [x] **068** — [QA coverage record: deploy hook, logs filters and free-tier sleep/wake all verified correct (no findings)](done/068.md) ← live `/qa-find-bugs` 2026-09-13; a record of what was exercised, not work to do
- [x] **067** — [An environment group's own page says its secret files are "mounted into this service"](done/067.md) ← live `/qa-find-bugs` 2026-09-13
- [x] **066** — [Key Value `persistenceMode` is invisible and unchangeable in the dashboard, though `w6/m127` made it updatable](done/066.md) ← live `/qa-find-bugs` 2026-09-13
- [x] **065** — [Blueprint "Managed Resources" tells a synced blueprint to sync](done/065.md) ← live `/qa-find-bugs` 2026-09-13
- [x] **064** — [Fix oversized static-site purge Job names](done/064.md) (55m) ← live CLI QA; deleted fixture remains terminating until operator fix

- [x] **063** — [Correct Bex CLI configuration guidance and command references in nested help](done/063.md) (55m) ← live CLI QA, 2026-09-11; reproduced in installed v0.2.1 and HEAD

> **061** transferred to [w7/044](../w7/044.md) on 2026-09-09 by user-approved `/pm-brainstorm for w7 for top 3 customer-impactfully work` #3; archived in `done/061.md`.

The earlier instance-identity findings **059** + **060** were absorbed into [w5/m87](../w5/done/m87/README.md) by user approval; their original evidence is preserved in [done/059.md](done/059.md) and [done/060.md](done/060.md). Their move recorded promotion; implementation and verification are recorded in that milestone. The earlier open alias note (filed as colliding `055`) closed as **056**; notes 052–055 remain done.

- [x] **056** — `logLabelValues` (+ datastore log aliases) inverted-range guard must live in shared `LogQuery.validate`, not only `QueryLogs` — already satisfied by w4/054's `ValidateQueryRange` placement; alias regression pins added to `TestInvalidQueryRangesAcrossSurfaces`. Renumbered from open `055` (collided with CLI `done/055`); closed 2026-09-07, see `done/056.md`.
- [x] **055** — CLI health-check updates for Dockerfile services — already fixed by `cca2f571d` and extended by `d58245084`; verified current runtime/read/PATCH tests. Renumbered from the README-only 052 collision; closed 2026-09-07, see `done/055.md`.
- [x] **054** — Inverted/equal log ranges — fixed in shared validation and before live-stream headers; direct App/DB/KV reads plus REST/GraphQL/MCP checks pass. Done 2026-09-07, see `done/054.md`.
- [x] **053** — Metrics input-validation errors — classified as public bad requests, including datastore siblings and invalid/defaulted ranges; upstream errors stay redacted. Done 2026-09-07, see `done/053.md`.
- [x] **052** — Malformed service IDs — invalid Kubernetes label/path lookups skipped; legacy names, authz, and real List-error behavior preserved. Corrected scope excludes DB/KV. Done 2026-09-07, see `done/052.md`.

- [x] **051** — Deploy detail page offered "Rollback" on the current live deploy (restarts the service + redundant no-op deploy; list/detail disagreed) ← live QA `$qa-find-bugs w4` 2026-09-07 — done 2026-09-07 (backend `Rollback` refuses the live target as a 409 only when it's a true no-op — `target.ResolvedImage == spec.image` — so a still-live last-good deploy whose image drifted after a failed deploy stays a valid recovery target; the note's "reject any live" would have broken `TestRollbackRestoresPreviousLiveImage`. Dashboard `isRollbackableDeployStatus` drops `live` → deactivated-only, fixing the detail page and the events feed (a third `DeployActions` surface with a `|| status === "live"` guard) to match the list. ADR018 parity row updated; new backend + frontend tests; backend `go test ./...` + lint and dashboard suite (3041) green; live re-probe deferred), moved to `done/051.md`

- [x] **050** — Metrics memory chart shows usage above the plan's stated limit (up to 153%) ← live QA `$qa-find-bugs w4` 2026-09-07 — done 2026-09-07 (diagnosed live on prod: the pod IS enforced at its plan's 512 MiB limit — Guaranteed QoS, 108 OOMKills/exitCode 137 — so the note's "not constrained" premise and hypotheses (a)/(b) are all disproved. The real bug: `promResourceQueryFor`'s `sum by (pod)(container_memory_working_set_bytes{container!=""})` **double-counts cAdvisor's overlapping old+new container instances across an OOM restart** (prod: single-instance peak 508.9 MiB, per-pod instance count reaching 2, `sum by(pod)` peak 743 MiB ≈ the phantom 145–153%). Fixed by `max by (pod, container)` dedup on the memory + cpu series in `source.go`, validated against live prod Prometheus (fixed peak 508.9 MiB ≈ 99%, never over limit); `instance_count` untouched; test locks the deduped queries; ADR010 documents it. Backend `go test ./...` + lint green; no operator/dashboard change; bex-api redeploy pending for prod charts to reflect it), moved to `done/050.md`

- [x] **049** — Workspace-wide Search palette collapses every service to a generic "Service" label + Globe icon, ignoring `service.type` (sibling entrypoint of 047) ← continuous live `$qa-find-bugs w4` 2026-09-06 — done 2026-09-07 (Cmd-K palette derives each service result's label + icon from the shared `deriveServiceType`/`SERVICE_TYPE_LABEL`/`SERVICE_TYPE_ICON` helpers, dropping the hardcoded `Globe2`; fuzzy value keeps the generic token and adds the specific type words; shipped with 047; live re-probe deferred), moved to `done/049.md`

- [x] **048** — Billing/usage page never surfaces `usage.coverage`, presenting a partial/degraded metering estimate as authoritative ← continuous live `$qa-find-bugs w4` 2026-09-06 — done 2026-09-07 (added `coverage { state through degradedSources }` to the `Usage` query + typed `Coverage` through `use-usage`; `ChargesCard` renders an amber "Partial data" caveat naming the coverage boundary + degraded meters, mirroring the metrics degraded badge, only when the estimate is genuinely not complete and there's something to disclose; dashboard-only, `coverage` already served; en/zh strings + hook mapping + 4 caveat tests; live re-probe deferred), moved to `done/048.md`

- [x] **047** — Projects/Overview resource-list Type column collapses every service type to a generic "Service" badge ← continuous live `$qa-find-bugs w4` 2026-09-06 — done 2026-09-07 (resource-table service rows delegate to the shared `ServiceTypeBadge`, so the Type column shows Web/Private/Worker/Cron/Static matching the detail header and Render; database/keyvalue/envgroup keep their generic kind; shipped with 049; live re-probe deferred), moved to `done/047.md`

- [x] **046** — Key Value eviction-policy selector is blank for underscore API values ← live QA pass 5 — done 2026-09-07 (new `maxmemoryPolicyToUi` helper maps bex-api's underscored read onto the UI's hyphen option vocabulary at the single `use-set-key-value-maxmemory-policy` hook boundary — `noeviction`/empty/unrecognized pass through untouched — so the detail-page selector shows the saved policy instead of blank; save/wire spelling and the generic Select unchanged; unit table + real-hook wire integration tests; live re-probe deferred), moved to `done/046.md`

- [x] **045** — Language bootstrap throws during ordinary service navigation — retired: fixed concurrently by `4de8ac4db`; moved to `done/045.md` (live post-deploy check pending)
- [x] **044** — Give Settings' Idle timeout dropdown an accessible name (20m) ← repeated live `$qa-find-bugs` hunt 2026-09-05, second run — done 2026-09-05; resolution and verification in `done/044.md`
- [x] **042** — Give the environment-group service selector a persistent accessible name (20m) ← repeated live `/qa-find-bugs` passes 2026-09-05 — done 2026-09-05; resolution and verification in `done/042.md`
- [x] **043** — Name Manual Scaling's instance controls and check the shared CPU/memory consumers (40m) ← repeated live `/qa-find-bugs` passes 2026-09-05 — done 2026-09-05; resolution and verification in `done/043.md`
- [x] **041** — `POST /v1/projects` opaquely rejects the Render-contract `environments` field (strict-decode vs contract mismatch; major REST/Render-parity gap) ← live `/qa-find-bugs` hunt 2026-09-04 — done 2026-09-05; resolution and verification in `done/041.md`

_(`040` done 2026-09-03 — Generate Blueprint now accepts `envGroupIds` on REST/GraphQL/MCP, emits root `envVarGroups` + per-service `fromGroup` for selected groups (keys as `generateValue`), and degrades unselected linked groups to `sync: false` keys; dashboard Generate dialog gained env-group multi-select; note moved to `done/`.)_

_(`039` done 2026-09-03 — webhook event picker humanizes `service_moved` as "Service Moved" via `event-catalog.ts` SINGLES + en/zh locales; no longer raw under Other; note moved to `done/`.)_

_(`035` done 2026-09-03 — ADR018 (+ ADR029 and linked evidence/runbook wording) now states shared hosting remains enabled under `#PSL`, not the outage-causing unset-base-domain remediation; note moved to `done/`.)_

_(`038` done 2026-09-03 — `GET /v1/projects` without `ownerId` now returns `400 {"error":"bad request: invalid query parameter \"ownerId\""}` instead of the opaque bare `bad request`, so a REST/CLI caller learns which param bex requires — matching the validator-gated siblings and the deliberately-parallel `GET /v1/environments`. One-line handler change in `lego/backend/internal/projects/rest.go:79` (`fmt.Errorf("%w: invalid query parameter %q", core.ErrBadRequest, "ownerId")`) plus a `rest_test.go` assertion that the missing-`ownerId` 400 body names `ownerId`. The intentional required-`ownerId` divergence from Render is untouched (that parity question stays with `w6/m126`); GraphQL/MCP need no change. `go test ./internal/projects/` + `go vet` + backend `go build ./...` green; note moved to `done/`.)_

_(`037` done 2026-08-29 — `/workspace/settings` now omits the Danger Zone navigation link, loading skeleton, and Delete Workspace card when the caller has only one workspace; two-or-more-workspace behavior is unchanged; a `local-bex` browser walk verified desktop and 390px-mobile states after deleting from two workspaces to one; focused coverage plus the full dashboard typecheck, lint, and 2,839-test suite are green; note moved to `done/`.)_

_(`036` done 2026-08-22 — `deployTriggerKey` (`dashboard/src/features/deploys/lib/deploy-status.ts`) gained the missing `new_commit` case + `deploys.triggerNewCommit` en/zh locale entries, so the Deploys list/detail "Trigger" column now renders "new commit" instead of leaking the raw enum as "New_commit"; the Duration column header's mid-word wrap was fixed alongside it (`whitespace-nowrap`); added `deployTriggerKey` unit coverage (previously untested) plus a `new_commit` case in the list page's trigger-humanization test; full dashboard suite (348 files / 2377 tests), typecheck, and lint all green — note moved to `done/`.)_

_(`034` done 2026-08-18 — the live-log tail revalidation watchdog shipped (`BEX_LOG_STREAM_REVALIDATE_INTERVAL`, default 1m, `AuthorizeAppFresh(can_view_logs)`); note moved to `done/`.)_

_(`030` promoted to **m37** 2026-08-16; `031` and `033` deleted in the same triage, see the note below.)_

_(`018.md` promoted to **m28**, `019.md` promoted to **m29**, `022.md` promoted to **m30**, and `025.md` promoted to **m32**; `021.md` — round-16's "finish the orphaned m23" note — done alongside m23's own closeout; `020.md` done — the webauthn-stub `mfaEnabled` fix shipped, `kratos.go` now inspects credential contents; `023.md` promoted to **m31**; `024.md` re-owned by `w10/002` after the w10/m4/t004 attempt hit the missing-`RENDER_API_KEY` blocker; `026.md` done 2026-07-15 — the two stale evidence docs it flagged rewritten as shipped behavior (ADR018:209 CLI-login row, owners-api.md:156 error-envelope paragraph); `027.md` done — `GetApp`'s three collision loops now call `resourceWorkspaceFor` directly, closing the same redundant-resolution shape `AuthorizeApp` had before m30; `028.md` done — `core.KeyedRateLimiter[K]` now backs all three (`api.RateLimiter`, `cliauth.DeviceRateLimiter`, `deploys.DeployHookRateLimiter`): notes moved to `done/` (026–028 confirmed and folded in 2026-08-16, alongside **m82**).)_

> Inbox triage 2026-08-16 (user decision): `030.md` (`bex upgrade` self-update) promoted to **m37** — its gate (m34 passive notice + m35 signed-asset pipeline + Homebrew tap) is now met, so every prerequisite is live and only the consuming command remains. `031.md` (remote provider catalog + drift probe + `bex code doctor`) and `033.md` (additional `@` mention types) were **deleted** in the same triage rather than kept parked: both are milestone-sized, explicitly gated on a product decision that hasn't been made (`031` important-not-urgent with the `--model <id>` escape valve already shipped; `033` "promote only on explicit product intent", each new mention type needing its own data source), and both are recoverable from git history if that intent later materializes.

> `029.md` (codex-security round-4 P2/P3 register) and `032.md` (release-staleness cron + tag ruleset) grouped into **m82** 2026-08-16 (a full-board `/goal` triage session) and shipped the same day — notes moved to `done/`, each annotated item by item.

> `013.md` (audit-log `direction`), `014.md` (`suspenders` array), `015.md` (fallback-loop audit serialization), and `017.md` (Environment `ipAllowList` wire shape) were grouped into **m23** 2026-07-15 and the implementation shipped the same day (a parallel session worked the notes directly; the two closures merged — m23's t001–t005 + t007 are done, see each note's resolution in `done/`): `013` implemented Render's `backward`/`forward` enum end-to-end (mirrored keyset cursor, named 400 on unknown values, real-Postgres-verified); `014` emits `["user"]`/`[]` across REST/GraphQL/MCP via one derivation helper; `015` resolved as its shape 2 — per-candidate denied audits capped at `core.maxFallbackCandidateAudits` (3) + one aggregate denial row against the caller's workspace, bounding both worst-case latency and the one-request→N-inserts amplification while keeping recording synchronous; `017` gave the standard environments POST/PATCH Render's full body (`{cidrBlock, description}` objects + `protectedStatus`/`networkIsolationEnabled`, per-field-partial PATCH, no orphan row on a 400) — notes moved to `done/`.

> `016.md` (`GET /v1/users`, Render's `whoami`) closed 2026-07-15 — route shipped in `dfff3034`; the "empty email" follow-up turned out to be missing harness config, not a resolution bug: wired `BEX_KRATOS_ADMIN_URL` into the dev-9/dev-4 harnesses and verified the populated case live (checklist row ◐ → ✅) — note moved to `done/`.

> `001.md` (renumbered `012.md` in `done/` — its number collided with the long-done original `001`) and `011.md` grouped into **m20** 2026-07-14; notes moved to `done/`.

> `002.md` (MFA) promoted to **m11** 2026-07-08; `004.md` (credential hygiene) promoted to **m13** 2026-07-09; `003.md` (GitHub social login) implemented directly 2026-07-11 (Kratos `oidc` + Dex e2e, docs/ADR012-auth.md §10); `007.md` (Audit Log IA placement) promoted to **m15** 2026-07-12; `010.md` promoted to **m17** with `009.md` folded in as its aal2-altitude task, and `006.md` (session management) folded into **m18** as its active-sessions task, 2026-07-12 — notes moved to `done/`.

> Inbox `057` transferred to [w9/059](../w9/done/059.md) on 2026-09-07 by user approval; fixed alongside 058 on 2026-09-08.

- [x] **058** — Reject blank environment renames; direct environment creates and sibling project names are guarded too. Closed 2026-09-08; see [done/058.md](done/058.md).

- [x] **121** — [Expose the immutable Blueprint service name](done/121.md) — shared detail header shows it when different from the display label; explicit query/mapping and suffixed-slug regression verified.

- [x] **122** — [Scope GraphQL sandbox reads and termination to the selected workspace](done/122.md) — added by-ID query and ownerId bindings, adapter and composed OAuth/MCP coverage, and complete generated sandbox schema.

- [x] **123** — [Enforce disk constraints before plan, scaling, and autoscaling writes](done/123.md) — shared preview guard; all three API surfaces preserve stored intent on refusal.

- [x] **126** — [Host suggestion investigation](done/126.md) — extra hosts confirmed as documented hand-applied Ingresses outside the App spec; proposed status/discovery fix withdrawn, QA record retained.

- [x] **128** — [Reach a registry credential in the workspace it was created in](done/128.md) — every by-id verb on GraphQL and REST now binds an optional `ownerId`; MCP was already correct, GraphQL's `serviceEvent(id:)` gained the same selector, and `clitelemetry` turned out to be a write path rather than an instance. The dashboard half is completed in [m134](done/m134/README.md) and [140](done/140.md).

- [x] **m134** — [Reach workspace-scoped credentials and event details](done/m134/README.md) (7 tasks) ← promoted from 128; REST/GraphQL selectors, dashboard propagation, and workspace authorization coverage.

- [x] **140** — [Registry credential dashboard workspace selection](done/140.md) — completed within m134; concurrent filing retained.

- [x] **129** — [Tombstone every usage row whose resource is gone](done/129.md) — `deleted` no longer depends on a retained name happening to exist; per-kind live enumerations claim deletion only when they answered, and sandboxes get a real liveness signal from the compute meter's phase cursor instead of a label that resolves for reaped sandboxes by design.

- [x] **130** — [QA re-probe record for m122, m126 and m127](done/130.md) — record-only; the three milestones' deferred live DoD probes all pass against the deployed fix, so nothing was left to implement.

- [x] **131** — [Answer `latestDeployId` as null on reads](done/131.md) — third member of the `url`/`maintenanceMode` family; `""` claimed "no deploy" for services with real history, and the create path still returns the id.

- [x] **132** — [Make suspend/resume projections read the resource's state](done/132.md) — all three lifecycle projections gained the state check the cron verbs already had, with a new `not_suspended` term wired through the dashboard's vocabulary and locales; the dashboard turned out to gate by a second route, so exposure was CLI/MCP/mobile only.

- [x] **133** — [Keep unsaved membership drafts across Manage resources tabs](done/133.md) — the four drafts moved to the form that survives tab changes and `ResourceChecklist` became controlled; reopen still reseeds from persisted membership, and a poll tick no longer stomps a draft. Live browser re-probe still owed.

- [x] **134** — [Name the project and environment rename fields](done/134.md) — visible labels on both rename dialogs, the Settings card heading associated with its editable and read-only inputs, and the Radix missing-description warning opted out of at the two callers; the three existing tests moved onto name-based queries. Live accessibility-tree re-probe still owed.
- [x] **m135** — [Keep usage attribution after resource deletion](done/m135/README.md) (6 tasks) ← promoted from 129; collector name retention and independent lifecycle evidence.

- [x] **135** — [Give CIDR editor rows a stable identity](done/135.md) — the row key was the editable value, so one keystroke remounted the row and dropped focus; rows now carry an editor-local id kept out of the wire shape, with sequential-keystroke regression coverage. Live browser re-probe still owed.

- [x] **136** — [Adopt the accepted rows after a static edge-rule save](done/136.md) — the setters now return the canonical rows from the refetch they were already discarding, so a whitespace-normalized save no longer leaves the editor permanently dirty; a later draft typed mid-flight is preserved. Live re-probe still owed.

- [x] **137** — [Let a blank Command actually clear the cron override](done/137.md) — the hook rewrote an explicit `""` into `null`, so clearing the field saved "successfully" and kept the old command; the caller now distinguishes keep/replace/clear, and a commandless reschedule still sends `null` so its authorization path is unchanged. Live re-probe still owed.

- [x] **138** — [Refresh cron Recent Runs while the page stays open](done/138.md) — page 1 now polls on the shared visible-tab cadence with the loaded tail held outside the cache entry, so a poll cannot truncate it; adds last-good retention on a failed refresh and drops a tail the refreshed head no longer joins rather than hiding a run mid-history. Live re-probe still owed.

- [x] **139** — [Re-key restored environment drafts through the live allocator](done/139.md) — a restored row and a newly added one could share an id, and `updateRow` acts on every matching row, so one edit rewrote two and one delete removed two; restoration now re-keys through this mount's counter, preserving every other field. Live re-probe still owed.

- [ ] **142** — [Custom-domain copy ignores the pending state: "without at least one custom domain" while one is listed, and a delete that removes an Ingress that never existed](142.md) ← live `/qa-find-bugs` 2026-09-25 pass 162, journey 5. The subdomain guard requires a verified domain, but its message says any domain and leaks `RenderSubdomainPolicy`. The delete dialog is state-blind.
