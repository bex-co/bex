---
id: w4/m130/t008
title: Closeout — close the milestone once the definition of done actually holds
worker: worker4
status: todo
estimate: 15m
depends_on: [w4/m130/t007]
---

## Objective

Close the milestone only once its DoD is observably true against the deployed build.

## Context

- Every DoD bullet is a live probe against `https://dashboard.bex.co`. The deployed build lags `main`.
- `w4/done/111.md` records the cheap precondition: introspect for a symbol the fix added before re-probing, so a pass is never spent re-diagnosing the old build. After t003, `EnvironmentPatchResult.revision` is exactly such a symbol — its presence in the deployed schema proves the build carries this work.
- `w4/130` is the precedent for the honest alternative: record owed bullets rather than closing over them.

## Steps

1. Introspect the deployed schema for `EnvironmentPatchResult.revision` before probing.
2. Run the 20-iteration loop live and record the error count.
3. Run the stale-revision probes and assert no write.
4. If the walk cannot be run, record which bullets are owed rather than closing.
5. Set every task done, move task files to `done/`, mark the table, set `**Status:** done`, move `w4/m130/` to `w4/done/m130/`, check the box in `.pm/w4/README.md`.

## Files

- `.pm/w4/m130/**`
- `.pm/w4/README.md`

## Acceptance criteria

- [ ] The deployed build is confirmed to carry the change before probing.
- [ ] The 20-iteration loop is run live with zero errors, or the bullet is recorded as owed.
- [ ] The milestone is moved and every status field agrees.

## Out of scope

- New work found during the walk — that is a new board item.
