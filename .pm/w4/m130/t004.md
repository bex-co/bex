---
id: w4/m130/t004
title: Carry the fix across REST and MCP, which expose the same expectedEnvRevision parameter
worker: worker4
status: todo
estimate: 30m
depends_on: [w4/m130/t002]
---

## Objective

Confirm the defect and the fix reach every caller of the CAS protocol, not just the GraphQL mutation this was found through.

## Context

- MCP advertises the parameter in its own schema (`secrets/mcp.go:101`): _"optional opaque revision from list_env_vars or get_env_var; when set, exactly one ordinary env value update is allowed"_ — so an agent is actively invited to use the path that currently misreports.
- REST exposes the same patch (`secrets/rest.go`), and `w1/m147` records the verb's three aliases: REST, GraphQL `patchServiceEnvironment`, and the MCP tool.
- Only GraphQL was probed in pass 158; REST and MCP are **unverified** for both the defect and the fix.

## Steps

1. Enumerate every entrypoint into `patchEnvironment*` and give the count.
2. Run the 20-iteration loop through REST and through MCP.
3. Confirm the error shape matches per surface — a conflict must be a 409-class answer on REST and a named error on MCP, not a generic failure.
4. Check the MCP schema description still matches the behavior after t003 adds the returned revision.

## Files

- `lego/backend/internal/secrets/rest.go`, `mcp.go`

## Acceptance criteria

- [ ] Every entrypoint enumerated with a count.
- [ ] The loop is clean on REST and MCP, not only GraphQL.
- [ ] Conflict error shapes are correct and consistent per surface.

## Out of scope

- The dashboard editor's retry behavior (t005).
