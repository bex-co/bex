---
id: w4/m130/t006
title: Simplify — /simplify over the code this milestone changed
worker: worker4
status: todo
estimate: 25m
depends_on: [w4/m130/t005]
---

## Objective

Run `/simplify` over the changed code, behavior-preserving.

## Context

- `patchEnvironmentCAS` and `patchEnvironmentSparse` share validation and result construction; if t002 adds a third path or duplicates the compare logic, that is the first candidate.
- The compensation helpers (`envUpdateRestored`, `envRestorationFailed`, `compensateEnvironment`) are adjacent and may become reachable or unreachable as a result of t002 — check rather than assume.

## Steps

1. Run `/simplify` over the changed files.
2. Apply only behavior-preserving changes.
3. Flag any helper t002 made unreachable rather than deleting it silently.

## Files

- `lego/backend/internal/secrets/batch.go`, `store.go`

## Acceptance criteria

- [ ] `/simplify` run, findings applied or declined with a reason.
- [ ] t007's suite passes unchanged.

## Out of scope

- Restructuring the two patch protocols into one.
