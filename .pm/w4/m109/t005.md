---
id: w4/m109/t005
title: Simplify the code this milestone touched
worker: worker4
status: todo
estimate: 20m
depends_on: [w4/m109/t004]
---

## Objective

Run `/simplify` over this milestone's diff — reuse, simplification, efficiency, altitude — behavior-preserving only.

## Context

- `internal/projects/rest.go`: `toRenderProject` gains a resolved-owner parameter in `t002`; check the call sites do not each re-derive what the batch already resolved.
- `internal/projects/service.go` and `internal/api/server.go`: the `Owners` wiring should match the three sibling services exactly, not invent a fourth variant of the same wiring.
- If `t003` produced an owner-block assertion helper, confirm all four families use the one helper rather than four near-copies.

## Steps

1. Run `/simplify` over the diff.
2. Apply the behavior-preserving findings; defer anything else to a new task.
3. Re-run `make lint` (all four modules).

## Acceptance criteria

- [ ] `make lint` passes with no new findings.
- [ ] The project owner wiring is recognizably the same pattern as apps/postgres/key-value.
- [ ] No behavior change: tests green before and after with no test edits.
