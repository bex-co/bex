---
id: w4/m122/t003
title: Do the same for `apps.AddDomain` / `apps.DeleteDomain`, which fire on refused calls today
worker: worker4
status: todo
estimate: 35m
depends_on: [w4/m122/t002]
---

## Objective

A refused add must not report an add. Live on 2026-09-21, `addCustomDomain("block-eden-mono.onbex.co")` was refused `400 … is a reserved platform hostname` and still produced a `custom_domain_added` row on a service that never held that domain.

## Context

- `lego/backend/internal/events/service.go:339-340` — `apps.AddDomain → custom_domain_added`, `apps.DeleteDomain → custom_domain_removed`.
- **Do not silently change the known pairing asymmetry.** `.pm/w7/done/050.md:259-260` records that one apex add mints two `custom_domain_added` events (apex + auto-paired `www`) while the cascading delete mints one `custom_domain_removed`, and traces it to the same per-verb minting. That behavior is documented and was deliberately not filed. If this task's change alters it in either direction, say so explicitly and update that record; if it does not, say that too.
- The no-op add (an existing pending claim re-added, which returned 200 live) is the case t001's decision has to cover — verify it here against the real code path.

## Steps

1. Apply t001's mechanism to both verbs.
2. Re-run the refused-add probe and confirm zero events.
3. Re-run the apex add/delete pairing from `w7/050` and record the before/after counts.
4. Confirm a no-op re-add mints nothing.

## Files

- `lego/backend/internal/events/service.go`
- `lego/backend/internal/apps/domains.go`

## Acceptance criteria

- [ ] A reserved-hostname add produces no event.
- [ ] A real add produces one event per claim actually created.
- [ ] A no-op re-add produces none.
- [ ] The `w7/050` pairing counts are re-measured and the record updated or explicitly confirmed unchanged.

## Out of scope

- Changing the auto-pairing behavior itself.
