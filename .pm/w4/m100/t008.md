---
id: w4/m100/t008
title: Test coverage for the shipped behavior
worker: worker4
status: todo
estimate: 40m
depends_on: [w4/m100/t006]
---

## Objective

Close the coverage gaps t005 did not reach, asserting real behavior and failure modes. No tautologies, no snapshot-everything, no test that cannot fail.

## Context

t005 covers the two shared-code blast radii and the admission control cases. What remains:

- **Dashboard, t002:** a test over the documents one Metrics page load issues — asserting the count and that no operation+variables pair repeats. This is the assertion that keeps the fan-out from creeping back one chart at a time.
- **Dashboard, t003:** the three render paths — throttled with no cache, throttled with cache, and recovery after backoff — plus the pre-settle state on a cold load. Assert the throttle state is distinct from `METRICS_UNAVAILABLE` and from the empty state, since conflating them is the original defect.
- **Locale parity** for every new string (`locale-parity.test.ts`, plural shapes per `dashboard/CLAUDE.md`).
- **Backend, t001:** the new env knob's parse/default/`0`-disables behavior in `cmd/api/config_test.go`, following the table-test pattern `w1/070` established there.

## Steps

1. Write the tests above.
2. For each, confirm it fails against the pre-fix behavior — a test that passes before and after asserts nothing about this milestone.

## Files

- `dashboard/src/features/metrics/**/__tests__/`
- `dashboard/src/features/deploys/**/__tests__/`
- `lego/backend/cmd/api/config_test.go`

## Acceptance criteria

- [ ] Each new test is recorded here with the pre-fix behavior it fails against.
- [ ] Fan-out count and no-duplicate-operations asserted for one Metrics page load.
- [ ] Throttled-no-cache, throttled-with-cache, and recovery paths asserted, with the throttle state distinct from unavailable and empty.
- [ ] New env knob's defaults and disable path table-tested.
- [ ] Every new string present in both `en.ts` and `zh.ts`.
- [ ] All four suites green: operator `make test`, `cd lego/backend && go test ./...`, `make lint`, `dashboard/ yarn test`.

## Out of scope

- Coverage of surfaces this milestone did not change.
