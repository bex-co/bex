---
id: w4/m126/t006
title: Simplify (`/simplify` over the changed code)
worker: worker4
status: todo
estimate: 30m
depends_on: [w4/m126/t005]
---

## Objective

Run `/simplify` over the code this milestone changed, behavior-preserving.

## Context

- The likely duplication is the adapter-side `confirm` plumbing: one `core.WithConfirm` wrap per verb per surface is the shape that wants a shared helper rather than N copies.
- `w6/m19`'s own `/simplify` pass found and hoisted `core.WithConfirm`/`ConfirmFrom` out of a single-feature import chain; the same reviewer instinct applies to whatever this milestone adds around it.

## Steps

1. Run `/simplify` over the files t002–t004 touched.
2. Prefer one shared confirm-threading seam over per-verb copies.
3. Keep behavior identical; t007's tests are the check.

## Files

- Whatever t002–t004 changed.

## Acceptance criteria

- [ ] `/simplify` run; findings applied or declined with a reason.
- [ ] Backend and dashboard suites stay green.

## Out of scope

- Refactoring the protection mechanism itself.
