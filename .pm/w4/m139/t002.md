---
id: w4/m139/t002
title: Trace why coverage.through is pinned at the month start with every source degraded
worker: worker4
status: todo
estimate: 60m
depends_on: []
---

## Objective

Name the stream(s) holding `usage.coverage.through` at `2026-09-01T00:00:00Z` for workspace `tea-d98210cbbpdc73dcrkvg`, and the reason each lacks healthy evidence. Then fix it, or file the follow-up.

## Context

- `lego/backend/internal/store/usage.go` `CurrentUsageCoverage` / `aggregateUsageCoverage` / `evaluateStreamCoverage`: `through` = min `observedThrough` over active streams.
- Writers: `usage/service.go:833` (unavailable datastore health after a failed name capture) and `:1036`. `reconcileSourceInventory` (`:840`) closes streams for resources that no longer exist, so a deleted resource's stream that is never closed would pin forever. Check that case first, since this workspace deletes QA fixtures constantly.
- Needs production reads: bex-api logs matching `usage:` and rows of `usage_source_streams` / `usage_source_health` for the workspace. Neither was available to the filing hunt.

## Acceptance criteria

- [ ] The pinning stream(s) and cause are written here with the query and output.
- [ ] Fixed with a regression test (for example, a deleted resource's stream gets `expected_through` and stops dragging `through`), or a follow-up is filed with the evidence.
- [ ] Separates "health evidence missing" from "usage actually missing".
