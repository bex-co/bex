---
id: w4/m128/t005
title: Render parity check — owner-scoped route and the ADR018 Notifications row
worker: worker4
status: todo
estimate: 30m
depends_on: [w4/m128/t002, w4/m128/t003, w4/m128/t004]
---

## Objective

Check the change across REST, GraphQL, MCP and the dashboard, and make ADR018's Notifications row describe what bex actually implements.

## Context

- The row (ADR018 line ~232) is **✅ on all four surfaces** and describes the bex shape — "Member defaults: `GET`/`PATCH /v1/notification-settings`" — without recording that Render's equivalent is owner-scoped. Its sibling in the same row *is* at Render's exact path: "Service overrides (w3/m15): exact Render `GET`/`PATCH /v1/notification-settings/overrides/services/{serviceId}`". So one half matches Render's path and the other does not, and the row does not say so.
- The audit that last blessed this family (`w5/m59`, 2026-07-30) states its own limitation: *"the live Render + bex captures were infrastructure-blocked this session; deferred as note `w5/031`"*. It was code-based, so it could not have caught a path-shape divergence.
- **This hunt did not check live Render either.** The evidence for Render's owner-scoped route is an in-repo citation — `.pm/DO_NOT_DO.md` round 14, *"Render's owner-level `slackEnabled` on `PATCH /notification-settings/owners/{ownerId}`"* — dated 2026-07-15. Confirm it against Render's current public OpenAPI before changing the ledger cell; if it has moved, the fix changes shape.

## Steps

1. Confirm Render's current notification-settings routes from its public OpenAPI (or a live capture), and record the date.
2. Diff bex's routes, arguments and error shapes across REST, GraphQL and MCP for the new scope.
3. Update ADR018's Notifications row: the true path shape, and the divergence if one remains.
4. If a `w5/031`-style capture note is still open, close or update it with what this pass established.

## Files

- `lego/backend/internal/notifications/` (REST, GraphQL, MCP)
- `docs/ADR018-render-parity.md`
- `docs/render-artifacts/` (new or updated capture)

## Acceptance criteria

- [ ] Render's current route shape is captured and dated.
- [ ] The three APIs agree on argument name, position and refusal shape.
- [ ] The ADR018 Notifications row matches the implementation, including any deliberate divergence.

## Out of scope

- Slack delivery (recorded non-goal).
