---
id: w4/m128/t006
title: Simplify (`/simplify` over the changed code)
worker: worker4
status: todo
estimate: 30m
depends_on: [w4/m128/t005]
---

## Objective

Run `/simplify` over the code this milestone changed, behavior-preserving.

## Context

- If the workspace argument was threaded through three adapters, the `core.WithWorkspace` wrap is the duplication to look at — the same observation `w4/m126` t006 and `w4/m127` t005 both make about confirm-phrase plumbing. If all three milestones land, that seam is worth extracting once rather than three times.
- `GetSettings` and `UpdateSettings` share a tenant+identity resolution prologue that will grow with this change.

## Steps

1. Run `/simplify` over the files t002–t004 touched.
2. Prefer one shared workspace-resolution seam over per-verb copies.
3. Keep behavior identical; t007's tests are the check.

## Files

- Whatever t002–t004 changed.

## Acceptance criteria

- [ ] `/simplify` run; findings applied or declined with a reason.
- [ ] Backend and dashboard suites stay green.

## Out of scope

- Refactoring the notification fan-out.
