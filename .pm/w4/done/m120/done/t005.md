---
id: w4/m120/t005
title: Simplify
worker: worker4
status: done
estimate: 15m
depends_on: [t004]
---

## Objective

Review the t001–t004 diff for reuse and simplification per the `/simplify` checklist: one `manifestOwnedKeys`-style helper shared by reads and writes (not two key enumerations), no duplicated flag-threading across the three API adapters, no new env-read path that bypasses `readAuthorizedEnvMap`.

## Steps

1. Re-read the full diff; fold any duplicated spec.Env enumeration, flag mapping, or refusal construction into one helper each.
2. Verify no caller reads the store directly where the merged read should be used (grep `readMap(` / `GetVersioned` callers in `internal/secrets`).
3. Keep the diff minimal — no drive-by refactors.

## Acceptance

- One helper per concept; all env reads flow through the merged boundary; lint clean.
