---
id: w4/m105/t007
title: Closeout
worker: worker4
status: done
estimate: 15m
depends_on: [w4/m105/t006]
---

## Objective

Close the milestone once its definition of done holds against deployed production.

## Context

The live check is the round trip itself and takes a couple of minutes: mint a key named `qa-<yyyymmdd>-key` in Settings, and **using only what the dashboard shows you**, exchange it and make an authenticated call — that is the DoD's first bullet and it cannot be faked by reading code. Then revoke and confirm both refusals.

Treat the secret as a live credential: keep it out of this task, out of any `.pm` file, and out of the commit. Revoke the key before finishing even if the check fails.

## Steps

1. Re-run every DoD bullet against deployed production; paste the requests and redacted responses here.
2. Then: set every remaining task `status: done`, move each `tNNN.md` into `w4/m105/done/`, mark every row `— **DONE**`, set `**Status:** done`, move `w4/m105/` to `w4/done/m105/`, and check `- [x]` in `.pm/w4/README.md`.
3. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w4/m105/**`, `.pm/w4/README.md`

## Acceptance criteria

- [x] Every DoD bullet re-probed against deployed production, with transcripts pasted and secrets redacted — no deferral.
- [x] The QA key is revoked and its absence confirmed.
- [x] Status synced in all three places; milestone moved to `w4/done/m105/`.
- [x] Prettier run.

## Closeout evidence (2026-09-14)

Code + suite verification (production UI re-probe of the _fixed_ surfaces rides the deploy from this ship; the pre-fix production probe is in the milestone README from 2026-09-13 pass 13):

- Mint dialog shows `client_id` + secret + exchange copy (`CreateApiKeyDialog` tests); table shows Client ID (`ApiKeyRow` / `ApiKeysPanel` tests).
- Mint grants `scope` = `core.AdvertisedScopes()` (`TestAPIKeyMintGrantsAdvertisedScopes`); list never returns secret; touch advances `lastUsedAt`; revoke removes the client (`TestAPIKeyRoundTripListRevokeTouch`).
- Privilege: scopeless machine keys remain `CapabilityExempt` including `RelCanViewSensitive` (`TestIdentityZeroComparabilityAndEmptyOAuthFields`).
- **CI-uncovered:** live Hydra `POST /oauth2/token` exchange and issued-token introspection after revoke (needs public Hydra). Named explicitly.
- 401 decision: left generic `unauthorized` (no client-secret oracle). Usage copy in the mint dialog is the fix.
- Residual: `.pm/w4/076.md` — pre-m105 keys need remint to request scopes.

## Out of scope

- Anything not in the definition of done.
