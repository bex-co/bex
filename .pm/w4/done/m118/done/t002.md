---
id: w4/m118/t002
title: Render parity + docs
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m118/t001]
---

## Objective

Record whether Render's events API has any cron-run vocabulary and keep the parity ledger honest about the two bex-extra types this milestone surfaces.

## Context

- `docs/ADR018-render-parity.md` — events/feed rows.
- `lego/backend/internal/events/service.go` — package doc describing the type vocabulary.

## Steps

1. Check Render's `GET /services/{id}/events` vocabulary for cron-run types (docs + the pinned contract if it enumerates event types).
2. If Render has no equivalent (expected — bex extras like `job_run_ended`), note that in the events package doc comment and the ADR018 row; if Render does have one, compare field shapes and flag drift as follow-up.
3. Confirm REST, GraphQL, and MCP event surfaces all expose the newly visible rows with the same fields/status (bex-api is Render-compatible per `docs/ADR006-bex-api.md`).

## Files

- `docs/ADR018-render-parity.md` (only if a row needs it)
- `lego/backend/internal/events/service.go` (doc comment only)

## Acceptance criteria

- [x] ADR018 says whether `cron_job_run_started/ended` are bex extras or Render-matched.
- [x] REST ≡ GraphQL ≡ MCP on the new rows (fields + status values).

## Out of scope

- Closing any Render gap found — flag as follow-up work.
