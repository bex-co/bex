---
id: w4/m118/t004
title: Test coverage
worker: worker4
status: done
estimate: 40m
depends_on: [w4/m118/t003]
---

## Objective

Prove the feed change with behavioral tests: scheduled-run facts appear, filters work, manual actions don't double-emit, and webhook/push consumption is unchanged.

## Context

- Events service + store tests in `lego/backend/internal/events/` and `lego/backend/internal/store/` (follow existing feed-vocabulary test shapes).
- `lego/backend/internal/store/reconciler.go:452-487` — fact-writing rules (started needs `StartedAt`; ended needs `FinishedAt` + terminal status).

## Steps

1. Feed test: a scheduled run with `StartedAt` + terminal `FinishedAt` yields `cron_job_run_started` and `cron_job_run_ended` rows with the right status; a run with no `FinishedAt` yields started only.
2. Filter test: `type=cron_job_run_ended` returns fact rows (today: audit-only).
3. No-duplicate test: manual trigger + cancel still produce exactly one row each.
4. Regression pins: webhook mapping and push filter for both types unchanged (assert the mapping, not the delivery).

## Files

- `lego/backend/internal/events/*_test.go`
- `lego/backend/internal/store/*_test.go` (only if the join needed changes)

## Acceptance criteria

- [x] New tests fail on the pre-t001 code (spot-check by stashing) and pass after.
- [x] No trivial/tautological assertions; each test names the behavior it guards.

## Out of scope

- Dashboard tests (no dashboard change).
