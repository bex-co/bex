---
id: w4/m112/t005
title: Simplify
worker: worker4
status: done
estimate: 15m
depends_on: [w4/m112/t004]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Context

- Likely overlap: the new probe-diagnosis branch vs the existing Waiting-container cases in `stuckPodMessage` (shared message builder?); the new reason-code plumbing vs the existing `readiness_failed` path; the deploy-detail line vs the terminal reason lines.
- `/simplify` is quality-only: no behavior change, no bug hunting.

## Steps

1. Run `/simplify` scoped to the milestone's touched files.
2. Apply the fixes it confirms; keep the diff behavior-identical (operator + backend + dashboard suites green before and after).

## Files

- Whatever t001–t003 touched

## Acceptance criteria

- [x] `/simplify` pass applied or explicitly clean
- [x] All three suites + lint still green

## Out of scope

- Behavior changes; new features

## Result, 2026-09-18 — applied

Three reuse decisions taken while building, rather than duplications fixed
after:

- **The stall line reuses `DeployFailureReason`** — the one cause presentation
  every surface already shares (deploy header, deploys list row, events row),
  with its existing `neutral` tone. No new component, no new styling, and the
  three surfaces stay visually consistent by construction.
- **The diagnosis reuses `stuckPodMessage`'s call site**, appended as its last
  branch rather than wired as a second inspector: one pod List, one Ready
  condition write, and the precedence question ("a pod that is both
  crash-looping and unready reports the crash") answered by ordering instead
  of by a new rule. Pinned by `TestCrashLoopWinsOverProbeStall`.
- **`deployStallReason` shares `failureReasonFor`'s vocabulary** —
  `stallDiagnosis` lists exactly the Ready reasons that name an actionable
  defect, and `HealthCheckFailing` was added to `failureReasonFor`'s recognized
  set at the same time, so the sentence a user reads mid-stall and the one
  they read after the budget expires are the same sentence rather than two
  independently-maintained strings.

Behavior-identical elsewhere: operator `make test` (controller 84.5% coverage),
backend `go test ./...`, dashboard 3513 tests, and all four lint passes green.
