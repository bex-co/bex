---
id: w4/m112/t006
title: Test coverage
worker: worker4
status: done
estimate: 45m
depends_on: [w4/m112/t004]
---

## Objective

Add meaningful tests for the stall-signal behavior this milestone shipped. Tests must assert real behavior and failure modes; never game coverage with trivial, tautological, or snapshot-everything tests.

## Context

- Operator: extend the stall-message suite at the `stuckPodMessage` call site (fake Running-unready pod + failing probe → path-naming message; Waiting cases byte-identical).
- Backend: projection tests for the new reason (emitted while stalled, cleared on live/failed/canceled, never an outage fact — the w3/m78 exclusion control).
- Dashboard: deploy-detail tests (reason renders In Progress, clears on terminal states, both locales, no terminal-state drift).

## Steps

1. Prove each new test red pre-fix where the pre-fix code can express it (revert the diagnosis branch and watch the operator test fail; withhold the projection and watch the UI test fail).
2. Run the full gates: `make test` (operator), backend suite, dashboard suite + typecheck + lint.

## Files

- `lego/operator/internal/controller/*_test.go` (stall-message suite)
- `lego/backend/internal/events/*_test.go`, `lego/backend/internal/store/*_test.go` (projection + exclusion)
- `dashboard/src/features/deploys/components/__tests__/*`

## Acceptance criteria

- [x] New tests fail against the pre-fix behavior
- [x] Operator + backend + dashboard gates green

## Out of scope

- Trivial/snapshot-only tests
