---
id: w4/m119/t003
title: "Render parity: cross-surface check for the metrics changes"
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m119/t001, w4/m119/t002]
---

## Objective

Check that the metrics changes are consistent across every surface they expose:
REST, GraphQL, and MCP in `lego/backend/` (same fields, semantics, error
shapes — bex-api is meant to be Render-compatible, see
`docs/ADR006-bex-api.md`) and the `dashboard/` UI. Compare against the
equivalent render.com behavior/API and flag any drift as follow-up work rather
than silently diverging.

## Context

- The fix touches `http_requests` (t001) and possibly bandwidth (t002) — both
  read on REST, GraphQL, MCP, and the dashboard Metrics page.

## Steps

1. Same window, same fixture: compare REST, GraphQL, MCP values field by field.
2. Compare the dashboard Total Requests / Bandwidth rendering against the API
   numbers.
3. Compare against render.com's equivalent metrics behavior; file drift as
   follow-up, don't silently diverge.

## Files

- `lego/backend/internal/metrics/` (adapters)
- `dashboard/src/features/metrics/` (chart consumers)

## Acceptance criteria

- [x] REST, GraphQL, MCP return identical values for the same window.
- [x] Dashboard shows the API numbers (no separate math).
- [x] Render comparison recorded; drift filed or absent.

## Out of scope

- New product behavior — verification only.
