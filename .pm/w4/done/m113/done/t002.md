---
id: w4/m113/t002
title: Router-attributed http_latency for static sites
worker: worker4
status: done
estimate: 45m
depends_on: [t001]
---

## Objective

Same structural gap as t001, one metric over: the static-site Response Times chart reads "No data in range" because `http_latency` queries `traefik_service_request_duration_seconds_bucket` on the nonexistent per-App Service. Give it the t001 router treatment.

## Context

### The observation (production, 2026-09-17)

Same fixture as t001 (`srv-dam398js0ils73bgp0lg`, ~25 served requests): dashboard Response Times p90 "No data in range"; GraphQL `metrics(name: HTTP_LATENCY, quantile: 0.9)` → `[]`.

### Fix shape

- In `promQueryFor` (`lego/backend/internal/metrics/source.go:404-411`), the static branch queries Traefik's per-router duration buckets (`traefik_router_request_duration_seconds_bucket{router=~"<routers>"}`) through the same `histogram_quantile` + `sumRate` shape, preserving quantile/groupBy behavior.
- Verify the router series actually carry `le` buckets (Traefik emits router-level duration histograms when metrics are enabled — confirm against the deployed Prometheus rather than assuming); if they don't, the task becomes wiring the static-server's own latency instrumentation instead, and the estimate is wrong — re-plan rather than faking a series.
- Compute-service latency queries stay byte-identical.

### Acceptance

- Builder unit test mirrors t001's: static → router buckets, web → today's service buckets.
- No new dashboard work: the chart renders whatever series the verb returns (t005 covers the empty-state copy only if a residual gap remains).

## Files

- `lego/backend/internal/metrics/source.go` (`promQueryFor` latency branch)
- `lego/backend/internal/metrics/*_test.go`

## Acceptance criteria

- [x] Static `http_latency` queries select per-App router bucket series; compute queries unchanged
- [x] `cd lego/backend && go test ./...` + `make lint` green

## Out of scope

- Emitting new series from the static-server (only if Traefik lacks router buckets — re-plan, don't silently scope-creep)
