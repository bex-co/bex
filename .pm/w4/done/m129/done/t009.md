---
id: w4/m129/t009
title: Closeout — close the milestone once the definition of done actually holds
worker: worker4
status: done
estimate: 15m
depends_on: [w4/m129/t008]
---

## Objective

Close the milestone, but only once its definition of done is observably true — not merely once the code is written.

## Context

- Every DoD bullet is a live probe against `https://dashboard.bex.co`, in the shape pass 156 ran them. They need a deployed build, and the deployed build lags `main`.
- `w4/done/111.md` records the cheap precondition: introspect `Mutation` for a symbol the fix added before re-probing, so a re-probe is never spent re-diagnosing the old build.
- `w4/130` is the precedent for the honest alternative: if production access or the deployed build is not available, say the walk is owed rather than marking it done — and `m122`/`m126`/`m127` each carried that debt for three passes before it was paid.

## Steps

1. Confirm the deployed build carries this milestone's change before probing.
2. Walk each DoD bullet live, including the delete-and-recreate sequence and the never-used-name control.
3. If the walk cannot be run, record which bullets are owed, in `w4/130`'s wording, rather than closing over them.
4. Set every task `status: done`, move each `tNNN.md` to `done/`, mark every row `— **DONE**`, set `**Status:** done`, move `w4/m129/` to `w4/done/m129/`, and check the box in `.pm/w4/README.md`.

## Files

- `.pm/w4/m129/**`
- `.pm/w4/README.md`

## Acceptance criteria

- [ ] The deployed build is confirmed to carry the change before any probe.
- [ ] Each DoD bullet is walked live, or explicitly recorded as owed.
- [ ] The milestone is moved and every status field agrees.

## Out of scope

- Any new work found during the walk — that is a new board item.

## Blocker (2026-09-21)

The release pipeline must deploy this commit, then the production QA runner must repeat the milestone's live delete/recreate sequence, never-used-name control, refused/no-op idle-timeout saves, full-page by-id reads, and webhook delivery verification. These production observations cannot be claimed from local tests or an unshipped build. All implementation and local verification (t001–t008) are complete; no production fixture was created in this run.

## Production replay, pass 190 (2026-09-26, `/qa-find-bugs`, `muse.env` credentials)

The fix is deployed and the core bullets hold live. The fixtures were Free `ealen/echo-server` web services named `qa-20260926-reuse`, all deleted the same pass.

- **Re-created service starts clean: PASS.** Service A `srv-darqrmpsmc7s73cq5iog` had three accepted `setIdleTimeout` calls (300, 600, 900). Its feed was `deploy_ended, idle_timeout_changed ×3, deploy_started`. After A was deleted, B was created with the same name (`srv-darqs4gd0qnc73d79ucg`, `createdAt 2026-09-26T11:35:46Z`). `serviceEvents(serviceId:B, startTime:"2026-09-01T00:00:00Z")` returned **only** `deploy_started@11:35:46`: zero rows older than B's `createdAt`, and none of A's `idle_timeout_changed`.
- **Deploy-derived arm not regressed: PASS.** A's own `deploy_started`/`deploy_ended` were present for A before deletion.
- **By-id read agrees with the list: PASS.** Every id B's list returned resolved through `serviceEvent(id:)`.
- **Refused `setIdleTimeout` writes no event: PASS.** `idleTTLSeconds: 999999999` → `bad request: idleTTLSeconds must be 0-604800`, and the `idle_timeout_changed` count stayed at 3.
- **No-op `setIdleTimeout`: observed suppressed.** 900 → 900 added no row (count stayed 3). Confirm this matches the decision t004 recorded before closing.
- **Webhook bullet: not probed in pass 190** (`idle_timeout_changed` is not a webhook event type). **Closed in pass 191, below.**
- **`w4/m122` classification comment:** verified in pass 191, below.

## Closing walk, pass 191 (2026-09-26): every DoD bullet holds

- **Outbound webhooks carry the same guarantee: PASS.** An "All events" endpoint `qa-20260926-wh3` (`whk-darrcfhsmc7s73cq5j80` → `https://example.com/qa-bex-hook3`) watched static site A `qa-20260926-reuse2` (`srv-darrci8d0qnc73d79umg`) while its auto-deploy was toggled off and on. A's deliveries were `auto_deploy_disabled` ×2 and `auto_deploy_enabled` ×2 (attempts 1+2), plus its own build/deploy events. After A was deleted and B was created with the same name (`srv-darresod0qnc73d79v1g`), 90s of `webhookDeliveries(endpointId:)` showed B with **only** its own `build_started/build_ended/deploy_started/deploy_ended` (attempts 1+2): **0** `auto_deploy_*` deliveries for B, and **0** B deliveries sharing an `eventId` with any of A's. B's `serviceEvents` feed was the same four types.
- **`w4/m122` classification corrected: PASS.** `lego/backend/internal/events/service.go:320-328` now lists `apps.SetIdleTTL`, `apps.SetDisplayName`, and `deploys.RegenerateDeployHook` among the gated verbs and states the no-op decision ("Idle-timeout and display-name re-saves still repair projections but emit no event unless their value changed"), matching pass 190's observed suppression of the 900 → 900 re-save.
- All fixtures (A, B, and the webhook) were deleted in pass 191.

With pass 190's bullets (re-created service starts clean, deploy arm not regressed, by-id reads agree, refused write emits nothing, no-op suppressed), the Definition of done is met and the milestone is closed.
