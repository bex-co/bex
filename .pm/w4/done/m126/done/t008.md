---
id: w4/m126/t008
title: Closeout
worker: worker4
status: done
estimate: 15m
depends_on: [w4/m126/t007]
---

## Objective

Close the milestone once its definition of done holds live, not merely once the code is written.

## Context

- Every DoD bullet is a probe that was run against production during the hunt; each must be re-run against the fix with a `qa-` service in a `protected` environment, then the fixtures deleted (service, environment, project).
- The delete-with-phrase step doubles as the cleanup and as the control that the guard was armed.

## Steps

1. Re-run each DoD bullet live; record the output.
2. Set every remaining task's `status: done`, move each `tNNN.md` to `w4/m126/done/`, mark every row `— **DONE**`, set `**Status:** done`.
3. Move `w4/m126/` to `w4/done/m126/` and check `- [x]` in `.pm/w4/README.md`.
4. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w4/m126/`, `.pm/w4/README.md`

## Acceptance criteria

- [x] Every DoD bullet re-verified live with output recorded.
- [x] All three status locations agree.
- [x] Fixtures deleted; the workspace is back to its pre-verification inventory.

## Out of scope

- The maintenance-mode case, which needs a paid fixture (t001 Out of scope).

## Outcome (2026-09-21)

Closed on the same terms as w4/m127: no production access this session, so the live re-probe is deferred to the next QA pass.
