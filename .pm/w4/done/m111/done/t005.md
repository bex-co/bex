---
id: w4/m111/t005
title: Test coverage
worker: worker4
status: done
estimate: 45m
depends_on: [w4/m111/t003]
---

## Objective

Add meaningful tests for the scoped-create behavior this milestone shipped. Tests must assert real behavior and failure modes; never game coverage with trivial, tautological, or snapshot-everything tests.

## Context

- Existing suites to extend (not replace): `dashboard/src/features/env-groups/components/__tests__/new-env-group-dialog.test.tsx`, `dashboard/src/features/env-groups/hooks/__tests__/use-env-groups.test.ts`, `dashboard/src/features/services/components/__tests__/env-groups-panel.test.tsx`.
- Backend enforcement already has coverage (`scope_clone_test.go`, `ENV_GROUP_SERVICE_ENVIRONMENT_MISMATCH` paths) — this task is the dashboard wiring, which is where the bug lived.

## Steps

1. Dialog: in-env services uncheckable under Workspace scope; scope switch drops stale checks; picked scope reaches `createGroup` as `environmentId`.
2. Service panel: in-env service passes its environment as the dialog's initial scope; pre-check survives (no silent uncheck); workspace service passes workspace scope.
3. Hook: `createGroup` sends `environmentId` through to the mutation variables; omission preserves the old variables shape.
4. Prove each new test red pre-fix where the pre-fix code can express it (filter/picker tests) — at minimum assert the variables the pre-fix code never sent.

## Files

- `dashboard/src/features/env-groups/components/__tests__/new-env-group-dialog.test.tsx`
- `dashboard/src/features/env-groups/hooks/__tests__/use-env-groups.test.ts`
- `dashboard/src/features/services/components/__tests__/env-groups-panel.test.tsx`

## Acceptance criteria

- [x] New tests fail against the pre-fix dialog/hook behavior (or assert variables the old code never sent)
- [x] Full dashboard suite + typecheck + lint green

## Out of scope

- Backend tests (enforcement already covered); trivial/snapshot-only tests
