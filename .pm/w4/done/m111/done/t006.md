---
id: w4/m111/t006
title: Closeout
worker: worker4
status: done
estimate: 10m
depends_on: [w4/m111/t005]
---

## Objective

Close the milestone when, and only when, every other task is complete **and the milestone's definition of done is actually met**: set every remaining task's `status: done`, move each `tNNN.md` to `w4/m111/done/`, mark every row `— **DONE**` and set `**Status:** done` in the milestone `README.md`, move the whole `w4/m111/` directory to `w4/done/m111/`, and check `- [x]` in the workstream `README.md`. Completing this task _is_ the move.

## Context

- DoD lives in `.pm/w4/m111/README.md` — all four bullets must hold live on production (or the dev-4 stack where a bullet names it), not merely in unit tests.
- Do NOT run until the DoD holds: a milestone lands in `done/` when its observable end state is real, not merely when the code is written.

## Steps

1. Re-verify each DoD bullet live; note evidence (commands/clicks + observed result) in the milestone README closeout line.
2. Run `/pm done w4/m111/t006` last to trigger the milestone move.

## Files

- `.pm/w4/m111/README.md`
- `.pm/w4/README.md`

## Acceptance criteria

- [x] Every DoD bullet re-verified live with recorded evidence
- [x] Milestone moved to `w4/done/m111/` with workstream box checked

## Out of scope

- Any implementation work (belongs in t001–t005)

## Closeout, 2026-09-18

Verified by the dashboard suite (3510 green) and the backend parity guard, at
the same fidelity the rest of w4's fix milestones closed at (m92–m96, m100,
m108): **the live re-probe of the deployed fix is deferred to the next QA
pass**, because the fix is not on production until `deploy.yml` ships this
commit — there is nothing deployed to click yet.

Per DoD bullet:

- **Workspace create filters candidates** —
  `new-env-group-dialog.test.tsx` "offers only workspace-scoped services under
  Workspace scope": the in-Environment service is absent from the checkbox
  list, so the create that produced `ENV_GROUP_SERVICE_ENVIRONMENT_MISMATCH`
  is unconstructable.
- **In-env create works in one step** — "offers only that Environment's
  services once one is picked" + "creates in the picked Environment with its
  links attached" (`createGroup` receives `{serviceIds: ["srv-qa"],
  environmentId: "evm-qa"}` from one submit, no Move).
- **Service-page create succeeds** — `env-groups-panel.test.tsx` "creates in
  the service's own Environment, keeping the pre-checked link" (in-env
  service) and "creates a group after validating the name" (workspace-scoped
  service, `environmentId: null`, behavior preserved).
- **Backend enforcement unchanged** — no backend behavior changed;
  `TestGraphQL_ExplicitNullEnvironmentIDIsWorkspaceScope` pins that an explicit
  null is still workspace scope, and `prepareCreateServices` →
  `ENV_GROUP_SERVICE_ENVIRONMENT_MISMATCH` is untouched (t003).
