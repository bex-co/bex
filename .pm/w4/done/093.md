# 093 — Journey 9 verified against the served response: redirects and header rules work, the 404 gap is undeployed `w4/m101`, and the stall covers the operator image too

Why: this is the first evidence that the stuck deploy is the **whole pipeline** and not just the dashboard image — which changes what "blocked on the deploy" means for every queued item — and it records journey 9's live result so the 404 gap is not re-filed as a regression.

Record from the live `/qa-find-bugs` loop, 2026-09-14 pass 41. A `qa-` static site was created and **deleted** (`srv-dak30e26m8ac739r62n0`, verified gone by id). **No new defect found.**

## Journey 9, end to end against the served response

Static site from `octocat/Spoon-Knife`, publish dir `.`, live at `rev-1` in under 90 seconds. One header rule (`path=/*`, `X-Qa-Marker: qa-20260914c`) and one redirect (`/qa-old` → `/index.html`), both saved through the dashboard, both confirmed persisted by reload **and** by GraphQL:

```json
{"headers":[{"name":"X-Qa-Marker","path":"/*","value":"qa-20260914c"}],
 "routes":[{"type":"redirect","source":"/qa-old","destination":"/index.html"}]}
```

Then `curl` against the real edge:

| request | result |
| --- | --- |
| `GET /` | `HTTP/2 200` · `x-qa-marker: qa-20260914c` ✅ |
| `GET /qa-old` | `HTTP/2 301` · `location: /index.html` · `x-qa-marker: qa-20260914c` ✅ |
| `GET /nope-missing.html` | `HTTP/2 404` · **no `x-qa-marker`** ❌ |

So the rules reach the wire, the redirect resolves, and the ordering copy on the page is accurate ("Matched in order, first match wins. A redirect returns 301; a rewrite serves another path (an SPA fallback rewrites `/*` to `/index.html`)").

## The 404 gap is `w4/m101`, already fixed on `main`, not deployed

`w4/m101`'s definition of done named this exact case:

> **A `/*` header rule reaches a 404.** … Today it is present on the 200 **and on a 301 redirect** and absent on the 404.

What production does today is a **three-way match** with that pre-fix sentence — present on 200, present on 301, absent on 404. The fix is `de4945aec` "fix(platform): refuse unscalable autoscaling and **honor static error headers**", and it postdates the build production is serving:

```text
de4945aec  2026-09-14 00:26:41 -0700
e6419af0b  2026-09-13 23:43:27 -0700
git merge-base --is-ancestor de4945aec e6419af0b → NO — landed after the deployed build
```

Production is still *behind* `e6419af0b`. So this is **deploy lag, not a regression** (Phase 5 rule 4) and is deliberately not filed. Whoever verifies `m101`'s DoD must wait for the deploy; re-running the three curls above is the check.

## The stall is the whole pipeline, not just the dashboard

This matters more than the journey result. The two discriminators used so far both test the **dashboard** image:

1. Settings → API Keys has no Client ID column (passes 22–40)
2. The env-group page says "this service" (`w4/091`)

This one tests the **operator / static-server** image, which serves `*.onbex.co` and is built and shipped separately from the dashboard within `deploy.yml`. It is stale too. So the correct statement is no longer "the dashboard has not redeployed" but **"nothing has redeployed since before `e6419af0b`"** — which is why `w4/078`'s concurrency re-probe (bex-api), `w4/m105`'s scoped exchange (bex-api) and `w4/m106`'s live refusal test (bex-api) are all still blocked: they depend on a third image that is equally stuck.

A cheap operator-image check for future passes, needing no login: create nothing, and instead note that any static site with a `/*` header rule will show the header on 200 and not on 404 until `de4945aec` lands.

## Verified working, deliberately out of scope

- The save → reload → GraphQL round-trip is honest for both rule types; no lost input.
- Both editor pages have named controls (`Path`/`Name`/`Value`, `Type`/`Source`/`Destination`, `Add header`/`Add rule`, `Save headers`/`Save routes` correctly disabled until dirty).
- `Save` is gated on a real change, and the redirect `Type` combobox defaults to `Rewrite` with only the two valid options.

## Cleanup

`srv-dak30e26m8ac739r62n0` deleted; lookup by id returns `app not found`. **No `qa-`-named service remains in the workspace at all** — `qa-20260914-sf`, the foreign resource `w4/092` recorded, has also been removed by whoever owns it. Nothing of this harness's is live.

## Unverified

- **Only a `/*` header rule was tested.** A path-scoped rule (`/assets/*`), multiple rules, and rule precedence were not exercised, so "matched in order, first match wins" is read from the page's copy rather than demonstrated.
- **Rewrites were not tested at all** — only the redirect half of the Type enum. The SPA-fallback case the copy advertises (`/*` → `/index.html`) is unverified.
- **No custom domain was attached to this site**, so header rules were only checked on the `.onbex.co` host; whether they apply identically on a custom domain is untested.
- The 301's `location` is a relative path (`/index.html`); whether an absolute-URL destination behaves the same was not checked.
- The claim that the operator image ships separately from the dashboard within `deploy.yml` is read from the repo's build layout, **not** from a pipeline run — the durable evidence here is only that a static-server-side fix dated after the deployed commit is absent in production.
- Deploy status, **nineteenth consecutive check**: production still serves a build older than `e6419af0b`.

## Closed 2026-09-15 — already met

Coverage / journey record with no product defect to implement. Closed during `/loopx w4` drain.
