---
id: w4/m104/t006
title: Simplify pass over this milestone's changes
worker: worker4
status: done
estimate: 25m
depends_on: [w4/m104/t005]
---

## Objective

Run `/simplify` over the milestone's diff — behavior-preserving only.

## Context

Likely targets:

- t001's adapter mapping: if Render's `criteria{cpu,memory}` is translated into `targetCPUPercent`/`targetMemoryPercent` by hand in the handler, check whether that belongs beside `toRenderAutoscaling` in `render.go` so the request and response translations sit together.
- t002's mechanical check: if it grew into a script, see whether it reduces to a table test over the pinned schema; if it is already a test, make sure it is not duplicating `TestSurfaceParityAndWiring`'s job.
- t003's error path: one place that turns a rejection into a message, not a branch per route.

## Steps

1. Run `/simplify` over the diff.
2. Apply behavior-preserving findings; file design-laden ones as a w4 note.

## Files

- Whatever the milestone touched.

## Acceptance criteria

- [ ] `/simplify` run and each finding dispositioned, recorded here.
- [ ] `cd lego/backend && go test ./...` and `make lint` green.
- [ ] t001–t004's assertions unchanged.

## Out of scope

- New fixes surfaced by the pass — file them.
