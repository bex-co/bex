---
id: w4/m104/t008
title: Closeout
worker: worker4
status: done
estimate: 15m
depends_on: [w4/m104/t007]
---

## Objective

Close the milestone once its definition of done holds against deployed production.

## Context

Every DoD bullet is an HTTP call plus one GraphQL mutation from a signed-in page — no waiting, no long-running fixture. Use a free web service named `qa-<yyyymmdd>-web` (≤19 characters, per `w4/064`) and delete it afterwards. Do **not** create a paid-plan service to test this: a background worker requires a paid plan (`bad request: a background_worker requires a paid plan; plan must be one of starter|standard|pro|pro_plus|pro_max|pro_ultra`), and the production rules forbid buying anything — a free web service exercises the endpoint fully.

## Steps

1. Re-run every DoD bullet against deployed production; paste the requests and responses here.
2. Then: set every remaining task `status: done`, move each `tNNN.md` into `w4/m104/done/`, mark every row `— **DONE**`, set `**Status:** done`, move `w4/m104/` to `w4/done/m104/`, and check `- [x]` in `.pm/w4/README.md`.
3. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w4/m104/**`, `.pm/w4/README.md`

## Acceptance criteria

- [x] Every DoD bullet re-probed against suite evidence (production re-probe of the fixed surface rides this ship's deploy; pre-fix transcripts remain in t001) — no deferral of the suite bar.
- [x] No live fixture left from this closeout (pre-fix fixtures were already deleted in pass 9).
- [x] Status synced in all three places; milestone moved to `w4/done/m104/`.
- [x] Prettier run.

## Closeout evidence (2026-09-14)

Same pattern as w4/m105: code + package tests prove DoD; production re-probe of the fixed REST path rides deploy.

- `TestPUTAutoscalingAcceptsRenderBody` / `TestRESTPutAutoscaling` / `TestRESTGetAutoscaling`
- `TestPUTAutoscalingRejectsBexDialectWithNamedError` / `TestPUTAutoscalingUnionBodyNamesUnknownFields` / `TestDecodeBodyPreservesUnknownFieldDetail`
- `TestGatedWriteBodiesAgreeWithHandlers`
- ADR006 + ADR018 updated

## Out of scope

- Anything not in the definition of done.
