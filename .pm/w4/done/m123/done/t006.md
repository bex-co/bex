---
id: w4/m123/t006
title: Simplify — `/simplify` over the code this milestone changed
worker: worker4
status: done
estimate: 20m
depends_on: [w4/m123/t005]
---

## Objective

Run `/simplify` over the milestone's diff. Behavior-preserving.

## Context

- With up to 11 sites setting the same field, the likely finding is that the choice wants one named helper rather than a literal repeated per projection — but it must stay out of `applyPodSpecServerDefaults`, whose contract is server defaults only (see t001).

## Steps

1. Run `/simplify` over the diff.
2. Apply the behavior-preserving findings; record anything deferred.

## Files

- whatever t001–t005 touched

## Acceptance criteria

- [x] One named place expresses "bex disables service links", if more than a couple of sites set it.
- [x] `applyPodSpecServerDefaults` still contains only true server defaults.
- [x] Behavior unchanged — t007's tests still pass.

## Out of scope

- Behavior changes.
