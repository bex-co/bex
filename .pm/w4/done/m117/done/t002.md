---
id: w4/m117/t002
title: Render parity for service allow-list semantics
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m117/t001]
---

## Objective

The fixed enforcement matches render.com's service allow-list semantics (match rule, refusal shape, empty-list behavior), or the divergence is recorded with a reason.

## Context

- t001 changes enforcement, not the API shape — but semantics (who gets in, what deny looks like) are user-visible contract. Render documents service IP allow lists; compare match semantics (exact CIDR, family handling) and the deny response.

## Steps

1. Document Render's service allow-list behavior for the DoD matrix cases (listed v4/v6 admitted, non-listed denied, empty list open).
2. Verify bex matches post-fix; record any deliberate divergence in the relevant ADR (or the checklist) with a reason.

## Files

- `docs/ADR018-render-parity.md` (or the hosting/networking ADR that owns this), `docs/cli-compatibility-checklist.md` (`services update` row)

## Acceptance criteria

- [ ] Parity statement on the record: match, or a reasoned divergence.
- [ ] Checklist `services update` row reflects the verified enforcement.

## Out of scope

- API shape changes (none expected).
