---
id: w4/m109/t006
title: Test coverage for the shipped behavior
worker: worker4
status: done
estimate: 40m
depends_on: [w4/m109/t004]
---

## Objective

Cover the two fixed fields and their failure modes at the unit level, complementing `t003`'s conformance guards.

## Steps

1. **`updatedAt` (`t001`).** Table test over all seven project handlers: each mutating verb advances the timestamp; `POST` returns `updatedAt == createdAt`; a read does not move it. Assert ordering (`after > before`), never a fixed clock value.
2. **Owner resolution (`t002`).** Cases: resolvable owner → populated; unresolvable → the documented omitted shape, never `""`; resolver returns nothing for one id in a multi-project page → only that project is affected; control-plane store unavailable → the project still renders. Assert the batch is called once for a page of N.
3. **Regression cover for the siblings.** Services, postgres and key-value owner blocks are correct today — assert they stay correct, since `t002` touches shared wiring.
4. Keep every assertion behavioral: no snapshot-everything fixtures, no test that passes because a field is any string.

## Files

- `lego/backend/internal/projects/rest_test.go`, `projects_test.go`
- `lego/backend/internal/api/*_test.go`

## Acceptance criteria

- [ ] Each of the two findings has at least one test that fails against the pre-fix code.
- [ ] The N+1 guard fails if owner resolution moves inside the per-project loop.
- [ ] `cd lego/backend && go test ./...` passes.
- [ ] No tautological or mock-echoing assertions.
