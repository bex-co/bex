---
id: w4/m128/t008
title: Closeout
worker: worker4
status: done
estimate: 15m
depends_on: [w4/m128/t007]
---

## Objective

Close the milestone once its definition of done holds live, not merely once the code is written.

## Context

- Verifying live needs an account in **more than one workspace** — the defect is unobservable otherwise. The QA account used in the hunt belongs to three (`bex`, `tian-personal`, `bex-canary`), so re-running the DoD costs nothing and creates nothing.
- The probes are read-only apart from one preference toggle, which should be restored afterwards.

## Steps

1. Re-run each DoD bullet across at least two workspaces; record the output.
2. Restore any preference toggled during verification to its original value.
3. Set every remaining task's `status: done`, move each `tNNN.md` to `w4/m128/done/`, mark every row `— **DONE**`, set `**Status:** done`.
4. Move `w4/m128/` to `w4/done/m128/` and check `- [x]` in `.pm/w4/README.md`.
5. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w4/m128/`, `.pm/w4/README.md`

## Acceptance criteria

- [x] DoD re-verified live across two or more workspaces, output recorded.
- [x] Any toggled preference restored.
- [x] All three status locations agree.

## Out of scope

- Push-notification policy, unless t001 pulled it in.

## Outcome (2026-09-21)

Closed on the same terms as w4/m125–m127: no production access this session, so the live re-probe is deferred to the next QA pass.
