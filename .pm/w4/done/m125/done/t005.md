---
id: w4/m125/t005
title: Simplify (`/simplify` over the changed code)
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m125/t004]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency — without changing behavior.

## Context

- The likely duplication is between the new row-level conflict and the existing resource-level one in `apps/blueprint_ownership.go`: two conflict builders, two confirm-phrase parsers, and two preview-surfacing paths would be one too many of each.
- The claim-release diff in t002 may overlap with `resolveBlueprintResourcesFromIR`'s walk — both enumerate the applied resource set.

## Steps

1. Run `/simplify` over the files t001–t003 touched.
2. Prefer collapsing the two conflict paths onto one shape over keeping parallel copies.
3. Keep behavior identical; the test suite from t006 is the check.

## Files

- Whatever t001–t003 changed.

## Acceptance criteria

- [x] `/simplify` run and its findings either applied or explicitly declined with a reason.
- [x] No behavior change: the full backend and dashboard suites stay green.

## Out of scope

- Refactoring blueprint code this milestone did not touch.

## Outcome (2026-09-21)

One blueprintConnectionConflict resolver serves both the create guard and the preview entry, so the rule cannot drift between them; isTakeoverOnlyConflict is one testable predicate rather than inline string matching in the route.
