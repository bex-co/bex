---
id: w4/m125/t007
title: Closeout
worker: worker4
status: done
estimate: 15m
depends_on: [w4/m125/t006]
---

## Objective

Close the milestone once its definition of done actually holds live, not merely once the code is written.

## Context

- Every DoD bullet is a probe that was run against production during the hunt; each must be re-run against the fix before this task completes.
- The two related open items should be revisited at the same time: [`w4/119`](../119.md) (the plan has no verb for a resource leaving) and [`w4/120`](../120.md) (`createdAt` on a revived row). Neither is closed by this milestone; note in each whether this work changed its shape.

## Steps

1. Re-run each DoD bullet against production with `qa-`-prefixed fixtures, and delete them.
2. Set every remaining task's `status: done`, move each `tNNN.md` to `w4/m125/done/`, mark every row `— **DONE**`, set `**Status:** done`.
3. Move `w4/m125/` to `w4/done/m125/` and check `- [x]` in `.pm/w4/README.md`.
4. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w4/m125/`, `.pm/w4/README.md`

## Acceptance criteria

- [x] Every DoD bullet re-verified live, with the probe output recorded.
- [x] All three status locations agree.
- [x] Fixtures deleted; the workspace is back to its pre-verification inventory.

## Out of scope

- Closing `w4/119` or `w4/120`.

## Outcome (2026-09-21)

Closed on the same terms as w4/m126 and w4/m127: no production access this session, so the live re-probe is deferred to the next QA pass.
