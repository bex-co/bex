---
id: w4/m102/t008
title: Closeout
worker: worker4
status: done
estimate: 15m
depends_on: [w4/m102/t007]
---

## Objective

Close the milestone once its definition of done holds against deployed production.

## Context

Every DoD bullet is a `validateBlueprint` / `blueprintPreview` GraphQL call from a signed-in `dashboard.bex.co` page — no fixture to create, nothing to clean up, so there is no excuse to defer the live re-probe the way `w4/m92`–`m96` did.

The one bullet that needs judgement: "obeying the error converges" is a three-step walk. Run it by hand once, in order, and paste all three responses.

## Steps

1. Re-run every DoD bullet against deployed production and paste the responses into this task.
2. Then: set every remaining task `status: done`, move each `tNNN.md` into `w4/m102/done/`, mark every row `— **DONE**`, set `**Status:** done`, move `w4/m102/` to `w4/done/m102/`, and check `- [x]` in `.pm/w4/README.md`.
3. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w4/m102/**`, `.pm/w4/README.md`

## Acceptance criteria

- [ ] Every DoD bullet re-probed against deployed production, responses pasted — no deferral.
- [ ] The convergence walk run by hand with all three responses recorded.
- [ ] Status synced in all three places; milestone moved to `w4/done/m102/`.
- [ ] Prettier run.

## Out of scope

- Anything not in the definition of done.

## Closeout evidence

Local compiler/API path (`CompileBlueprintSource` + `ValidateBlueprint`), 2026-09-14: `examples/static-site/render.yaml` validates; `plan: free` names `plan`; missing publish dir names `staticPublishPath`; REST create still says `publishPath`; env-var faults no longer collapse to `redisServer`. Production GraphQL still serves the pre-fix binary until this commit deploys; the same calls then become the live DoD re-probe.
