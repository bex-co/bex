---
id: w4/m102/t005
title: Render parity sweep over the changed surfaces
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m102/t001, w4/m102/t004]
---

## Objective

Confirm the blueprint validation contract reads the same on every surface that exposes it, and that the vocabulary decision matches Render's.

## Context

- Blueprint validation surfaces on GraphQL (`validateBlueprint`, `blueprintPreview`), on MCP, on the sync/apply path, and in the dashboard's Blueprints pages (`dashboard/src/features/blueprints/`). All read one renderer (`blueprint_compiler.go:1072`), so the check is that each surface *presents* the problems faithfully — paths, lines, and order.
- The schema is Render's own pinned snapshot (`RenderBlueprintSchemaSHA256`, 2026-08-02). Do not change it or its SHA here. If Render's live schema has since changed for `staticService`, that is a separate, deliberate update with its own capability-registry pass.
- `docs/ADR006-bex-api.md:167` fixes `staticPublishPath` as accepted and `publishPath` as rejected; `docs/ADR018-render-parity.md:13` records the blueprint field set. Update whichever rows this milestone moves.
- Worth capturing while here: whether Render's own validator names the offending property for an ambiguous service, and what its enum error says. If it is not already in `docs/render-artifacts/`, capture it there rather than asserting it from memory.

## Steps

1. Diff the problem list across GraphQL and MCP for the t001 manifest: same properties, same paths, same count.
2. Check the dashboard's Blueprints validate/preview panels render every problem, with its path, and do not silently show only the first.
3. Update `docs/ADR049-render-yaml-parity.md` / `docs/ADR018-render-parity.md` for the vocabulary decision from t003.
4. File any residual drift as a w4 inbox note.

## Files

- `lego/backend/internal/apps/{graphql.go,mcp.go}`
- `dashboard/src/features/blueprints/components/validate-panel.tsx`
- `docs/ADR049-render-yaml-parity.md`, `docs/ADR018-render-parity.md`, `docs/render-artifacts/`

## Acceptance criteria

- [ ] GraphQL and MCP report identical problem lists for the same manifest.
- [ ] The dashboard shows all problems, not just the first.
- [ ] ADR rows updated for the vocabulary decision.
- [ ] Any Render behavior the fix leans on is captured in `docs/render-artifacts/`, not asserted from memory.

## Out of scope

- Implementing newly found divergence — file it.
