---
id: w4/m102/t007
title: Test coverage for the shipped behavior
worker: worker4
status: done
estimate: 40m
depends_on: [w4/m102/t005]
---

## Objective

Close the gaps t004 did not reach, asserting real behavior. No test that cannot fail.

## Context

t004 covers branch selection and the control messages. What remains:

- **t001's walk** as a standing CI gate, proven by reintroducing `plan: free` and watching it fail.
- **t003's convergence walk**: the three-step loop from its Context, asserted as a sequence — each message's instruction applied, ending valid.
- **The dashboard side** (t005 step 2): a test that a multi-problem validation renders every problem.
- **The enum union** (t002 step 4): assert the message contains `cron`, so a future branch addition that forgets the message fails here.

## Steps

1. Write the tests above.
2. For each, confirm it fails against the pre-fix behavior.

## Files

- `lego/backend/internal/apps/*_test.go`
- `dashboard/src/features/blueprints/components/__tests__/validate-panel.test.tsx`

## Acceptance criteria

- [ ] Each new test recorded here with the pre-fix behavior it fails against.
- [ ] Manifest walk, convergence walk, dashboard multi-problem rendering, and enum union all asserted.
- [ ] `cd lego/backend && go test ./...`, `make lint`, `dashboard/ yarn test` green.

## Out of scope

- Surfaces this milestone did not change.
