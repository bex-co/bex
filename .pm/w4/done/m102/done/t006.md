---
id: w4/m102/t006
title: Simplify pass over this milestone's changes
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m102/t005]
---

## Objective

Run `/simplify` over the code this milestone changed — behavior-preserving only.

## Context

Likely targets:

- `appendBlueprintSchemaProblems` after t002 will hold two selection strategies (discriminator match, then leaf count) plus the existing `kind.AdditionalProperties` path special case. Check it still reads as one decision rather than three accumulated branches.
- t003's translation layer: if it is a lookup table of one entry, say so in a comment rather than building a framework; if it grew, check it is not duplicating knowledge the schema already holds.
- `blueprintSchemaLeafCount` may become dead or near-dead depending on t002's fallback choice.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply behavior-preserving findings; file anything needing a design decision as a w4 inbox note.

## Files

- Whatever the milestone touched.

## Acceptance criteria

- [ ] `/simplify` run and each finding dispositioned, recorded here.
- [ ] `cd lego/backend && go test ./...` and `make lint` green after the pass.
- [ ] No behavior change — t001–t004's assertions still hold.

## Out of scope

- New fixes surfaced by the pass — file them.
