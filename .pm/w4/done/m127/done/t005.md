---
id: w4/m127/t005
title: Simplify (`/simplify` over the changed code)
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m127/t004]
---

## Objective

Run `/simplify` over the code this milestone changed, behavior-preserving.

## Context

- `postgres/protection.go` and `keyvalue/protection.go` are near-identical files (both `requireUnprotected` at line 32, same shape). Growing both in parallel is the obvious duplication to look at — but check whether an earlier pass deliberately kept them separate before merging them.
- The adapter-side `confirm` plumbing is the other candidate: one `core.WithConfirm` wrap per verb per surface wants a shared seam, the same observation `w4/m126` t006 makes.

## Steps

1. Run `/simplify` over the files t002–t003 touched.
2. Consider hoisting the two protection helpers, but only if nothing depends on their separation.
3. Keep behavior identical; t006's tests are the check.

## Files

- Whatever t002–t003 changed.

## Acceptance criteria

- [x] `/simplify` run; findings applied or declined with a reason.
- [x] Backend and dashboard suites stay green.

## Out of scope

- Restructuring the protection mechanism itself.
