---
id: w4/m115/t003
title: 'Render parity + docs'
worker: worker4
status: done
estimate: 20m
depends_on: [t002]
---

## Objective

Confirm the insights visibility fix is consistent across every surface that exposes it (REST, GraphQL, MCP, dashboard) and record the grant decision where the next provisioner change will see it.

## Context

- t001 changes what the managed user can see; t002 changes what the dashboard renders. bex-api is meant to be Render-compatible ([docs/ADR006-bex-api.md](../../../docs/ADR006-bex-api.md)): the same fields, semantics, and error shapes must hold on REST (`rest.go`), GraphQL (`graphql.go`), and MCP (`mcp.go`) in `lego/backend/internal/postgres/`.
- The managed-Postgres ADR ([docs/ADR009-postgresql-management.md](../../../docs/ADR009-postgresql-management.md)) is where the grant decision must be recorded.

## Steps

1. Exercise Processes + TopQueries on REST, GraphQL, and MCP against a throwaway database: same rows, same masking behavior, same error shapes on all three.
2. Compare against the equivalent render.com behavior/API; flag any drift as follow-up work rather than silently diverging.
3. Add one paragraph to ADR009 recording the `pg_monitor` grant (or the dedicated-role choice from t001) and why the insights path needs stats visibility beyond table ownership.

## Files

- `lego/backend/internal/postgres/rest.go`, `graphql.go`, `mcp.go` (read + verify, change only on drift)
- `docs/ADR009-postgresql-management.md` (grant paragraph)

## Acceptance criteria

- [x] REST/GraphQL/MCP return identical insights rows for the same database; drift (if any) filed, not hidden
- [x] ADR009 records the grant decision

## Out of scope

- The grant itself (t001), the dashboard guard (t002)
