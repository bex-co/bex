---
id: w4/m101/t007
title: Simplify pass over this milestone's changes
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m101/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, altitude — behavior-preserving only.

## Context

Likely targets given the shape of the work:

- t003's error-response helper: the point of it is that six call sites collapse into one seam. If the pass finds six `applyHeaders` calls instead, the fix missed its own intent.
- t001's type predicate: the eligible set now exists in the backend validator **and** in the dashboard gate **and** in the nav. Three copies of one rule is how they drift — check whether the dashboard can derive it from something the API already reports (the service object, a capability field) instead of restating it.
- `service-nav.tsx`'s `manageTail`: after t002 it has two type-gated entries built by hand. See whether one small eligibility table reads better than stacked ternaries.
- `autoscalingSpec`'s validation chain (`service.go:4363-4394`) is eight sequential guards plus the new type check — a candidate for the `/simplify` altitude lens, not a rewrite.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply behavior-preserving findings; file anything needing a design decision as a w4 inbox note.

## Files

- Whatever the milestone touched.

## Acceptance criteria

- [ ] `/simplify` run and each finding dispositioned (applied or filed), recorded here.
- [ ] `cd lego/backend && go test ./...`, `make test` + `make lint` from `lego/operator/`, and `dashboard/ yarn typecheck && yarn lint && yarn test` all green after the pass.
- [ ] No behavior change — t001/t003's acceptance tests still assert the same outcomes.

## Out of scope

- New features or fixes surfaced by the pass — file them.
