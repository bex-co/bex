---
id: w4/m101/t009
title: Closeout
worker: worker4
status: done
estimate: 15m
depends_on: [w4/m101/t008]
---

## Objective

Close the milestone — but only once its definition of done holds against deployed production, not merely once the code is written.

## Context

Every DoD bullet in `README.md` is a live probe, and two of them need a throwaway resource each:

- a **cron job** to prove the autoscaling write is refused on REST and GraphQL and that the Scaling surface is gone;
- a **static site** with one `/*` header rule to prove the header reaches a 404.

Keep the static site's name short. `w4/064` established that the purge Job name is `purge-<tenant>-<name-truncated-to-20>-<12-char-hash>`, which exceeds Kubernetes' 63-character label limit once the name slot fills — with the `tea-d98210cbbpdc73dcrkvg` tenant a name of 19 characters or fewer is safe. This run used `qa-0913b-static` (15) and it purged cleanly.

Several prior w4 milestones (m92–m96) closed with "live re-probe of the deployed fix deferred to the next QA pass". Do not repeat that: these probes are two `curl` calls and two clicks.

## Steps

1. Re-run every DoD bullet against deployed production and paste the results into this task.
2. Only then: set every remaining task `status: done`, move each `tNNN.md` into `w4/m101/done/`, mark every row `— **DONE**`, set `**Status:** done` in the milestone README, move `w4/m101/` to `w4/done/m101/`, and check `- [x]` in `.pm/w4/README.md`.
3. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w4/m101/**`, `.pm/w4/README.md`

## Acceptance criteria

- [ ] Every DoD bullet re-probed against deployed production, with results pasted here — no deferral.
- [ ] Every `qa-` resource deleted and its absence confirmed (service detail 404, public URL 404, project and environment gone from their lists, workspace `terminating` count back to its pre-run value).
- [ ] Status synced in all three places; milestone moved to `w4/done/m101/`.
- [ ] Prettier run.

## Out of scope

- Anything not in the definition of done.
