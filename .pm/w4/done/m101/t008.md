---
id: w4/m101/t008
title: Test coverage for the shipped behavior
worker: worker4
status: done
estimate: 40m
depends_on: [w4/m101/t006]
---

## Objective

Close the coverage gaps t005 did not reach, asserting real behavior and failure modes. No tautologies, no snapshot-everything, no test that cannot fail.

## Context

t005 covers the two shared-code blast radii and the control cases. What remains:

- **Dashboard, t002:** the cron/static gate on both the route and the nav, plus the pre-settle state (no autoscaling card flashes while `service` is `null`). A test that only checks the ready state would miss the flash the nav comment at `service-nav.tsx:97-102` exists to prevent.
- **Dashboard, t004:** accessible-name assertions via `getByRole("switch", { name: … })` for all three switches — including `#autoscaling-enabled` as the control — and the spelling.
- **Locale parity** for every new or changed string, both `en.ts` and `zh.ts`.
- **Operator, t003:** if the worker exclusion at `app_controller.go:2294` changed, a controller test that a background worker's replicas actually move under the autoscaler — the assertion that makes "honored" mean something.

## Steps

1. Write the tests above.
2. For each, confirm it fails against the pre-fix behavior — a test green before and after asserts nothing about this milestone.

## Files

- `dashboard/src/routes/__tests__/`
- `dashboard/src/features/services/components/__tests__/`
- `lego/operator/internal/controller/*_test.go`

## Acceptance criteria

- [ ] Each new test is recorded here with the pre-fix behavior it fails against.
- [ ] Route gate, nav gate and pre-settle state all asserted.
- [ ] All three switches asserted by accessible name.
- [ ] If workers became eligible, a controller test proves a worker's replicas move.
- [ ] Every new string present in both `en.ts` and `zh.ts`.
- [ ] All four suites green: `make test`, `cd lego/backend && go test ./...`, `make lint`, `dashboard/ yarn test`.

## Out of scope

- Coverage of surfaces this milestone did not change.
