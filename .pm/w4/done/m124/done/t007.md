---
id: w4/m124/t007
title: Test coverage — export → plan is `noop` for every kind, and a real change is named exactly
worker: worker4
status: done
estimate: 40m
depends_on: [w4/m124/t005]
---

## Objective

Cover both halves — the round trip and the diff's truthfulness — with tests that fail on today's code.

## Context

- `w6/m125`'s `TestValidateBlueprintCurrentStatePlanPredictsStoreManagedApply` is the model: it drives a real apply and asserts create → noop → update-naming-the-field → noop. Extend that shape rather than inventing a parallel one; its gap was simply that its fixture had no `domains`.
- The `pg_changed` probe is the regression guard that the fix did not make the planner blind.

## Steps

1. A service fixture **with** `domains` (and a second with two domains) asserting `noop` on re-plan.
2. A key-value fixture with the full exported field set asserting `noop`.
3. Per-kind assertions that a single changed field is reported alone.
4. A negative: `changedFields` never contains `name` for a matched resource, and never contains a value.
5. Show each failing against pre-fix code.

## Files

- `lego/backend/internal/apps/blueprint_test.go`
- `lego/backend/internal/apps/blueprint_generate_test.go`

## Acceptance criteria

- [x] Each new test demonstrated failing pre-fix.
- [x] Backend `go test ./...` and `golangci-lint` green.
- [x] No test asserts only that the call succeeded.

## Out of scope

- Kinds t004 exempted with a recorded reason.
