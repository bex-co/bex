---
id: w4/m122/t005
title: Render parity — the feed's event vocabulary and the domain rows across REST/GraphQL/MCP/UI
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m122/t004]
---

## Objective

Check the change is consistent across every surface the events feed is exposed on, and compare against render.com's own service-events behavior.

## Context

- The feed is read on REST (`GET /v1/services/{id}/events`), GraphQL (`serviceEvents`), MCP, and the dashboard Activity tab. `grep -rn --include='*.go' "TypeCustomDomain" lego/ | grep -v _test | grep -v events/service.go` → **0**, so webhooks and notifications are not in scope; re-run that grep after t002–t004 to confirm it is still 0.
- `w6/m122` (done) fixed the dashboard hiding `custom_domain_verified` from its catalog. If this milestone removes or renames a type, that catalog and `SERVICE_EVENT_TYPES` must move with it or the type becomes unselectable again.
- Render's events API is the parity reference for which events exist at all; bex's domain types are a bex spelling (`.pm/w6/done/068.md:39`).

## Steps

1. Diff the event vocabulary across the four surfaces after the change.
2. Re-run the dashboard catalog check from `w6/m122` so no type becomes hidden or unselectable.
3. Compare with render.com's service-events behavior for a failed domain verification and record agreement or divergence in `docs/ADR018-render-parity.md`.
4. File any drift found and not fixed here.

## Files

- `docs/ADR018-render-parity.md`
- `dashboard/src/features/events/`

## Acceptance criteria

- [x] The four surfaces agree on the vocabulary, asserted by the existing parity guards.
- [x] No event type is hidden or unselectable in the dashboard after the change.
- [x] The webhook/notification grep is still 0.

## Out of scope

- Implementing follow-up drift.
