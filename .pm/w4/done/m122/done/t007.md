---
id: w4/m122/t007
title: Test coverage — a failed verb produces no event; a real transition produces exactly one
worker: worker4
status: done
estimate: 40m
depends_on: [w4/m122/t005]
---

## Objective

Lock the rule in with tests that fail on today's code.

## Context

- The precedent test to match is webhooks' `TestCronWebhookEventsComeFromObservedFactsNotIntentVerbs`, cited in the `eventTypes` comment block as the thing that settled the same question one surface over.
- Verbs that behave correctly today do so because the projection is unconditional; under Option A they all change code path and need regression coverage.

## Steps

1. Domain cases: five failing verifies → 0 events; one promotion → 1; re-verify a verified domain → 0; refused add → 0; real add → 1 per claim; no-op re-add → 0.
2. A table test over the `eventTypes` entries t004 classified as "can fail and is now gated", asserting the refusal path mints nothing.
3. A regression test over a sample of the "cannot routinely fail" entries, asserting they still mint exactly one.
4. Dashboard: the Activity row renders only for real transitions.
5. Show the key tests failing against pre-fix code by stash-run.

## Files

- `lego/backend/internal/events/*_test.go`
- `lego/backend/internal/apps/domains_test.go`
- `dashboard/src/features/events/**/__tests__/`

## Acceptance criteria

- [x] Each new test demonstrated failing pre-fix and passing after.
- [x] Backend `go test ./...` and `golangci-lint` green; dashboard typecheck/lint/tests green.
- [x] No tautological tests.

## Out of scope

- Coverage for verbs t004 deferred.
