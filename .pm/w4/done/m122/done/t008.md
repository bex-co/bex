---
id: w4/m122/t008
title: Closeout — close the milestone once the definition of done actually holds
worker: worker4
status: done
estimate: 15m
depends_on: [w4/m122/t007]
---

## Objective

Close `w4/m122` when its observable end state is real on production.

## Context

- The DoD is written as live probes against `https://api.bex.co` and the dashboard Activity tab. The fixes must be deployed first; a green suite is not the bar.
- Reproducing it needs a throwaway service plus an unverifiable custom domain (`qa-<date>.example.com` works — RFC 2606 reserved, nobody can accidentally verify it), then delete both.

## Steps

1. Re-run every DoD bullet live and record the output.
2. Set remaining tasks `status: done`, move task files to `done/`, mark rows `— **DONE**`, set `**Status:** done`.
3. Move `w4/m122/` to `w4/done/m122/`, check `- [x]` in `.pm/w4/README.md`.
4. Delete the throwaway fixtures; run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w4/m122/**`
- `.pm/w4/README.md`

## Acceptance criteria

- [x] Every DoD bullet re-run live with output recorded.
- [x] Status agrees in all three places.
- [x] Fixtures deleted; anything deferred named as deferred.

## Out of scope

- New findings from the re-probe — file them.
