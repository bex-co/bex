---
id: w4/m107/t004
title: Render parity across REST / GraphQL / MCP / UI
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m107/t002, w4/m107/t003, w4/m107/t008]
---

## Objective

Confirm the logs read answers with one contract on every surface it exposes, and that the contract still matches render.com's.

## Context

- REST is the reference: `logs/render.go:62-66` `{hasMore,nextStartTime,nextEndTime,logs}`, with Render marking both cursors REQUIRED (`:94`). Its field set must not change, but its cursor **values** change in t008, which makes them followable. Check that all three surfaces page with the fixed cursors.
- `w4/m96/t003` records this envelope as the unchanged Render wire contract — check that record is still accurate after this milestone rather than assuming it.
- `docs/ADR018-render-parity.md`'s logs rows, and the `TestMCPParity*` guards, which compare bex's MCP against a hash-pinned capture of upstream.

## Steps

1. Diff the three surfaces' responses for one identical query; they must agree on `hasMore` and both cursors.
2. Compare against render.com's documented logs response and record any deliberate divergence as divergence.
3. Update ADR018's logs rows only if the surface actually moved.

## Files

- `docs/ADR018-render-parity.md` (only if a row changed)

## Acceptance criteria

- [ ] REST, GraphQL and MCP return the same `hasMore` and cursors for the same query.
- [ ] Any divergence from render.com is written down as a decision, not left implicit.
- [ ] `TestMCPParity*` passes without a new accepted-divergence entry, or with one that says why.

## Out of scope

- The broader cross-surface response-shape audit `w4/087` names as the open follow-on. This task covers the logs read only.
