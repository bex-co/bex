---
id: w4/m107/t006
title: Test coverage
worker: worker4
status: done
estimate: 40m
depends_on: [w4/m107/t004]
---

## Objective

Pin the behavior this milestone shipped, including the failure modes — a capped result, a complete result, and a second page that neither duplicates nor skips the boundary line.

## Context

- The bug existed because nothing asserted that GraphQL and MCP carry the same response shape as REST. A test that only checks "the field returns entries" would not have caught it and must not be what lands.
- `w4/087` records that bex's three surfaces are pinned against each other by nothing; a shared-shape assertion here is a small down payment on that.

## Steps

1. Backend: assert `hasMore` true at the cap and false below it, on all three surfaces, from one table.
2. Backend: assert the second page is strictly older, with the boundary line neither duplicated nor dropped.
3. Dashboard: assert the truncation notice appears only when `hasMore`, and that prepending preserves scroll position.

## Files

- `lego/backend/internal/logs/*_test.go`
- `dashboard/src/features/logs/**/__tests__/`

## Acceptance criteria

- [ ] A regression that drops the envelope from GraphQL or MCP fails a test.
- [ ] A regression that shows the truncation notice on a complete view fails a test.
- [ ] No tautological or snapshot-everything tests.

## Out of scope

- Load-testing Loki.
