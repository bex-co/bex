---
id: w4/m106/t005
title: Simplify pass over this milestone's changes
worker: worker4
status: done
estimate: 25m
depends_on: [w4/m106/t004]
---

## Objective

Run `/simplify` over the code this milestone touched — behavior-preserving reuse, simplification and efficiency only.

## Steps

1. The AST walk added in t001 runs on every GraphQL request; check it reuses the single existing parse and does not walk twice (once for scope, once for telemetry).
2. Look for duplication between the sensitive-field registry and `classifiedOps` — one source of truth if possible.

## Acceptance criteria

- [ ] No behavior change; tests still green.
- [ ] One parse per request preserved.

## Out of scope

- Any change to what is classified.
