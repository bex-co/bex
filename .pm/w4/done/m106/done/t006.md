---
id: w4/m106/t006
title: Test coverage for the shipped behavior
worker: worker4
status: done
estimate: 40m
depends_on: [w4/m106/t004]
---

## Objective

Tests that assert the real failure modes, not the happy path.

## Steps

1. Table-drive the classifier: nested sensitive field → sensitive; keys-only sibling → read; sensitive field behind a fragment spread and an inline fragment → sensitive; unknown nested field → today's class, with the guard catching it separately.
2. Assert the **role** gate independently: a non-sensitive role is refused even with a `bex.sensitive` token, so the two axes cannot be collapsed by a later refactor.
3. Assert the permissive control: a `bex.sensitive` token with a sensitive role still reads the value.
4. Assert telemetry label cardinality is unchanged.

## Acceptance criteria

- [ ] Each bullet above has a test that fails before t001 and passes after.
- [ ] No snapshot-everything or tautological assertions.
- [ ] `cd lego/backend && go test ./...` + `make lint` green.

## Out of scope

- Dashboard tests; the UI path is unchanged.
