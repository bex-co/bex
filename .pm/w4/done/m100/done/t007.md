---
id: w4/m100/t007
title: Simplify pass over this milestone's changes
worker: worker4
status: done
estimate: 30m
depends_on: [w4/m100/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, altitude — behavior-preserving only.

## Context

Likely targets, given the shape of the work:

- `authadmission.go`'s budget arithmetic: after t001 there are a per-source failure budget, a per-credential request budget, a per-credential in-flight budget and a global in-flight budget in one `admit`. Check whether the four read as one coherent policy or as four accumulated patches.
- The metrics hooks after t002's collapse: one shared query feeding several charts is exactly the place a pass-through wrapper hook tends to survive after it stops earning its keep.
- t003's throttle handling should be one helper, not a branch repeated per card — `use-metrics.ts` and `use-datastore-metrics.ts` are the same family and already share `graphql-series.ts`.
- t004's commit inheritance should live in one place, not be re-derived per caller.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply the behavior-preserving findings; file anything that needs a design decision as a w4 inbox note rather than doing it here.

## Files

- Whatever the milestone touched.

## Acceptance criteria

- [ ] `/simplify` run over the milestone's diff and its findings dispositioned (applied or filed, each recorded here).
- [ ] `cd lego/backend && go test ./...`, `make lint`, and `dashboard/ yarn typecheck && yarn lint && yarn test` all green after the pass.
- [ ] No behavior change — the t001/t004 acceptance tests still assert the same outcomes.

## Out of scope

- New features or fixes surfaced by the pass — file them.

## Resolution

Fix landed: per-cred knob + `AUTH_OVERLOADED`; GraphQL batch + `BatchHttpLink`; throttle UI; commit carry via `LatestDeployCommit`.
