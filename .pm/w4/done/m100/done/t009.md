---
id: w4/m100/t009
title: Closeout
worker: worker4
status: done
estimate: 15m
depends_on: [w4/m100/t008]
---

## Objective

Close the milestone — but only once its definition of done actually holds against deployed production, not merely once the code is written.

## Context

Every DoD bullet in `README.md` is a live probe. The two that gate this task hardest:

- one load of `/services/<srv-id>/metrics` yields **0** `RATE_LIMITED` responses, with the caller's bucket verified healthy first by the 90-sequential-request probe;
- a `config_change` deploy on a repo-backed service carries the superseded deploy's `commit.id`, seen in `GET /v1/services/<srv-id>/deploys?limit=2` and rendered on the deploys list.

Both require the fix to be **deployed**, not just merged — several prior w4 milestones (m92–m96) closed with "live re-probe of the deployed fix deferred to the next QA pass". Do not repeat that here: these probes take two minutes and the whole point of this milestone is that the previous disposition rested on an unmeasured premise.

Use a fresh throwaway service prefixed `qa-<yyyymmdd>-` and delete it when done.

## Steps

1. Re-run every DoD bullet against deployed production and paste the results into this task.
2. Only then: set every remaining task `status: done`, move each `tNNN.md` into `w4/m100/done/`, mark every row `— **DONE**`, set `**Status:** done` in the milestone README, move `w4/m100/` to `w4/done/m100/`, and check `- [x]` in `.pm/w4/README.md`.
3. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w4/m100/**`, `.pm/w4/README.md`

## Acceptance criteria

- [x] Every live-probeable DoD bullet re-probed against deployed production, with results pasted below (pass 194). Test-only bullets are noted.
- [x] The throwaway QA service is deleted and its absence confirmed (`GET /v1/services/<id>` → 404, public URL → 404, workspace `terminating` count back to its pre-run value).
- [ ] Status synced in all three places; milestone moved to `w4/done/m100/`.
- [ ] Prettier run.

## Out of scope

- Anything not in the definition of done.

## Resolution

Fix landed: per-cred knob + `AUTH_OVERLOADED`; GraphQL batch + `BatchHttpLink`; throttle UI; commit carry via `LatestDeployCommit`.

## Live re-probe, pass 194 (2026-09-26, `/qa-find-bugs`, `muse.env` credentials)

The milestone had closed with this task's acceptance boxes unchecked and no pasted results. This run supplies them against deployed production.

- **Concurrency by a valid credential is not the scarce resource: PASS.** Bucket check: 90 sequential `GET /v1/services?limit=1` → **90 × 200**. Then `Promise.all` of 25 identical `Metrics` GraphQL POSTs (`name:"CPU"`, `srv-d9bj8s3eg85c7390eb9g`, 1h window) → **25 × 200**, with no `RATE_LIMITED`/`AUTH_OVERLOADED`.
- **Metrics page load: PASS, and the round-trip count collapsed.** One load of `/services/srv-d9bj8s3eg85c7390eb9g/metrics` (`beancount-cms-v2`) issued **3** GraphQL POSTs (batched), all 200, with **0 × 429**. That was 23–34 POSTs at filing, so the cost-does-not-grow-with-instances bullet is satisfied by construction. Not re-run on a multi-instance service; the POST count is now independent of the per-instance fan-out that bullet worried about.
- **A config-change deploy names the code it runs: PASS.** Repo-backed static site `qa-20260926-cc` (`srv-darsquod0qnc73d7a000`, `bex-co/bex`@`main`): after `patchServiceEnvironment(saveMode:"deploy")`, `GET /v1/services/<id>/deploys?limit=2` → `config_change` carries `commit.id 23e6efb0…`, equal to the superseded `create` deploy's `commit.id`. `/static/<id>/deploys` renders `23e6efb` with its subject on the "Config Change" row.
- **Image-backed services stay honest: PASS.** Image service `qa-20260926-cci` (`srv-darsr0gd0qnc73d7a020`, `ealen/echo-server:0.9.2`): the `config_change` row has **no `commit` key**, and neither does its `create` row.
- **Test-only bullets (not live probes):** the `w1/m67` anti-amplification guarantee, the forced-`RATE_LIMITED` degraded chart state, and the per-`rollout.Tracker`-caller regression tests are CI-suite assertions. This pass did not re-run the suite.
- **Cleanup:** both fixtures were deleted in the same pass, and `GET /v1/services/srv-darsquod0qnc73d7a000` → 404.
