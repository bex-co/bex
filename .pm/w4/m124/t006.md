---
id: w4/m124/t006
title: Simplify — `/simplify` over the code this milestone changed
worker: worker4
status: todo
estimate: 20m
depends_on: [w4/m124/t005]
---

## Objective

Run `/simplify` over the milestone's diff. Behavior-preserving.

## Context

- The likely finding is two field-diff implementations: `changedBlueprintFields` in `blueprint_ir.go:306` and whatever t003 adds. One of them should win.
- The three `ApplyBlueprint*Spec` functions share the `before := DeepCopy()` / `!DeepEqual` idiom; if t003 makes each record paths, that scaffolding wants to be shared rather than written three times.

## Steps

1. Run `/simplify` over the diff.
2. Apply the behavior-preserving findings; record anything deferred.

## Files

- whatever t001–t005 touched

## Acceptance criteria

- [ ] One field-diff implementation, not two.
- [ ] Behavior unchanged — t007's tests still pass.

## Out of scope

- Behavior changes.
