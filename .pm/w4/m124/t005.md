---
id: w4/m124/t005
title: Render parity — plan semantics across REST/GraphQL/MCP and the dashboard's plan summary
worker: worker4
status: todo
estimate: 30m
depends_on: [w4/m124/t004]
---

## Objective

Confirm the plan this milestone changes reads the same on every surface, and compare with render.com's Blueprint plan behavior.

## Context

- `w6/m125` t004 established that REST `POST /v1/blueprints/validate`, `PreviewBlueprint`, GraphQL `BlueprintValidation.plan` and the MCP validate/preview tools all return `blueprintValidationFor`, so they move together. Re-verify rather than inherit — that was 18 days ago.
- m125 also recorded that `blueprint-plan-summary.tsx` renders neither `operation` nor `changedFields` (only `views.ts` maps them), and that the page was not opened live. It was not opened in pass 110 either. If the dashboard shows a plan at all, a user has been reading these wrong values — find out what it shows.
- Render's Blueprint UI shows a per-resource create/update/no-change summary; that is the comparison point.

## Steps

1. Re-run the same manifest through all four surfaces; assert identical operations and field lists.
2. Open the dashboard blueprint plan view live and record what it renders.
3. Compare with render.com's plan presentation and record agreement or divergence in `docs/ADR018-render-parity.md`.
4. File any drift not fixed here.

## Files

- `docs/ADR018-render-parity.md`
- `dashboard/src/features/blueprints/`

## Acceptance criteria

- [ ] Four surfaces agree, asserted not assumed.
- [ ] What the dashboard renders is recorded from a live page.
- [ ] ADR018 carries the comparison.

## Out of scope

- Redesigning the plan summary UI.
