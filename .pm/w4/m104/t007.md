---
id: w4/m104/t007
title: Test coverage for the shipped behavior
worker: worker4
status: todo
estimate: 40m
depends_on: [w4/m104/t005]
---

## Objective

Close what t004 did not reach, asserting real behavior.

## Context

t004 covers the route shapes and the cross-surface pair. What remains:

- **The standing invariant**: for every gated route, a body conforming to the pinned schema is accepted. This is the assertion whose absence let three instances ship; it belongs as a loop over the routes, not a hand-written case per route.
- **The error-shape invariant** from t003: no unpointed body rejection.
- **A re-pin guard**: changing `RenderBlueprintSchemaSHA256`'s sibling — the API OpenAPI pin — should fail the mismatch check rather than silently widening the contract. Confirm the pinned API spec has an equivalent digest guard; if it does not, that is worth filing.

## Steps

1. Write the loop-style tests above.
2. Confirm each fails against the pre-fix behavior.

## Files

- `lego/backend/internal/api/*_test.go`
- `lego/backend/internal/apps/*_test.go`

## Acceptance criteria

- [ ] The schema-conformant-body invariant is asserted as a loop over gated routes, not per-route copies.
- [ ] The no-unpointed-rejection invariant is asserted.
- [ ] Whether the API OpenAPI pin has a digest guard is answered; filed if absent.
- [ ] All suites green.

## Out of scope

- Surfaces this milestone did not change.
