---
id: w4/m105/t005
title: Simplify pass over this milestone's changes
worker: worker4
status: todo
estimate: 20m
depends_on: [w4/m105/t004]
---

## Objective

Run `/simplify` over the milestone's diff — behavior-preserving only.

## Context

Likely targets: the mint dialog now renders two copyable credentials, so check it is not two near-duplicate blocks; the shared `CopyButton` that `w4/m8`'s own simplify pass moved to `common/` should be reused rather than re-implemented; and if t001 added usage prose, confirm the token endpoint comes from config in one place rather than being formatted at several call sites.

## Steps

1. Run `/simplify` over the diff.
2. Apply behavior-preserving findings; file design-laden ones as a w4 note.

## Files

- Whatever the milestone touched.

## Acceptance criteria

- [ ] `/simplify` run and findings dispositioned, recorded here.
- [ ] All suites green after the pass.
- [ ] t001–t003's assertions unchanged.

## Out of scope

- New fixes surfaced by the pass — file them.
