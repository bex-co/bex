---
id: w4/m105/t006
title: Test coverage for the shipped behavior
worker: worker4
status: todo
estimate: 30m
depends_on: [w4/m105/t004]
---

## Objective

Close what t003 did not reach.

## Context

t003 covers the round trip. What remains:

- **The discovery/exchange agreement** from t002: a test that every scope in `scopes_supported` is either requestable by an API-key client or not advertised to it. This is the assertion that would have caught the `invalid_scope` trap.
- **Locale parity** for t001's new strings.
- **The privilege boundary** t002 measured: if a scopeless key is meant to be refused somewhere, assert the refusal; if it is meant to have full access, assert that too so a later scope change cannot silently narrow it.

## Steps

1. Write the tests above.
2. Confirm each fails against the pre-fix behavior where applicable.

## Files

- `lego/backend/internal/api/*_test.go`
- `lego/backend/internal/apikeys/*_test.go`
- `dashboard/src/features/api-keys/**/__tests__/`

## Acceptance criteria

- [ ] Advertised scopes and requestable scopes are asserted to agree.
- [ ] The privilege boundary t002 settled is asserted in whichever direction it was settled.
- [ ] New strings present in `en.ts` and `zh.ts`.
- [ ] All four suites green.

## Out of scope

- Surfaces this milestone did not change.
