---
id: w4/m116/t009
title: Test coverage for the shipped behavior
worker: worker4
status: todo
estimate: 45m
depends_on: [w4/m116/t007]
---

## Objective

Add meaningful tests for the behavior this milestone shipped: real assertions over real behavior and failure modes — never trivial, tautological, or snapshot-everything tests.

## Context

- Gaps this milestone must close: launcher CA provisioning (t001), KV publish effect beyond read-back (t002 — extend the `w7/m45` test), jobs RBAC/creation-failure surfacing (t003), suspend/resume data-path agreement (t004), single-login + helper currency (t005), empty-list envelopes everywhere (t006).

## Steps

1. Add regression tests for each shipped behavior, including the failure modes (creation refused, reason surfaced, transitional status honest, empty lists `[]`).
2. Extend the repo's live verifiers where they went stale (`scripts/psql-compat-verify.sh` sslmode precondition, `kv-cli-verify` for the publish-then-connect walk).
3. Mutation spot-check: each new test fails when its fix is reverted.

## Files

- `lego/backend/internal/*/…_test.go`, `lego/cli/…_test.go`, `dashboard/…/__tests__/`, `scripts/*verify*.sh`

## Acceptance criteria

- [ ] Every DoD bullet has at least one automated test that fails without its fix.
- [ ] No test added that cannot fail (no assert-nothing / tautological / mock-echoing tests).

## Out of scope

- Coverage-percentage gaming.
