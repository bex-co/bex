---
id: w4/m121/t009
title: Closeout — close the milestone once the definition of done actually holds
worker: worker4
status: todo
estimate: 15m
depends_on: [w4/m121/t008]
---

## Objective

Close `w4/m121` when its observable end state is real on production, not merely when the code is written.

## Context

- The DoD is written as live probes on `https://dashboard.bex.co` and `https://api.bex.co`. The fixes must be **deployed** before this task can pass — a green test suite is not the bar.
- Prior w4 milestones defer the live re-probe to the next QA pass; if that happens here, say so explicitly rather than closing on inference.

## Steps

1. Re-run every DoD bullet live against production.
2. Set each remaining task `status: done`, move every `tNNN.md` into `done/`, mark each row `— **DONE**`, set `**Status:** done`.
3. Move `w4/m121/` to `w4/done/m121/` and check `- [x]` in `.pm/w4/README.md`.
4. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w4/m121/**`
- `.pm/w4/README.md`

## Acceptance criteria

- [ ] Every DoD bullet re-run live, with its output recorded.
- [ ] Status agrees in all three places (workstream README, milestone README, task frontmatter).
- [ ] Anything deferred is named as deferred, not implied as done.

## Out of scope

- New work found during the re-probe — file it, do not absorb it.
