---
id: w4/m121/t007
title: Simplify — `/simplify` over the code this milestone changed
worker: worker4
status: todo
estimate: 25m
depends_on: [w4/m121/t006]
---

## Objective

Run `/simplify` over the code this milestone changed: reuse, simplification, efficiency and altitude cleanups, behavior-preserving.

## Context

- The likeliest duplication is a second port-bounds validator alongside `normalizeCreateDefaults` (`lego/backend/internal/apps/service.go:2513-2522`), and a second port-binding-type list alongside the dashboard's existing `showPortHint` predicate (`service-source-picker.tsx:35-36`).

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply the findings that are behavior-preserving; record anything deferred.

## Files

- whatever t001–t006 touched

## Acceptance criteria

- [ ] One validator for port bounds, shared by create and update.
- [ ] One type predicate for "binds a port", shared by the hint and the control.
- [ ] Behavior unchanged — the t008 tests still pass.

## Out of scope

- Behavior changes.
