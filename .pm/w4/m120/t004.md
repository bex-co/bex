---
id: w4/m120/t004
title: Render parity + docs
worker: worker4
status: todo
estimate: 20m
depends_on: [t003]
---

## Objective

Record the merged-read + fail-loud-write contract in ADR018's Env vars row (extending the `w6/m45` carve-out sentence), and verify Render parity: Render's dashboard shows blueprint-defined vars — confirm whether Render marks them managed/read-only and match the flag semantics.

## Context

- `docs/ADR018-render-parity.md:116` — Env vars row; the `w6/m45` sentence documents manifest-owned writership but not read visibility.
- `docs/ADR013-secrets.md` — secrets architecture; update if it describes the store as the only env source.
- If Render's own acceptance can't be verified live, record the evidence boundary explicitly (row convention).

## Steps

1. Re-read Render docs/OpenAPI for blueprint env read/write semantics; capture what Render shows and whether same-key edits are refused.
2. Update the ADR018 Env vars row: merged flagged reads + named refusal; keep the carve-out sentence accurate.
3. Update ADR013 if it claims store-only reads.

## Acceptance

- ADR018 row describes the shipped contract; any Render-unverifiable claim is marked as such, not asserted.
