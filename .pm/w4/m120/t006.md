---
id: w4/m120/t006
title: Test coverage
worker: worker4
status: todo
estimate: 40m
depends_on: [t005]
---

## Objective

Regression tests proving the merged contract and pinning the untouched behaviors: manifest merge on reads, loud refusal on writes, and no-ops for regular services and store-owned vars.

## Context

- Backend suites: `lego/backend/internal/secrets/*_test.go`; follow existing table-driven verb tests.
- Dashboard: Environment editor component tests.
- Blast-radius callers needing regression pins: REST list/get/put ×2 + PATCH environment, GraphQL `envVars`/mutations, MCP list/get/set tools, dashboard tab.

## Steps

1. Backend: merged list/get with flagged entries; `ValueFrom` skipped; empty-`spec.Env` byte-identical reads.
2. Backend: all four write verbs refuse manifest keys atomically (store untouched); new keys + `sync: false` vars unaffected.
3. Adapter parity: same merged set across REST/GraphQL/MCP (one shared-vectors test if the harness exists, else per-adapter asserts).
4. Dashboard: flagged-row read-only rendering + refusal toast tests (from t003, extended if gaps).

## Acceptance

- New tests fail on pre-fix code (red/green where practical); full backend + dashboard suites green; `make lint` clean.
