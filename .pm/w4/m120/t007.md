---
id: w4/m120/t007
title: Live re-probe on production
worker: worker4
status: todo
estimate: 30m
depends_on: [t006]
---

## Objective

Repeat this milestone's source probes against deployed production and watch every DoD bullet succeed, on a throwaway blueprint + service that is deleted afterwards.

## Steps

1. Sign in via `scripts/qa-login.sh --serve` (never read `.env`); work only in the QA workspace.
2. Create blueprint `qa-m120-bp` (`bex-co/bex`, `main`, `examples/hello-go/render.yaml`, `autoSync` default) — expect initial sync `success`.
3. `GET /v1/services/{id}/env-vars` on the created service — expect `MESSAGE` present, flagged manifest-managed (DoD: reads show manifest env).
4. `GET .../env-vars/MESSAGE` — expect the value + flag (DoD: single-key read agrees).
5. `PUT .../env-vars/MESSAGE` — expect the named manifest refusal, then re-GET to confirm the value unchanged (DoD: same-key write refused).
6. `PUT .../env-vars/QA_M120_NEW` — expect success (DoD: new keys work); `DELETE` it afterwards.
7. GraphQL `envVars(serviceId)` + MCP list/get — expect the same merged flagged set (DoD: surfaces agree).
8. Delete the service, disconnect the blueprint, verify 404s; `qa-login.sh --logout`; remove the jar.

## Acceptance

- Every DoD bullet observed live; zero QA residue (no `qa-m120-*` resources, session revoked).
