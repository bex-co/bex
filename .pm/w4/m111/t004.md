---
id: w4/m111/t004
title: Simplify
worker: worker4
status: todo
estimate: 15m
depends_on: [w4/m111/t003]
---

## Objective

Run `/simplify` over the code this milestone changed (reuse / simplification / efficiency; behavior-preserving).

## Context

- Likely overlap: the dialog's new candidate filter vs `linked-services-card.tsx:45-54`'s `isCompatible` — graduate to one shared helper if the shapes match; the scope picker's option labels vs the Move dialog's.
- `/simplify` is quality-only: no behavior change, no bug hunting.

## Steps

1. Run `/simplify` scoped to the milestone's touched files.
2. Apply the fixes it confirms; keep the diff behavior-identical (dashboard suite green before and after).

## Files

- Whatever t001–t002 touched

## Acceptance criteria

- [ ] `/simplify` pass applied or explicitly clean
- [ ] Dashboard suite + typecheck + lint still green

## Out of scope

- Behavior changes; new features
