---
id: w4/m111/t006
title: Closeout
worker: worker4
status: todo
estimate: 10m
depends_on: [w4/m111/t005]
---

## Objective

Close the milestone when, and only when, every other task is complete **and the milestone's definition of done is actually met**: set every remaining task's `status: done`, move each `tNNN.md` to `w4/m111/done/`, mark every row `— **DONE**` and set `**Status:** done` in the milestone `README.md`, move the whole `w4/m111/` directory to `w4/done/m111/`, and check `- [x]` in the workstream `README.md`. Completing this task _is_ the move.

## Context

- DoD lives in `.pm/w4/m111/README.md` — all four bullets must hold live on production (or the dev-4 stack where a bullet names it), not merely in unit tests.
- Do NOT run until the DoD holds: a milestone lands in `done/` when its observable end state is real, not merely when the code is written.

## Steps

1. Re-verify each DoD bullet live; note evidence (commands/clicks + observed result) in the milestone README closeout line.
2. Run `/pm done w4/m111/t006` last to trigger the milestone move.

## Files

- `.pm/w4/m111/README.md`
- `.pm/w4/README.md`

## Acceptance criteria

- [ ] Every DoD bullet re-verified live with recorded evidence
- [ ] Milestone moved to `w4/done/m111/` with workstream box checked

## Out of scope

- Any implementation work (belongs in t001–t005)
