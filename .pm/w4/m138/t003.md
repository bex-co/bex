---
id: w4/m138/t003
title: "Sync dialog copy names what is applied: the reviewed repository commit, not the stored render.yaml"
worker: worker4
status: todo
estimate: 10m
depends_on: []
---

## Objective

`blueprints.syncConfirmBody` (`dashboard/src/features/blueprints/locales/en.ts:549-551`, plus zh) says the sync "re-applies the stored render.yaml". The dashboard sync computes a preview from the repository and pins it (`reviewed.commitId`, `lego/backend/internal/apps/blueprint.go` `SyncBlueprint`: "the apply pins that revision and never re-resolves the branch tip"). Reword it so it says the reviewed commit's file is applied, consistent with the "Reviewed commit <sha>" line beneath it.

## Acceptance criteria

- [ ] The copy matches `SyncBlueprint`'s reviewed-commit behavior in en and zh.
