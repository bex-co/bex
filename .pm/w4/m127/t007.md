---
id: w4/m127/t007
title: Closeout
worker: worker4
status: todo
estimate: 15m
depends_on: [w4/m127/t006]
---

## Objective

Close the milestone once its definition of done holds live, not merely once the code is written.

## Context

- Every DoD bullet is a probe run against production in the hunt. Re-running them is cheap: both datastore kinds have a **free** plan (`databaseInstanceTypes` and `keyValueInstanceTypes` each expose `free`), so a full protected-environment fixture costs nothing.
- Deleting the fixtures goes through the guard with the phrase, which doubles as the control that it was armed.

## Steps

1. Create a free Postgres and a free Key Value directly into a `protected` environment (`createDatabase`/`createKeyValue` accept `environmentId`), re-run each DoD bullet, record the output.
2. Delete both with their confirm phrases, then the environment and project.
3. Set every remaining task's `status: done`, move each `tNNN.md` to `w4/m127/done/`, mark every row `— **DONE**`, set `**Status:** done`.
4. Move `w4/m127/` to `w4/done/m127/` and check `- [x]` in `.pm/w4/README.md`.
5. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w4/m127/`, `.pm/w4/README.md`

## Acceptance criteria

- [ ] Every DoD bullet re-verified live with output recorded.
- [ ] All three status locations agree.
- [ ] Fixtures deleted; the workspace is back to its pre-verification inventory.

## Out of scope

- `UpdatePostgres`'s plan/parameter paths if t001 left them out of the set.
