# Private services run on the Free tier, which Render does not offer, and no ADR decides it (workers got that decision in `w6/025`)

Why: `docs/ADR030-pricing.md` §7 made background workers paid-only to match Render (decided 2026-09-06, `w6/025`), but private services, which Render also sells only on paid instance types, still default to and accept **Free**. That is an unrecorded divergence in exactly the area where a recorded decision exists for the sibling type.

Found live, `/qa-find-bugs` pass 189, 2026-09-26 (w4-targeted, `muse.env` credentials), journey 10. From the Existing Image form with no plan chosen, `qa-20260926-priv` (`private_service`, `srv-darqc38d0qnc73d79u10`) was created on **`plan: free`**, while `qa-20260926-wrk` (`background_worker`, `srv-darqc4psmc7s73cq5ihg`) defaulted to **`starter`**, per ADR030 §7 (GraphQL `service { plan }` and REST `serviceDetails.plan` agree). Both were deleted within the pass.

Checked and correct this pass: neither is publicly reachable (`https://<name>.onbex.co/` → 404), and `url` is empty on GraphQL and REST, so `w6/m130` and `w4/124` still hold. The private service exposes `internalAddress: qa-20260926-priv:3000`.

- **Where the rule lives:** `lego/backend/internal/apps/service.go:2914-2929` (`normalizeTierForType`) special-cases only `TypeBackgroundWorker`. `dashboard/src/features/services/lib/instance-type.ts:26-32` (`offeredInstanceTypes`) hides Free only for `background_worker`.
- **Decision needed (not a code change yet):** either (a) extend §7 to private services (paid-only, the Render behavior, with the same transition rule for existing free private services, several of which exist in QA history such as `w4/139`), or (b) record keeping Free private services as a deliberate `—` divergence in `docs/ADR018-render-parity.md` with the reason. Whichever is chosen, the backend, dashboard, and ADR text must agree.
- **Unverified:** Render's current policy was taken from its pricing docs as understood at filing, not re-checked live this pass.

Estimate: ~15m to record the decision; ~45m if (a) is chosen (mirror `w6/025`'s change for one more type).
