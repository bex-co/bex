---
id: w4/m115/t005
title: 'Test coverage'
worker: worker4
status: todo
estimate: 45m
depends_on: [t004]
---

## Objective

Pin the visibility fix and the UI guard so neither regresses silently. Tests must assert real behavior and failure modes; never game coverage with trivial, tautological, or snapshot-everything tests.

## Context

- t001 changes what the managed user can see; t002 changes what the dashboard renders. Both need tests that fail on the 2026-09-19 behavior (34 masked cells, zero explanation).
- Existing coverage to extend: `lego/backend/internal/postgres/insights_test.go`.

## Fix shape

- Backend: a test proving the insights queries return unmasked query text for activity owned by another role (fixture: two roles, activity on one, insights as the managed user), plus an assertion that the insights connection is still read-only (write attempt rejected, statement timeout set).
- Dashboard: component tests for the t002 fixtures (fully-masked → explanatory state, no mask literals; partially-masked → mixed render).

## Files

- `lego/backend/internal/postgres/insights_test.go` (extend)
- `dashboard/src/features/databases/components/insights-panel.*` (tests colocated)

## Acceptance criteria

- [ ] New tests fail against the pre-fix behavior (verified by stash-run, not assumed)
- [ ] Backend suite + `dashboard/yarn test` + `make lint` green

## Out of scope

- Closeout (t006)
