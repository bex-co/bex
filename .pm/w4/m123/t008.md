---
id: w4/m123/t008
title: Closeout — close the milestone once the definition of done actually holds
worker: worker4
status: todo
estimate: 15m
depends_on: [w4/m123/t007]
---

## Objective

Close `w4/m123` when a live tenant container really does show only the contracted environment.

## Context

- The DoD is a live read from inside a container. The reproduction is cheap: an image service running `docker.io/mendhak/http-https-echo` with `ECHO_INCLUDE_ENV_VARS=1` and `HTTP_PORT` matching the service port answers its whole environment over HTTP as JSON. That is how pass 109 measured the 185/174 baseline.
- The fix is in the operator, so it needs the operator image rolled before this can pass — not just merged to `main`. `w4/m115` is the cautionary example: merged 2026-09-20, still not live at pass 108.

## Steps

1. Deploy a throwaway echo service, read its environment, confirm the contracted set exactly.
2. Confirm the same for a cron run and a build, per t003.
3. Set remaining tasks `status: done`, move to `done/`, mark rows `— **DONE**`, set `**Status:** done`.
4. Move `w4/m123/` to `w4/done/m123/`, check `- [x]` in `.pm/w4/README.md`, delete the fixture, run prettier.

## Files

- `.pm/w4/m123/**`
- `.pm/w4/README.md`

## Acceptance criteria

- [ ] Live container environment captured and equal to the contract.
- [ ] Operator image confirmed to contain the fix, not just `main`.
- [ ] Status agrees in all three places; fixture deleted.

## Out of scope

- New findings from the re-probe — file them.
