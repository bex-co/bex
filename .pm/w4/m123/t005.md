---
id: w4/m123/t005
title: Render parity — compare the delivered environment against a render.com service
worker: worker4
status: todo
estimate: 30m
depends_on: [w4/m123/t004]
---

## Objective

The environment a process receives is user-facing even though the fix is operator-internal. Compare bex's delivered set with Render's and record the result.

## Context

- Render documents the variables it sets on a service (`RENDER`, `RENDER_*`, `PORT`, and the user's own). bex's equivalent set should be a deliberate, written list, not whatever Kubernetes leaves behind.
- No REST/GraphQL/MCP field changes here, so the parity work is the delivered-environment comparison plus the ADR018 row from t004 — say so in the completion note rather than skipping the task.

## Steps

1. List Render's platform-injected variables from its current docs, with the fetch date.
2. List bex's post-fix set from a live container.
3. Record matches, deliberate divergences and gaps in `docs/ADR018-render-parity.md`.
4. File anything found and not fixed here as a follow-up note.

## Files

- `docs/ADR018-render-parity.md`

## Acceptance criteria

- [ ] Both lists captured with dates and sources.
- [ ] Every difference is classified as parity, deliberate divergence, or gap.
- [ ] Gaps are filed, not left in the task.

## Out of scope

- Implementing any newly-found gap.
