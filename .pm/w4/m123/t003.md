---
id: w4/m123/t003
title: Place the other 10 PodSpec sites — builds, pre-deploy, cron runs, backups, exports, publish, Key Value
worker: worker4
status: todo
estimate: 45m
depends_on: [w4/m123/t001]
---

## Objective

The Deployment is one of **11** `corev1.PodSpec{}` sites in the operator. Decide each one explicitly, because several of them also run tenant-authored code.

## Context

Exact list (`grep -rn --include='*.go' 'corev1.PodSpec{' lego/operator/internal/ | grep -v _test`, 11 hits):

| site                                    | runs                       |
| --------------------------------------- | -------------------------- |
| `controller/deployment_projection.go:426` | the tenant's service — t001 |
| `controller/app_controller.go:3864`       | cron runs — **tenant code** |
| `build/build.go:1022`                     | builds — **tenant code**    |
| `predeploy/predeploy.go:158`              | pre-deploy — **tenant code** |
| `controller/keyvalue_controller.go:686`   | Valkey                     |
| `controller/keyvalue_backup.go:288,452`   | platform maintenance       |
| `controller/disk_backup.go:268`           | platform maintenance       |
| `controller/database_controller.go:1639`  | Postgres-adjacent          |
| `controller/database_exports.go:310,384`  | platform maintenance       |
| `publish/publish.go:383,632`              | static publish             |

- Cron runs, builds and pre-deploy are the ones where a user's own program reads the environment, so they carry the same footgun as the Deployment.
- Platform maintenance pods need a stated reason either way — if one of them *relies* on a link var today, disabling it there breaks the platform, so check before flipping.

## Steps

1. For each of the 10, decide: disable, or keep with a reason.
2. Flip the tenant-code ones (cron, build, pre-deploy) unless something reads a link var — check first.
3. For each kept site, write the reason inline where a reader will find it.
4. Produce the table as the task's completion note.

## Files

- the 10 files above

## Acceptance criteria

- [ ] All 11 sites accounted for, count stated.
- [ ] Cron, build and pre-deploy containers show no link vars.
- [ ] Every kept site has its reason recorded in code.
- [ ] No platform pod broke — verified by running one of each.

## Out of scope

- Non-operator pods (bex-api itself, Traefik, cert-manager).
