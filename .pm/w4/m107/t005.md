---
id: w4/m107/t005
title: Simplify
worker: worker4
status: todo
estimate: 20m
depends_on: [w4/m107/t004]
---

## Objective

Run `/simplify` over the code this milestone changed — behavior-preserving reuse, simplification and efficiency.

## Context

- The envelope computation should exist **once** and be shared by all three adapters; three copies of `hasMore` is the specific duplication this milestone is most likely to have introduced.
- The viewer's paging and its live-tail append are both prepend/append paths over the same buffer; check they did not become two parallel implementations.

## Steps

1. Run `/simplify` over the diff.
2. Confirm no behavior change: the t001-t003 acceptance criteria still hold afterwards.

## Files

- Whatever this milestone touched.

## Acceptance criteria

- [ ] `hasMore` and the cursors are computed in one place.
- [ ] All three backend suites and `dashboard/yarn test` stay green.

## Out of scope

- Refactors unrelated to this milestone's diff.
