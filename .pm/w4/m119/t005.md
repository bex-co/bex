---
id: w4/m119/t005
title: "Test coverage for the shipped behavior"
worker: worker4
status: todo
estimate: 40m
depends_on: [w4/m119/t003]
---

## Objective

Add meaningful tests for the behavior this milestone shipped. Tests must assert
real behavior and failure modes; never game coverage with trivial,
tautological, or snapshot-everything tests.

## Context

- The core regression: bursty traffic misreported by query alignment. A test
  must pin the query construction (no extrapolating primitive for count
  semantics) and, at the service level with a stubbed source, that identical
  underlying events yield identical sums across alignments.

## Steps

1. Add query-construction tests pinning the exact-count form for
   `http_requests` (and bandwidth if t002 changed it).
2. Add service-level tests: same events, shifted windows → same sums.
3. Cover the w4/m113 static-site attribution path against regressions.

## Files

- `lego/backend/internal/metrics/*_test.go`

## Acceptance criteria

- [ ] Tests fail on the old `increase()` formulation.
- [ ] Tests assert alignment-invariance of bursty counts.
- [ ] Static-site attribution covered.

## Out of scope

- Live/prod test fixtures — unit + stubbed-source level only.
