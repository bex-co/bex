---
id: w4/m137/t003
title: "Dashboard: map the restart status on both datastores, and name the restart in KV config copy"
worker: worker4
status: todo
estimate: 30m
depends_on: [w4/m137/t001, w4/m137/t002]
---

## Objective

Label `config_restart` as "Restarting" on every Key Value and Postgres status surface, keep polling while it holds, and tell the user before they save that a maxmemory or persistence change restarts the store.

## Context

- `dashboard/src/features/keyvalue/lib/status.ts` `STATUS_MAP`, `lib/labels.ts` `STATUS_LABEL`, `types.ts:53` `KeyValueStatusKey`.
- `dashboard/src/features/databases/lib/status.ts:94` map, and `:121` transitional predicate (drives refetch).
- Copy: `features/keyvalue/components/key-value-maxmemory-policy-section.tsx` ("applies the new policy on the next reconcile") and `key-value-persistence-mode-section.tsx` ("a rolling update that preserves data": a single replica has no zero-downtime roll; pass 159 measured about 40–60s unreachable).
- en + zh strings (`locale-parity.test.ts`).

## Acceptance criteria

- [ ] `config_restart` renders "Restarting" in list rows, project pages, and the detail header for both datastores. It is never "Unknown".
- [ ] The detail page keeps polling until the status leaves `config_restart`.
- [ ] Both KV config sections state that saving restarts the store briefly.

## Out of scope

- Postgres restart dialog copy (already honest).
