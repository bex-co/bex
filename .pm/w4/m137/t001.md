---
id: w4/m137/t001
title: "Key Value: kvStatus distinguishes a restart from creation, and a stale Ready from a live one"
worker: worker4
status: todo
estimate: 30m
depends_on: []
---

## Objective

`kvStatus` should return `config_restart` for a once-Ready store that is not currently Ready, and for a `Ready` phase whose Ready condition trails the current generation. `creating` stays for never-Ready stores.

## Context

- `lego/backend/internal/keyvalue/service.go:225-234` (`kvStatus`), with its one caller at `:241` (`kvView`).
- `Status.CredentialRevision` is set only when the operator declares Ready (`lego/operator/internal/controller/keyvalue_controller.go:827`), so it is a usable "has been Ready" signal. Confirm there is no path that sets it otherwise.
- Pass-159 timeline: see the milestone DoD.

## Steps

1. Change `kvStatus` to take the whole `*KeyValue` (or the needed status fields) and apply the precedence in the milestone's target behavior.
2. Keep `deleting` > `suspended` precedence in `kvView` unchanged.

## Files

- `lego/backend/internal/keyvalue/service.go`, `keyvalue_test.go`

## Acceptance criteria

- [ ] A table test covers never-Ready Provisioning → `creating`, once-Ready Provisioning → `config_restart`, stale Ready (condition `ObservedGeneration < Generation`) → `config_restart`, Ready → `available`, Failed → `unavailable`, plus suspended and deleting precedence.

## Out of scope

- Operator phase changes (the mapping is enough).
