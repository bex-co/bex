---
id: w4/m101/t006
title: Render parity sweep over the changed surfaces
worker: worker4
status: todo
estimate: 30m
depends_on: [w4/m101/t002, w4/m101/t004, w4/m101/t005]
---

## Objective

Check that everything this milestone changed is consistent across REST, GraphQL, MCP and the dashboard, and compare each against render.com's captured behavior — recording deliberate divergence as divergence.

## Context

- The repo's dated capture is unambiguous about the scaling surface: `docs/render-artifacts/manual-scaling.md:7` — "Render's Scaling page (web/private/background services only—not cron jobs)" — and `:21` — "Scaling tab, below Autoscaling (non-cron/static only)". t001/t002 bring bex into line with that; confirm the whole eligible set matches, including the background-worker decision.
- Autoscaling spans REST (`apps/rest.go:1116-1119`), GraphQL (`apps/graphql.go:1683-1692`), the settings patch (`apps/settings.go:295`), MCP (`apps/mcp.go:865` delete; confirm the setter's status per `docs/render-artifacts/mcp-setter-fold-2026-08-18.md`), and the Blueprint `scaling:` block. All must refuse identically and report the refusal in each surface's own error shape.
- Static header rules are a served-response surface: `docs/ADR029-static-sites.md` owns the contract, and Render's own documented behavior is [header patterns match request paths](https://render.com/docs/static-site-headers). Whether Render applies them to its 404s is **not** captured in this repo — if the fix needs that fact, capture it into `docs/render-artifacts/` rather than asserting it from memory.
- `docs/ADR018-render-parity.md` is the ledger; update the rows this milestone moves.

## Steps

1. Diff the autoscaling refusal across all four write surfaces: same eligible set, same error class, same message shape per surface convention.
2. Confirm the dashboard gate agrees with the backend's eligible set — a type the API accepts must not be hidden, and vice versa.
3. Check whether the static-site Headers and Redirects editors should warn that a rule does not apply to some response class, if t003 decided any class stays excluded.
4. Record the background-worker decision in `docs/ADR018-render-parity.md` (parity or deliberate divergence, with the reason).
5. File any residual drift as a w4 inbox note rather than fixing it here.

## Files

- `lego/backend/internal/apps/{rest.go,graphql.go,mcp.go,settings.go}`
- `dashboard/src/features/services/`
- `docs/ADR018-render-parity.md`, `docs/ADR029-static-sites.md`, `docs/render-artifacts/`

## Acceptance criteria

- [ ] The autoscaling refusal is identical across REST, GraphQL, MCP and the settings patch, asserted for at least REST and GraphQL.
- [ ] The dashboard's eligible set matches the backend's exactly.
- [ ] The background-worker decision is recorded in ADR018.
- [ ] Any Render fact the fix leans on is either already captured in `docs/render-artifacts/` or newly captured there — never asserted from memory.
- [ ] Residual drift filed as a note with its own `Why:` line.

## Out of scope

- Implementing newly discovered divergence — file it.
