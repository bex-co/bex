---
id: w4/m108/t007
title: Simplify the code this milestone touched
worker: worker4
status: todo
estimate: 20m
depends_on: [w4/m108/t006]
---

## Objective

Run `/simplify` over the code this milestone changed — reuse, simplification, efficiency, altitude — behavior-preserving only.

## Context

- `lego/backend/internal/metrics/source.go`, `lokisource.go`: after `t001`/`t003` the file may hold both a rate builder and a count builder with only one caller each. Collapse or clearly name them so the next metric cannot pick the wrong one by copy-paste.
- `lego/backend/internal/egressquery/query.go`: `Rate`, `SumRates` and `Increase` — check whether `SumRates` still has a caller after `t003`, and delete it if it does not (whole-program dead-code analysis runs in `make lint`).
- `lego/backend/internal/github/service.go`: `t004`'s fallback should not duplicate the resolution/error-classification logic already in `resolveCommit`.
- `dashboard/src/features/deploys/`: `triggerLabel` is duplicated in `deploy-header.tsx` and `deploys-list-page.tsx`; `t005` touches both, so unifying them is in scope here if it stays behavior-preserving.

## Steps

1. Run `/simplify` over the milestone's diff.
2. Apply the findings that are behavior-preserving; defer anything that changes behavior to a new task.
3. Re-run `make lint` (all four modules) and `dashboard/yarn lint`.

## Acceptance criteria

- [ ] `make lint` and the dashboard lint/typecheck pass with no new findings.
- [ ] No dead helper is left behind by `t001`/`t003`.
- [ ] No behavior change: the test suite is green before and after with no test edits.
