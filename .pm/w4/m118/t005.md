---
id: w4/m118/t005
title: "Live re-probe: a failing scheduled run lands started + ended in Activity"
worker: worker4
status: todo
estimate: 30m
depends_on: [w4/m118/t004]
---

## Objective

Repeat the pass-26 fixture against the deployed fix and watch a scheduled run's start and failure land in the service Activity feed, then delete the fixture.

## Context

- Pass-26 recipe: cron service from image `busybox:stable`, schedule `* * * * *`, command `echo <marker> && exit 3`; first run fails after ~11 min of crash-loop backoff (`ForbidConcurrent`, default backoffLimit).
- Dashboard renders both types already (`dashboard/src/features/events/lib/timeline.ts`, `service-event-catalog.ts`) — expect zero dashboard changes.

## Steps

1. Create a `qa-` prefixed cron in the QA workspace (image + failing command above); note the service id.
2. Wait for the first scheduled run to reach Failed in Recent Runs (~11 min); confirm Activity shows `cron_job_run_started` and a failed `cron_job_run_ended` for it.
3. Confirm the type filter offers and returns both cron types.
4. Trigger one manual run + cancel; confirm exactly one row each (no duplicates).
5. Delete the service; verify zero `qa-` residue.

## Files

- None (live verification only).

## Acceptance criteria

- [ ] Screenshots or transcript excerpts show started + failed-ended rows in Activity for a scheduled run.
- [ ] Fixture deleted; no `qa-` services remain.

## Out of scope

- Re-probing m114's cancel lifecycle (separate milestone).
