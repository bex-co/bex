---
id: w4/m136/t003
title: "Blast radius: every LogsDocument consumer either pages or states why it cannot need to"
worker: worker4
status: todo
estimate: 20m
depends_on: [w4/m136/t001, w4/m136/t002]
---

## Objective

Close the family, so a fifth log surface cannot repeat m107's scoping miss.

## Context

- `grep -rln LogsDocument dashboard/src/features` at filing gave 5 files: `logs/hooks/use-log-history.ts` (pages), `keyvalue/hooks/use-key-value-logs.ts`, `databases/hooks/use-postgres-logs.ts`, `deploys/hooks/use-deploy-logs.ts`, and `audit/hooks/use-audit-log.ts`. Re-run the grep. Audit has its own `hasMore` panel (`audit/components/audit-log-panel.tsx:44`); confirm whether its `LogsDocument` use is covered by that.

## Steps

1. Re-run the grep and paste the count.
2. For each consumer, record whether it pages, warns, or provably cannot exceed one page.
3. Add a small guard test (or a lint-style assertion) so a new `LogsDocument` consumer that ignores `hasMore` fails CI.

## Files

- the consumers above, and a test under `dashboard/src/features/logs/`

## Acceptance criteria

- [ ] Every consumer is classified with a reason.
- [ ] The guard fails on a consumer that drops `hasMore`.

## Out of scope

- Backend changes.
