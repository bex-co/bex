---
id: w4/m136/t006
title: Test coverage
worker: worker4
status: todo
estimate: 30m
depends_on: [w4/m136/t004]
---

## Objective

Add behavioral tests: paging prepends older lines without seam duplicates, the notice appears only for `hasMore:true`, datastore requests carry no service-only filters, and the existing service Logs tab behavior is unchanged (it already works, so it needs a regression guard).
