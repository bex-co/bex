---
id: w3/m163/t003
title: Update ADR075 § Positioning to match the new exit semantics
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m163/t002]
---

## Objective

ADR075 § Positioning describes the payment wall's self-host exit as a link out to the open-source project. After t002 it is an account deletion. Update the ADR so the record matches the system.

## Context

- The locale strings cite the ADR directly: `onboarding.paymentSetupSelfHostHint`'s `description` reads "Lead-in to the self-host exit on the payment wall (ADR075 § Positioning)".
- ADR075 D7 is what makes the wall apply to every plan including free (`BEX_REQUIRE_PAYMENT_METHOD=all` in production) — that decision is unchanged and must not be edited.

## Steps

1. Update § Positioning to describe the exit as a confirmed account deletion followed by a hand-off to the self-hosting guide.
2. Record why it is two steps (irreversibility on an evaluation-stage screen), so a later reader does not "simplify" it back into one click.
3. Leave D7 and the rest of the wall's decisions untouched.

## Files

- `docs/ADR075-user-onboarding.md`

## Acceptance criteria

- [x] § Positioning matches the shipped behavior.
- [x] The two-step rationale is recorded.
- [x] D7 is unchanged.

## Out of scope

- Revisiting whether the card gate should apply to the free tier.
