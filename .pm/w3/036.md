# Reconcile the 257 permanently-rejected Stripe billing rows behind `BillingPermanentReject`

Why: every row is usage that never billed, and the alert has been firing since at least 2026-09-08 — since the 2026-09-11 alert-noise cleanup it is a line in the daily warning digest instead of an email every 4h, which makes it visible but does not make it owned.

- Live on 2026-09-11: `bex_billing_export_rejected_rows` = 257 (`bex_billing_enabled` = 1).
- Needs the out-of-band control-plane token: port-forward `bex-api` `:8091`, then `BEX_CP_TOKEN=… scripts/stripe-billing-reconcile.sh issues` (read-only) to classify the rows by reject reason.
- Then per row class, per the runbook (`docs/runbooks/stripe-billing-setup.md#operations-reconciliation-and-repair`): `retry` only after the cause is fixed and inside the 34-day horizon; `mark_repaired` only after reconciliation proves the provider outcome; otherwise `acknowledge`. `repair` refuses live keys — a guarded live repair is its own decision.
- Close by watching the gauge return to 0 and the digest line disappear.
