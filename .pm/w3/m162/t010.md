---
id: w3/m162/t010
title: Run /simplify over the milestone's changes
worker: worker3
status: todo
estimate: 30m
depends_on:
  [
    w3/m162/t001,
    w3/m162/t002,
    w3/m162/t003,
    w3/m162/t004,
    w3/m162/t005,
    w3/m162/t006,
    w3/m162/t007,
    w3/m162/t008,
    w3/m162/t009,
  ]
---

## Objective

Run `/simplify` over everything this milestone changed — reuse, simplification, efficiency, altitude — and apply the findings. Behavior-preserving only.

## Context

- The milestone touches three areas that each already have an established pattern to conform to rather than re-invent: the billing purger chain (`workspaces/service.go:1199`), the benign-ignore convention in the webhook handlers (`billing/lifecycle.go:70-73`), and the `trap … EXIT` cleanup idiom shared across `scripts/*verify*.sh`.
- Watch in particular for a second Stripe error-classification helper appearing next to the existing one, and for the 23503 translation in t001 being duplicated rather than shared.

## Steps

1. Run `/simplify` against the milestone's diff.
2. Apply the findings that are genuinely behavior-preserving.
3. Note anything deliberately left alone and why.

## Files

- whatever t001–t009 touched

## Acceptance criteria

- [ ] `/simplify` has been run over the milestone's diff and its findings applied or explicitly declined with a reason.
- [ ] No behavior change is introduced by this task.

## Out of scope

- New functionality.
