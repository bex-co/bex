---
id: w3/m162/t012
title: Closeout
worker: worker3
status: todo
estimate: 15m
depends_on: [w3/m162/t011]
---

## Objective

Close the milestone once its definition of done actually holds — not merely once the code is written.

## Context

- Several DoD items are observable only against production and cannot be satisfied from the repo alone:
  - the stuck event `evt_1UGSEjEqsEqs2tLVTHfQYNzv` reaching `pending_webhooks=0` and the webhook route's `status="503"` counter going flat,
  - `cus_VCV5qtrtlYh0N0` and `cus_VGzT7XpXc4z57t` showing zero attached payment methods and anonymized fields,
  - `verify-iso-a`/`verify-iso-b`/`verify-iso-eg` and their two namespaces gone,
  - no workspace resolving to `unclassified`.
- Verify each against the live system before closing.

## Steps

1. Verify every DoD bullet, including the production-observable ones above.
2. Set every remaining task `status: done`, move each `tNNN.md` into `w3/m162/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in the milestone README.
4. Move `w3/m162/` to `w3/done/m162/` and check `- [x]` in the w3 README.
5. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w3/m162/**`
- `.pm/w3/README.md`

## Acceptance criteria

- [ ] Every DoD bullet verified, with the production-observable ones checked against production.
- [ ] Milestone moved to `w3/done/m162/` with no stub left behind.
- [ ] w3 README checkbox ticked and the task table fully marked.

## Out of scope

- Shipping. Committing and pushing happens only under `/ship`.
