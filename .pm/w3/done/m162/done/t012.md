---
id: w3/m162/t012
title: Closeout
worker: worker3
status: done
estimate: 15m
depends_on: [w3/m162/t011]
---

## Objective

Close the milestone once its definition of done actually holds — not merely once the code is written.

## Context

- Several DoD items are observable only against production and cannot be satisfied from the repo alone:
  - the stuck event `evt_1UGSEjEqsEqs2tLVTHfQYNzv` reaching `pending_webhooks=0` and the webhook route's `status="503"` counter going flat,
  - `cus_VCV5qtrtlYh0N0` and `cus_VGzT7XpXc4z57t` showing zero attached payment methods and anonymized fields,
  - `verify-iso-a`/`verify-iso-b`/`verify-iso-eg` and their two namespaces gone,
  - no workspace resolving to `unclassified`.
- Verify each against the live system before closing.

## Steps

1. Verify every DoD bullet, including the production-observable ones above.
2. Set every remaining task `status: done`, move each `tNNN.md` into `w3/m162/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in the milestone README.
4. Move `w3/m162/` to `w3/done/m162/` and check `- [x]` in the w3 README.
5. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w3/m162/**`
- `.pm/w3/README.md`

## Acceptance criteria

- [x] Every DoD bullet verified, with the production-observable ones checked against production.
- [x] Milestone moved to `w3/done/m162/` with no stub left behind.
- [x] w3 README checkbox ticked and the task table fully marked.

## Out of scope

- Shipping. Committing and pushing happens only under `/ship`.

## Outcome

Closed 2026-09-17 after verifying every DoD bullet against production, not just the repo.

Shipped as `b0fa3b6ee` + `2f2a057db`; deploy run completed successfully and bex-api is
running the new image on two ready replicas.

Production evidence:

- **The stuck event converges.** `evt_1UGSEjEqsEqs2tLVTHfQYNzv` — the
  `customer.subscription.deleted` that had been 503-looping — was replayed against the
  live bex-api with a valid signature and answered **HTTP 204**. The same delivery also
  proved t003: it carries `api_version 2020-08-27`, which the old code would have
  rejected with a silent 400. The log now reads
  `billing: Stripe webhook event=evt_1UGSEjEqsEqs2tLVTHfQYNzv ... carries API version
  "2020-08-27", expected "2026-06-24.dahlia" — dispatching anyway`, and
  `webhook_version{result="error"}` incremented while the request still succeeded.
- **Orphaned Stripe customers remediated.** `cus_VCV5qtrtlYh0N0` and
  `cus_VGzT7XpXc4z57t` now report zero attached payment methods, no
  `default_payment_method`, empty email/name, and `bex_deleted_at` — with both Customer
  objects kept so their invoices stay linked.
- **Leaked fixtures gone.** No `verify-iso-*` App CRs remain and both fixture
  namespaces were pruned.
- **Audience dimension usable.** 0 unclassified workspaces (47 customer, 1 qa, 1
  canary). Grouping services by audience now reads 5 qa / 2 customer / 1 canary — which
  is the honest answer to the question that started this milestone: of the 8 standing
  web services, only 2 belong to real customers.
- **Migrations applied:** `checkout_started_at`, `states_observed`, and the
  `tenants_default_audience` trigger are all present in the production database.
- **Drift guard green:** `scripts/stripe-webhook-drift.sh` reports the endpoint enabled
  and subscribing to exactly the 10 events the code handles.

Unrelated observations recorded rather than chased: `gitops (render)` has been failing
on main since at least 2026-09-16 04:02 on a `tenant-headroom tolerates
bex.co/build-only` assertion this milestone never touched, and seven bex-api pods were
evicted mid-rollout by node `DiskPressure` (all nodes now report
`DiskPressure=False`; the rollout recovered on its own and the tombstones were cleared).
