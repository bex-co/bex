---
id: w3/m163/t007
title: Closeout
worker: worker3
status: done
estimate: 15m
depends_on: [w3/m163/t006]
---

## Objective

Close the milestone once its definition of done actually holds.

## Context

- The DoD is verifiable from the repo and a running dashboard; no production-only checks are required here.

## Steps

1. Verify every DoD bullet against a running dashboard, including the dismiss path and the disabled-confirm gate.
2. Set remaining tasks `status: done` and move each `tNNN.md` into `w3/m163/done/`.
3. Mark every row `— **DONE**` and set `**Status:** done` in the milestone README.
4. Move `w3/m163/` to `w3/done/m163/` and check `- [x]` in the w3 README.
5. Run `npx prettier@3.4.2 --write "**/*.md"`.

## Files

- `.pm/w3/m163/**`
- `.pm/w3/README.md`

## Acceptance criteria

- [x] Every DoD bullet verified against a running dashboard.
- [x] Milestone moved to `w3/done/m163/` with no stub left behind.
- [x] w3 README checkbox ticked and the task table fully marked.

## Out of scope

- Shipping. Committing and pushing happens only under `/ship`.

## Outcome

Closed 2026-09-17. Shipped in `b0fa3b6ee`; the dashboard image built and deployed with
the rest of the platform.

DoD verified: the exit reads "Delete this account and self-host", opens a confirmation
stating the account is permanently deleted and was never charged, requires the typed
`delete my account` phrase, forwards to the self-hosting guide only on success, and
cannot delete in a single click. `docs/ADR075-user-onboarding.md` § Positioning and D6/D7
now describe the exit as it behaves, including why it is two steps.

The route skeleton was widened (`w-36` → `w-52`) to preview the longer label; the
footer's structure and text height are otherwise unchanged, so pending and ready still
agree.
