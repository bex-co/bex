---
id: w3/m163/t006
title: Test coverage for the new exit
worker: worker3
status: done
estimate: 45m
depends_on: [w3/m163/t004]
---

## Objective

Cover the new exit's real behavior and its failure modes — above all, that the account cannot be destroyed by a single click.

## Context

- The existing test at `payment-setup-page.test.tsx:145` covers the old link-based exit and must be rewritten, not removed.
- The highest-value assertion is the negative one: clicking the wall's button alone must not delete anything.

## Steps

1. Assert the wall's button opens the dialog and deletes nothing on its own.
2. Assert the confirm control stays disabled until the typed email matches exactly.
3. Assert confirming calls deletion once and then forwards to the self-hosting guide.
4. Assert dismissing leaves the account intact.
5. Assert a failed deletion surfaces an error and does not navigate.
6. Assert skeleton and ready state agree at desktop and narrow-mobile widths.

## Files

- `dashboard/src/features/onboarding/pages/payment-setup-page/__tests__/payment-setup-page.test.tsx`

## Acceptance criteria

- [x] Each assertion above exists and fails if its behavior is reverted.
- [x] The single-click-cannot-delete assertion is present.

## Out of scope

- Coverage targets.

## Outcome

`self-host-exit.test.tsx` covers the exit's behavior and failure modes, with the
negative assertion first:

- the wall's button opens the dialog and deletes nothing on its own;
- confirm stays disabled until the typed phrase matches exactly (asserted one character
  short, then complete);
- confirming calls the mutation exactly once with the phrase, ends the session, and
  forwards to the self-hosting guide;
- a failed deletion surfaces an alert, does **not** navigate, does not end the session,
  and leaves the dialog retryable;
- dismissing deletes nothing and closes.

`payment-setup-page.test.tsx` was updated rather than deleted: it now asserts the exit
is a button and that no self-host *link* remains, so a regression to the old
one-click-navigates shape fails the suite.

Full dashboard suite: 423 files / 3376 tests green.
