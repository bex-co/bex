---
id: w3/m163/t005
title: Run /simplify over the milestone's changes
worker: worker3
status: done
estimate: 20m
depends_on: [w3/m163/t004]
---

## Objective

Run `/simplify` over the milestone's diff and apply the behavior-preserving findings.

## Context

- The dialog should reuse the repo's existing dialog primitives and the established typed-confirmation pattern rather than introducing a bespoke one.

## Steps

1. Run `/simplify` against the diff.
2. Apply what is genuinely behavior-preserving; note anything declined and why.

## Files

- whatever t001–t004 touched

## Acceptance criteria

- [x] `/simplify` run and findings applied or explicitly declined.
- [x] No behavior change introduced.

## Out of scope

- New functionality.

## Outcome

Reviewed; nothing to change. `SelfHostExit` reuses the repo's existing primitives
rather than introducing parallels: the `Dialog` kit, `SudoCommandField`, the shared
`accountDeletionConfirmation` phrase, `graphQLErrorMessage`, and the same
`endBrowserSession` / `clearBrowserAccountState` fallback chain the settings-page card
uses. The submit guard (`submitGuard` ref + `busy`) mirrors that card deliberately —
the two are the only places an account can be destroyed and should behave identically.

Extracting a shared component from the two was considered and rejected: the settings
card also renders a workspace-disposition preview and a blocked state that the wall has
no equivalent for, so the shared part is the phrase and the session teardown, both of
which are already shared.
