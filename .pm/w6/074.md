# 074 — Live-verify m143/m144 permission UI on dev-6

Why: m143/m144 closed with mounted tests and green dashboard gates, but the DoD walkthrough (two authenticated roles, focus/reconnect, empty-membership removal) needs a running kind/`dev-6` stack that was unavailable at closeout.

## Scope

- Bring up `scripts/dev-env.sh 6` (and mock-cluster if needed).
- Exercise authorized/denied/unavailable service+deploy controls as viewer vs contributor/admin on list and detail (desktop + narrow-mobile).
- Exercise m144: role downgrade/removal from a second session, focus/reconnect freshness, failed refresh recovery copy, open confirmation cleared on access loss, last-workspace empty route.
- Record evidence on the closed milestones' Verification sections (append) and check the remaining open DoD boxes.
- Clean temporary identities/workspaces afterward.
