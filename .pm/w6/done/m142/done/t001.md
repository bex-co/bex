---
id: w6/m142/t001
title: Diff Render OpenAPI ops (+ CLI regenerated client if past v2.26) vs w8/m33 baseline
worker: worker6
status: done
estimate: 45m
depends_on: []
---

## Objective

Produce an operation-level (and material request/response shape) diff of Render's current public OpenAPI — and, if the pinned CLI has moved past v2.26.0, its regenerated REST client surface — against the w8/m33 round-3 baseline so t003 can update ADR018 from evidence.

## Context

- Round-3 baseline: `docs/ADR018-render-parity.md` § "Upstream drift re-baseline round 3 (w8/m33, 2026-09-07)" and `.pm/w8/done/m33/README.md`.
- Prior round used the CLI's regenerated client as a practical OpenAPI proxy when live fetch was awkward; either live `api-docs.render.com` or the current pin's client is fine if the source is recorded.
- Request-validator pin `lego/backend/internal/api/openapi/render-public-api-1.json` is read-only here (w6/m96 gate) — needed refreshes are findings for t003 to file.

## Steps

1. Establish the round-3 operation set (208-op lineage + round-3 deltas recorded in ADR018 / m33).
2. Fetch or derive today's operation surface; list adds, removals, and material schema/enum changes with candidate ADR018 rows.
3. If `render-oss/cli` tip/tag is past v2.26.0, note regenerated-client deltas separately for the optional pin bump in t003.
4. Hand a written diff to t003; do not edit ADR018 or pins in this task.

## Files

- `docs/ADR018-render-parity.md` (read)
- `lego/backend/internal/api/openapi/render-public-api-1.json` (read-only)
- `lego/cli/` pin / `UPSTREAM_RENDER_CLI.md` (read, for version check)

## Acceptance criteria

- [ ] A written operation-level diff vs the w8/m33 baseline exists for t003.
- [ ] No pinned OpenAPI/CLI artifact was modified by this task.

## Out of scope

- ADR018 edits, CLI pin bumps, filing board notes (t003).
