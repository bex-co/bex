---
id: w6/m142/t003
title: Re-baseline ADR018 (+ CLI checklist / pin only if upstream moved); file unowned gaps
worker: worker6
status: done
estimate: 45m
depends_on: [w6/m142/t001, w6/m142/t002]
---

## Objective

Write the dated round-4 re-baseline into ADR018 from t001/t002 evidence, update the CLI checklist/pin only when upstream has actually moved, and open owned `.pm` notes (or milestones via `/pm`) for every unowned real gap.

## Context

- Gap backlog rule: nothing filed twice; reuse owners for already-tracked cells (e.g. closed outbound-IPs UI via `w8/010`, open plan-name decision lineage from `w8/011` if still relevant).
- CLI pin bump implies `docs/cli-compatibility-checklist.md` pin-conditional re-check; live `scripts/cli-compat.sh verify` may record an environment carve-out like m33 if no cluster is up — do not fake green.
- Minting `bex-cli/v*` is `/release`, out of scope.

## Steps

1. Add a dated "Upstream drift re-baseline round 4 (w6/m142, YYYY-MM-DD)" block to `docs/ADR018-render-parity.md` summarizing OpenAPI + changelog/MCP findings and row updates.
2. Refresh the gap-backlog table only where ownership or status changed; do not reopen `—` / DO_NOT_DO rows.
3. If the CLI moved past the current pin: bump pin artifacts following m33's pattern (or file a follow-up note if the bump exceeds this task); refresh checklist bullets.
4. For each unowned real gap, create a `.pm` inbox note on an available workstream (prefer w6) with `Why:` and evidence pointers — do not silently accept.
5. If the request-validator OpenAPI pin needs a refresh, file that as follow-up rather than swapping it here unless the change is trivial and gated.

## Files

- `docs/ADR018-render-parity.md`
- `docs/cli-compatibility-checklist.md` (conditional)
- `lego/cli/` + related pin docs (conditional)
- `.pm/w6/` (or other) new inbox notes as needed

## Acceptance criteria

- [ ] ADR018 has a dated round-4 block and consistent row/backlog updates from the diffs.
- [ ] Every newly surfaced real gap has an owned board item; no DO_NOT_DO / deliberate `—` reopen.
- [ ] CLI pin/checklist either matches current upstream or an owned follow-up records the lag.
- [ ] Prettier run on touched markdown.

## Out of scope

- Implementing product fixes for filed gaps (separate milestones).
- `/release` of bex-cli.
- Building anti-goal surfaces.
