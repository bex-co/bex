---
id: w6/m142/t005
title: Test coverage
worker: worker6
status: done
estimate: 30m
depends_on: [w6/m142/t003]
---

## Objective

Add or extend meaningful tests for any behavior this milestone shipped (e.g. CLI launcher/analytics/bridge changes on a pin bump). Docs-only rounds assert via checklist/ADR evidence, not vacuous tests.

## Context

- m33 added hermetic bridge tests when the pin introduced analytics opt-out — follow that pattern only when code lands.
- Do not add tautological "ADR018 contains the string round 4" tests.

## Steps

1. If t003 changed code: add/adjust tests that fail on the pre-change behavior and pass after.
2. Run the relevant package/`bex-cli-validate` gates that the pin docs require.
3. If docs/board only: record that coverage is the dated ADR018 + checklist evidence and that no new product behavior shipped.

## Files

- Tests adjacent to any code t003 changed.

## Acceptance criteria

- [ ] New/changed behavior has real tests, or an explicit docs-only N/A is recorded.
- [ ] Required gates for touched modules are green (or environment limits recorded like m33).

## Out of scope

- Snapshotting the entire ADR018 file.
- Live `cli-compat.sh verify` when no cluster is available (record carve-out instead).
