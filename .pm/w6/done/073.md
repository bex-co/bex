# Refresh request-validator OpenAPI pin to include outbound-ips

Why: w6/m142's 2026-09-10 capture found 208 Render OpenAPI operations including `GET /services/{serviceId}/outbound-ips`, while `lego/backend/internal/api/openapi/render-public-api-1.json` pinned 207 — close this validator-coverage residual without regressing the w6/m96 compatibility adaptations.

**Status:** done 2026-09-11 — approved 2026-09-10 by `$pm for them all for w6`, brainstorm proposal 3. **Worker:** worker6. **Estimate:** ~45–60m. Keep as an inbox note under the sub-hour sizing rule. Scheduled third after m143/m144 in the approved queue, but independently actionable with no dependency on either milestone.

## Work

1. Refresh `render-public-api-1.json` from live `api-docs.render.com` (or an evidenced capture) so `retrieve-service-outbound-ips` is present.
2. Update the pin SHA / operation inventory in [openapi-request-validation.md](../../docs/render-artifacts/openapi-request-validation.md) and any `TestRenderConformance` / gate counts that assume 207.
3. Confirm bex's existing outbound-ips handlers still validate cleanly under the refreshed pin; do not change product behavior beyond accepting the documented Render path through the gate.
4. Run the OpenAPI request-validation/conformance suite through the composed validator and record evidence, including the existing Blueprint `blp-…` adaptations and ID-pattern inventory from w6/m96.

## Done when

- Pin op count matches live for the outbound-ips path (and any other documented adds from the capture date).
- Gates green; ADR018 round-4 note can point at the closed pin rather than this residual.
- Valid outbound-IPs requests reach the existing handler through the composed request validator, and Blueprint ID compatibility remains green.

Goal linkage: ADR008's Render-compatible REST pillar and ADR018's **Service outbound IPs** row, which already marks the product surfaces shipped. This work refreshes the validation contract; it does not add dedicated outbound IPs or reopen another deliberate non-goal.

## Out of scope

- Implementing new Render ops that appear in a fresher capture beyond outbound-ips — file separately.
- Casual pin swaps without the m96 refresh contract.

← from w6/m142 round-4 re-baseline, 2026-09-10.

## Done (2026-09-11)

Pin swapped to the live `api-docs.render.com` document fetched 2026-09-11 (345,074 bytes, SHA-256 `c6851bc830fa0f01fa58face22c8cf46d4762054c6fe879d724fae5ade72a520`, 131 paths · 208 operations · 58 request bodies · 164 component schemas) after a full structural live-vs-pin diff: exactly one operation added (`retrieve-service-outbound-ips` + the `outboundIps` schema), none removed or renamed; plan enums grew by pure addition (spec-based names), `$ref` targets restructured upstream (component refs, percent-encoded braces — kin resolves both), additive response/schema changes only. No loader exception was broadened. Updated in the same change: `renderOpenAPISHA256` + fetch date, `TestRenderOpenAPIPinGuards` counts, the moved `newTrigger` entry in `TestRenderOpenAPIRefPolicyGuard` (still exactly three `$ref` siblings), and `TestRenderRouteIntersectionInventory` (119 → 120 with `retrieve-service-outbound-ips`).

Evidence: new `TestOutboundIPsThroughComposedServer` drives the real composed server (auth + rate limit + validator) — a created service's `GET /v1/services/{id}/outbound-ips` returns 200 `{"type":"shared","ips":[]}` that conforms to the pinned `outboundIps` response schema, an undeclared query is now a 400 `unsupported query parameter` (the route was ungated before), and an unknown id is a 404 (gate passed, store miss). New `TestRenderRequestValidatorAdmitsRefreshedPlanSpellings` records the other documented delta: w8/011's Postgres/Key Value input aliases (`0.1c-256mb`, `0.5c-1g`, `256mb`, `1g`) and the `0.5c-512mb` compute name now clear the REST gate, while an unknown plan is still a value-free 400. w6/m96 guards (`TestBlueprintIDRoutesPassTheRenderOpenAPIGate`, `TestEveryConstrainedIDPathParameterAcceptsItsBexID`, `TestRenderPathParameterOverridesOnlyWiden`), `TestRenderConformance`, and `TestConformanceAllowlistEntries` all green under the refreshed pin without changes. Artifact doc gained a §Refresh history; ADR018's round-4 note now points at the closed pin.
