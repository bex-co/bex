---
id: w8/m40/t002
title: "Couple resource writes to the authorized claim"
worker: worker8
status: todo
estimate: 60m
depends_on: [w8/m40/t001]
---

## Objective

Couple resource writes to the authorized claim, so that concurrent and partially failed Blueprint syncs preserve one authorized resource owner.

## Context

- Parent: [w8/m40](README.md), approved 2026-09-09 brainstorm proposal 2.
- m23 checks ownership before apply and stamps labels best-effort only after the entire stack succeeds. Partial apply can leave changed resources unclaimed; two different Blueprints can pass the check concurrently. m37 coordinates one Blueprint, not competing resource owners. This is corrective coverage for the shipped ownership guarantee.
- Read cascading workspace/component instructions before implementation. Use isolated dev-8 for live checks; see [workstream instructions](../README.md).

## Steps

1. Apply claim enforcement to App, Postgres, and Key Value create/update paths and their shared callees.
2. Persist authoritative ownership with each successful resource mutation rather than a best-effort end-of-stack sweep.

## Files

- `lego/backend/internal/apps/deploy.go`
- `lego/backend/internal/apps/blueprint_ownership.go`

## Acceptance criteria

- [ ] The losing Blueprint cannot modify configuration even when both contenders passed an earlier preview.
- [ ] A successful resource write cannot be silently left unowned by a later stack failure.

## Out of scope

No automatic takeover, sync-delete, new resource families, or ownership redesign for ordinary non-Blueprint operations beyond the enforcement needed for this guarantee.
