---
id: w8/m42/t006
title: Test coverage
worker: worker8
status: done
estimate: 30m
depends_on: [w8/m42/t005]
---

## Objective

Make sure the fixes are covered by tests that fail on the old behavior: the t002 tests keep their behavioral assertions, and t004’s interleaved-cancellation fixture fails on the old fetch. Do not add tautological or snapshot tests.

## Acceptance criteria

- [x] Each new test fails when its fix is reverted (mutation spot check).

## Outcome (2026-09-25)

Mutation spot checks: (1) reverting `scripts/ci-red-streak.sh` to the global `gh run list --limit N` fails self-test case (j) (`OK: no workflow has failed 3 or more times`); (2) weakening the env-group delete gate to a prefix match (`deletePhrase.startsWith(confirmation)`) fails the typed-confirm test at the new partial-phrase `toBeDisabled()` assertion. Both reverted.
