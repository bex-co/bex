---
id: w8/m42/t007
title: Closeout
worker: worker8
status: todo
estimate: 15m
depends_on: [w8/m42/t006]
---

## Objective

Close m42 only when its definition of done holds live: three consecutive successful `deploy.yml` runs, a production sandbox copy round trip, and t004's live or fixture evidence. Then move tasks to `done/`, mark rows, set Status done, move `m42/` to `w8/done/m42/`, and check the box in `w8/README.md`.

## Acceptance criteria

- [ ] Each DoD bullet has recorded evidence (run IDs, the CLI transcript, and the drill output).

## Evidence so far (2026-09-25)

- deploy.yml `36217068753` (`dbf24a217`, 2026-09-26T04:11Z): **success**, build + deploy ran — production unfrozen, 1 of 3.
- deploy.yml `36221384845` (`2a2a71338`, this milestone's commit): every gate green (dashboard, backend, operator, opensandbox-controller, secret-scan) but build/deploy **skipped** as superseded — does not count toward the DoD. `a3fae64cd`/`25280f876`/`d3d57fe6c` were supersession-cancelled while queued; `726042a28` queued.
- ci-red-streak live read: `deploy (bex via Argo)` reported with first failure `ee9652171` / last good `8f07882f6` while the streak lasted (t004).
- Production sandbox copy round trip: **not run** — the local `bex` CLI token is expired (`your token is expired; run render login`) and re-login is an interactive device flow.
