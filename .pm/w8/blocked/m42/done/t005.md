---
id: w8/m42/t005
title: Simplify
worker: worker8
status: done
estimate: 20m
depends_on: [w8/m42/t001, w8/m42/t002, w8/m42/t003, w8/m42/t004]
---

## Objective

Run `/simplify` over the code and workflow files this milestone changed. Changes must preserve behavior.

## Acceptance criteria

- [x] `/simplify` findings are applied or explicitly declined with a reason.

## Outcome (2026-09-25)

`/simplify` (reuse, quality, efficiency reviewers): applied — `gh workflow list --limit 200` (default 50 would silently drop workflows), dropped the redundant active-state filter (`gh workflow list` already hides disabled workflows), merged per-workflow runs once with `jq -s` instead of re-parsing a growing array per workflow, and made the stub `gh` honour `--jq` so the id filter is exercised. Declined: parallel fetches — a once-daily cron where ~64 s is fine does not justify background-job error plumbing.
