---
id: w8/m41/t002
title: "Enforce reviewed source across API adapters"
worker: worker8
status: todo
estimate: 55m
depends_on: [w8/m41/t001]
---

## Objective

Enforce reviewed source across API adapters, so that manual sync cannot silently apply a Git source different from the one the user or agent reviewed.

## Context

- Parent: [w8/m41](README.md), approved 2026-09-09 brainstorm proposal 3.
- m21 provides a pre-sync preview and m36 binds each Git read to its reported commit, but dashboard confirmation sends only Blueprint ID and confirmation phrase; runSync resolves the branch again. The gap is between review and apply, not within one Git read.
- Read cascading workspace/component instructions before implementation. Use isolated dev-8 for live checks; see [workstream instructions](../README.md).

## Steps

1. Validate the precondition before any workload mutation and preserve exact source provenance in history.
2. Expose equivalent optional inputs and errors across REST, GraphQL, and MCP; retain documented behavior on omission.

## Files

- `lego/backend/internal/apps/blueprint.go`
- `lego/backend/internal/apps/rest.go`
- `lego/backend/internal/apps/graphql.go`
- `lego/backend/internal/apps/mcp.go`

## Acceptance criteria

- [ ] Preview A, move the branch to B, then confirm: no B configuration is applied without renewed review.
- [ ] Repository/path/settings mismatches cannot bypass the precondition or token-backed fetch authorization.

## Out of scope

No full live-state plan locking, PR preview environments, additional Git providers, bespoke CLI, or automatic-sync source policy change; m38 owns automatic intake.
